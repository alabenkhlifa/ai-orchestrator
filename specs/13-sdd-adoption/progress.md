# SDD Adoption Coordination Progress Log

### 2026-08-13 - Task 1 implemented and verified: child capability and release contracts reconciled, capability:sdd-adoption-coordination ready

- Completed: AC-01, AC-02, AC-03. This is a coordination-only task — no domain, persistence, or UI code was added. Confirmed before starting that all three required child capabilities are ready: `capability:repository-execution-profile` (provider `specs/30-repository-execution-profile-completion#Task 2`), `capability:repository-sdd-kit` (provider `specs/15-repository-sdd-kit-integration#Task 9`), `capability:initialized-sdd-repository` (provider `specs/16-empty-repository-initialization#Task 7`), via `python3 .agents/scripts/capability_index.py --capability` (each capability name in turn).

- **Individual child validators and global graph** (all real exit status):
  - `python3 .agents/scripts/validate_spec.py specs/13-sdd-adoption` — exit `0`.
  - `python3 .agents/scripts/validate_spec.py specs/14-repository-execution-profile` — exit `0`.
  - `python3 .agents/scripts/validate_spec.py specs/15-repository-sdd-kit-integration` — exit `0`.
  - `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` — exit `0`.
  - `python3 .agents/scripts/validate_spec.py specs/30-repository-execution-profile-completion` — exit `0`.
  - `python3 .agents/scripts/validate_spec.py --all specs` — exit `0` ("Spec graph validation passed: 35 specifications under specs").

- **Manual contract review** (read `requirements.md`/`design.md` of specs/14, /15, /16, /30 in full, plus the code they describe):
  - *Uniform SDD meaning, no permanent installation prerequisite*: specs/14 `requirements.md` business rule: "Managed runtime skills and authoritative specification revisions are sufficient for Orchestrator-managed SDD. Permanent repository files are not required." specs/15 `requirements.md` business rule: "The permanent kit is optional. Declining, canceling, or being unable to install it must not disable managed runtime SDD." specs/16 `requirements.md` AC-06 and business rule: declining the kit "must explain that independently launched repository agents will not automatically receive Orchestrator's managed skills, profile, or authoritative project specifications," while managed runtime SDD stays available. All three agree with specs/13 `requirements.md`'s own rule: "Permanent repository installation is optional and cannot be a prerequisite for managed Orchestrator execution."
  - *Four-axis readiness stays independent per child, no fifth/collapsed concept*: `lib/sdd_orchestrator/repository_readiness.ex` (mature/pilot path, specs/14+30) and `lib/sdd_orchestrator/repository_initialization/readiness.ex` (empty-repo path, specs/16) each define their own disjoint `reason` atom set (profile/pilot/staleness/conflict/multi-root/check reasons vs. worker-paired/specification-created/execution-profile/release-gate reasons — zero overlap), the same four-field shape (`assistant`/`specification`/`agent_execution`/`release`, each `:ready | {:blocked, reason}`, plus `earliest_blocked_stage`), and the initialization module's own moduledoc states it "never calls `RepositoryReadiness` or its dependencies." Each is consumed by exactly one LiveView (`grep` confirmed `RepositoryReadiness` only in `repository_pilot_live.ex:24`, `RepositoryInitialization.Readiness` only in `repository_initialization_live.ex`) — the two screens are never rendered together for one project. specs/15's kit surfaces (`lib/sdd_orchestrator_web/live/repository_kit_offer_live.ex`, `repository_kits_live.ex`) introduce a separate `stage` vocabulary (`:offer`/`:plan`/`:installed`/`:update_plan`/`:removal_plan`/`:removed`, etc.) for the kit's own install lifecycle — this is never presented as a readiness axis and never collapses or replaces any of the four readiness values; the kit offer screen reads pilot/profile state only to gate its own eligibility (`RepositoryKitOfferLive.eligible?/2`) and touches no readiness struct.
  - *No second authoritative specification store, no silent mutation, no undisclosed processing boundary*: specs/15 `requirements.md`: "Installation does not create bidirectional repository synchronization, background export, or another specification identity," and "Project specifications remain authoritative only through `capability:project-specification-store`." specs/16 `requirements.md`: "After onboarding, the first project specification is created through `capability:project-specification-store` as one complete immutable document-set revision. The repository kit contains no project-specific specification copy." Kit mutation is never silent: `RepositoryKitOfferLive.handle_event("apply_plan", ...)` (`repository_kit_offer_live.ex:134-140`) only proceeds through `plan_actionable?/1` (owner + an already-reviewed `:plan`/`:update_plan`/`:removal_plan` stage), the UI copy is explicit ("nothing is applied automatically" / "Review the exact plan below... when you're ready"), and `RepositoryKits.apply_plan/4` independently refuses a plan with any safety or ordinary conflict regardless of what the screen shows.
  - *Source-of-truth and trust-boundary rules agree*: specs/13 `design.md` "Data and Access Boundaries" states child workflows retain their own authoritative data, and "Specification documents remain authoritative only through `capability:project-specification-store`." This exact rule is independently restated by specs/14 (pilot references, "does not copy the documents or create another specification identity"), specs/15 (quoted above), and specs/16 (quoted above) — all three children converge on one authority, matching specs/13's own boundary rule with no child claiming a competing store.
  - *AC-01 routing without a kit prerequisite*: `lib/sdd_orchestrator/repository_initialization/eligibility.ex#classify/1` routes any repository with a root commit to `{:error, :mature_repository}` (mature-repo path) and an unborn/empty directory to `{:ok, :unborn_repository | :empty_directory}` (empty-repo path). Router (`lib/sdd_orchestrator_web/router.ex`): `/onboarding/empty-repository` → `RepositoryInitializationLive` (line 130); `/projects/:id/pilot` and `/local/projects/:id/pilot` → `RepositoryPilotLive` (lines 168, 122). `/projects/:id/kit` (line 169) is a separate, independently-reached route — neither the pilot nor the profile route is gated behind it, and `RepositoryKits.eligible_for_kit_offer?/2` only controls whether the kit's own optional offer is shown (after the pilot reaches `Ready for review`/`Done`), never whether the mature- or empty-repo path itself is reachable.
  - No conflict, cycle, duplicated authority, or hidden mutation was found in any of the four children's contracts.

- **Coordinated browser evidence** (AC-01 routing, AC-02 four-axis readiness): reused existing per-child e2e specs rather than adding a new one — no genuine gap was found. `repository-pilot.spec.js` and `repository-execution-profile.spec.js` already exercise the full mature-repo path end to end without any kit interaction; `empty-repository-initialization.spec.js` already exercises the empty-repo entry path; `repository-readiness.spec.js` already proves the four independent axes (including agent-execution blocked while release stays ready under the same profile) for the mature path; `repository-kits.spec.js` proves the kit catalog is independently reachable and read-only. Together these prove both routing legs work standalone, neither requires kit installation, and readiness is shown correctly per child — which is what a single artificial cross-screen test could not add, since a project's empty-repo initialization screen and mature-repo pilot screen are never shown together (confirmed above: disjoint code paths, disjoint LiveViews). Ran all six specs together, desktop project, isolated env:
  - Command: `E2E_MODE=true E2E_BASE_URL=http://localhost:4013 E2E_DATABASE_NAME=sdd_orchestrator_e2e_spec13_coord E2E_DEVICE_STORE_PATH=/tmp/spec13-coord.dets E2E_ISOLATED_RUN=true MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator/_build/e2e-spec13-coord ./node_modules/.bin/playwright test --project=chromium e2e/repository-readiness.spec.js e2e/repository-pilot.spec.js e2e/repository-execution-profile.spec.js e2e/repository-assessment.spec.js e2e/repository-kits.spec.js e2e/empty-repository-initialization.spec.js`
  - Result: 10 passed, 2 skipped (pre-existing, documented in `empty-repository-initialization.spec.js`'s own header as blocked on an out-of-process device-worker-stub-folder seam specs/16 already accepted — not owned by this task), exit `0`.
  - Isolated test database and build path removed after the run.

- Failed checks: None.
- Capability graph: `capability:sdd-adoption-coordination` — ready after Task 1, now satisfied.
- Slice status: Task 1 is the slice's only task. Its proof and the slice Verification Gate are the same act of work (see `tasks.md`); both are now complete. `## Status` set to `Verified`.
