# GitHub Project Onboarding Progress Log

### 2026-07-23 - Specification split checkpoint

- Completed: Narrowed the original project-onboarding specification to the GitHub sign-in, repository discovery, project creation, naming, and disconnected-state slice.
- Remaining: Resolve the listed architecture, integration, storage, privacy, and verification decisions before approval.
- Failed checks: None; implementation has not started.
- Spec updates: Moved local onboarding, hosted passwordless access, identity linking, storage lifecycle, and portability into separate ordered specifications without changing accepted behavior.

### 2026-07-23 - Technical design checkpoint

- Completed: Selected the Phoenix application foundation, GitHub App authorization and installation contract, protected session and credential boundaries, stable repository identity, storage adapter, Unicode name comparison, provider-neutral deployment, and canonical verification toolchain.
- Remaining at this checkpoint: Approve the Slice 01 privacy contract; resolved by the 2026-07-24 checkpoint below.
- Failed checks: None; implementation has not started.
- Spec updates: Removed the resolved engineering questions and identified the then-remaining privacy and legal blocker.

### 2026-07-24 - Privacy contract approval

- Completed: Approved the development-time controller, purposes, lawful bases, data minimization, retention, access, rights, processor role, no-analytics, and cleanup contract.
- Remaining: No active-slice decision blocks implementation. Each public hosted deployment must still complete its deployment-specific privacy release gate.
- Failed checks: None; implementation has not started.
- Spec updates: Separated stable implementation requirements from controller contact, vendor, region, transfer, notice, incident, and final-review evidence that depends on the production deployment.

### 2026-07-24 - Application skeleton bootstrap complete

- Completed: Bootstrapped the single Phoenix application at the repository root and established every canonical development check. Task 1 is done.
- Toolchain (pinned in `mise.toml`): Erlang/OTP 29.0.3, Elixir 1.20.2-otp-29, Node 22.13.1. Phoenix 1.8.9, Phoenix LiveView 1.2.7, Ecto SQL 3.14.0, Bandit 1.12.0 (locked in `mix.lock`).
- Passing proofs: `mise install`; `docker compose up -d postgres` (Postgres 17, healthy); `mix setup`; `mix check` (format-check, compile `--warnings-as-errors`, `credo --strict`, `mix test` = 5 passed) as the standard alias; `mix dialyzer` (0 errors); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets ci`; `npm --prefix assets run test:e2e` (Playwright/Chromium smoke = 1 passed); `MIX_ENV=prod mix assets.deploy`; `MIX_ENV=prod mix release`.
- Failed checks: None. Secrets remain runtime-only; none committed.
- Deferred: Content-Security-Policy is intentionally not yet enforced. Sobelow's `Config.CSP` is the only ignored check (`.sobelow-conf`, documented). A correct strict CSP needs a nonce/hash for the device-local pre-paint inline theme script, so it is owned by the theme interface and the "Complete security and observability review" task, not the skeleton.
- Local engineering decisions (implementation mechanisms, non-behavioral): the dev/test Postgres publishes host port `5433` (localhost-only) to avoid clashing with an existing local Postgres on 5432; `mix assets.deploy` compiles first so Phoenix 1.8 colocated CSS/JS is generated before Tailwind/esbuild; `mix check` runs under `MIX_ENV=test`; `Credo.Check.Design.AliasUsage` is disabled for Phoenix-generated code.
- Note: Only the skeleton task is complete. The remaining slice tasks are not started; the slice is not `Verified`.

### 2026-07-24 - Frontend delivery plan correction

- Completed: Replaced the incomplete task decomposition with an early shared presentation task followed by explicit vertical frontend workflows for entry and authentication, project catalog, repository access and selection, storage choice, confirmation and creation, project dashboard, and connection recovery.
- Remaining: Tasks 2 through 11 are not started. Every frontend surface must be implemented and pass its attached LiveView and browser proof before its owning task can complete.
- Failed checks: None. Task 1 proof remains valid; no implementation proof was removed or invalidated.
- Spec updates: Requirements and technical design are unchanged. The active-slice task plan now assigns each frontend and supporting surface one primary owner and keeps the final browser task limited to integration and verification.

### 2026-07-24 - Frontend plan review checkpoint

- Verdict: Independent second-pass review of the added frontend task decomposition (Task 2 shared presentation foundation, Tasks 3-9 vertical frontend workflows, Tasks 10-11 GDPR and security). The plan is coherent and trustworthy to start implementing: every Slice 01 acceptance criterion and frontend business rule maps to exactly one owning task, scope stays inside the active-slice boundary, and the delivery-ownership contract (one primary owner per surface, Task 9 integration-only) holds. No implementation exists for Tasks 2-11, so there was no task proof to re-run for them.
- Re-run evidence: `python3 .agents/scripts/validate_spec.py specs/01-github-project-onboarding` passed. Task 1's bootstrap proof was not re-run in this review (out of the requested frontend-plan scope); it remains claimed-complete per the 2026-07-24 bootstrap entry.
- Findings (plan-clarity; route to `update-spec`; no implementation defects, no code change):
  - Major - `ProjectOnboardingAttempt` lifecycle has no single explicit owner. Its creation (at `Add project` and empty-workspace continuation), persistence of the selected repository into it, and idempotency-key generation fall in a seam between Task 4 ("new onboarding-attempt handoff"), Task 5 (repository selection), Task 6 ("storage-selection state and persistence"), and Task 7 ("consumes the existing attempt"). The delivery-ownership section promises one owner per behavior; assign the attempt's schema/migration, repository-metadata persistence, and idempotency-key creation to one task before those tasks start.
  - Minor - Repository-picker row metadata display (owner, name, visibility, organization; requirements business rule) is not explicit in Task 5's proof, although keyboard selection and personal/private/organization categories are.
  - Minor - The `ProjectStorage` behaviour is split across tasks (`availability/2` in Task 6; `prepare/3`, `abort/2`, hosted adapter in Task 7). Name a single owner for the shared behaviour/contract module to avoid a seam.
  - Minor - Task 2's pre-paint inline theme script and Task 11's strict CSP must coordinate a nonce or hash so the script is not blocked (already noted in the Task 1 CSP deferral).
  - Nit - Accessibility proof (keyboard, focus, non-color, contrast) is concentrated in Tasks 2, 5, and 9; Tasks 3, 4, 6, 7, 8 do not each restate it.
- Route: The refinements are agreement-clarity items owned by `update-spec`; none require application code. No task checkbox or slice status was changed by this review.
- Blockers: None.

### 2026-07-24 - Traceability coverage adoption

- Completed: Adopted the machine-checkable coverage convention on this slice. Tagged all 43 acceptance criteria with stable `[AC-01]`-`[AC-43]` IDs and added an `Owns:` line to every task naming the criteria and `## Data and Access Boundaries` entities it primarily owns. `validate_spec.py` now enforces that each acceptance criterion has exactly one owning task and each of the 14 data entities at least one; the slice passes.
- Resolved: The `ProjectOnboardingAttempt` ownership gap raised in the review checkpoint. Task 4 now owns the attempt's schema, creation, initial state, and idempotency key; Task 5 writes the selected repository metadata, Task 6 the storage and device-setup return state, and Task 7 consumes it. Reassign if a different owner is preferred.
- Remaining: The Minor review items (repository-picker row metadata in Task 5's proof, a single owner for the shared `ProjectStorage` behaviour module, Task 2 and Task 11 CSP nonce coordination) are unchanged and still route to `update-spec` if pursued. Tasks 2-11 remain not started.
- Failed checks: None. `python3 .agents/scripts/validate_spec.py specs/01-github-project-onboarding` passes with coverage enforced.
- Spec updates: Acceptance criteria carry stable IDs and task ownership is now explicit and validator-enforced; no acceptance-criterion wording, scope, or design decision changed. The convention is wired into the `add-spec` and `update-spec` Delivery Coverage Gates and templates so later specs adopt it when created or next edited.

### 2026-07-24 - Shared presentation foundation complete (Task 2)

- Completed: Delivered the shared LiveView presentation foundation. Replaced the generator's daisyUI Phoenix/Elixir themes with the approved graphite/teal token system (light and dark) mapped into Tailwind v4; self-hosted `Public Sans` (400/500/600/700 + 400 italic woff2) with no Google Fonts request; a locally bundled Lucide icon component (`SddOrchestratorWeb.Icons.lucide/1`); a device-local, pre-paint theme with OS-preference fallback and a delegated toggle handler that never contacts the server; and the reusable primitives in `SddOrchestratorWeb.UI` (app shell, button, badge/status, notice, spinner, skeleton, empty/failure states, single-select radio row, text field) with keyboard-visible focus and non-color status cues. A dev/test-only design-system preview at `/_ui` is the render surface for the proofs.
- Owns satisfied: AC-03, AC-04, AC-05, AC-06 (mechanism), AC-07 (mechanism), entity:ClientThemePreference (browser `localStorage` only; no server record).
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 17 passed); `mix dialyzer` (0 errors); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (10 Playwright tests incl. axe light/dark, no external font/icon/analytics request, self-hosted Public Sans, OS fallback, device-local persistence across reload, keyboard focus visibility, non-color status cues, mobile+desktop layout fit); `MIX_ENV=prod mix assets.deploy` (fonts digested) and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Deferred/coordination: AC-07's cross-sign-in browser assertion and AC-06's true cross-device check are inherently end-to-end; the mechanism is device-local and server-independent now, and the sign-in/out theme-continuity scenario is verified by Task 9 once the auth surface exists. Task 11 must add the strict CSP nonce/hash for the pre-paint inline theme script (already recorded as the Task 1 CSP deferral). Removed the unused `daisyui` dependency; added `@axe-core/playwright` for the accessibility proof.
- Local engineering decisions (non-behavioral): design tokens are defined as CSS custom properties that switch on `:root[data-theme=dark]` and are exposed to Tailwind via `@theme inline`; the theme toggle is a delegated `document` click listener (not a LiveView hook) so it works before the socket connects and stays device-local; the `/_ui` preview route is compile-guarded by `config :sdd_orchestrator, :ui_preview` (dev + test only).

### 2026-07-24 - Entry, GitHub authentication, and protected session complete (Task 3)

- Completed: Delivered the unauthenticated two-action entry (`Login with GitHub` + `Work without GitHub`, with cancelled/failed recovery and retry), the GitHub App user OAuth flow (random `state` + PKCE `S256`, one-time single-use consumption bound to a browser-flow nonce, code exchange, stable-identity resolution), account/identity/encrypted-credential provisioning and restoration, opaque digest-only application sessions (24h idle / 30d absolute, sliding idle, rotation on sign-in, revocation on sign-out), protected-route resolution that fails closed, and sign-out back to the entry. The local action hands off to a placeholder owned by `specs/02`.
- Owns satisfied: AC-01, AC-09, AC-10; entity:Account, entity:GitHubIdentity, entity:GitHubAuthorizationAttempt, entity:GitHubCredential, entity:ApplicationSession.
- Architecture: new `Accounts` context (5 schemas + migration), `GitHubIntegration` context with a `Provider` behaviour (real `Req` adapter + deterministic test fake), `SddOrchestrator.Vault` (Cloak AES-256-GCM) with `Encrypted.Binary` for tokens and PKCE verifiers, `SddOrchestratorWeb.UserAuth` plug + `on_mount` hooks, `AuthController`, and `EntryLive`/`ProjectsLive`/`LocalOnboardingLive`. `/` now routes to `EntryLive` (the old page controller was removed).
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 48 passed) covering domain/persistence/session/security, provider-contract (`Req.Test`), controller integration (full sign-in, cancel, failure, replay, sign-out), and LiveView (two-action entry, recovery states, valid-session bypass, protected fail-closed, no token in payload); `mix dialyzer` (0, 1 documented Ecto.Multi/MapSet opaqueness skip); `mix deps.audit`; `mix sobelow --config`; `npm --prefix assets run test:e2e` (18 tests incl. entry two-action surface, recovery states, local handoff, protected redirect, axe light/dark); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Environment-blocked (not an implementation defect): the tagged live GitHub App smoke test (real OAuth round trip) needs a secret-backed GitHub App; ordinary CI uses the deterministic fake. It runs in the secret-backed staging environment per the release gate.
- Seam handoffs: `PersonalWorkspace` restoration and the real project catalog (AC-08/AC-11+) are Task 4; the `Metadata: read-only` scope assertion (AC-12) is Task 5. `ProjectsLive` is a Task 3 placeholder the catalog task replaces. Theme continuity across sign-in/out (AC-07) is unaffected because the theme lives only in device `localStorage`; its end-to-end browser assertion remains Task 9.
- Local engineering decisions (non-behavioral): the opaque session token lives in the signed `HttpOnly`, `SameSite=Lax` Plug session cookie (server stores only its SHA-256 digest); the OAuth browser-flow nonce is carried in the same session; `state`/token digests use SHA-256 hex; the dev vault key is checked in for local development only (prod supplies `CLOAK_KEY` at runtime); `ReqProvider` accepts injected `req_options` so `Req.Test` can exercise its request/response contract; a narrow `.dialyzer_ignore.exs` suppresses only the known Ecto.Multi opaqueness false positive; the entry CTA buttons are full-width on mobile and the shared `lg` button was enlarged with `whitespace-nowrap` so labels never wrap.

### 2026-07-25 - Personal-workspace restoration and project catalog complete (Task 4)

- Completed: Delivered the protected project catalog and its post-authentication routing. `Accounts.get_or_create_personal_workspace/1` restores the account's one stable workspace (race-safe via the unique `account_id` index plus `on_conflict: :nothing`). The rewritten `ProjectsLive` routes a restored non-empty workspace to its catalog with a non-mutating `Add project` control, and continues an empty workspace straight to the repository-access check without another action. The new `Projects` context owns the workspace-scoped catalog read model (`list_catalog/1`, `has_projects?/1`) and the onboarding-attempt lifecycle (`start_onboarding_attempt/1`, `get_or_start_onboarding_attempt/1`, workspace-scoped `get_onboarding_attempt/2`). Both onboarding entry points (`Add project` and the empty-workspace continuation) open or reuse one active `ProjectOnboardingAttempt` and hand off to the repository-access check; neither creates a project or repository connection.
- Owns satisfied: AC-08 (valid-session restore + catalog), AC-11 (stable workspace created/restored without exposing credentials), AC-13 (non-empty workspace → catalog with `Add project`), AC-14 (empty workspace → repository-access check without another action), AC-15 (`Add project` → repository-access check with no project/connection created); entity:PersonalWorkspace, entity:ProjectOnboardingAttempt.
- Architecture: new `PersonalWorkspace` schema and workspace get-or-create added to the `Accounts` context (one-to-one with `Account`, `has_one :personal_workspace`); new `Projects` context with a minimal `Project` read-model schema and the `ProjectOnboardingAttempt` schema; one migration adding `personal_workspaces`, `projects`, and `project_onboarding_attempts`; a `RepositoryAccessLive` placeholder at `/onboarding/repository-access/:attempt_id` inside the `:authenticated` live_session.
- Seam decision (product owner, 2026-07-25): the catalog reads projects through a stable base `Project` table introduced by this task (id, workspace_id, name, timestamps) so AC-13 and "visible catalog projects" are fully proven now. The project-confirmation task (Task 7) extends this table additively with the canonical name key and workspace uniqueness, storage mode, lifecycle state, and repository connection, and owns the atomic registration transaction. Tasks 4 and 7 share `entity:Project` ownership at those distinct delivery boundaries.
- Onboarding-attempt lifecycle (non-behavioral mechanism): `get_or_start_onboarding_attempt/1` reuses the workspace's active (unconsumed, unexpired) attempt so entry is idempotent under reconnects and repeated navigation and a workspace never accumulates parallel live attempts; a new attempt is started once the active one is consumed (Task 7) or expires. Attempt fields for the selected repository (Task 5), storage mode and device-setup return state (Task 6), and consumption (Task 7) are created now as nullable columns and filled by those tasks.
- Empty-state interpretation: AC-14 makes an empty workspace continue to the repository-access check rather than render an empty catalog page, so the catalog is only shown for a non-empty workspace; the shared `empty_state/1` primitive remains available for later reuse. Flag to `update-spec` if a literal empty catalog page is wanted instead.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 73 passed) covering workspace get-or-create (stable restore, idempotent retry, unique-constraint duplicate rejection, cross-account isolation), the catalog read model (workspace-scoped listing and `has_projects?`), the attempt lifecycle (initial state, distinct idempotency keys, active reuse, no reuse after consumption/expiry, workspace-scoped and malformed-id lookup), and LiveView routing (restored non-empty catalog with visible projects and no token in the payload, empty-workspace continuation opening exactly one attempt and no project, `Add project` handoff, and repository-access placeholder scoping and auth requirement); `mix dialyzer` (0, 1 documented skip); `mix deps.audit` (no vulnerabilities); `mix sobelow --config` (exit 0); `npm --prefix assets run test:e2e` (19 tests incl. the new repository-access protected-route check); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Carried to Task 9 (integration): the authenticated end-to-end browser scenarios for the catalog, `Add project`, and empty-workspace continuation need a live GitHub-backed session, which is environment-blocked locally (same constraint Task 3 recorded); Playwright covers the protected-route redirects and Task 9 orchestrates the authenticated flow once the full onboarding chain exists.
- Seam handoffs: Task 5 replaces the `RepositoryAccessLive` placeholder with the grant screen and repository picker and writes the selected repository into the attempt via the Task 4 context; Task 6 writes storage-selection and device-setup return state; Task 7 consumes the attempt inside the registration transaction and extends the `projects` table; Task 8 adds per-row connection-state indicators to the catalog. No auth-controller change was needed: workspace restoration is lazy and idempotent at the catalog, so the existing `/projects` redirect from the callback is unchanged.
- Local engineering decisions (non-behavioral): the routing decision lives in `ProjectsLive.mount/3` (empty → `push_navigate` to the repository-access check, non-empty → catalog), so it applies to both a fresh sign-in redirect and direct navigation; the onboarding attempt is carried by id in the repository-access path so the picker task receives a validated, workspace-scoped input; abandoned attempts expire after 24 hours (matching the approved privacy contract) and the `expires_at` index supports the retention pruner; catalog rows show only the project name for now because per-row connection status is owned by Task 8.

### 2026-07-25 - Execution-sequence audit

- Completed: Added an explicit dependency DAG for Tasks 1 through 11 and checked the listed execution order for later-owned prerequisites.
- Resolved: Recorded the Task 4 base `Project` schema and catalog read model as an approved early contract shared additively with Task 7; assigned the complete stable `ProjectStorage` behavior contract to Task 6 before Task 7 implements the hosted adapter; made Task 5's repository-row metadata and selected-repository persistence explicit; removed the stale Task 4 empty-catalog ownership claim because the approved empty-workspace behavior routes directly to onboarding; and clarified that Task 10 enforces an already-approved privacy baseline rather than defining it after implementation.
- Remaining: Task 5 is the next implementation task. Tasks 5 through 11 retain their existing implementation and proof work.
- Failed checks: None; this checkpoint changes only the approved agreement and task sequencing.

### 2026-07-25 - Repository-access grant and repository picker complete (Task 5)

- Completed: Delivered the repository-access check and picker. `RepositoryAccessLive` re-reads the user's `Orchestra-workflow` access on every mount (and `Check again`) with the user's token — never trusting an installation return — and drives a state machine: `:checking`, `:grant` (dedicated `Grant repository access` screen with `Continue to GitHub`), `:pending` (`Waiting for organization approval`, matched to the authenticated GitHub id so a pending request is never treated as granted), `:picker` (searchable, single-selection radio list showing owner, name, visibility, and organization metadata), `:empty` (no authorized repositories), and `:error` with distinct `:unauthorized`, `:rate_limited`, `:org_restricted`, and `:provider_failure` copy. `Continue` persists only the approved repository metadata onto the workspace-scoped `ProjectOnboardingAttempt` and hands off to the storage step; no project or repository connection is created on any path.
- Owns satisfied: AC-12 (approved `Metadata: read-only` scope, no write permission), AC-16 (grant screen, no picker/partial project), AC-17 (`Continue to GitHub` opens the public installation flow), AC-18 (pending org approval + `Check again`, never granted), AC-19 (valid grant opens the picker directly), AC-20 (every returned repository available and searchable), AC-21 (exactly-one selection, keyboard operable via the shared radio primitive), AC-22 (search no-match, empty, retrieval failure, and organization-restricted are distinct states), AC-23 (empty/failure are actionable with no partial project).
- Architecture: extended the `Provider` behaviour with `list_user_installations/1`, `list_installation_repositories/2`, and `list_pending_installation_requests/0`; `ReqProvider` follows GitHub `Link` pagination, normalizes 401 → `:unauthorized` and 403 → `:rate_limited`/`:org_restricted`, and signs the pending-request read with a short-lived RS256 app JWT (new `AppJwt` module + `joken`/`jose`, `iss` = client id, `iat` backdated 60s, `exp` < 10 min); the `FakeProvider` gained deterministic login-keyed scenarios. `GitHubIntegration` added `check_repository_access/2` (granted/pending/none/error), `list_accessible_repositories/2` (dedupe by numeric id, owner/name ordering, on-demand and unpersisted), `installation_url/1` with one-time state, and `approved_repository_permissions/0`. `Projects.select_repository/3` writes the approved metadata subset onto the attempt (`select_repository_changeset`). New `GitHubSetupController` (`GET /github/install` binds a one-time state to the browser and redirects to the public app page; `GET /github/setup` validates and routes back to the attempt's re-check) behind a new `:require_account` pipeline. Added `StorageSelectionLive` as the Task 6 handoff placeholder at `/onboarding/storage/:attempt_id`.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 120 passed) covering the access-check outcomes and error normalization, dedup/ordering, approved read-only scope (AC-12), pending-requester matching, `select_repository` metadata minimization and workspace scoping, the picker LiveView (all states, search, single-selection, persisted selection + handoff, resumable preselect), the installation handoff/return controller, and the storage placeholder scoping/auth; `Req.Test` provider-contract tests for `Link` pagination, 401/403 classification, and the RS256 app-JWT pending lookup; `mix dialyzer` (0, 1 documented skip); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (21 tests incl. the new storage and install protected-route checks); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None. Secrets remain runtime-only; the app-JWT contract test generates a throwaway RSA key at runtime (none committed).
- Environment-blocked (not an implementation defect): the tagged live GitHub App smoke test (real installation + pending-request round trip) needs a secret-backed GitHub App; ordinary CI uses the deterministic fake and the `Req.Test` adapter contract. It runs in the secret-backed staging environment per the release gate.
- Pre-existing dependency advisory (out of Task 5 scope): `mix hex.audit` flags `bandit 1.12.0` (EEF-CVE-2026-65623, HIGH — quadratic CPU blow-up reassembling fragmented WebSocket messages), pinned by Task 1. The canonical gate check `mix deps.audit` reports no vulnerabilities. Recommend a follow-up Bandit bump owned by the toolchain, not this task.
- Seam handoffs: Task 6 replaces the `StorageSelectionLive` placeholder with the real `Where should your project work be saved?` step and reads `selected_repository` from the attempt; Task 7 consumes the attempt in the registration transaction; Task 8 adds per-row connection state; the authenticated end-to-end browser scenarios for the grant/picker flow are carried by Task 9 (they need a live GitHub-backed session, the same constraint Tasks 3–4 recorded).
- Local engineering decisions (non-behavioral): the access check runs only on the connected mount so the initial paint shows the checking state and the network read happens once; the installation handoff carries a one-time `state` in the signed session (compared with `Plug.Crypto.secure_compare`) purely as defense-in-depth, because the return is always re-validated by re-reading access; repository search filters the already-fetched in-memory catalog (no re-fetch); `GITHUB_APP_ID`/`GITHUB_APP_PRIVATE_KEY` are optional runtime config so the app boots and degrades to the grant screen without them.

### 2026-07-25 - Storage selection and device-setup handoff complete (Task 6)

- Completed: Delivered the storage-selection step. `StorageSelectionLive` asks `Where should your project work be saved?`, explains the project work is separate from the linked repository (which stays on GitHub), and always shows both choices: `In my SDD Orchestrator account` (hosted, available in this slice) and `On this device` (device, available only with a valid readiness receipt). When device storage is unavailable it stays visible, explains the one-time setup prerequisite, and offers a `Set up on this device` action that hands off without selecting the mode or creating a project. The explicitly chosen mode is persisted onto the workspace-scoped `ProjectOnboardingAttempt`; continuation stays blocked until an available mode is explicitly chosen (no silent default) and then hands off to confirmation. A recorded device receipt refreshes availability without auto-selecting device.
- Owns satisfied: AC-24 (question, explanation, both choices), AC-25 (unavailable mode stays visible with a non-selecting setup action), AC-26 (device setup returns to the same step with the repository preserved; a recorded receipt refreshes availability without implicit selection; cancellation/return leaves it unavailable and creates no project), AC-27 (continuation blocked until an available mode is explicitly selected); entity:DeviceStorageReceipt.
- Architecture: new `SddOrchestrator.ProjectStorage` behaviour (the single owner of the shared `availability/2`, `prepare/3`, `abort/2` contract) plus the slice-01 availability rules (hosted always available; device gated on a valid receipt), `modes/0`, `label/1`, `parse_mode/1`; new `ProjectStorage.DeviceStorageReceipt` entity (opaque token + expiry, `from_attempt/1`, `valid?/1`, map round-trip). `ProjectOnboardingAttempt` gained `select_storage_changeset` and `device_setup_changeset`; `Projects` gained `select_storage_mode/3` and the device-handoff boundary `record_device_receipt/3`, both routed through a shared workspace-scoped `update_attempt/3` helper (and `select_repository/3` refactored onto it). New `DeviceSetupLive` (specs/02 seam) and `ProjectConfirmationLive` (Task 7 seam) placeholders at `/onboarding/device-setup/:attempt_id` and `/onboarding/confirm/:attempt_id`.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 149 passed) covering `ProjectStorage` availability and receipt validation, `select_storage_mode`/`record_device_receipt` persistence, minimization, and workspace scoping, the storage LiveView (question/explanation/both choices, unavailable-device visibility + non-selecting setup handoff, continue gating with no silent default, receipt-driven availability without auto-select, handoff to confirmation, mount guards), and the device-setup and confirmation placeholders; `mix dialyzer` (0, 1 documented skip); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (23 tests incl. the new device-setup and confirm protected-route checks); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Seam handoffs: Task 7 replaces the `ProjectConfirmationLive` placeholder with the real confirmation/naming/creation surface, implements the hosted `ProjectStorage` adapter's `prepare/3`/`abort/2` in the registration `Ecto.Multi`, and consumes the attempt (repository + storage mode); `specs/02` replaces `DeviceSetupLive` with real device setup and calls `Projects.record_device_receipt/3` after preparing on-device storage. The authenticated end-to-end browser scenarios for the storage step are carried by Task 9.
- Local engineering decisions (non-behavioral): `ProjectStorage.availability/2` computes the two slice-01 modes directly (hosted → available; device → receipt validity) rather than dispatching to adapter modules that do not exist yet, so the storage step has no forward dependency on Task 7 or specs/02; the device readiness receipt is stored in the attempt's existing `device_setup` jsonb column (string-keyed, ISO-8601 expiry) rather than a new table; selecting an unavailable/unknown mode is a no-op at both the LiveView (guarded) and, for unknown modes, the changeset (`validate_inclusion`).

### 2026-07-25 - Project confirmation, naming, and atomic creation complete (Task 7)

- Completed: Delivered the final-confirmation and creation surface. `ProjectConfirmationLive` reviews the selected repository (owner/full name, visibility, organization), the chosen storage mode, and an editable project name pre-filled with the repository-derived default; on submit it registers the project and routes to its dashboard. Naming preserves the user's natural display text (spaces, Unicode, case) with no slug conversion, trims boundary whitespace, rejects blank and control-character names, and enforces workspace-scoped case-insensitive uniqueness via an `NFKC` + Unicode default case-fold comparison key. Accepting the suggested default allocates the lowest free numeric suffix and stays safe under a name race; an edited name that collides returns inline feedback instead of being silently changed. `Projects.register_project/3` creates the project, its canonical `RepositoryConnection`, and (for hosted) its `HostedProjectStorage` in one `Ecto.Multi`, then consumes the onboarding attempt — all-or-nothing, with idempotent retry of an already-consumed attempt, `{:repository_already_linked, existing_project}` feedback that identifies the existing project, and no partial project or connection on any failure. `Projects.rename_project/2` is the reusable rename operation (Task 8 wires the post-creation control to it).
- Owns satisfied: AC-28 (confirmation shows repository, name, and storage mode), AC-29 (exactly one project and one connection created atomically), AC-31 (`example` default), AC-32 (`Café Roadmap` preserved, no slug/character loss), AC-33 (`example-2` lowest-suffix allocation), AC-34 (separate workspaces enforce uniqueness independently — same repository and name reused across workspaces), AC-35 (duplicate repository blocked via the `(workspace, provider, repository ID)` constraint with the existing project identified), AC-36 (rename enforces case-insensitive uniqueness without changing project or repository identity), AC-39 (no repository mutation and no agent start — registration only reads the attempt's stored metadata and writes its own tables; Slice 01 has no agent runtime), AC-40 (no duplicate workspace, project, or connection on any failure path); entity:Project (additively extended), entity:RepositoryConnection, entity:HostedProjectStorage.
- Architecture: extended the Task 4 `projects` table additively (new migration) with `name_key`, `storage_mode`, `lifecycle_state`, and `onboarding_attempt_id`, plus a unique `(workspace_id, name_key)` index; new `repository_connections` table with unique `(workspace_id, provider, provider_repository_id)` and unique `project_id` indexes; new `hosted_project_storages` table with unique `project_id`. New schemas `Projects.RepositoryConnection` and `ProjectStorage.HostedProjectStorage`; new `ProjectStorage.Hosted` adapter implementing the Task 6 behaviour (`availability/2` → available, `prepare/3` joins the registration multi, `abort/2` no-op). `Project` gained `registration_changeset/2`, `rename_changeset/2`, `name_key/1`, and name validation; its base `changeset/2` now also derives the comparison key so every persisted project participates in uniqueness. `Projects` gained `get_project/2`, `default_project_name/2`, `register_project/3`, `rename_project/2`; `ProjectOnboardingAttempt` gained `consume_changeset/1`. New `ProjectDashboardLive` placeholder + `/projects/:id` route as the Task 8 seam; `ProjectConfirmationLive` placeholder replaced with the real surface.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 180 passed) covering default-name derivation and lowest-suffix allocation, natural display names, `NFKC` + case-fold comparison, boundary whitespace, blank/control-character rejection, case-insensitive conflict with inline feedback, cross-workspace independence, the repository-uniqueness constraint with duplicate feedback, hosted `Ecto.Multi` creation, device receipt validation (device mode creates no hosted storage; missing receipt is not registerable), idempotent retry, no-partial rollback, the rename operation, and the confirmation and dashboard LiveViews (review, create-and-route, edited/duplicate feedback, and all mount guards); `mix dialyzer` (0, 2 documented Ecto.Multi opaqueness skips); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (24 tests incl. the new project-dashboard protected-route check); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Seam handoffs: Task 8 replaces the `ProjectDashboardLive` placeholder at `/projects/:id` with the full dashboard (AC-30 visible repository/storage/connection proof, the post-creation rename control wired to `Projects.rename_project/2`, and connected/disconnected/temporarily-unavailable recovery in both the catalog rows and the dashboard); the placeholder already shows repository, storage mode, and connected status so the post-commit handoff is visible end to end but does not carry AC-30's ownership. `specs/02` supplies the device readiness receipt that makes device-mode registration reachable. The authenticated end-to-end browser scenarios are carried by Task 9.
- Local engineering decisions (non-behavioral): idempotency uses a nullable `projects.onboarding_attempt_id` back-reference (nilified if the attempt is later pruned) so a retry of a consumed attempt resolves to the already-created project rather than a second one; the accepted-default suffix retry is bounded (`@max_suffix_retries`, with a `System.unique_integer` fallback) and re-derives the next free default on each name-conflict retry, while an edited name never retries; the hosted storage `root` is an internal key built from the literal prefix `hosted/` and the project id, not a filesystem path or repository location; device-mode readiness is validated before the transaction through the shared `ProjectStorage.available?/2` contract, and hosted preparation is the only adapter joined into the multi (the production device storage adapter stays owned by `specs/02`); the LiveView distinguishes an accepted default from an edited name by comparing the trimmed submission to the suggested default; a second narrow `call_without_opaque` Dialyzer suppression was added for `projects.ex` (the same known Ecto.Multi/MapSet false positive already documented for `accounts.ex`).

### 2026-07-25 - Project dashboard, rename control, and connection recovery complete (Task 8)

- Completed: Replaced the `ProjectDashboardLive` placeholder with the real dashboard and delivered connection revalidation and the post-creation rename control. The dashboard shows the linked repository, selected storage mode, and current connection status, and is the destination the registration transaction routes to after commit. `Projects.Connections` revalidates GitHub access when the catalog or dashboard loads and on `Check again`: a repository still accessible stays `connected` (persisted state and display metadata refreshed); a missing installation, unapproved/pending request, authorization failure, or failed credential becomes `disconnected` without deleting the project; and a transient outage (rate limit, org policy, provider failure) shows a display-only `temporarily_unavailable` that never overwrites the last confirmed state. The catalog now shows a per-row connection status with a `Check again` recovery control, and the dashboard shows the status plus a disconnected/temporarily-unavailable notice with its own `Check again`. The post-creation name field is wired to the reusable `Projects.rename_project/2` operation with inline `Saved` confirmation and inline case-insensitive conflict feedback that never changes project or repository identity.
- Owns satisfied: AC-30 (post-commit dashboard shows repository, storage mode, and connection status), AC-37 (access loss keeps the project visible with a disconnected status and no stale credential in the payload), AC-38 (access return revalidates the same project back to connected without replacing it).
- Architecture: new `Projects.Connections` module (`catalog/3`, `project/4` with a `revalidate:` option; one access read per load reused across all connections; depends on `Accounts.valid_access_token/1` and `GitHubIntegration.check_repository_access/2` + `list_accessible_repositories/2`); `RepositoryConnection` gained `status_changeset/2` and `states/0` (only `connected`/`disconnected` are persisted; `temporarily_unavailable` is display-only). New shared `SddOrchestratorWeb.ConnectionStatus` component (`connection_badge/1`, non-color icon+text) used by the catalog and dashboard. `ProjectsLive` now loads catalog entries with per-row status and a `Check again` control; `ProjectDashboardLive` renders status, recovery notices, and the rename form. No schema migration was needed — the Task 7 `repository_connections.state`/`last_validated_at` columns already exist.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 193 passed) covering `Connections` revalidation (connected + metadata refresh, reconnect from disconnected, disconnect on missing installation / authorization failure / missing credential, transient outage preserving the last confirmed state, and persisted-state catalog read without a provider call) and the dashboard/catalog LiveViews (connected review, token absent from the payload, inline rename save with stable identity, case-insensitive rename conflict, disconnected indicator + `Check again` in both surfaces, and mount guards); `mix dialyzer` (0, 2 documented skips); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (24 tests); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Seam handoffs: Task 9 orchestrates the authenticated end-to-end browser scenarios across the whole flow (still environment-blocked locally without a live GitHub App). Task 10 enforces the approved GDPR data contract and retention across the delivered slice; Task 11 completes the security and observability review.
- Local engineering decisions (non-behavioral): revalidation runs on the connected mount (`revalidate: connected?(socket)`) so the initial paint shows the last confirmed persisted state without a provider call and the network read happens once; the access token stays inside `Connections` and the provider adapter and is never assigned to the socket, so it cannot reach the client payload (asserted for both the catalog and dashboard); a project with no repository connection (only possible for a Task 4 read-model row) renders as `disconnected` with no provider call; connected revalidation also refreshes the connection's display metadata (owner/name/full_name/url/visibility/organization) from the fresh read, keeping numeric repository identity stable; `org_restricted` is treated as transient (recoverable via SSO/policy) rather than a confirmed disconnect.

### 2026-07-25 - GDPR data-contract enforcement complete (Task 10)

- Completed: Enforced and proved the approved Slice 01 privacy contract. `Privacy.ProcessingInventory` (backed by `Privacy.DataProcessingRecord`) encodes the approved processing activity for every personal-data category — identity, session, credential, transient authorization state, workspace, project + repository metadata, transient onboarding state, and operational-security logs — each with its purpose, lawful basis, personal-data fields, access boundary, retention, rights behaviour, processors, and transfers; purpose limitation is explicit (no analytics/advertising/model-training activity). `Privacy.Retention` deletes authorization attempts (24h after creation), abandoned or consumed onboarding attempts (24h), and expired/revoked sessions (24h past expiry/revocation); `Privacy.RetentionPruner` runs it hourly under a PostgreSQL advisory lock so only one instance prunes and the work is idempotent (started in dev/prod, disabled in test where `Retention` is driven directly). `Privacy.Rights` serves the verified operator workflow: `export_account/1` gathers a credential-free access/portability export and `erase_account/1` cascades erasure from the account across identity, credentials, sessions, workspace, projects, connections, hosted storage, and onboarding attempts. `Privacy.DeploymentPrivacyProfile` is the release gate: an incomplete profile blocks release and names the missing evidence, while never blocking implementation or local verification.
- Owns satisfied: AC-41 (no product-analytics event, identifier, or metric — a data-store scan finds no analytics table and the inventory declares none; the transmitted side stays covered by the presentation-foundation browser test), AC-42 (verified rights request reaches active copies through operator export and cascading erasure), AC-43 (an incomplete deployment privacy profile blocks release only); entity:DataProcessingRecord, entity:DeploymentPrivacyProfile.
- Architecture: new `SddOrchestrator.Privacy` modules — `DataProcessingRecord`, `ProcessingInventory`, `Retention`, `RetentionPruner` (GenServer + advisory lock), `DeploymentPrivacyProfile`, and `Rights`. Supervision tree gains the pruner via `retention_children/0` gated on `config :sdd_orchestrator, :start_retention_pruner` (false in test). No migration or schema change — the pruner and rights workflow operate on existing tables, and account-owned tables already cascade on delete.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 212 passed) covering retention (auth-attempt/onboarding-attempt/session deletion past their windows, keeping live records, idempotent re-run), the processing-inventory coverage and purpose-limitation checks, the deployment-privacy release gate (incomplete → blocked with the missing fields named; complete → ready), the operator export (credential-free) and cascading erasure (all copies removed), and the analytics data-store absence scan; `mix dialyzer` (0, 2 documented skips); `mix deps.audit` (no vulnerabilities); `mix sobelow --config`; `npm --prefix assets run test:e2e` (24 tests); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Seam handoffs: Task 11 completes the cross-cutting security and observability review (structured-diagnostics redaction, CSP, failure-path secret absence). The deployment privacy profile's actual controller/vendor/region/transfer/notice values are supplied per deployment at the release gate; this task delivers the enforcement mechanism, not the deployment's evidence.
- Local engineering decisions (non-behavioral): the pruner is disabled in the test environment (`start_retention_pruner: false`) so it never races the Ecto sandbox or deletes another test's data — retention is proven by driving `Retention.prune_all/1` directly; auth attempts are pruned by `inserted_at` age (they carry no account id, so they are not reached by account erasure and are covered by time-based retention alone); consumed onboarding attempts are pruned 24h after consumption to drop redundant repository metadata once the project exists (the project keeps its own connection; `onboarding_attempt_id` nilifies); operational-security-log and backup retention (30d/35d) are deployment-infrastructure concerns recorded in the deployment privacy profile rather than DB pruning; the advisory-lock key is a fixed arbitrary constant so all instances contend for the same lock.

### 2026-07-25 - Onboarding navigation integration complete (Task 9)

- Completed: Proved the onboarding navigation end to end without re-owning any screen. A new `OnboardingFlowTest` drives the whole authenticated flow with the deterministic GitHub fake — empty-workspace continuation to the repository-access check, repository selection in the picker, the storage step, confirmation and atomic creation, and the new-project dashboard — asserting each navigation hop, that the same onboarding attempt id threads through every step (resumable continuity), that `Add project` from a non-empty catalog re-enters the same flow non-mutatingly, and that an interrupted flow resumes on the storage step with the selected repository preserved. No new screen or business behavior was implemented; the navigation was already wired by Tasks 3–8 and this task connects and verifies it.
- Owns satisfied: none (frontend integration proof; owns no unique acceptance criterion or data entity). Each workflow screen and its behavior remain owned by Tasks 3–8.
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 215 passed) including the new cross-screen end-to-end LiveView integration flow; `npm --prefix assets run test:e2e` (24 tests: unauthenticated entry, protected-route redirects for every onboarding and project route, theme/OS-fallback/device-local persistence, keyboard focus visibility, non-color status cues, and desktop+mobile layout fit in both themes). `mix dialyzer`, `mix deps.audit`, `mix sobelow --config`, `MIX_ENV=prod mix assets.deploy`, and `MIX_ENV=prod mix release` were unaffected (test-only change) and remain green from the Task 10 boundary.
- Failed checks: None.
- Environment-blocked (not an implementation defect): the desktop and mobile browser scenarios in both themes that exercise the authenticated flow (grant/pending recovery, keyboard repository search and selection, storage setup return, confirmation and naming, duplicate prevention, direct dashboard routing, rename, connected/disconnected recovery, focus placement after navigation, browser history, and text-fit across the authenticated screens) need a live GitHub-backed session. The dev server the Playwright harness drives uses the live provider and there is no fake-backed browser login, so these run in the secret-backed staging environment per the release gate. The deterministic integration flow above covers the same seams at the LiveView layer locally.
- Seam handoffs: Task 11 completes the cross-cutting security and observability review. The slice is not marked `Verified` while the authenticated browser scenarios remain to be run in staging.
- Local engineering decisions (non-behavioral): the integration test follows `push_navigate` hops by re-mounting each target path with the same `conn`, and drives screen events directly (`select`, `continue`, `select_mode`, form submit) so the flow is deterministic; the "octo" login selects the fake provider's normal scenario whose installation repositories include id 101 (`octo/example`), matching the default fixture repository metadata so revalidation on the dashboard resolves to connected.

### 2026-07-25 - Security and observability review complete (Task 11)

- Completed: Closed the cross-cutting security review. Implemented a strict Content-Security-Policy via `SddOrchestratorWeb.Router.put_content_security_policy/2`: `default-src 'self'` with same-origin-only script/style/font/img/connect/form-action, `object-src 'none'`, `frame-ancestors 'none'`, `base-uri 'self'`, and a per-request nonce so the single device-local pre-paint theme script (its tag now carries a `nonce` bound to `@csp_nonce`) runs while any injected or third-party inline script is blocked — no `unsafe-inline`/`unsafe-eval`. Removed the previously-deferred Sobelow `Config.CSP` ignore so `mix sobelow --config` passes with an empty ignore list. Confirmed the review's other properties by proof: provider access and refresh tokens are `redact: true` and are omitted from struct inspection and structured logs; the GitHub access token is absent from the catalog and dashboard client payloads (Tasks 4/8 tests); and the browser issues no external font, icon, analytics, or other third-party request (Task 2 e2e). Reviewed the vertical tasks' credential isolation, authorization, rollback, and failure controls without changing their implementation.
- Owns satisfied: none (cross-cutting security and observability review; owns no acceptance criterion or data entity).
- Passing proofs: `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 218 passed) including the new CSP header/nonce test and the credential log-redaction test; `mix dialyzer` (0, 2 documented Ecto.Multi opaqueness skips); `mix deps.audit` (no vulnerabilities); `mix sobelow --config` (exit 0, no ignored checks); `npm --prefix assets run test:e2e` (25 tests incl. the new strict-CSP browser check proving the nonce'd theme script still runs under the policy); `MIX_ENV=prod mix assets.deploy` and `MIX_ENV=prod mix release`.
- Failed checks: None.
- Local engineering decisions (non-behavioral): a static baseline `content-security-policy` is passed to `put_secure_browser_headers` (so Sobelow's `Config.CSP` static check recognizes a CSP is set) and is then replaced at runtime by the full per-request nonce policy in the following plug, since a nonce cannot be a compile-time plug option; the nonce is a base64 of 18 random bytes assigned as `@csp_nonce` and consumed only by the root layout on the dead render (the script runs once at page load); `connect-src 'self'` covers the same-origin LiveView WebSocket; `img-src 'self' data:` allows inline data images though none are used yet; the CSP is applied to the browser pipeline, so static assets served by `Plug.Static` (before the router) are unaffected.

### 2026-07-25 - Slice implementation complete and locally verified

- Completed: Tasks 1–11 are implemented and their attached proofs pass. The full deterministic gate is green — `mix check` (format, `compile --warnings-as-errors`, `credo --strict`, `mix test` = 218), `mix dialyzer` (0, 2 documented Ecto.Multi opaqueness skips), `mix deps.audit`, `mix sobelow --config` (no ignored checks), `npm --prefix assets run test:e2e` (25), `MIX_ENV=prod mix assets.deploy`, and `MIX_ENV=prod mix release` — and the end-to-end onboarding navigation is proven at the LiveView layer across every screen seam.
- Readiness (reported separately):
  - Product requirements: Approved (`requirements.md`).
  - Technical design: Approved (`design.md`).
  - Implementation: Complete for Tasks 1–11.
  - Verification: Locally verified for every deterministic proof. Two required checks are environment-blocked locally and run in the secret-backed staging environment: the tagged live GitHub App smoke test, and the authenticated desktop and mobile end-to-end browser scenarios (the dev server the Playwright harness drives uses the live provider with no fake-backed browser login). Their behavior is covered deterministically at the LiveView and provider-contract layers.
  - Release: Blocked. A public hosted deployment remains gated on a complete deployment privacy profile (AC-43 enforcement is implemented; the deployment's actual controller/processor/region/transfer/notice evidence is supplied per deployment) and on the coordinated first-release requirement that `specs/02-local-project-onboarding/` and the shared dependencies also pass their gates (AC-02).
- The slice stays `In Progress` (not `Verified`) until the staging browser and live smoke proofs run and the release gates clear.

### 2026-07-26 - Independent review checkpoint (review-spec)

- Verdict: Implementation of Tasks 1–11 is complete and every deterministic proof was independently re-run and reproduces green. The slice is correctly still `In Progress` (not `Verified`): two required checks remain runnable only in the secret-backed staging environment, and one of them (below) has no test artifact to run yet. No checkbox or the slice status was changed by this review.
- Re-run evidence (this environment, all green): `python3 .agents/scripts/test_validate_spec.py` (14 OK); `python3 .agents/scripts/validate_spec.py specs/01-github-project-onboarding` (passed — 43 ACs each with one owner, entities covered); `docker compose up -d postgres` (healthy, 127.0.0.1:5433); `mix setup`; `mix check` (format, `compile --warnings-as-errors`, `credo --strict` no issues, `mix test` = 218 passed, zero skips/excludes — the suite contains no `@tag`/`ExUnit.configure exclude`, so every test ran); `mix format --check-formatted` (clean); `mix dialyzer` (0 real errors, 2 documented skips); `mix deps.audit` (no vulnerabilities); `mix sobelow --config` (scan complete, no findings, no ignored checks); `npm --prefix assets ci` (0 vulnerabilities); `npm --prefix assets run test:e2e` (25 passed); `MIX_ENV=prod mix assets.deploy`; `MIX_ENV=prod mix release`. Hygiene: `cmp -s AGENTS.md CLAUDE.md` identical; `find -L .claude/skills -type l` no broken links; `git diff --check` clean; `.env`/`erl_crash.dump` untracked, no committed secrets.
- Findings:
  - Major (security; route to implement-spec / toolchain) — `bandit 1.12.0` carries a known HIGH advisory EEF-CVE-2026-65623 / CVE-2026-65623 / GHSA-vg8x-66vg-5pxh (quadratic-CPU DoS reassembling fragmented WebSocket messages), surfaced by `mix deps.get` and `mix hex.audit`. Bandit is the HTTP/WebSocket server and LiveView rides WebSockets, so the weakness is reachable. The canonical gate `mix deps.audit` reports clean, so no gate currently fails, and the 2026-07-25 Task 5 entry already disclosed and routed it as a toolchain follow-up — but it is still unremediated. Recommended action: bump Bandit to the patched release and re-pin `mix.lock`. Does not block local verification; should be fixed before the slice is trusted for release.
  - Minor (proof/gate integrity; route to implement-spec or update-spec) — the "tagged live GitHub App smoke test" referenced in the Status, Tasks 3/5/9, and the verification gate does not exist as a test artifact. `test/` contains no `@tag`/`@moduletag`, no ExUnit exclusion, and no smoke/live module; the deterministic `req_provider_test.exs` (`Req.Test` stubs) honestly covers the adapter contract, but there is nothing tagged for staging to actually run. Recommended action: either add an excluded/tagged live smoke test so the staging round trip has a concrete proof, or reword the gate to state the live round trip is deferred with the `Req.Test` contract test standing in. The slice being kept `In Progress` with this as an open `[ ]` gate line means the verdict is not overstated, only the scaffolding is.
  - Nit — `design.md` has no `## Status` header (requirements.md does); readiness is tracked in this file, so this is a consistency nit only.
- Readiness (reported separately): Product requirements Approved; Technical design Approved; Implementation Complete (Tasks 1–11); Verification locally verified for every deterministic proof, with the authenticated browser scenarios and the live GitHub round trip remaining staging-only (the latter lacking a test artifact per the Minor finding); Release Blocked (deployment privacy profile evidence + coordinated `specs/02` first-release gate, AC-02/AC-43).
- Blockers: None newly introduced. The Major Bandit advisory is routed to the toolchain owner; the Minor smoke-test gap is routed to the implementer or `update-spec`.

### 2026-07-26 - Review-fix implementation (Bandit advisory + tagged live smoke test)

- Completed: Applied the two routed findings from the 2026-07-26 review checkpoint.
  - Major (security) resolved: bumped `bandit 1.12.0 → 1.12.3` via `mix deps.update bandit` (within the existing `~> 1.5` constraint in `mix.exs`; no design decision changed — `design.md` pins Bandit only generically, the exact version lives in `mix.lock`). The resolver also carried a transitive patch bump `plug_crypto 2.1.1 → 2.2.0`. `mix hex.audit` now reports "No retired or security advisory packages found" (exit 0), clearing EEF-CVE-2026-65623; `mix deps.audit` remains clean.
  - Minor (proof integrity) resolved: added `test/sdd_orchestrator/github_integration/live_smoke_test.exs`, a `@tag :live` proof that signs a real RS256 app JWT from the secret-backed GitHub App key and asserts `api.github.com` accepts an app-authenticated read (`ReqProvider.list_pending_installation_requests/0`) — the real round trip the deterministic `Req.Test` contract cannot make. `test/test_helper.exs` now sets `ExUnit.configure(exclude: [:live])`, so the standard gate is unaffected; the proof runs in staging with `mix test --include live`. Opting into `:live` without `GITHUB_CLIENT_ID`/`GITHUB_APP_PRIVATE_KEY` skips the module with a reason (compile-time `@moduletag skip:`) rather than failing, so a missing credential never masquerades as a pass or a defect. The verification-gate line now names this concrete artifact.
- Passing proofs (full deterministic gate re-run after the lockfile change): `mix check` (format, `compile --warnings-as-errors`, `credo --strict` no issues, `mix test` = 218 passed, 1 excluded [`:live`]); `mix format --check-formatted` (clean); `mix dialyzer` (0 real errors, 2 documented skips); `mix deps.audit` (no vulnerabilities); `mix hex.audit` (no advisories); `mix sobelow --config` (scan complete, no ignored checks); `npm --prefix assets run test:e2e` (25 passed, server on Bandit 1.12.3); `MIX_ENV=prod mix assets.deploy`; `MIX_ENV=prod mix release`. Opt-in check: `mix test --include live` without staging secrets → `0 tests, 1 skipped` (exit 0).
- Failed checks: None.
- Status unchanged: the slice stays `In Progress`. These fixes close the two review findings but do not clear the staging-only proofs (the tagged live smoke test now exists but is still environment-blocked locally) or the release gates (deployment privacy profile evidence, coordinated `specs/02` first-release gate). No acceptance criterion, scope, or design decision changed.
- Local engineering decisions (non-behavioral): the live smoke test overrides the test-env `:github` config (which points at `FakeProvider`) to the real `ReqProvider` plus env-supplied credentials for the duration of the test and restores it on exit, preserving the shared `api_base_url`/`api_version`; the credential guard is evaluated at compile time because staging exposes the secrets before the suite compiles.

### 2026-07-26 - Live verification of the two staging-blocked proofs (real GitHub App)

- Completed: Ran both previously environment-blocked proofs locally against the real `orchestra-workflow` GitHub App with owner-supplied credentials.
  - Live smoke test: `mix test --include live` → `1 passed` (exit 0). The real RS256 app JWT (signed with the App's private key, client id as issuer) was accepted by `api.github.com` on the app-authenticated read — the round trip the deterministic `Req.Test` contract cannot make.
  - Authenticated onboarding flow: an assisted manual run drove a dev server (App key injected) through a real GitHub OAuth login and the full onboarding chain. Server-log + DB evidence confirms every workflow screen mounted in order — `EntryLive` → real `/auth/github` → callback → `ProjectsLive` → `RepositoryAccessLive` → `StorageSelectionLive` → `ProjectConfirmationLive` → `ProjectDashboardLive` — and that a real project (`alabenkhlifa/moveu`, hosted storage `ready`, repository connection `connected`) was created atomically in one transaction (project + repository_connection + hosted_project_storage), landing on its dashboard. No credential appeared in the dead-render HTML or LiveView client payload.
- Scope/caveat (why the gate boxes and slice status are NOT flipped): this was an assisted manual browser run verified via server logs, not the gate's specified automated desktop + mobile Playwright scenarios in both themes. Those automated scenarios (and the tagged live smoke test running in a secret-backed CI/staging environment) remain the formal proof form for the verification gate. The manual run is strong corroborating evidence that the authenticated flow works end to end with real credentials.
- Supporting change: added `*.pem` and `*.private-key` to `.gitignore` so the local GitHub App private key is never committed.
- Failed checks: None. The GitHub App private key is stored outside the repo and injected only at runtime; no secret is committed.
- Status unchanged: the slice stays `In Progress`. Release gates (deployment privacy profile evidence, coordinated `specs/02` first-release gate) are unchanged.
