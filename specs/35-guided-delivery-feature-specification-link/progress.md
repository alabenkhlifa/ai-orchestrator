# Guided Delivery Feature-Specification Link Progress Log

### 2026-08-10 - Task 2 implemented and verified: owner-facing specification-link control on the feature detail screen

- Completed: AC-01, AC-05. Added `Delivery.Features.available_specifications/4` (owner-only, lists the project's current specifications minus any already linked to a different feature, keeping the calling feature's own link included). Wired `feature_detail_live.ex`: `assign_available_specifications/4` in the `assign_feature/4` pipe (owner-only, `[]` on any `{:error, _}` such as an unreachable store), two `handle_event("link_specification", ...)` clauses mirroring the existing assignment handler's blank/non-blank split, `apply_specification_link/2` and `specification_link_message/1` mirroring `apply_assignment/2`/`assignment_message/1`, and a new owner-gated `data-specification-link` section immediately after the existing assignment section, with `data-specification-link-select` / `data-specification-link-error` test hooks.
- No discrepancies against the brief: the target file's `owner?`, `storage_authority/1`, the assignment section's HTML/handler shape, and the `assign_feature/4` pipe's `:project`-then-derived-assigns ordering all matched what the brief described.
- New test file `test/sdd_orchestrator_web/live/feature_specification_link_live_test.exs` (6 tests): owner sees the control with "Not linked" selected and every current specification listed; a specification already linked to another feature is excluded from the picker; a non-owner participant gets no `data-specification-link` section at all; the owner links, and separately clears, a specification through `render_change` on `#specification-link-form`, each confirmed against the stored `Feature` and (for linking) `fetch_by_specification/2`; a non-owner sending the `"link_specification"` event directly is redirected to `/projects` (mirroring the existing forged-review-decision pattern) and the feature is unchanged.
- Playwright/e2e-bootstrap scenario: skipped. The `"features"` e2e-bootstrap case (`e2e_bootstrap_controller.ex`) seeds no current specification today, so adding a browser scenario would mean extending that bootstrap case in a way this task's focused proof scope cannot verify (`npm run test:e2e` is out of scope here). Left for slice verification.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/feature_specification_link_live_test.exs test/sdd_orchestrator_web/live/feature_detail_live_test.exs test/sdd_orchestrator/delivery/feature_specification_link_test.exs` — exit `0`.
- 114 tests passed. Also ran `mix format --check-formatted` and `mix compile --warnings-as-errors` scoped to the touched files — both clean.
- Both slice tasks are now complete. The Verification Gate (full `mix check`, browser matrix, production proof, validators) has not been run and remains open.

### 2026-08-10 - Task 1 implemented and verified: Feature gains its owner-only specification link

- Completed: AC-02, AC-03, AC-04. `Feature` gained `specification_id` (migration `20260810100000_add_specification_id_to_features.exs`, nullable, unique per `(project_id, specification_id)` where not null), `Feature.specification_link_changeset/2`, and `Delivery.Features.link_specification/5`, `unlink_specification/3`, `fetch_by_specification/2`. `## Status` set to `In Progress`.
- Two corrections to the brief's assumed mechanism, found and fixed during implementation, not left as silent workarounds: (1) `authority` is not the `{:hosted, id} | {:device, workspace}` tagged-tuple shape `RepositoryPilots` uses — every module actually under `lib/sdd_orchestrator/delivery/` (`Readiness`, `DeliveryStore`, `Start`, `ArtifactStore`) types it as `PersonalWorkspace.t() | DeviceWorkspace.t()` directly, matching what `SpecificationStore.get_current/3` itself pattern-matches on; `Features.authority/0` now follows that same existing local convention. (2) `Feature.from_value/1` has no `@fields`/`@value_keys`/`MapSet` exact-keyset machinery — it validates per-field with individual `true <- ...` clauses that already tolerate extra or missing keys; backward compatibility for a pre-migration stored value (missing the `"specification_id"` key entirely) is handled with `Map.put_new(value, "specification_id", nil)` before validation, not by inventing machinery that doesn't exist.
- Notable finding, not a defect in this task: `Delivery.Features`/`Feature` (the whole existing board/lifecycle system, from `specs/07`) is hosted-only end to end — every existing function goes through `ParticipantGuard` → `Participation.Boundary` with no device branching, and a device-authoritative project's board data lives in an entirely separate `Delivery.DeliveryStore.Device` abstraction backed by the worker-owned local store, bypassing `Delivery.Features`/`Repo` entirely. This task's new functions correctly inherit that same hosted-only boundary (consistent with everything else in the module), so a device-authoritative project has no route to this capability today. This is a pre-existing limitation of the underlying `Feature` system, not introduced here, and out of this task's scope to fix — flagging it because `specs/15-repository-sdd-kit-integration`'s Task 2 (the eventual consumer of this capability) supports device-authoritative pilots for everything else, so its device path may find no linked feature reachable via this capability at all. Left for `specs/15`'s own Task 2 preflight to confirm and handle (likely as the same "not yet eligible" not-linked result Task 1 already returns for the hosted case) rather than resolved here.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/feature_specification_link_test.exs test/sdd_orchestrator/delivery/feature_lifecycle_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly, 25 passed with a real exit 0, plus `mix compile --warnings-as-errors` and `mix format --check-formatted` on every touched file — both clean. Confirmed the one repo-wide `mix format --check-formatted` failure (`test/sdd_orchestrator/participation/identity_lifecycle_compatibility_test.exs`) is pre-existing on `main` and untouched by this task. Read every delivered diff directly rather than trusting the sub-agent's self-report.
- Capability graph: `capability:guided-delivery-feature-specification-link` is ready — its named provider task (Task 1), its complete focused proof, and this readiness write-back are all satisfied. Consumer: `specs/15-repository-sdd-kit-integration#Task 2` (on its own `slice/15` branch, not yet rebased onto this one).
- Failed checks: None.
- Remaining: Task 2 (owner-facing link control in the feature detail LiveView) is next, `Depends on: Task 1`, now satisfied.
