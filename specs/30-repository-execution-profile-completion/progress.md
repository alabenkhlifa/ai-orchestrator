# Repository Execution Profile Completion Progress Log

### 2026-08-09 - Task 2 complete: deterministic execution-profile capability published; slice Verified

- Completed: Added `SddOrchestrator.ManagedRuntimeProfile` with `build/3`, building one deterministic, allowlisted, digest-bound managed-runtime value on demand from the exact approved profile (Slice 14), the selected pilot (Task 4), and the computed readiness (Task 12). Nothing new is persisted — no migration, no schema — consistent with readiness's own on-demand precedent. `build/3` accepts only a hosted or device `authority()`, not a participant `viewer()`, because resolving the pilot's specification-revision freshness needs specification-store authority a participant is never granted (`RepositoryPilots.store_authority/1`'s own precedent); "separately approved managed-runtime consumers" is a distinct actor class from the existing participant-read surfaces.
- Mechanism — staleness at publish time, not just at selection time: `build/3` refuses `:stale_profile` when the pilot's bound profile id/version is no longer the latest approved profile (a newer profile was approved after pilot selection), and refuses `:stale_pilot_revision` when the pilot's specification revision is no longer the specification's current head (re-checked live via `SpecificationStore.get_current/3`, matching the design's "obtain authoritative specification content only through its existing capability at execution time"). Task 4 only guarantees freshness at the moment of selection; this task re-verifies it at the moment of publication, per the design's stated risk ("A stale profile or pilot revision may be published").
- Mechanism — readiness is staged, not gated on: the value always embeds the full four-axis `RepositoryReadiness` (as `"ready"` or a `"blocked:"`-prefixed reason string) rather than refusing to build when `agent_execution` or `release` is blocked. Only the two structural preconditions (approved profile, selected pilot) and the two staleness checks above gate `build/3` itself; a blocked axis is a value a downstream consumer reads, not a build refusal — matching the business rule "no state may imply agent-execution readiness" and AC-09 (assistant readiness stays independent of an agent-execution conflict). Proven directly: a test approves a profile, selects a pilot, then completes a second assessment with a different commit without approving a new profile — `build/3` still succeeds while `readiness["agent_execution"]` reads `"blocked:stale_base_revision"`; a parallel test does the same for an unreliable required-check contract and `readiness["release"]`.
- Mechanism — runtime-skill versioning: no version metadata exists anywhere for `.agents/skills/*/SKILL.md` today, so `runtime_skill_refs` versions each of the four canonical skills (`add-spec`, `update-spec`, `implement-spec`, `review-spec`) by the sha256 content digest of its own `SKILL.md`, computed at **compile time** via `@external_resource` + a module attribute (not a runtime file read), so the value never depends on `.agents/` existing in a deployed release and the version updates automatically whenever a skill's instructions change. Verified against a real `MIX_ENV=prod mix assets.deploy` compile, not just dev.
- Mechanism — allowlist scope: the value carries the operational execution contract (repository location, root, base revision, commands, required checks, allowed scope, pilot specification/revision identity, readiness, runtime-skill refs) and deliberately excludes the approved profile's `instruction_precedence`, `gaps`, `conflicts`, and `multi_root_blockers` — those are Slice 14's own review/audit evidence, and readiness's reason codes already communicate the fact of a conflict or gap without re-exporting the raw internal lists, keeping the allowlist to what a managed-runtime consumer actually needs.
- Mechanism — integrity: `to_value/1`/`from_value/1` mirror the existing `RepositoryExecutionProfile`/`RepositoryPilotSelection` round-trip convention (exact key-set allowlist, including one level of recursion into `readiness` and each `runtime_skill_refs` entry) plus a self-referential sha256 digest recomputed and compared on `from_value/1`, so an unknown field, a missing field, or a tampered digest all refuse restoration. Proven against a real `Jason.encode!/1` → `Jason.decode!/1` round trip as the downstream-compatibility fixture, since JSON is the plausible wire format a real managed-runtime consumer would receive.
- Discovered and fixed, out of this task's own scope but blocking the slice-wide `mix credo --strict` gate: two pre-existing alias-ordering violations in Task 12's own test files (`test/sdd_orchestrator/repository_readiness_test.exs:125` and `test/sdd_orchestrator/repository_assessments/assessment_store_test.exs:14`), present since Task 12's commit and unrelated to any file this task owns. Fixed by reordering each file's alias block (moving each multi-alias `{...}` group before the single `Devices` aliases it was interleaved with) — no behavioral change, `git diff` on both files is alias-line-order only.
- Proof receipt: `Task 2` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 2` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/managed_runtime_profile_test.exs` — exit `0`. 15 passed.
- Capability readiness: `capability:repository-execution-profile` is ready after this task; its focused proof passed and every deterministic slice-gate receipt below is recorded, so publication is complete.
- Verification gate — proof receipts (all `scope: Broad`, run through `python3 .agents/scripts/run_proof.py slice --`):
  - `mix check` — exit `0`. 3184 tests passed (6 properties, 3178 tests), 1 excluded.
  - `mix dialyzer` — exit `0`. Total errors: 23, Skipped: 23 (all pre-existing `.dialyzer_ignore.exs` entries; unchanged by this task).
  - `mix deps.audit` — exit `0`. No vulnerabilities found.
  - `mix sobelow --config` — exit `0`.
  - `npm --prefix assets ci` — exit `0`.
  - `npm --prefix assets run test:e2e` — exit `0`. 122 passed.
  - `env MIX_ENV=prod mix assets.deploy` — exit `0`.
  - `env MIX_ENV=prod mix release --overwrite` — exit `0`. (An earlier attempt without `--overwrite` hit an interactive "Release already exists" prompt from a prior local build in this worktree; re-run with `--overwrite` for a clean non-interactive receipt. No code implication — release directories are gitignored build output.)
  - `python3 .agents/scripts/validate_spec.py specs/30-repository-execution-profile-completion` — passed.
  - `python3 .agents/scripts/validate_spec.py --all specs` — passed (34 specifications).
  - `python3 .agents/scripts/split_progress_log.py --check` — passed.
- Failed checks: None.
- Implementation readiness: complete — all four tasks done, all owned acceptance criteria (AC-01, AC-02, AC-08, AC-09, AC-10, AC-11) proven.
- Local-verification readiness: complete — the full deterministic verification gate above passes.
- Release readiness: not evaluated here. Live configured worker smoke proof per deployment profile and deployment-specific controller/processor/region/transfer/notice/retention-enforcement/incident/accountable-privacy evidence remain open release-gate items, unaffected by this task.
- Remaining: None for this slice. Merge `slice/30-repository-execution-profile-completion` into `main`.

### 2026-08-09 - Task 1 complete: privacy, lifecycle, and storage-boundary controls

- Completed: Added `test/sdd_orchestrator/privacy/repository_pilot_and_readiness_privacy_test.exs`, proving AC-01 for this slice's two new surfaces — `RepositoryPilotSelection` (Task 4) and the computed, never-persisted `RepositoryReadiness` value (Task 12) — with real deletes and real `Retention`/`Rights` calls rather than mocks: a closed field-level allowlist over both shapes, hosted/device-authoritative parity with no durable hosted copy for device data, deletion reaching the pilot selection through the real project cascade, the real profile-delete cascade, and `Rights.erase_account/2`, `Retention.prune_all/1` correctly not needing to reach any of these records (they are bounded by project lifecycle, not a timer), and neither boundary logging or inspecting its own data. It also confirms rather than re-proves Slice 14's own existing locality/minimization coverage for `RepositoryAssessment` and `RepositoryExecutionProfile`.
- Discovered and fixed a genuine, pre-existing deletion gap in scope for this task's owned "project ... deletion" surface: `Devices.DeviceStore.Local`'s `delete_project/1` purged only `{:project, id}`, `{:package_provenance, id}`, and `{:specification, id, _}` DETS keys — it never swept `{:repository_assessment, id, _}`, `{:repository_assessment_proposal_envelope, id, _}`, `{:repository_execution_profile, id, _}`, or `{:repository_pilot_selection, id}`, so deleting a device project left them orphaned in the local store indefinitely. Fixed with a generic `project_scoped_keys/3` sweep over the shared `{tag, project_id, sub_id}` key shape, plus a direct delete for the pilot selection's `{tag, project_id}` shape, and extended `Devices.delete_project/1`'s documented return map with `deleted_repository_assessments`, `deleted_repository_execution_profiles`, and `deleted_pilot_selection`. This also closes the same gap for Slice 14's own device-authoritative assessment and profile records, which this task's privacy proof covers but does not otherwise redefine.
- Proof receipt: `Task 1` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 1` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/privacy/repository_pilot_and_readiness_privacy_test.exs` — exit `0`. 11 passed.
- Regression check (not the task's own proof receipt, run because the device-store fix touches shared production code beyond this task's new test file): `mix test test/sdd_orchestrator/devices/device_store_test.exs test/sdd_orchestrator/repository_pilots_test.exs test/sdd_orchestrator/repository_readiness_test.exs test/sdd_orchestrator/repository_assessments/assessment_store_test.exs test/sdd_orchestrator/repository_assessments/repository_execution_profile_test.exs` — exit `0`. 46 passed.
- Observation, not active work: `Privacy.Rights.export_account/1` and `erase_account/2` do not cover the repository-assessment/profile/pilot/readiness domain at all (no export entry, though `erase_account/2` still removes the rows via the real FK cascade proven above). This is consistent with the sibling Delivery domain (Features, AgentRuns, Evidence, ReviewDecisions), which is equally absent from both — a pre-existing, project-wide condition outside this task's narrow AC-01 ownership. Recorded here for the next specification refinement to decide whether it warrants a dedicated export-coverage task, rather than deciding it silently inside a focused task.
- Capability readiness: no new capability edge; `Task 2` is now the next executable task.
- Failed checks: None. The individual specification validator, the global capability graph, and the progress-log split check pass.
- Remaining: Implement Task 2 with focused proof and a task-boundary commit, run the full slice verification gate, then publish `capability:repository-execution-profile`.

### 2026-08-05 - Task 12 complete: independent repository readiness and verification blockers

- Completed: Added the `SddOrchestrator.RepositoryReadiness` context with `evaluate/3`, deriving four independent axes (`assistant`, `specification`, `agent_execution`, `release`) plus `earliest_blocked_stage` from the approved profile, the selected pilot, and the project's latest completed assessment. Added a new `AssessmentStore.latest_completed/2` capability (hosted Ecto query and device DETS query, both additive siblings of the existing `latest/2`) because the profile a readiness value reads may be bound to an older completed assessment than the project's newest (possibly pending or failed) one. Extended `RepositoryPilotLive` with a read-only readiness section, visible to owner and participant on both hosted and device authority, with `data-readiness-*` attributes per axis and a reason-code label map. No Slice 14 file, `repository_pilots.ex`, `verification_completion.ex`, or `router.ex` was touched.
- Proof receipt: `Task 12` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 12` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 12` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_readiness_test.exs test/sdd_orchestrator/repository_assessments/assessment_store_test.exs test/sdd_orchestrator_web/live/repository_pilot_live_test.exs test/sdd_orchestrator/delivery/verification_completion_test.exs` — exit `0`. 102 passed, including the unmodified existing `:required_check_contract_unknown` regression that proves the run-verification denial AC-11 requires is unchanged.
- Proof receipt: `Task 12` — scope `Focused` — command `NODE_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/assets/node_modules E2E_BASE_URL=http://localhost:4031 E2E_MODE=true E2E_DATABASE_NAME=sdd_orchestrator_e2e_s30_task12 E2E_DEVICE_STORE_PATH=/tmp/sdd-orchestrator-s30-task12-e2e.dets E2E_ISOLATED_RUN=true MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/_build/e2e-task12 node /Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/assets/node_modules/@playwright/test/cli.js test e2e/repository-readiness.spec.js --config=playwright.config.js` — exit `0`. Desktop and mobile passed with no serious axe violations; the scenario reuses the existing `repository_pilot` e2e-bootstrap fixture (unmodified) to prove the pilot-selection cascade and the agent-execution/release independence live in the browser, not just in unit tests. Port 4031 is reserved for this task so it cannot collide with Task 4's 4030.
- Mechanism — axis gating: assistant blocks only on `:no_approved_profile`; specification additionally blocks on `:no_pilot_selected` (cascading from assistant); agent_execution additionally blocks on `:stale_base_revision`, `:changed_root`, `:unresolved_evidence_conflict`, or `:unsupported_multi_root_boundary` (checked in that order, cascading from specification); release additionally blocks only on `:unreliable_required_check_contract` (`required_checks == []` or `"missing_required_checks" in gaps`, cascading from specification). Agent-execution's own blockers do not propagate to release and release's own blocker does not propagate to agent-execution — proven directly by both the unit suite and the browser scenario, matching AC-08's 1:1 condition-to-state pairing and the requirements' "independent values" language.
- Mechanism — latest completed assessment: `latest/2` returns the newest assessment regardless of state, but an approved profile is bound to whichever assessment was completed and current at approval time, which a later pending or failed restart can leave behind. `latest_completed/2` mirrors `latest/2` with a `state == "completed"` filter in both adapters; the device adapter filters `repository_assessment_values/2`'s full in-memory list before `Enum.max_by/3`, exactly mirroring the existing `latest_repository_assessment/2` private helper it sits beside.
- Mechanism — approved profile read: duplicated `RepositoryPilots`' own private `current_profile/2` convention (`profile_review/3` → `List.last(profiles)` → `{:error, :no_approved_profile}` on empty or unavailable) rather than exporting it from either module, consistent with Task 4's precedent of not reaching into Slice 14.
- Mechanism — no distinct unauthorized case: `evaluate/3` never returns `{:error, _}`. Every read it is built from — `profile_review/3`, `RepositoryPilots.current/3` — already folds an unauthorized or unsupported viewer into the same `:not_found`/empty-profile shape a legitimately-empty project produces, so readiness follows the same fail-closed convention rather than inventing a distinction those reads do not make.
- Capability readiness: `capability:repository-profile-readiness` is ready after this task. Its focused proof passed and this write-back is recorded, so `Task 1` is now the next executable task.
- Environment observation: the focused test run logged an Ecto migration-order warning (`20260730010000` applied after `20260805100000` in this worktree's partitioned test database) because the rebase onto updated `main` brought migrations the test database's prior partition state had not seen in file order. The run still exited `0` with all 102 tests passing; no schema or code change resulted, and this is not a Task 12 defect.
- Failed checks: None. The individual specification validator, the global capability graph, and the progress-log split check pass.
- Remaining: Implement Tasks 1 and 2 in dependency order with focused proof and task-boundary commits, run the full slice verification gate, then publish `capability:repository-execution-profile`.

### 2026-08-05 - Task 12 agreement corrected before implementation

- Completed: No implementation. The Task 12 preflight found that three parts of the recorded agreement could not be built from the evidence the approved profile carries, so implementation stopped and the specification was corrected instead. Task 12 was briefly marked `In Progress` and returned to unstarted; it is now executable again under the corrected contract.
- Discovery — conflict vocabulary: the assessment produces exactly one conflict code, `ambiguous_command_evidence`, and the words "safety" and "assistant" appear nowhere in `lib/`. AC-08 and the readiness rules had required distinguishing an unresolved instruction conflict from a safety conflict, which no available data supports and which `specs/14-repository-execution-profile/` owns deriving.
- Discovery — staleness reference: nothing compares an approved profile's base revision against live repository state, and obtaining the live commit requires an authorized worker plus a confirmed disclosure digest through Slice 14's binding lifecycle. The agreement had not said what staleness is measured against.
- Discovery — check contract: the run-verification contract is read from application configuration as `%{"name", "command"}` maps, while the approved profile carries bare command strings, and no code links them. AC-11 had implied this continuation owns run-result evaluation, which `specs/07-guided-specification-delivery/` owns.
- Decisions accepted: readiness consumes the single unresolved-evidence conflict meaning and does not claim a distinction its evidence lacks; staleness compares the profile's base revision and root against the project's latest completed assessment, so readiness needs no worker; and readiness judges the approved profile's own required-check list while proving, without modifying, the existing verified-completion and `Ready for review` denial.
- Scope classification: focused specification, narrowed rather than broadened. No new outcome, workflow, integration, trust boundary, or data lifecycle was added, and no capability edge, slice-size, task-size, or proof-scope declaration changed. Task 12 still owns AC-08, AC-09, and AC-11.
- Spec updates: rewrote AC-08 and AC-11, replaced the conflict and staleness business rules and added rules for the staleness reference, the conflict meaning, and the profile-carried check contract, excluded the conflict taxonomy and the manifest binding, recorded two new design decisions with their consequences, added two risks about over-reading a narrowed vocabulary and assessment-relative staleness, and restated Task 12's owned surfaces and proof. Also recorded that the readiness value is derived on demand rather than persisted, so no stored readiness can go stale behind the records it summarizes.
- Readiness: product requirements and technical design are corrected and approved; Task 12 implementation is unblocked and unstarted; verification and release are unchanged, with deployment-specific evidence still release-blocking only.
- Failed checks: None. The individual specification validator, the global capability graph, and the progress-log split check pass.

### 2026-08-05 - Task 4 complete: one authoritative pilot specification revision

- Completed: Added the `SddOrchestrator.RepositoryPilots` context with `selectable_specifications/3`, `select/4`, and `current/3`; the `RepositoryPilotSelection` schema and its `repository_pilot_selections` table; the `PilotStore` behaviour with hosted and device adapters; `RepositoryPilotLive` on new `/projects/:id/pilot` and `/local/projects/:id/pilot` routes; and additive device-store support for the `{:repository_pilot_selection, project_id}` record. The implementation follows the mechanism recorded in the previous entry without change to its ownership, reference-not-copy, staleness, participant-boundary, or web-surface decisions.
- Proof receipt: `Task 4` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 4` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_pilots_test.exs test/sdd_orchestrator_web/live/repository_pilot_live_test.exs` — exit `0`. 17 passed.
- Proof receipt: `Task 4` — scope `Focused` — command `NODE_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/assets/node_modules E2E_BASE_URL=http://localhost:4030 E2E_MODE=true E2E_DATABASE_NAME=sdd_orchestrator_e2e_s30_task4 E2E_DEVICE_STORE_PATH=/tmp/sdd-orchestrator-s30-task4-e2e.dets E2E_ISOLATED_RUN=true MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/_build/e2e-task4 node /Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s30/assets/node_modules/@playwright/test/cli.js test e2e/repository-pilot.spec.js --config=playwright.config.js` — exit `0`. Desktop and mobile passed with no serious axe violations. Port 4030 is reserved for this task so it cannot collide with the Slice 14 worktree's 4014 and 4015.
- Mechanism — approved profile: Slice 14 exposes no single-profile read; `profile_review/3` returns its profiles ascending by version, so the pilot binds `List.last(profiles)` and refuses with `:no_approved_profile` on an empty list. Taking the highest version is a read convention over Slice 14's documented monotonic versioning, not a second profile contract, and Slice 14 remains the only writer.
- Mechanism — revision digest: `SpecificationSnapshot.Entry` carries no digest, so the selectable list cannot supply one. `select/4` resolves the head through `SpecificationStore.get_current/3` at commit and takes `revision.content_digest` from there, which makes the same call both the digest source and the staleness check.
- Mechanism — fail-closed shape: `store_authority/1` intentionally has no catch-all clause. `authorize_project/2` has already refused every authority that is not a hosted owner or the owning device workspace, so Elixir 1.20 proves a catch-all dead and `--warnings-as-errors` rejects it. The fail-closed answer is unchanged and still `{:error, :unsupported_authority}` from `authorize_project/2`; a regression test asserts it.
- Mechanism — selection attributes: `select/4` accepts both atom and string keys because LiveView params arrive as strings and context callers use atoms. Both shapes are covered by tests.
- Mechanism — unused option: `selectable_specifications/3` keeps its `opts` parameter for interface symmetry but does not read it, because it touches only the specification store, which takes no store override. No `:specification_store` seam was invented to justify the parameter.
- Capability readiness: `capability:repository-approved-pilot` is ready after this task. Its focused proof passed and this write-back is recorded, so `Task 12` is now the next executable task.
- Ownership decision: navigation into `/projects/:id/pilot` is owned by no Slice 30 task, so the routes exist and nothing links to them yet. This repeats the Slice 14 profile-screen situation and is recorded as an observation for the next specification refinement, not as active work.
- Failed checks: None. The individual specification validator, the global capability graph, and the progress-log split check pass.
- Environment incident: The worktree needed `mise trust` once before the browser proof could start its server; the first attempt exited `1` on untrusted config files. No implementation change resulted, and the receipt above is from the passing run.
- Remaining: Implement Tasks 12, 1, and 2 in dependency order with focused proof and task-boundary commits, run the full slice verification gate, then publish `capability:repository-execution-profile`.

### 2026-08-04 - Task 4 implementation mechanism resolved before a paused start

- Completed: No implementation. The task preflight passed and the Task 4 engineering mechanism below is settled, so a resuming session does not need to re-derive it. The user paused the session before any code was written; `tasks.md` therefore stays `Not Started` and Task 4 carries no status line.
- Environment: The slice worktree `../sdd-orchestrator-s30` is created on `slice/30-repository-execution-profile-completion` from `main` at `572f546` and primed. PostgreSQL is healthy on port 5433 and the worktree compiles.
- Mechanism — ownership: Task 4 lands in a new `SddOrchestrator.RepositoryPilots` context with its own `PilotStore` hosted and device adapters. Slice 14's context, schemas, and LiveView stay untouched, so the consumer never redefines the provider contract, and the pilot proof can fail independently of the profile-review proof.
- Mechanism — reference, do not copy: the pilot persists as a pointer only — `specification_id`, `revision_id`, `revision_digest`, plus the bound `profile_id` and `profile_version` — in a new `repository_pilot_selections` table with one row per project. It follows the established `readiness_assessments` convention of plain `:string` columns with no foreign key, because a device-authoritative revision has no hosted row. The record carries no title and no document field, which keeps "stores only stable identifiers" literally true; `selectable_specifications/3` strips the document text that `SpecificationSnapshot.Entry` carries before it leaves the context.
- Mechanism — staleness and authority: staleness is decided at commit by comparing the submitted `revision_id` against a freshly resolved current revision. The context bridges the two authority shapes, mapping `{:hosted, account_id}` through `Accounts.get_personal_workspace/1` and passing `%DeviceWorkspace{}` straight to `SpecificationStore`.
- Mechanism — participant boundary: participants read the stored pointer through `current/3` but never reach the specification store, whose hosted reads are owner-only. Implementing that store's unused `SpecificationAuthorizationPolicy` seam was rejected because it would widen `specs/09-project-specification-storage/`'s access contract from a consumer.
- Mechanism — web surface: Task 4 adds its own `RepositoryPilotLive` on new `/projects/:id/pilot` and `/local/projects/:id/pilot` routes rather than extending Slice 14's verified profile screen. Navigation into the new screen is owned by no Slice 30 task; it is recorded here as an observation for the next specification refinement, not as active work.
- Mechanism — focused browser proof: `assets/run-e2e.js` ignores command-line selectors and always runs the full desktop and mobile matrix, so a focused browser proof must invoke the Playwright CLI directly with per-task `E2E_BASE_URL`, `E2E_DATABASE_NAME`, `E2E_DEVICE_STORE_PATH`, `E2E_ISOLATED_RUN`, `E2E_MODE`, and `MIX_BUILD_PATH`, wrapped in `python3 .agents/scripts/run_proof.py task --task 4 -- ...`, matching the receipts in `specs/14-repository-execution-profile/progress.md`.
- Failed checks: None. The individual specification validator, the global capability graph, and the progress-log split check pass.
- Remaining: Implement Task 4 against the mechanism above, then Tasks 12, 1, and 2 in dependency order with focused proof and task-boundary commits.

### 2026-08-04 - Unblocked after the profile-review handoff landed

- Completed: `specs/14-repository-execution-profile/` is `Verified` and merged to `main` at `572f546`, so its Task 11 readiness write-back is recorded and `capability:repository-profile-review` is available. `python3 .agents/scripts/capability_index.py --capability` reports `ready` for all four required capabilities: `repository-profile-review`, `project-specification-store`, `project-storage-governance`, and `project-specification-governance`.
- Status transition: `tasks.md` moved from `Blocked` to `Not Started`. Removed Task 4's stale `Status: Blocked` line, cleared the blocked decision, and checked the verification-gate item for the Slice 14 profile-review prerequisite. `requirements.md` stays `Approved` with no open questions.
- Scope classification: Unchanged focused standard specification. No requirement, design decision, acceptance criterion, capability edge, task boundary, size declaration, or proof expectation changed; this correction only removed a stale prerequisite blocker.
- Remaining: Implement Tasks 4, 12, 1, and 2 in dependency order with focused proof and task-boundary commits, run the full slice verification gate, then publish `capability:repository-execution-profile`.
- Failed checks: None.

### 2026-08-04 - Pilot and readiness moved behind the profile-review handoff

- Completed: Preserved Slice 14 authority for assessment, cache, proposal-envelope, immutable profile, and owner review; moved the still-unimplemented stable pilot-selection and independent-readiness tasks here; and replaced the two unavailable provider edges with one focused `capability:repository-profile-review` prerequisite. Tasks 4 and 12 preserve their stable labels and acceptance-criterion ownership.
- Scope classification: Focused standard completion specification with four tasks and a four-task critical path. Pilot selection establishes the exact feature context for readiness, then governance and deterministic final publication follow.
- Remaining: Wait for Slice 14 Task 11, implement Tasks 4, 12, 1, and 2 in dependency order with focused proof and task-boundary commits, run the full slice verification gate, and publish final readiness.
- Failed checks: None. The slice remains blocked only on one unavailable implementation capability; deployment-specific evidence remains release-blocked.
- Spec updates: Added pilot and readiness requirements, design surfaces, AC-08 through AC-11 ownership, stable Tasks 4 and 12, project-specification-store consumption, three capability providers, updated governance coverage, and the profile-review dependency without implementing application code.
