# AI Runtime Governance Progress Log

### 2026-08-05 - Slice verification gate run; one gate item blocked (slice)

- Completed: Ran the slice verification gate for the first time in this slice's history. Three real defects surfaced and were fixed; every gate command now passes except the live OpenAI Codex smoke test, which cannot pass locally as written. The slice therefore stays `In Progress` and is not marked `Verified`.
- Proof receipt: slice — scope `Broad` — command `mix check` — exit `0`. 2983 passed, 6 properties, 2977 tests, 1 excluded; `credo --strict` found no issues across 7477 modules and functions.
- Proof receipt: slice — scope `Broad` — command `mix dialyzer` — exit `0`. Total errors 23, skipped 23, unnecessary skips 0, so no live finding remains and no suppression was added.
- Proof receipt: slice — scope `Broad` — command `mix sobelow --config` — exit `0`. Scan complete with no findings.
- Proof receipt: slice — scope `Broad` — command `mix deps.audit` — exit `0`. No vulnerabilities found.
- Proof receipt: slice — scope `Broad` — command `npm --prefix assets ci` — exit `0`. No vulnerabilities.
- Proof receipt: slice — scope `Broad` — command `npm --prefix assets run test:e2e` — exit `0`. 120 desktop and 120 mobile scenarios passed, including all five AI Connections scenarios on both projects: worker guidance for missing, unavailable and incompatible workers, multiple credential-local connections linked then renamed and revoked, reachability from Projects with visible keyboard focus, desktop and mobile viewport fit, and no serious or critical WCAG violations.
- Proof receipt: slice — scope `Broad` — command `env MIX_ENV=prod mix assets.deploy` — exit `0`.
- Proof receipt: slice — scope `Broad` — command `env MIX_ENV=prod mix release --overwrite` — exit `0`. Release created at `_build/prod/rel/sdd_orchestrator`.
- Proof receipt: slice — scope `Broad` — command `python3 .agents/scripts/test_validate_spec.py` — exit `0`.
- Proof receipt: slice — scope `Broad` — command `python3 .agents/scripts/test_run_proof.py` — exit `0`.
- Blocking defect found and fixed, migration version collision: the Slice 14 rebase left two pairs of migrations sharing one version. `20260803110000` was claimed by Slice 14's `add_repository_assessment_terminal_results` and Slice 11's `create_personal_ai_connections`, and `20260803130000` by Slice 14's `create_repository_execution_profiles` and Slice 11's `create_quota_snapshots`. Ecto records a version once, so on a fresh database only the alphabetically first file of each pair runs and the other is skipped permanently; `create_model_catalog_snapshots` then failed with `relation "personal_ai_connections" does not exist`. A clean checkout of this branch could not set up its database, and the existing worktree databases hid it because each migration had been applied incrementally before the rebase. The two Slice 11 migrations were renumbered to `20260803110500` and `20260803130500`, preserving the required order in both directions, because Slice 14 is already merged into the default branch and its versions must not move. Proven by dropping and re-migrating both the unpartitioned and the partitioned test databases from scratch, each exiting `0`.
- Why this was invisible until now: `run_proof.py` injects `MIX_TEST_PARTITION` only at task scope, so every focused task proof in this slice ran against the healthy partitioned database while the gate's own unpartitioned run was the first to exercise a fresh migration path. Task 17's log already recorded the symptom as a pre-existing oddity of bare `mix test`; it was in fact this defect.
- Quality debt cleared, 25 `credo --strict` violations: nineteen refactoring and six readability issues, all in this slice's code, which had never been through the gate. Fixed by extracting named private helpers out of transaction bodies in `runtime_costs`, `personal_connections`, `model_catalogs`, `quotas`, `codex_app_server`, and the Codex catalog adapter, by extracting one branch out of `validate_selection_transaction` to bring its cyclomatic complexity under the limit, by swapping single-clause `with`/`else` for `case` and a single-condition `cond` for `if`, and by sorting three alias groups. No threshold was raised, no `.credo.exs` change was made, and no `credo:disable` comment was added. Every fix is a pure extraction or a mechanical construct swap; no return value, error atom, rollback reason, guard, lock order, or transaction boundary changed, and all extracted helpers that call `Repo.rollback/1` are still invoked inside their original transaction.
- Type defect found and fixed, and it was a real contract error rather than noise: `PersonalConnectionAdapter.validate_result/2` declared `request()`, a closed two-key map of `provider` and `authentication_mode`, but `PersonalConnections` calls it with the three-key output of `normalize_link_request/1`, which also carries `label`. Dialyzer therefore concluded the call could never succeed and reported the whole `persist_link` subtree — eight functions — as unreachable. `request()` is correct for the `link/4` callback, which really does receive two keys, so the accurate fix was a second `provider_choice()` type describing what `validate_result/2` actually accepts, rather than loosening the spec to `map()`. The remaining findings were a `MapSet` accumulator whose opacity cannot survive crossing a local function boundary under Elixir 1.20 and OTP 29, replaced by a bounded list because `max_pages` caps it at four entries and `validate_next_cursor/2` already rejects a repeated cursor before it is recorded; an impossible binary comparison in the Codex quota adapter, where a `model_specific` scope cannot arise because the App Server exposes only account-wide rate limits and setting `model` would have failed the adapter's own validation; and an unreachable channel clause whose predecessor already covers every atom reason. Dialyzer went from 37 errors with 14 live to 23 with none live, and the 23 remaining are the pre-existing documented skips. No new suppression was added anywhere.
- Both `sobelow` findings were false positives and were still fixed properly: `String.to_atom/1` was applied to a compile-time module attribute rather than to worker- or provider-supplied data, so there was no atom-exhaustion exposure, but replacing the runtime conversion with literal atom attributes removes the calls entirely and needs no suppression.
- Test-isolation defect fixed in this slice's own proofs: Task 6's logging test decoded whichever `[ai_runtime_security]` line appeared first in a global `ExUnit.CaptureLog` capture, so under the full concurrent suite it read a `worker_rpc_request` line emitted by another module and failed asserting `unavailable` where `rejected` was expected. Both AI-runtime test files now supply an explicit correlation identifier per call and select their own line by it, and prove a success emits nothing by the absence of that identifier rather than the absence of any AI-runtime line, which a concurrent test could always falsify. The single-element match also proves the identifier appears exactly once.
- Gate item that cannot pass, and why it is not being worked around: the live OpenAI Codex smoke test is the one excluded test in the suite. `CodexAppServer.Compatibility` ships an empty registry and fails closed, and no supported Codex version and generated-schema-digest pair is configured, so the check cannot run locally as written. It appears both as an unchecked verification-gate line and inside `Release gates`, and that ambiguity must be resolved through `update-spec` rather than by ticking the box or weakening the check. Every other verification-gate line is now checked with the evidence recorded above.
- Environment note: no database was created, dropped, or migrated outside the test environment, and `MIX_ENV=dev` was not used. The browser matrix builds into its own `_build/e2e` and its own `sdd_orchestrator_e2e_desktop` and `sdd_orchestrator_e2e_mobile` databases, so it did not touch the partitioned test database.
- Failed checks: Four, all corrected and re-proven. The first `mix check` halted at `credo --strict` with 25 issues before the suite ran. The full suite then failed at migration time, which exposed the version collision. The next `mix check` failed one test on the log-capture race. The fourth failed `mix format --check-formatted` on the correlation-identifier edit itself. The final state was confirmed on the main thread by real exit status for every command listed above.

### 2026-08-05 - Active-slice privacy and security review completed (Task 6)

- Completed: Added the consolidated Task 6 review as `test/sdd_orchestrator/privacy/ai_runtime_privacy_review_test.exs`, 24 tests across 14 named review dimensions. Task 6 owns AC-15 and claims it here. No `lib/` file was created or changed: this task reviews contracts and boundaries across the completed tasks rather than re-proving their internals or refactoring them, following the Slice 06 Task 7 precedent. The two findings parked for this review were investigated and classified below; neither was fixed here, because fixing either would change a completed task's approved surface.
- Proof: The focused proof runner passed 24 tests, all new, covering the cross-task data flow and field-purpose completeness, credential locality in both the schemas and the source, the App Server fail-closed boundary, price-source safety, purpose limitation and least privilege across every data class, owner-versus-participant key separation, the five distinct retention lock keys and their wiring into the single pruner, rights coverage with correction refused for both immutable records, the four-key failure-only log, the processor and transfer inventory, cache and backup, the absence of analytics and secondary use, the release-gate classification, and the downstream capability contract.
- Proof receipt: `Task 6` — scope `Focused` — command `mix test test/sdd_orchestrator/privacy/ai_runtime_privacy_review_test.exs` — exit `0`.
- Proof receipt: `Task 6` — scope `Focused` — command `mix format --check-formatted test/sdd_orchestrator/privacy/ai_runtime_privacy_review_test.exs` — exit `0`.
- Proof receipt: `Task 6` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- AC-15 outcome: credentials remain local, proven both by the schemas carrying no column that could hold one and by a line-by-line scan of all thirty-six AI-runtime source files plus the three web surfaces for key, PEM, bearer, and literal-to-secret-name material; access is purpose-limited, proven by walking every declared route in `AIRuntimeDataUsePolicy` and by the participant key allowlist containing no account-wide quota, credit, spend, connection, consumer, or provenance key; retention is enforced, proven by five pairwise-distinct advisory lock keys all wired into one supervised pruner that reports every AI-runtime delete category; provider data is minimized, proven by the field-purpose map still equalling all six schemas field for field; and no analytics or secondary use exists, proven by the fail-closed refusal of every prohibited purpose and recipient across all seven data classes together with the inventory declaring none.
- Non-vacuity was designed into the review rather than assumed: the documented column exceptions must still exist in the schema or the test fails, the named source files must still be covered by the wildcard or the test fails, five genuinely allowed policy routes must still return `:ok`, and a valid price registration must still resolve. A review that can only pass by finding nothing is not a review.
- Finding A, zero unit price — classified as a release-gate item, and the recorded blast radius was understated. Confirmed at `runtime_costs/price_snapshot.ex`: `bounded_unit_price/1` requires `Decimal.compare(value, 0)` to be `:gt`, so a registered price of exactly zero is rejected as indistinguishable from a missing price. The review traced and empirically confirmed that the rejection is not local to the free model: because each normalization step halts the enclosing `map_ok`, one invalid entry invalidates the entire registry, so registering a legitimately free model would return `missing_price` for every model in the deployment and refuse every API-key reservation. The direction of failure is conservative — nothing leaks, over-spends, or under-reserves — and `Release gates` already requires current official price-source configuration, so this is covered rather than uncovered. Recorded for the release gate: a unit price of exactly zero is not an accepted registration, and one invalid entry invalidates the whole registry. Making the release-gate line say so explicitly, or supporting officially free models under an API-key ceiling, is an `update-spec` decision and not an implementation defect here.
- Finding B, read-side visibility after revocation — classified as a presentation-correctness issue owned by the UI, not a privacy or access defect. Confirmed that `ModelCatalogs.current_catalog/3` and `Quotas.current_quota/3` never call `eligible/1`, while every other path does, including `prepare_refresh`, `persist`, and `validate_selection`. The review answered the access question with evidence rather than assertion: no party other than the owning active account can read it, because both functions scope the account, the connection, and the snapshot by account and connection identity, and the participant projection carries no quota key at all; it cannot fund work, because `resolve_for_consumer/3` and `validate_selection/5` both refuse `revoking` and `revoked` before anything else; and it is capped at the snapshot's own lifetime — at most one hour, 300 seconds by default — by the `expires_at` currency boundary rather than by the sweep, which matters because `terminal_connection_ids/0` selects only acknowledged or deletion-scheduled connections and therefore does not clear a `requested`-state connection's snapshots. The reader is the data subject reading their own already-stored projection inside its recorded purpose and inside its recorded retention window, which is what the right of access protects rather than something it forbids, so AC-15 is satisfied. What is genuinely wrong is narrower: `AIConnectionsLive.refresh_connections/1` fetches both projections for every connection and re-fetches immediately after a successful revocation request, and the panel markup is not filtered by revocation state, so the full model list, effort chips, and provenance line keep rendering beside a "Revocation pending" badge. The screen already carries unreachable `:revoking` and `:revoked` copy that only a live refresh could produce, and the Refresh buttons are already disabled in that state. Gating the panels' data rendering on the same predicate the buttons use would make that copy reachable; adding `eligible/1` to the two read functions would instead change their declared error unions and their callers' contracts. Either is a change to Task 9's approved AC-17 surface with browser proof, so both route to `update-spec` rather than being taken here.
- No other finding. Two things were checked and cleared: the personal-connection export omits the redacted `worker_profile_ref`, and the snapshot sweep's terminal definition matches the inventory's recorded retention text, so the `requested`-state gap in Finding B is a documented boundary rather than drift.
- Assertions shaped differently from the dispatch brief, all recorded rather than silently substituted. The App Server frame-level refusal of raw errors and credential-shaped content could not be reached through the public API, because the frame handlers are private and a request against a dead server short-circuits to `process_unavailable` before any frame is parsed; the review instead asserts the reachable typed refusals plus a source-level proof that all three App Server modules contain no `Logger.` and no `inspect(` call while declaring the `credential_content` and `app_server_error` vocabularies, so raw provider text has no exit. Frame-level refusal remains Task 2's proof. The index scan runs over the migration sources rather than `pg_indexes`, because Task 15's test already owns the live `information_schema` and `pg_indexes` scan and duplicating it would prove nothing new. The retention dimension asserts five distinct lock keys across the three modules that expose them plus `prune_all/1` reporting all seven AI-runtime delete categories, which is a stronger claim than counting sweeps, since it proves every sweep is actually wired into the one supervised pruner; the numeric windows are asserted through the recorded inventory text, because the constants are private and their behavioral enforcement belongs to Tasks 12, 14, and 17.
- Failed checks: None. Each proof passed on the main thread's own confirmation by real exit status, with no compiler or test warnings, and both findings were independently re-confirmed at their source lines on the main thread before being classified. `python3 .agents/scripts/validate_spec.py specs/11-ai-runtime-governance`, `python3 .agents/scripts/validate_spec.py --all specs`, `python3 .agents/scripts/split_progress_log.py --check`, and `git diff --check` were re-run after the change and all passed.
- Spec updates: Task 6 checked complete with its Delivered record, and `capability:ai-runtime-governance` is now ready for its two downstream consumers. Every task in the slice is complete. Slice status remains `In Progress` and must not be marked `Verified`: repository-wide verification has never run for this slice, and the verification gate's live OpenAI Codex smoke test cannot pass locally as written, because the compatibility registry ships empty and fails closed and no supported version and generated-schema digest pair is configured. That item appears both as an unchecked verification-gate line and inside `Release gates`, which is the ambiguity that must be resolved through `update-spec` before the gate can be completed rather than by ticking a box.

### 2026-08-05 - Minimized runtime operations and prohibited-use controls enforced (Task 15)

- Completed: Extended `Privacy.ProcessingInventory` with the seven AI-runtime processing activities and `ai_runtime_field_purposes/0`, added `Privacy.AIRuntimeDataUsePolicy` and `AIRuntime.SecurityLog`, wired one audit call site in `AIRuntime.PersonalWorkerRPC`, and added the focused Task 15 governance test. Task 15 owns no acceptance criterion and claimed none; the work supports AC-15 without redefining it. No migration, no schema change, and no fixture change was required — the six governed entities and their lifecycles were already delivered by Tasks 1, 2, 3, 4, 11, 12, 13, 14, 16, and 17, and this task governs them rather than reshaping them.
- Proof: The focused proof runner passed 45 tests, 23 of them new, covering the seven inventory activities and their required fields, the field-purpose map proven key-for-key equal to all six schemas' fields, the contract and service-security bases, the least-privilege route map, the processor and transfer inventory, the four-key log event, the 30-day expiry, credential, raw-account and prompt-content redaction, silent success, redaction at rest, the encrypted 35-day backup lifecycle, the fail-closed refusal of analytics, advertising, model training, unrelated improvement, identity tracking and every prohibited consumer, and the database negative scan over the six AI-runtime tables' columns and indexes.
- Proof receipt: `Task 15` — scope `Focused` — command `mix test test/sdd_orchestrator/privacy/ai_runtime_governance_test.exs test/sdd_orchestrator/privacy/processing_inventory_test.exs test/sdd_orchestrator/privacy/analytics_absence_test.exs test/sdd_orchestrator/ai_runtime/personal_worker_rpc_test.exs` — exit `0`.
- Proof receipt: `Task 15` — scope `Focused` — command `mix test test/sdd_orchestrator/privacy/specification_governance_test.exs test/sdd_orchestrator/privacy/deployment_privacy_profile_test.exs test/sdd_orchestrator/privacy/portability_data_use_policy_test.exs test/sdd_orchestrator/privacy/portability_privacy_review_test.exs test/sdd_orchestrator/privacy/storage_boundary_test.exs test/sdd_orchestrator/privacy/retention_test.exs test/sdd_orchestrator/privacy/rights_test.exs` — exit `0`.
- Proof receipt: `Task 15` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/privacy/processing_inventory.ex lib/sdd_orchestrator/privacy/ai_runtime_data_use_policy.ex lib/sdd_orchestrator/ai_runtime/security_log.ex lib/sdd_orchestrator/ai_runtime/personal_worker_rpc.ex test/sdd_orchestrator/privacy/ai_runtime_governance_test.exs test/sdd_orchestrator/privacy/processing_inventory_test.exs test/sdd_orchestrator/privacy/analytics_absence_test.exs` — exit `0`.
- Proof receipt: `Task 15` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Lawful bases, and why they split where they do: the six record activities are `contract`, because a personal connection reference, a proven catalog, an allowance projection, a pinned configuration, a ceiling ledger, and a run's observation trail are each necessary to deliver the runtime the user asked for and none of them exists without a request. The operational log alone is `legitimate_interests`, because diagnosing failures and protecting the boundary is the service's own security and reliability interest rather than something the user asked for. This is the same split Slice 01 already made for `operational_security_log`, and no third basis was introduced.
- Least privilege is expressed in the route map, not in prose: across the six record classes `:approved_operations` appears only under `:retention_cleanup` and `:verified_rights_operator` only under `:verified_rights`, so operations and support personnel are given a lifecycle route and a verified-rights route but never a content-reading one. `:participant_run_visibility` exists only for the pinned session and the observations and only for a current authorized project participant; a participant appears nowhere under `:ai_quota_snapshot` or `:ai_runtime_cost_ledger`, which keeps account-wide allowance, credits, and spend owner-only exactly as Task 5's projection already enforces at runtime. The test walks the map and asserts these rather than trusting the moduledoc.
- `:model_provider` is a prohibited consumer even though runs really do reach a provider: the provider is contacted only by the user's own worker-local official client, for the work the user asked for, and these control-plane governance records are a separate thing that is never sent to it. Recording the prohibition against the governance records is what makes the no-training and no-secondary-use claim checkable; it does not contradict the runtime's purpose.
- The operational log emits only on failure, deliberately differing from `Portability.SecurityLog`, which logs `:info` on success. A success line here would be a running record of what each account is doing with its own connection, which is neither necessary for security nor minimized; the failure trail is what diagnoses and protects the boundary. This follows `SpecificationSecurityLog`'s established shape while keeping the structured JSON event and correlation identifier of the portability log.
- Redaction is structural rather than filtered: `audit/3` accepts only an allowlisted event-type atom, an optional `%DateTime{}`, and a correlation identifier cast through `Ecto.UUID.cast/1` that is regenerated whenever it is absent or invalid, so no caller-supplied text can reach the line through the options. The outcome vocabulary is closed at `denied`, `paused`, `unavailable`, `rejected`, and `failed`, and only an allowlisted reason atom reaches the first four; the final clause maps anything else — an unknown atom, a struct, a changeset, a binary, a nested tuple — to `failed` without inspecting or encoding it. Every reason atom in the four lists was confirmed to exist in this slice by grep; none was invented. The test proves the mechanism by auditing failures carrying an API key, an access token, a provider email, a raw provider account marker, and prompt text, and asserting none of them appears while the line still reports its outcome.
- One wiring point, chosen as the boundary rather than the sum of the callers: `PersonalWorkerRPC.request/…` is where every catalog, quota, connection, and observation operation crosses to the worker, so auditing there covers the whole provider surface with one call and touches one completed task's file by one alias and one line. The `with` chain was lifted into a bound `result` and handed to `audit/3`, which returns its argument unchanged, so the function's return value and behavior are identical. Auditing each of the seven boundary modules instead would have edited six completed tasks' files for the same coverage.
- The field-purpose map is proven complete rather than asserted complete: for all six entities the test asserts `Map.keys(...)` sorted equals `__schema__(:fields)` sorted, which is the Slice 09 `specification_field_purposes/0` precedent. A field added later without a recorded purpose fails this test, so the minimization claim cannot silently go stale. The six key sets are 19, 13, 17, 22, 21, and 24 fields.
- Two negative-scan assertions were shaped differently from the plan, and in both cases the plan's shape would have been the weaker check. The purpose-map minimization scan runs over the map's values rather than `inspect/1` of the whole map, because the map's keys are the schemas' own column names and `personal_ai_connections` owns four columns literally named `credential_removal_*`; scanning the inspected map could therefore never pass while the key-set equality assertion above simultaneously requires those keys to be present. The column scan keeps a documented eleven-name exception list — the four `credential_removal_*` lifecycle columns and seven `*token*` counter and bounded-limit columns — and defends it three ways: every exception must still exist in the schema, so the list cannot go stale; every other column is scanned; and the two textual credential-lifecycle columns are asserted to hold the entity's fixed `removed`/`absent` and five-reason vocabularies, so the exception cannot become a free-text credential sink.
- Visible change to suite output, accepted as the control working: `personal_worker_rpc_test.exs` deliberately exercises ten failure paths, so it now emits ten `[ai_runtime_security]` warning lines. Each carries only the four allowlisted keys and reports `rejected` or `unavailable`, which is the end-to-end evidence that the boundary audits real failures rather than only synthetic ones. The test still passes at 14 tests, unchanged from its pre-change baseline.
- Test-isolation notes: the success-emits-nothing test asserts the absence of an `[ai_runtime_security]` line rather than an empty capture, because `ExUnit.CaptureLog` is global and the module runs `async: true`, which the database negative scan requires; a strict-empty assertion would be flaky against a concurrent test's output. The non-allowlisted event-type refusal is dispatched through `apply/3` because a literal call produced an Elixir 1.20 type warning that would have failed the zero-warning bar.
- Judgment recorded: the catalog and quota snapshot records carry the plain review line while the other five carry the release-gate clause, because a projection that expires within the hour holds no independent deployment evidence to gate. Their retention text still names the deployment's encrypted-backup expiry.
- Environment: all proofs ran through `run_proof.py`, which injected `MIX_TEST_PARTITION=794498` for this worktree. No migration was run and no `MIX_ENV=dev` database was touched; `mix compile --warnings-as-errors` builds `_build/dev` but opens no database connection.
- Failed checks: None. Each proof passed on the main thread's own confirmation by real exit status, with no compiler or test warnings. `python3 .agents/scripts/validate_spec.py specs/11-ai-runtime-governance`, `python3 .agents/scripts/validate_spec.py --all specs`, `python3 .agents/scripts/split_progress_log.py --check`, and `git diff --check` were re-run after the change and all passed.
- Reported by the sub-agent, outside this task's ownership and not fixed: the canonical slice-gate commands should pin `MIX_ENV=test` explicitly for `mix compile --warnings-as-errors`, which currently builds the dev environment by default.
- Spec updates: Task 15 checked complete with its Delivered record. Slice status remains `In Progress`; no cross-specification capability becomes ready from Task 15, because `capability:ai-runtime-governance` is published by Task 6. Task 6, the consolidated privacy and security review, is now the next and last executable task, and its two parked review items and the live-Codex verification-gate ambiguity remain open as recorded below and in the slice status.

### 2026-08-04 - Runtime-observation lifecycle and rights controls enforced (Task 17)

- Completed: Added the observation sweep to `Privacy.Retention`, observation access, correction, restriction, objection, erasure, retirement, and termination handling to `Privacy.Rights`, and the focused Task 17 lifecycle test. Task 17 owns no acceptance criterion and claimed none; the work supports AC-09, AC-13, and AC-15 without redefining them. No migration, schema, or fixture change was required, and no existing test file changed except the one assertion recorded below.
- Proof: The focused proof runner passed 99 tests, 19 of them new, covering the exact 30-day boundary in both directions, the session and ledger left untouched by the observation sweep, owner and participant access degrading to unknown rather than zero after expiry, project and conversation deletion handoff with an unrelated run left intact, retirement convergence on repeat, the session cascade, the schema-derived export allowlist, the correction refusal at the rights boundary and structurally in the entity and at ingest, account erasure, restriction and objection with their processor, derived-copy, and encrypted-backup handoff, the exact index set, every read path reporting absence rather than a memoized value, advisory-lock contention against a genuinely separate connection, lock-key distinctness from all four other sweeps, whole-pruner integration without double-counting, cross-account and malformed-id refusal, and service termination retaining the trail. The five pre-existing retention, rights, session-lifecycle, projection, and observation test files were re-run as a regression check.
- Proof receipt: `Task 17` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/runtime_observation_lifecycle_test.exs test/sdd_orchestrator/privacy/retention_test.exs test/sdd_orchestrator/privacy/rights_test.exs test/sdd_orchestrator/ai_runtime/runtime_lifecycle_test.exs test/sdd_orchestrator/ai_runtime/runtime_projections_test.exs test/sdd_orchestrator/ai_runtime/runtime_observations_test.exs` — exit `0`.
- Proof receipt: `Task 17` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/privacy/retention.ex lib/sdd_orchestrator/privacy/rights.ex test/sdd_orchestrator/ai_runtime/runtime_observation_lifecycle_test.exs test/sdd_orchestrator/ai_runtime/runtime_lifecycle_test.exs` — exit `0`.
- Proof receipt: `Task 17` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Retention window, and why it is shorter than the session's: an observation is the operational trail of one run — elapsed time, token counters, an estimated cost, the quota that applied, and the status at that moment. Its purpose is operating and pausing work safely and answering a near-term question about what a run was doing, which is the same purpose and lifetime the deployment privacy profile already fixes for its operational security log at 30 days. The pinned configuration and the reconciled ledger answer the accountability question and keep their own 90-day and 30-day windows, so deleting the per-observation trail earlier removes detail without removing the account of the run. The window is a module constant rather than a read of the deployment profile, matching Task 14's precedent, because this is stored data governed by this module rather than a log the deployment infrastructure expires.
- Sweep ordering: the observation sweep is registered last in `prune_all/1`, after the session sweep, under advisory lock key `271_058_349`, distinct from the pruner's `748_213_905`, the revocation sweep's `613_477_218`, the snapshot sweep's `529_140_776`, and the runtime-session sweep's `384_612_907`, with distinctness asserted in the test. A session deleted earlier in the same pass has already cascaded its observations away, so the count reports only what the observation window itself removed and never double-counts the cascade. This is the same reason Task 16 placed the snapshot sweep after the terminal-connection delete. The test proves it by adding a second run pinned late enough to survive the session sweep whose trail is nonetheless past its shorter window, then asserting the pass reports one expired session, one expired ledger, and exactly one expired observation.
- Access loss proven rather than assumed: expiring the trail genuinely removes what the owner and a current participant could see, so the test asserts the projections keep their stable shape and report `source: :unknown` for elapsed time, tokens, estimated cost, and availability, and separately refutes each value reading as `0` or still claiming a `worker_observed`, `local_estimate`, or `provider_fact` label. The participant case asserts the trail was readable before the sweep and unknown after it, so the loss is demonstrated rather than inferred from an empty fixture.
- Correction refusal is structural, not policy: the test asserts the disposition and the named available actions, then proves the entity has no correction path at all — `AgentRuntimeObservation` exports `create_changeset/2` and neither `update_changeset/2` nor `changeset/2`, its `(session_id, sequence)` and `(session_id, event_key)` indexes are both unique, and restating the stored event key with different token counts is refused at ingest as `duplicate_event` with the stored row's counters asserted unchanged afterward.
- Cache and index claim proven at the database: the test reads `pg_indexes` and asserts the table's index set is exactly the primary key plus the three declared indexes, so a search index holding a second copy could not be added unnoticed, and it asserts every read path reports absence after the sweep rather than a memoized value, including two consecutive owner projections returning the identical absent result. The composite index name is truncated by PostgreSQL to 63 bytes as `agent_runtime_observations_account_id_session_id_observed_at_in`, which the assertion records.
- Advisory-lock contention is genuine here, unlike Task 11's sandbox limitation: the test opens a separate `Postgrex` connection outside the Ecto sandbox to hold the lock, so the sweep contends against a real competing session rather than serializing at the pool. It asserts `:locked`, that nothing was deleted, that `prune_all` reports zero for the category while contended, and that the next pass after release deletes and then converges to zero.
- Reconciled against Task 14's test, and the assertion was wrong rather than the design: adding `observations:` to `retained_runtime_records/1` broke `runtime_lifecycle_test.exs`, which asserted exact map equality on `termination.personal_ai_runtime`. The main thread added `observations: 0` to that assertion rather than dropping the field. The termination report exists to tell an operator what termination retained; sessions and ledgers were named and the observation trail, which is also retained, was not, so the report was incomplete. Task 14's behavior is unchanged — the map it returns gained a sibling key contributed by the entity Task 17 owns. Elixir's type checker had already been warning that the three-key literal and the four-key map are disjoint; that warning is now gone.
- Retention versus termination, and why they differ from the snapshots: `terminate_personal_ai_service/1` counts the observation trail as retained rather than purging it, because an observation describes work that already ran and revoking the connection cannot undo it, exactly as Task 14 reasoned for the session and ledger. Catalog and quota snapshots are purged at termination instead, because they describe a live connection's current entitlement, which after termination no longer exists.
- Environment incident, recorded not fixed: the sub-agent ran `mix ecto.migrate` in `MIX_ENV=dev` to read the real PostgreSQL index names. That applied this branch's Slice 11 migrations to the shared `sdd_orchestrator_dev` database, which the main worktree also uses. Every applied migration is additive `up` and nothing was dropped, but the main worktree currently sits on a default branch that does not contain them, so a `mix ecto.rollback` run from there would not find their definitions. The partitioned test database was untouched; all three proofs ran through `run_proof.py`, which injected `MIX_TEST_PARTITION=794498` for this worktree.
- Reported by the sub-agent, outside this task's ownership and not fixed: a bare `mix test` without the proof runner fails against the unpartitioned `sdd_orchestrator_test` database with `relation "personal_ai_connections" does not exist`. This is pre-existing and does not affect the canonical proof path.
- Failed checks: One, corrected before the receipts above. The first focused run was 98 of 99 with the single Task 14 termination assertion failing for the reason recorded above. After the one-key correction the full command passed on the main thread's own confirmation by real exit status, with no compiler or test warnings.
- Branch rebased onto the default branch after the task commit: Slice 14 merged into the default branch as `572f546` while this task was in flight, so `slice/11-ai-runtime-governance` was rebased onto it and its sixteen commits replayed. One conflict occurred, in `lib/sdd_orchestrator_web/controllers/e2e_bootstrap_controller.ex` at Task 9's commit, and only there: both slices add their own compile-gated browser scenarios to that one controller, and only one slice-11 commit touches the file, so the resolution was needed once rather than at every replayed commit. It was resolved as a union with both sides intact — this slice's `ai_connections` scenario with its `seed_ai_workers`, `pair_ai_worker`, `seed_github_identity`, `start_ai_responder`, and `ai_responder_loop` helpers, and Slice 14's `repository_assessment` and `repository_execution_profile` scenarios with their `seed_completed_assessment`, `profile_scan`, and `reachable_worker` helpers. Git had spliced the two pairing helpers into one because both open with the same `start_pairing` and `complete_pairing` shape; they differ in `os_major`, protocol version, and the `mark_seen` call, and both were restored verbatim from their own sides.
- Rebase verification, and what it does not cover: `mix compile --warnings-as-errors` passed on the rebased tree, which is a real check on the hand-resolved file because a dropped private helper would surface as unused and a dropped clause as undefined. Task 17's focused proof passed unchanged at 99 tests, and the controller's own test file plus the AI Connections LiveView test passed at 32 tests. The Task 17 implementation and test files are byte-identical across the rebase, confirmed by diff against the pre-rebase commit. The `repository_assessment` and `repository_execution_profile` scenarios are exercised by browser tests rather than by that controller test, so their end-to-end proof belongs to the slice verification gate. A recovery tag `pre-rebase-task17` marks the pre-rebase commit `2ddbb06` and can be deleted once the gate has run.
- Publishing state: `origin/slice/11-ai-runtime-governance` holds a single stale commit, `08a7bc6`, that already diverged from local history well before this session, so the rebase did not rewrite anything the remote currently shares. The eventual push of this branch requires a force regardless.
- Spec updates: Task 17 checked complete with its Delivered record. Slice status remains `In Progress`; no cross-specification capability becomes ready from Task 17 alone, because `capability:ai-runtime-governance` is published by Task 6. Task 15 is now executable with all four of its prerequisites complete.

### 2026-08-04 - Access-safe runtime-observation projections published (Task 5)

- Completed: Added `AIRuntime.RuntimeProjections` and its focused Task 5 test. The module is the provider of `capability:ai-runtime-observation` and is read-only: it composes `RuntimeSessions`, `RuntimeObservations`, `Quotas`, `RuntimeCosts`, and `Participation.Boundary` as a caller, contacts no adapter or worker, and inserts, updates, or deletes nothing. No migration, no schema change, no fixture change, and no Slice 07 or Slice 12 presentation surface were needed or added. Task 5 owns AC-13 and claimed no other criterion.
- Proof: The focused proof runner passed 15 tests, all new, covering the owner projection of a working-agent run and of a support-assistant conversation, account-wide buckets with credits and paid continuation, the strict API-key ceiling, ChatGPT spend reported as not applicable, absent and expired quota reported as unknown rather than zero, stored history still readable after connection detachment, cross-account refusal, the participant projection for both a participant and the project owner, the exact participant and per-observation key allowlists, forbidden-field absence at every depth, exact-versus-safe agreement on model, effort, and availability, preserved append order, one uniform denial across every unauthorized caller, and the downstream capability contract.
- Proof receipt: `Task 5` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/runtime_projections_test.exs` — exit `0`.
- Proof receipt: `Task 5` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/runtime_projections.ex test/sdd_orchestrator/ai_runtime/runtime_projections_test.exs` — exit `0`.
- Proof receipt: `Task 5` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Mechanism, project-to-run binding deliberately not invented: `AIRuntimeSession` records an opaque consumer reference and no project, because Slice 07 lifecycle integration is deferred, so Slice 11 cannot itself decide whether a session belongs to a project. No column, table, or inference was added to make it look as though it could. What the boundary enforces is stated plainly in its moduledoc and proven: participant authorization is re-asked against the project the caller names on every call, and the answer never widens past the single session reference the caller names, so a current participant of another project is denied under this project's scope. Binding a run reference to its project remains the calling slice's obligation, exactly as `Privacy.Rights.retire_runtime_consumers/2` already requires the project to name its own consumer references.
- Mechanism, participant reads without account scope: a participant is generally not the connection owner, so an account-scoped read cannot serve the participant path. The session row is resolved un-scoped by its own reference only to discover its owning account, and every projected fact is then re-read through `RuntimeSessions.get_session/2` inside that account, so Task 4 keeps sole authority over session facts and this task adds no second session-reading path.
- Mechanism, uniform denial: every participant failure — non-member, wrong project in either direction, malformed or unknown project, malformed or unknown session, support-assistant session, empty actor, nil actor, and revoked participation — returns one identical `unavailable`, asserted in a single test as `Enum.uniq(denials) == [{:error, :unavailable}]`. A denied caller learns neither whether the project exists, nor whether the session exists, nor whether they used to be a member, nor which check failed.
- Mechanism, absent evidence never reads as a value: an unknown or expired quota and a detached connection project `state: :unknown`, a ChatGPT session's ceiling projects `state: :not_applicable`, and unobserved usage keeps its stable shape with `source: :unknown`. An API-key session whose ledger is not yet open projects `state: :unknown` rather than `not_applicable`, because a ceiling does apply to it and is merely not known here; reporting it as inapplicable would be false.
- Mechanism, the forbidden-field test cannot pass by being blind: absence is proven by a real recursive key walker over maps and lists that treats structs as leaves, and the same test plants `spending_ceiling` at depth three and asserts the walker finds exactly that key.
- Corrected during implementation, and the dispatch brief was wrong rather than the code: the brief asked for one test proving account-wide quota buckets, credits, and spend together on an API-key session. `QuotaAdapter.validate_authentication_boundary/1` structurally forbids that, requiring an API-key result to carry `status: "unknown"` with no buckets, credits, or token activity, which is Task 8's approved contract. The proof was split rather than the design changed: a ChatGPT session proves the account-wide buckets, credits, and paid continuation, and an API-key session proves the ceiling, reserved, observed, and remaining spend. AC-13 remains fully proven, and the API-key test additionally asserts its quota is the current provider fact rather than the missing-evidence marker, so the two stay distinguishable.
- Mechanism, owner spend excludes ceiling internals: the owner projection carries currency, ceiling, reserved, observed, remaining, paused, and pause reason, and deliberately omits the outstanding reservation map, the versioned price evidence, and the bounded request configuration. Those are Task 11's internal enforcement mechanics, not an owner runtime view.
- Environment notes: the detached-session test performs the permitted present-to-absent `connection_id` transition directly, because Task 14 delivered the migration that allows exactly that transition but no caller yet performs it. Participation is revoked with `Revocations.leave/3` rather than `remove/3`, which keeps the test `async: true` since `remove/3` sends an email.
- Failed checks: None. The focused test, formatting, and warnings-as-errors compilation each passed on the first main-thread confirmation by real exit status.
- Spec updates: Task 5 checked complete with its Delivered record. `capability:ai-runtime-observation` is now ready. Slice status remains `In Progress`; Task 17 is the next executable task.

### 2026-08-04 - Runtime-session and cost-ledger lifecycle and rights controls enforced (Task 14)

- Completed: Added session and ledger retention to `Privacy.Retention`, session and ledger access, correction, restriction, objection, portability, erasure, and consumer-retirement handling to `Privacy.Rights`, the connection-detachment migration, and the focused Task 14 lifecycle test. Delivered in parallel with Task 12 under the recorded intra-slice ownership partition. Task 14 owns no acceptance criterion and claimed none; the work supports AC-06, AC-07, and AC-15 without redefining them.
- Proof: The focused proof runner passed 36 tests, 21 of them new, covering an attached session retained, a terminal session and its ledger pruned at their boundary, a paused session still resumable within its window, connection-removal behavior, project and conversation deletion handoff, verified access export, correction refusal on immutable pinned configuration including the database-level refusal, restriction, objection, portability, derived-copy propagation, processor deletion request, encrypted-backup handling, idempotent repeated cleanup, advisory-lock contention, and the minimized-field allowlist. The two pre-existing privacy test files were re-run as a regression check. Focused formatting passed on all five owned files.
- Proof receipt: `Task 14` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/runtime_lifecycle_test.exs test/sdd_orchestrator/privacy/retention_test.exs test/sdd_orchestrator/privacy/rights_test.exs` — exit `0`.
- Proof receipt: `Task 14` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/privacy/retention.ex lib/sdd_orchestrator/privacy/rights.ex lib/sdd_orchestrator/ai_runtime/ai_runtime_session.ex priv/repo/migrations/20260804160000_detach_ai_runtime_session_connection.exs test/sdd_orchestrator/ai_runtime/runtime_lifecycle_test.exs` — exit `0`.
- Resolved decision, replacing Task 4's provisional mechanism: `ai_runtime_sessions.connection_id` is now nullable with `on delete set null`, and the immutability trigger permits exactly one additional transition, a present reference becoming absent, while still refusing null to non-null and any change of connection. The reason is that the approved business rule states connection removal revokes the opaque reference and leaves governed historical run configuration for as long as project accountability requires; under a cascade that clause was vacuous, because nothing survived beyond the connection reference's own lifetime. A pinned configuration is accountability evidence rather than refreshed fact, which is the opposite of the catalog and quota snapshots that correctly cascade, and credential rotation through unlink and relink is routine, so a cascade would silently erase every past run's record at each rotation. Detaching is itself the minimizing act: it destroys exactly the opaque reference and leaves a record carrying no credential, no provider account identity, no worker-profile reference, and no raw provider error. `restrict` and `nothing` were rejected because they would block the revoked-connection delete and put account erasure at risk.
- Propagation: `runtime_cost_ledgers.session_id` keeps `on delete cascade` unchanged, so a ledger never outlives its session and now survives connection removal alongside it. That is correct, because the reserved and observed amounts under a removed connection are the minimized usage the rule retains for accountability and the ledger holds no invoice or payment credential. The retention sweep deletes ledgers explicitly before their sessions in the same transaction, only so reported counts describe what the pass actually removed. Re-pinning a consumer reference whose session was detached now fails closed as `configuration_conflict` rather than crashing, because a null reference never equals a requested one.
- Retention windows: an attached session is retained 90 days from its pin, the outer bound of the project's account of the run, which already exceeds the 30-day operational log and 35-day encrypted-backup windows. A detached session is retained 30 days from its pin, matching the terminal-invitation and departed-participation windows, because once the funding connection is gone no further execution can follow the pin. The ledger follows its session in both cases. Pause is deliberately never a deletion trigger, so a paused session and its ledger stay intact and resumable for the whole window. The sweep runs last in `prune_all/1` under advisory lock key `384_612_907`, distinct from the pruner, revocation, and snapshot keys and asserted as distinct in the test; contention returns `locked` and deletes nothing.
- Reported gap, deliberately not invented here: `AIRuntimeSession` records no terminal lifecycle marker, and the ledger's paused state is explicitly resumable rather than terminal. No lifecycle column was added, because Slice 07 lifecycle integration is deferred and adding one would cross that boundary. Terminal therefore currently means the funding connection is gone, and both windows measure from the pin. When Slice 07 records run and conversation completion, the terminal window should key on that instead and can then be considerably shorter than 30 days.
- Cross-slice obligation recorded for the deferred Slice 07 `update-spec` and the support-conversation consumer: run and conversation deletion must call `Rights.retire_runtime_consumers/2` with their own references. A session has no project column and its consumer reference is opaque, so a project deletion can only retire the references the project itself names; without that call a deleted run's pin survives to its retention window instead of being retired at deletion.
- Corrected at reconciliation: the sub-agent reported that Task 12's `agent_runtime_observations.session_id` carried no foreign key and that observations would therefore be orphaned. The main thread verified the migration directly and found the claim false. The composite `(account_id, session_id)` foreign key to `ai_runtime_sessions(account_id, id)` with `on delete cascade` is added by raw SQL after the create-table block, following the same shape the quota snapshots use; the sub-agent read only the column declaration while Task 12 was still in flight. Observations do follow their session, and no further work is required.
- Failed checks: None. The first full run of the new lifecycle test passed, no test outside this task's ownership regressed, and a rollback and re-migrate cycle confirmed the migration is reversible.
- Mechanisms: Retention windows are module constants matching the existing participation window rather than application configuration. If the deployment privacy profile is later meant to own these numbers, that is a separate change.
- Spec updates: Task 14 checked complete with its Delivered record. Slice status remains `In Progress`; no cross-specification capability becomes ready from Task 14 alone.

### 2026-08-04 - Ordered runtime observations ingested (Task 12)

- Completed: Added the `AgentRuntimeObservation` schema and migration, the `RuntimeObservations` ordered-append boundary, the strict provider-neutral observation adapter with its authenticated `observation/1` RPC implementation and Codex notification ingestion, a deterministic observation double, and focused Task 12 tests. Delivered in parallel with Task 14 under the recorded intra-slice ownership partition, sharing one working tree and separated by disjoint paths, distinct test partitions, and distinct build paths. This task ingests and stores only; owner-exact and participant-safe projections remain owned by Task 5, and no Slice 07 or Slice 12 presentation surface was added.
- Proof: The focused proof runner passed 29 tests covering ordered append, ordering enforced by the database constraint under direct insert, idempotent duplicate events, conflicting duplicates, stale and out-of-order refusal, clock-skew bounds, elapsed time, token counters present and absent, estimated cost with its calculation basis, non-calculable cost, applicable quota references, all four statuses, all four source labels, unknown values never reading as zero or unlimited, support-assistant and working-agent equivalence, malformed and oversized adapter output, credential-shaped content and worker-profile echo rejection, the minimized column allowlist, the deterministic adapter contract, and the detached-connection path. Focused formatting passed on all nine owned files, and `mix compile --warnings-as-errors` passed with both parallel tasks applied.
- Proof receipt: `Task 12` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/runtime_observations_test.exs` — exit `0`.
- Proof receipt: `Task 12` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/agent_runtime_observation.ex lib/sdd_orchestrator/ai_runtime/runtime_observations.ex lib/sdd_orchestrator/ai_runtime/observation_adapter.ex lib/sdd_orchestrator/ai_runtime/observation_adapter/rpc.ex lib/sdd_orchestrator/ai_runtime/observation_adapter/codex.ex priv/repo/migrations/20260804150000_create_agent_runtime_observations.exs test/support/observation_adapter_double.ex test/support/ai_runtime_fixtures.ex test/sdd_orchestrator/ai_runtime/runtime_observations_test.exs` — exit `0`.
- Proof receipt: `Task 12` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Mechanisms: The caller supplies the source's monotonic sequence and observation time; appends serialize on the session row `FOR UPDATE` and re-read the latest row under that lock, while a unique `(session_id, sequence)` index gives the database an independent ordering guarantee proven by direct insert rather than only through the boundary. Idempotency is a unique `(session_id, event_key)`: an identical replay returns the stored row and a replay carrying different facts fails as `duplicate_event`, comparing the decoded stored payload with the cost amount normalized because the `decimal(12,4)` column returns a different representation of the same number. An event is appended only when its sequence is greater than the latest and its observation time is not earlier, and it is refused when it predates the session pin or runs more than sixty seconds ahead of now, matching the clock-skew discipline already used for quota. Source labelling is structural rather than cosmetic: five value groups each carry one label, and database check constraints restrict each column to the labels it can honestly carry, so `cost_source` admits only `local_estimate` or `unknown` and an estimate is physically incapable of being stored as a provider invoice, while `unknown` is equivalent to an absent value and every absent value must also name itself in `unknown_fields`. An estimate stores its basis — price version, price source, model, the token counts used, and both unit prices — and is cross-checked so that the basis counts equal the reported counters and the basis model equals the session's pinned model. Adapter provenance is validated as an authenticity gate but deliberately not persisted, because the session already owns the provider and the entity is limited to values, labels, and observation time; a test asserts the table's exact column set. A paused status must name a resumable reason, enforced in the database, so it can never read as terminal. Observations carry a composite foreign key on `(account_id, session_id)` referencing `ai_runtime_sessions(account_id, id)` with `on delete cascade`, following the precedent set by the quota snapshots, so no row can hold an account and session that disagree and observations always follow their session.
- Cross-task interaction resolved at reconciliation: Task 14's parallel decision made `ai_runtime_sessions.connection_id` nullable with `on delete set null`. Ingest against a session whose connection has been removed fails closed as `not_found`, while the stored history remains readable, which satisfies the rule that the runtime preserves observation history. The sub-agent initially declined to test that path to avoid coupling its proof to an in-flight migration; once Task 14 was final the main thread required the coverage, and it now exercises the real path by deleting the connection row and letting the cascade fire rather than writing a null directly.
- Failed checks: The parallel agent's mid-edit `rights.ex` broke tree compilation for one run; Task 12 waited and retried rather than touching a file it does not own. A private `limit/1` helper shadowed the `Ecto.Query` macro and was renamed. No final focused proof failed.
- Spec updates: Task 12 checked complete with its Delivered record. Slice status remains `In Progress`; Task 5 is now executable and no cross-specification capability becomes ready from Task 12 alone, because `capability:ai-runtime-observation` is published by Task 5.

### 2026-08-04 - Strict API-key cost reservations enforced and runtime-session readiness published (Task 11)

- Completed: Added the `RuntimeCostLedger` schema and migration, the `RuntimeCosts` reservation boundary, the fail-closed `RuntimeCosts.PriceSnapshot` versioned official-price registry, ledger and price fixtures, and focused Task 11 tests. Delivered in parallel with Task 16 under the recorded intra-slice ownership partition, sharing one working tree and separated by disjoint paths, distinct test partitions, and distinct build paths. `capability:ai-runtime-session` is published by this task.
- Proof: The focused proof runner passed 19 tests covering current, stale, missing and changed-version pricing, the bounded conservative maximum, reservation below capacity, reservation exactly at capacity, insufficient capacity, atomic reservation, idempotent duplicate reservation, concurrent reservers, observed-usage reconciliation, over-reconciliation refusal, reservation release, abandoned-reservation recovery, no over-allocation, resumable pause preserving the pinned session, API-key-only application with a ChatGPT session refused as not applicable, the capability contract, and the minimized-field allowlist. Focused formatting passed on all six owned files, and `mix compile --warnings-as-errors` passed across all 279 modules with both parallel tasks applied.
- Proof receipt: `Task 11` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/runtime_costs_test.exs` — exit `0`.
- Proof receipt: `Task 11` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/runtime_cost_ledger.ex lib/sdd_orchestrator/ai_runtime/runtime_costs.ex lib/sdd_orchestrator/ai_runtime/runtime_costs/price_snapshot.ex priv/repo/migrations/20260804130000_create_runtime_cost_ledgers.exs test/sdd_orchestrator/ai_runtime/runtime_costs_test.exs test/support/ai_runtime_fixtures.ex` — exit `0`.
- Proof receipt: `Task 11` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Mechanisms: Every mutation runs inside one transaction that locks the active account, then the `ai_runtime_sessions` row, then the ledger row, each `FOR UPDATE`. Locking the session row also serializes concurrent ledger opens, so no insert-conflict path is needed, and it is where a ChatGPT session is refused as `not_applicable` and a foreign session as `not_found`. The conservative maximum is computed only in `Decimal` and rounded with `:ceiling` at scale 4, so a one-token turn reserves `0.0001` rather than `0.0000` and the estimate can never under-reserve; unit prices are stored per one million tokens at `numeric(18,8)` and amounts at `numeric(12,4)`, matching the pinned ceiling. Outstanding reservations live in one `jsonb` map on the ledger row keyed by idempotency key, deliberately not a second table, because a reservation entity is not part of the approved data model. A replay with identical token bounds returns the stored reservation even if the registered price has since moved, while the same key with different bounds fails as `duplicate_reservation`, mirroring the `pin_session` idempotent-versus-conflict pattern. Over-allocation is defended three times: the row lock, an application invariant that `reserved_amount` always equals the decoded reservation sum, and a database `CHECK (reserved_amount >= 0 and observed_amount >= 0 and reserved_amount + observed_amount <= ceiling)`. Abandoned reservations are released, never charged, after a configurable 900-second default read through `Application.get_env/3` with no configuration-file entry. The price registry defaults to an empty map, so an unconfigured deployment refuses every reservation; each lookup revalidates the whole registry, and any malformed, negative, zero, unparseable, oversized, credential-shaped, or key-versus-version mismatched entry makes the registry untrustworthy as a whole and yields `missing_price`, while an expired newest registration yields `stale_price` and a currency mismatch against the ledger yields `missing_price`.
- Accepted implementation decisions: A pause writes only `paused`, `pause_reason`, and `paused_at`; `reserved_amount`, `observed_amount`, and `outstanding_reservations` are provably untouched and a later successful reservation clears the pause. This follows the approved entity, which lists pause state as ledger row content, while preserving the rule that a pause allocates no capacity and preserves the pinned configuration. Reconciliation takes the observed cost rather than observed tokens, because per-entry unit prices are not retained, and refuses an observed cost above its own reservation. Per-turn token bounds are required and must not exceed the ledger's approved bounded configuration, and re-opening a ledger with different bounds fails as `configuration_conflict`.
- Discovered, recorded for the Task 6 review and the release gate: a registered unit price of exactly zero is rejected as indistinguishable from a missing price, which would make an officially free model unusable under an API-key ceiling. The behavior fails closed and is correct for the currently approved providers; the deployment's real price source is release-gated, so the decision is recorded rather than resolved here.
- Evidence limitation, not an implementation defect: under the Ecto SQL sandbox all test processes share one checked-out connection, so concurrent reservers serialize at the pool instead of contending on the `FOR UPDATE` row lock. The focused proof therefore establishes the outcome — five concurrent reservers against a `1.0000` ceiling yield exactly three reservations and two resumable pauses with `reserved + observed` never exceeding the ceiling — and separately proves the database constraint as the genuine last line of defence, by having four writers deliberately bypass the application lock and observing three accepted and one rejected by `runtime_cost_ledgers_capacity_check`. True multi-connection contention is not reachable inside the sandbox and belongs to the slice verification gate's atomic concurrent allocation check.
- Failed checks: Three development failures, all corrected before the final receipts. `import Ecto.Query` shadowed a private `update/2` and broke compilation until the helper was renamed; this left the shared working tree uncompilable for roughly 25 minutes and blocked the parallel Task 16 agent, which worked around it in a separate copy rather than editing this task's file. Decimal projection equality broke between insert-time and read-time scales and was fixed at the source by rounding unit prices before persistence and normalizing amounts in the projection. A check-constraint violation inside a nested transaction returned `{:error, :rollback}` instead of a changeset until the deliberate lock-bypass test helper used `mode: :savepoint`. No final focused proof failed.
- Spec updates: Task 11 checked complete with its Delivered record. Slice status remains `In Progress`. `capability:ai-runtime-session` is now ready and available to `specs/10-ai-assisted-project-operations#Task 1`, `specs/12-project-assistant#Task 2`, and `specs/16-empty-repository-initialization#Task 1`.

### 2026-08-04 - Model-catalog and quota-snapshot expiry enforced (Task 16)

- Completed: Added the catalog and quota snapshot sweep to `Privacy.Retention`, explicit snapshot export and termination purge to `Privacy.Rights`, and the focused Task 16 expiry test. Delivered in parallel with Task 11 under the recorded intra-slice ownership partition, sharing one working tree and separated by disjoint paths, distinct test partitions, and distinct build paths. Task 16 owns no acceptance criterion and claimed none; the work supports AC-02, AC-04, and AC-15 without redefining them.
- Proof: The focused proof runner passed 58 tests, 19 of them new, covering the catalog and quota expiry boundaries, configured non-default lifetimes, the maximum-lifetime refusal, refresh producing current evidence, selection refusal on expired evidence, refusal to pin a runtime session on expired catalog evidence, terminal-connection cleanup, cascade deletion when a connection row is deleted, sweep idempotency across repeated runs, the advisory lock yielding `:locked` under contention, interrupted-run convergence, account erasure and personal-AI service termination leaving no snapshot, and account export handling both entities without leaking a worker-profile reference. The four pre-existing catalog, quota, retention, and rights test files were re-run unmodified as a regression check. Focused formatting passed on all seven owned files.
- Proof receipt: `Task 16` — scope `Focused` — command `mix test test/sdd_orchestrator/ai_runtime/snapshot_expiry_test.exs test/sdd_orchestrator/ai_runtime/model_catalogs_test.exs test/sdd_orchestrator/ai_runtime/quotas_test.exs test/sdd_orchestrator/privacy/retention_test.exs test/sdd_orchestrator/privacy/rights_test.exs` — exit `0`.
- Proof receipt: `Task 16` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/model_catalogs.ex lib/sdd_orchestrator/ai_runtime/quotas.ex lib/sdd_orchestrator/ai_runtime/model_catalog_snapshot.ex lib/sdd_orchestrator/ai_runtime/quota_snapshot.ex lib/sdd_orchestrator/privacy/retention.ex lib/sdd_orchestrator/privacy/rights.ex test/sdd_orchestrator/ai_runtime/snapshot_expiry_test.exs` — exit `0`.
- Mechanisms: The sweep takes advisory lock key `529_140_776` inside `Repo.checkout/1`, because a session-scoped lock and its unlock must share one connection. The key is deliberately distinct from the revocation sweep's and the pruner's, so a contended revocation pass cannot silently suppress snapshot expiry and neither sweep waits on the other; contention yields `:locked` and deletes nothing. Each table gets one bounded `DELETE` rather than a batched loop, because both tables hold at most one live row per connection and a single statement is atomic, so an interrupted pass leaves no partial state and adds no resumption boundary. The sweep is merged into `prune_all/1` last, after reconciliation and after the terminal-reference delete, so a connection that becomes terminal in the same pass loses its evidence in that pass and the reported counts never double-count rows the connection cascade already removed. The terminal predicate is `revocation_state == "acknowledged" or deletion_scheduled_at is not null`. Erasure adds no explicit delete because the existing cascading `connection_id` foreign key already removes both snapshot tables; that cascade is proven by test rather than assumed. Account export reports what is actually stored, including a row whose lifetime has passed but which the sweep has not yet deleted, and exports the stored expiry alongside it so the access copy cannot be read as current.
- Failed checks: The shared working tree could not compile for roughly 25 minutes while the parallel Task 11 agent's untracked `runtime_costs.ex` held a malformed Ecto update expression. Task 16 did not modify that file; development continued against a read-only copy and both official proofs were then run in the real working tree after the file compiled. One self-inflicted always-true guard warning was removed. No final focused proof failed.
- Discovered, not owned by this task: `ModelCatalogs.current_catalog/3` and `Quotas.current_quota/3` do not re-check connection eligibility on read, so between revocation and the next sweep a connection owner can still read their own revoked connection's projection. It cannot fund work, because `validate_selection` and `pin_session` both refuse `revoking` and `revoked`, and the sweep removes the row. Recorded as a read-side visibility question for the Task 6 privacy and security review rather than widened here.
- Gap found and closed inside this task: `terminate_personal_ai_service/1` previously left catalog and quota evidence alive until the next retention pass, because termination revokes connections without deleting them. It now purges that evidence in the same account-scoped or global scope it already revokes, and reports aggregate counts that name no connection.
- Spec updates: Task 16 checked complete with its Delivered record. Slice status remains `In Progress`; no cross-specification capability becomes ready from Task 16 alone.

### 2026-08-04 - Personal-connection cleanup and credential-revocation reconciliation delivered (Task 13)

- Completed: Added the `PersonalConnectionRevocations` lifecycle module, the revocation-lifecycle migration, the adapter `revoke/4` contract and its RPC implementation, extended the deterministic adapter double, and integrated `PersonalAIConnection` into retention and rights. Delivered under the recorded intra-slice ownership partition with Task 4, which ran in parallel in the same working tree on disjoint paths, its own test partition, and its own build path. Task 13 owns no acceptance criterion and claimed none; the work supports AC-01 and AC-15 without redefining them.
- Proof: The focused proof runner passed 36 tests covering immediate denial, acknowledgement, pending retention, retry, unavailable and unreachable workers, adapter mismatch and malformed acknowledgement, idempotent repeat runs, account erasure, service termination, deletion scheduling, and credential and raw-error absence, plus 54 tests across the affected rights, retention, portability, participation, and AI Connections LiveView surfaces. Focused formatting passed on all eleven owned files.
- Proof receipt: `Task 13` — scope `Focused` — command `MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s11/_build/task13 MIX_TEST_PARTITION=121 mix test test/sdd_orchestrator/ai_runtime/personal_connection_revocations_test.exs test/sdd_orchestrator/ai_runtime/personal_connections_test.exs` — exit `0`.
- Proof receipt: `Task 13` — scope `Focused` — command `MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s11/_build/task13 MIX_TEST_PARTITION=121 mix test test/sdd_orchestrator/privacy/rights_test.exs test/sdd_orchestrator/privacy/retention_test.exs test/sdd_orchestrator/privacy/portability_retention_test.exs test/sdd_orchestrator/privacy/participation_retention_test.exs test/sdd_orchestrator/privacy/portability_rights_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Proof receipt: `Task 13` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/personal_connections.ex lib/sdd_orchestrator/ai_runtime/personal_ai_connection.ex lib/sdd_orchestrator/ai_runtime/personal_connection_adapter.ex lib/sdd_orchestrator/ai_runtime/personal_connection_adapter/rpc.ex lib/sdd_orchestrator/ai_runtime/personal_connection_revocations.ex lib/sdd_orchestrator/privacy/rights.ex lib/sdd_orchestrator/privacy/retention.ex priv/repo/migrations/20260804120000_add_personal_ai_connection_revocation_lifecycle.exs test/support/personal_connection_adapter_double.ex test/sdd_orchestrator/ai_runtime/personal_connections_test.exs test/sdd_orchestrator/ai_runtime/personal_connection_revocations_test.exs` — exit `0`.
- Mechanisms: The existing `active → requested → acknowledged` vocabulary is unchanged and `requested` is the pending state, so no new state had to be introduced. Sweeps take a session-scoped PostgreSQL advisory lock inside `Repo.checkout/1` because the lock and unlock must share one connection, and yield `locked` under contention instead of duplicating worker requests; independently, every bookkeeping write re-reads its row `FOR UPDATE` and refuses to overwrite a row a concurrent pass already acknowledged, so a lost lock still cannot corrupt state. Reconciliation is driven by the already-supervised retention pruner rather than a new supervision-tree child, because `application.ex` lies outside this task's ownership; the reconcile pass runs before the delete pass so a connection acknowledged in that pass starts its own terminal window instead of being deleted in the same sweep. Sweeps are batched at 25 oldest-attempt-first with a five-second per-worker timeout so one pass cannot hold a database connection open. Account erasure asks each worker for removal before the delete transaction, because once the account row is gone nothing names which profile still holds a credential; it never blocks on an unreachable device, and the outstanding request is deliberately not persisted since retaining it would keep data about an erased account.
- Failed checks: Focused formatting failed twice on the owned file list during development and was corrected with `mix format` before the final receipts. No test failure was caused by this task, and no final focused proof failed.
- Discovered slice-gate blocker, not owned by this task: `test/sdd_orchestrator/privacy/specification_governance_test.exs:214` fails on this branch. The assertion joins every `information_schema` public table name into one space-separated string and applies the unanchored pattern `specification.*(cache|snapshot|analytic|telemetry)`, so it matches across table boundaries from `project_specifications` to `model_catalog_snapshots`. The main thread reproduced the failure and confirmed from the captured table list that `model_catalog_snapshots` and `quota_snapshots` both precede the tables added by Tasks 4 and 13, so the false positive was introduced by Task 2's migration and is independent of this parallel work. `ai_runtime_sessions` contains none of the matched words and Task 13 added columns only. The fix is to evaluate the pattern per table name rather than over the joined list. This blocks the slice verification gate and needs an owning task before that gate can pass.
- Spec updates: Task 13 checked complete with its Delivered record. Slice status remains `In Progress`; Tasks 11 and 16 are executable, and no cross-specification capability becomes ready from Task 13 alone.

### 2026-08-04 - Provider-neutral runtime sessions pinned (Task 4)

- Completed: Added the immutable `AIRuntimeSession` schema and migration, the `RuntimeSessions` pinning boundary, session fixtures, and focused Task 4 tests. Delivered under the recorded intra-slice ownership partition with Task 13, which ran in parallel in the same working tree on disjoint paths, its own test partition, and its own build path.
- Proof: The focused proof runner passed 17 tests covering support and working-agent pins, the shared-contract equality that proves both consumer kinds follow one rule set, connection required, not-found, invalid-consumer, cross-account, unavailable, incompatible, revoking and revoked refusals, unproven model, unsupported effort, missing catalog, expired catalog, scarce, model-specific and paid-continuation opt-ins, required API-key ceiling with eight invalid ceiling shapes, refused ChatGPT ceiling, database-level immutability across all pinned columns, catalog refresh leaving an active pin unchanged, concurrent reuse with non-inherited opt-ins and ceilings, idempotent re-pin, configuration conflict, the minimized-field allowlist with no worker-profile disclosure, and rejection of a rogue policy adapter result. Focused formatting passed on all five owned files.
- Proof receipt: `Task 4` — scope `Focused` — command `MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s11/_build/task4 MIX_TEST_PARTITION=120 mix test test/sdd_orchestrator/ai_runtime/runtime_sessions_test.exs` — exit `0`.
- Proof receipt: `Task 4` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/ai_runtime_session.ex lib/sdd_orchestrator/ai_runtime/runtime_sessions.ex priv/repo/migrations/20260804110000_create_ai_runtime_sessions.exs test/sdd_orchestrator/ai_runtime/runtime_sessions_test.exs test/support/ai_runtime_fixtures.ex` — exit `0`.
- Mechanisms: Idempotency is a unique index on `(account_id, consumer_kind, consumer_ref)`, and the equality check compares only the chosen configuration — connection, consumer, model, effort, configuration version, opt-in list and ceiling — deliberately excluding catalog provenance and pin time, so a catalog refresh between two identical pin calls returns the original session instead of a false conflict. Ceilings are `numeric(12,4)` validated through `Decimal` string form rather than float comparison. Catalog provenance is stored as a plain evidence column plus frozen source, method, version, retrieval and expiry fields instead of a foreign key, because `ModelCatalogs.refresh/3` deletes prior snapshots per connection and a foreign key would either cascade an active pin away or block refresh. Staleness is judged by `ModelCatalogs.validate_selection/5` at pin time; the pinned expiry only freezes that evidence for later readers. A pin proceeds on `proceed` or `proceed_to_cost_reservation` and converts any `pause` into a typed error, so quota rules apply identically to both consumer kinds as AC-14 requires; a ChatGPT session therefore cannot be pinned while applicable quota is unknown or stale, which matches the approved unknown-capacity refusal and no-silent-fallback contract.
- Provisional mechanism owned downstream: the session's `connection_id` foreign key uses `on_delete: :delete_all`, matching `model_catalog_snapshots` and `quota_snapshots` and keeping Task 13's terminal connection deletion unblocked. Business rule 104 caps how long historical run configuration may be retained rather than requiring it to outlive its connection, so the minimizing default satisfies the active contract. Task 14 owns `AIRuntimeSession` connection-removal behavior and must confirm or replace this default when it implements session retention; no consumer depends on post-deletion session history in this slice because Slice 07 lifecycle integration is deferred.
- Failed checks: One development compile failed in `personal_connections.ex` with an undefined `locked_connection/1`, caused by the parallel Task 13 agent's in-flight edit to a file Task 4 does not own. The file was left untouched and the later compile and focused proof both succeeded. No final focused proof failed.
- Spec updates: Task 4 checked complete with its Delivered record. Slice status remains `In Progress`; Task 11 is now executable, Task 16 remains executable, and no cross-specification capability becomes ready from Task 4 alone because `capability:ai-runtime-session` is published by Task 11.

### 2026-08-04 - Explicit quota and paid-use policy delivered (Task 10)

- Completed: Added the owner-scoped `QuotaPolicy`, strict `QuotaPolicyAdapter` contract, deterministic default policy, deterministic adapter double, bounded policy fixtures, and focused Task 10 tests. General capacity applies without an opt-in; every exact model-specific bucket and scarce-model classification requires its own current owner choice; provider-paid continuation proceeds only for exhausted applicable capacity with an exact current approval; and API-key work is routed to cost reservation without treating unknown quota or billing as free.
- Proof: The focused proof runner passed 15 tests covering general, matching and nonmatching model-specific, scarce, paid, unknown, missing, stale, opt-in, expiry, revocation before and during evaluation, connection-owner mismatch, connection and model binding, unchanged effort, no fallback, no silent paid use, provider-defined applicability, API-key cost-reservation routing, malformed and credential-shaped input, exact adapter output, and deterministic-double behavior. Focused formatting passed on all six owned implementation, fixture, double, and test paths.
- Proof receipt: `Task 10` — scope `Focused` — command `MIX_TEST_PARTITION=110 mix test test/sdd_orchestrator/ai_runtime/quota_policy_test.exs` — exit `0`.
- Proof receipt: `Task 10` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/quota_policy.ex lib/sdd_orchestrator/ai_runtime/quota_policy_adapter.ex lib/sdd_orchestrator/ai_runtime/quota_policy_adapter/default.ex test/support/ai_runtime_fixtures.ex test/support/quota_policy_adapter_double.ex test/sdd_orchestrator/ai_runtime/quota_policy_test.exs` — exit `0`.
- Mechanisms: The public request is an exact bounded map with no fallback field and one explicit `standard`, `scarce`, or `unknown` scarcity classification. Each opt-in has an exact kind, owner, connection, model, optional bucket, cost boundary, start and expiry; expired, future, duplicate, cross-owner, cross-connection, cross-model, malformed, oversized, and credential-shaped values fail closed. The policy evaluates every general and exact matching model-specific bucket, treats any provider-defined scope as unknown applicability, never derives paid consent from credits, and rechecks connection eligibility after the deterministic adapter returns. Adapter output is accepted only when it exactly matches the authoritative default decision and preserves the selected connection, model and effort.
- Failed checks: The first local focused test invocation passed fixture attributes as a keyword list where the established fixture requires a map, so all 13 initial cases stopped in setup. The invocation was corrected before behavior debugging; the final 15-test proof-runner command and focused formatting both passed with exit 0.
- Spec updates: Task 10 checked complete with its Delivered record. Slice status remains `In Progress`; Task 4 is now executable alongside Tasks 13 and 16, and no cross-specification capability becomes ready from Task 10 alone.

### 2026-08-03 - Live quota and token-activity normalization delivered (Task 3)

- Completed: Added the minimized `QuotaSnapshot` schema and migration, strict provider-neutral quota contract, authenticated `quota/1` RPC adapter, exact Codex rate-limit and usage adapter, bound sparse-notification refetch handler, owner-scoped refresh and projection boundary, deterministic adapter double and fixtures, and live AI Connections quota projection. ChatGPT quota preserves independent general, provider-defined, and explicitly proven model-specific buckets plus reset, credit, paid-continuation, and token-activity facts; API-key quota and billing remain explicitly unknown.
- Proof: The focused proof runner passed 60 tests covering exact App Server requests and profile binding, arbitrary and historical bucket shapes, mirror reconciliation, sparse update validation and complete refetch, general, provider-defined and proven model-specific scope, reset credits, paid continuation, token activity, API-key unknowns, missing and unsupported methods, malformed, oversized, stale, provenance, privacy rejection, failed-refresh invalidation, owner scoping, database ownership and cardinality, cross-node refresh serialization, safe AI Connections projection, and deterministic adapter behavior. Focused formatting passed on all 16 owned implementation, migration, fixture, double, and test files.
- Proof receipt: `Task 3` — scope `Focused` — command `MIX_TEST_PARTITION=113 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs test/sdd_orchestrator/ai_runtime/quota_adapter_codex_test.exs test/sdd_orchestrator/ai_runtime/quotas_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Proof receipt: `Task 3` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/codex_app_server.ex lib/sdd_orchestrator/ai_runtime/quota_adapter.ex lib/sdd_orchestrator/ai_runtime/quota_adapter/codex.ex lib/sdd_orchestrator/ai_runtime/quota_adapter/codex_notification_handler.ex lib/sdd_orchestrator/ai_runtime/quota_adapter/rpc.ex lib/sdd_orchestrator/ai_runtime/quota_snapshot.ex lib/sdd_orchestrator/ai_runtime/quotas.ex lib/sdd_orchestrator_web/live/ai_connections_live.ex priv/repo/migrations/20260803130000_create_quota_snapshots.exs test/support/quota_adapter_double.ex test/support/ai_runtime_fixtures.ex test/support/codex_app_server_fixtures.ex test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs test/sdd_orchestrator/ai_runtime/quota_adapter_codex_test.exs test/sdd_orchestrator/ai_runtime/quotas_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Mechanisms: The Codex adapter validates the installed generated response shape while production admission still depends on the release-gated verified version and schema digest. `rateLimitsByLimitId` values remain provider-defined unless an authenticated contract explicitly proves a model binding; absent, null, empty, omitted, and mirrored maps preserve the historical general bucket without exact-ID duplication. Paid continuation is never derived from credit fields. A sparse notification is source-tagged by its App Server process, accepted only by the handler immutably bound to the same worker profile, and followed by complete rate-limit and usage reads. The control plane deletes prior evidence before contacting the worker, holds a stable per-connection PostgreSQL session advisory lock across deletion, fetch, re-authorization, persistence, and release, and revalidates minimized provenance, expiry, account ownership, connection binding, and forbidden content before projection.
- Failed checks: Independent review found that earlier development revisions inferred model scope from arbitrary limit identifiers, derived paid continuation from credits, left sparse notifications unbound, did not bind facts to their source methods, allowed concurrent refresh races, lacked composite database ownership and one-current-snapshot constraints, crashed the UI on provider-defined buckets, could drop the historical general bucket for an empty or incomplete mirror map, used a node-local lock, and silently ignored notifications when the App Server was bound through a registered name instead of a PID. Each implementation defect was corrected before the final proof-runner receipts. No final focused proof failed.
- Environment incident: During development, an incorrectly scoped migration reset dropped and recreated the local `sdd_orchestrator_dev` database. A read-only count after recreation showed zero rows in `accounts`, `projects`, `personal_ai_connections`, and `quota_snapshots`; prior local development rows require an external backup or snapshot for recovery. The final proof used the isolated `sdd_orchestrator_test113` database; no production database was in scope.
- Spec updates: Task 3 checked complete with its Delivered record. Slice status remains `In Progress`; Tasks 10, 13, and 16 are now executable, and no cross-specification capability becomes ready from Task 3 alone.

### 2026-08-03 - Live model catalog and compatible effort selection delivered (Task 2)

- Completed: Added the minimized `ModelCatalogSnapshot` schema and migration, strict provider-neutral catalog contract, authenticated `catalog/1` RPC adapter, exact Codex `model/list` adapter, account-scoped refresh and compatible-selection boundary, deterministic adapter double and fixtures, and live AI Connections model, effort, provenance, and expiry projection. Enumerated catalogs preserve only verified visible models; enumeration-unsupported results require one proven current or default model; unknown compatibility, invalid selections, stale evidence, and failed refreshes fail closed.
- Proof: The focused proof runner passed 50 tests covering the Codex App Server compatibility seam and typed unsupported-method response, exact generated model schema, bounded pagination, hidden-model exclusion, authenticated RPC projection, live and unsupported catalogs, current and default designation, supported effort validation, stale and unknown denial, account and connection authorization, failed-refresh invalidation, stored-provenance revalidation, malformed, oversized and credential-shaped refusal, deterministic adapter behavior, and safe AI Connections rendering. Focused formatting passed on all 14 owned implementation, migration, fixture, double, and test files.
- Proof receipt: `Task 2` — scope `Focused` — command `MIX_TEST_PARTITION=112 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs test/sdd_orchestrator/ai_runtime/model_catalog_adapter_codex_test.exs test/sdd_orchestrator/ai_runtime/model_catalogs_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Proof receipt: `Task 2` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/codex_app_server.ex lib/sdd_orchestrator/ai_runtime/model_catalog_adapter.ex lib/sdd_orchestrator/ai_runtime/model_catalog_adapter/codex.ex lib/sdd_orchestrator/ai_runtime/model_catalog_adapter/rpc.ex lib/sdd_orchestrator/ai_runtime/model_catalog_snapshot.ex lib/sdd_orchestrator/ai_runtime/model_catalogs.ex lib/sdd_orchestrator_web/live/ai_connections_live.ex priv/repo/migrations/20260803120000_create_model_catalog_snapshots.exs test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs test/sdd_orchestrator/ai_runtime/model_catalog_adapter_codex_test.exs test/sdd_orchestrator/ai_runtime/model_catalogs_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs test/support/ai_runtime_fixtures.ex test/support/model_catalog_adapter_double.ex` — exit `0`.
- Mechanisms: The Codex adapter uses the documented `model/list` request with bounded pages and excludes hidden entries. Exact response validation follows the generated schema of the installed Codex CLI used for focused proof while production admission still depends on the release-gated verified version and schema digest. The control plane never receives the worker-local profile reference in the catalog request or response, stores only allowlisted compatibility facts, deletes prior snapshots before attempting a refresh, and revalidates provenance plus connection eligibility under lock before returning selection evidence. The App Server boundary now exposes only its already verified compatibility pair and normalizes exact JSON-RPC method-not-found failures without exposing raw error content.
- Failed checks: Independent review found that an earlier implementation could retain a prior selectable snapshot after refresh failure, accepted provenance not bound to the verified App Server pair, could not reach the enumeration-unsupported fallback from the normalized App Server error, incompletely validated the generated model schema, permitted a selection-versus-revocation race, and did not revalidate persisted provenance. Each issue was corrected before the final proof-runner receipts. No final focused proof failed.
- Spec updates: Task 2 checked complete with its Delivered record. Slice status remains `In Progress`; Task 3 is now the next executable task, and no cross-specification capability becomes ready from Task 2 alone.

### 2026-08-03 - Account-level AI Connections workflow delivered (Task 9)

- Completed: Added the authenticated `/ai-connections` workflow and Projects entry, current-device paired-worker selection, negotiated personal-AI readiness classification, workspace re-authorization, multiple labelled ChatGPT and API-key handoffs, pending and safe typed results, account-scoped rename, inline revocation confirmation, safe availability, worker recovery actions, and explicit unknown catalog and quota panel boundaries. Added the compile-gated deterministic browser responder over the real `PersonalWorkerRPC`, focused controller and LiveView coverage, and desktop and mobile Playwright scenarios.
- Proof: The focused Elixir proof passed 25 tests covering authentication, missing, unavailable, incompatible and ready workers, selection re-authorization, ChatGPT and API-key handoffs, pending, success, failure, duplicate labels, multiple connections, malformed rename refusal, rename, revoke, forbidden-field absence, browser bootstrap, and existing catalog navigation safety. The focused browser proof passed 10 scenarios across Chromium and mobile Chromium covering worker guidance, link, rename, revoke, Projects navigation, keyboard and result focus, responsive overflow, serious and critical WCAG checks, and secret and raw-identity absence. Focused formatting passed on the seven owned Elixir files.
- Proof receipt: `Task 9` — scope `Focused` — command `MIX_TEST_PARTITION=119 mix test test/sdd_orchestrator_web/live/mixed_catalog_live_test.exs test/sdd_orchestrator_web/controllers/e2e_bootstrap_controller_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Proof receipt: `Task 9` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/personal_connections.ex lib/sdd_orchestrator_web/controllers/e2e_bootstrap_controller.ex lib/sdd_orchestrator_web/live/ai_connections_live.ex lib/sdd_orchestrator_web/live/projects_live.ex lib/sdd_orchestrator_web/router.ex test/sdd_orchestrator_web/controllers/e2e_bootstrap_controller_test.exs test/sdd_orchestrator_web/live/ai_connections_live_test.exs` — exit `0`.
- Proof receipt: `Task 9` — scope `Focused` — command `E2E_MODE=true E2E_BASE_URL=http://localhost:4011 E2E_DATABASE_NAME=sdd_orchestrator_e2e_task9_root E2E_DEVICE_STORE_PATH=/tmp/sdd-orchestrator-s11-task9-proof-root.dets E2E_ISOLATED_RUN=true MIX_BUILD_PATH=/Users/alabenkhlifa/IdeaProjects/sdd-orchestrator-s11/_build/e2e-task9-root ./node_modules/.bin/playwright test e2e/ai-connections.spec.js` — exit `0`.
- Mechanisms: The LiveView derives worker candidates only from the current device workspace, exposes short-lived ordinal display keys instead of raw worker ids, rechecks the selected worker against that workspace before calling the existing connection boundary, and treats missing RPC, wrong protocol, and missing capability as distinct unavailable or incompatible states. Catalog and quota panels remain explicit unknown placeholders until Tasks 2 and 3 deliver authenticated projections. Revocation records immediate control-plane denial only; Task 13 still owns worker credential removal and reconciliation.
- Failed checks: Development caught an over-broad browser-script invocation, a selector ambiguity, an isolated compile-environment mismatch, unsupported icons, and missing focus or malformed-label handling; each was corrected before the final proof-runner receipts. No final focused proof failed.
- Spec updates: Task 9 checked complete with its Delivered record. Slice status remains `In Progress`; Task 2 is now the next executable task, and no cross-specification capability becomes ready from Task 9 alone.

### 2026-08-03 - Account-owned personal AI connections delivered (Task 1)

- Completed: Added the minimized `PersonalAIConnection` schema and migration, account-scoped `PersonalConnections` lifecycle boundary, strict `PersonalConnectionAdapter` contract, production `connection/1` RPC adapter, deterministic adapter/RPC double, focused fixtures, and focused domain tests. Multiple labelled connections are supported; labels are trimmed and account-scoped case-insensitively unique; one worker-local profile cannot be shared across accounts; the same account, worker, profile, provider, and authentication binding replays idempotently without rebinding or renaming; and consumer resolution requires an explicit available connection for either support-assistant or working-agent use.
- Proof: `MIX_TEST_PARTITION=111 mix test test/sdd_orchestrator/ai_runtime/personal_connections_test.exs` passed through the focused proof runner (17 passed, 0 failures); focused `mix format --check-formatted` on the eight owned implementation, migration, fixture, double, and test files passed through the proof runner.
- Proof receipt: `Task 1` — scope `Focused` — command `MIX_TEST_PARTITION=111 mix test test/sdd_orchestrator/ai_runtime/personal_connections_test.exs` — exit `0`.
- Proof receipt: `Task 1` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/personal_ai_connection.ex lib/sdd_orchestrator/ai_runtime/personal_connection_adapter.ex lib/sdd_orchestrator/ai_runtime/personal_connection_adapter/rpc.ex lib/sdd_orchestrator/ai_runtime/personal_connections.ex priv/repo/migrations/20260803110000_create_personal_ai_connections.exs test/support/ai_runtime_fixtures.ex test/support/personal_connection_adapter_double.ex test/sdd_orchestrator/ai_runtime/personal_connections_test.exs` — exit `0`.
- Mechanisms: The control plane stores its own connection id plus the approved account, worker, opaque worker-profile, label, provider, authentication mode, availability, adapter compatibility and revocation fields only. Exact nested adapter-result validation is required because the completed transport intentionally validates the outer envelope rather than provider-specific result fields. Consumer projections keep the worker-local profile reference internal, preserve typed unavailable, incompatible, revoking and revoked outcomes, and have no default-connection or Orchestrator-funded fallback path. Revocation requests and acknowledgements are idempotent state transitions only; Task 13 still owns worker credential removal, retry, reconciliation, terminal reference deletion and rights integration.
- Failed checks: Initial development caught an invalid guard in the adapter validation and a multi-statement migration execution shape; both were corrected before the proof-runner receipts above. No final proof failed.
- Spec updates: Task 1 checked complete with its Delivered record. Slice status remains `In Progress`; Task 9 is now the next executable task, and no cross-specification capability becomes ready from Task 1 alone.

### 2026-08-03 - Version-checked Codex App Server adapter delivered (Task 8)

- Completed: Added the worker-local `CodexAppServer` GenServer, fail-closed compatibility registry, stdio process boundary, protocol fixtures, deterministic process double, and focused adapter tests. The adapter uses newline-delimited JSON-RPC with the `jsonrpc` header omitted, initializes before admitting calls, allowlists only `account/login/start`, `account/login/cancel`, `account/read`, `model/list`, `account/rateLimits/read`, and `account/usage/read`, and admits only the `account/login/completed`, `account/rateLimits/updated`, and `thread/tokenUsage/updated` notifications. Managed ChatGPT browser and device-code login plus worker-local API-key login are supported; external ChatGPT tokens, unknown authentication modes, and non-stdio transports fail closed. Task 1 connection persistence, Task 2 catalog normalization, Task 3 quota normalization, AI Connections UI, and Slice 14 surfaces are untouched.
- Proof: `MIX_TEST_PARTITION=118 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` passed through the focused proof runner (17 passed, 0 failures); focused `mix format --check-formatted` on the six owned implementation, fixture, double, and test files passed through the proof runner. `git diff --check`, the Slice 11 validator, and the global specification graph validator also pass.
- Proof receipt: `Task 8` — scope `Focused` — command `MIX_TEST_PARTITION=118 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` — exit `0`.
- Proof receipt: `Task 8` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/codex_app_server.ex lib/sdd_orchestrator/ai_runtime/codex_app_server/compatibility.ex lib/sdd_orchestrator/ai_runtime/codex_app_server/process.ex test/support/codex_app_server_fixtures.ex test/support/codex_app_server_process_double.ex test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` — exit `0`.
- Mechanisms: Compatibility is an explicit configured map from an installed Codex version to one or more lowercase SHA-256 generated-schema digests and defaults to no accepted production pair; the actual supported deployment pair stays in the release gate. The fixed stdio launcher passes the discovered executable as a quoted positional argument, invokes only `app-server --stdio`, and suppresses stderr at the operating-system boundary. Responses are capped at 256 KiB, require exact correlated JSON-RPC result or error shapes, and are recursively scanned for credential-shaped keys, bearer tokens, API-key shapes, and JWT shapes. Raw App Server errors normalize to `app_server_error`; default request timeout is 15 seconds; cancelled and timed-out ids are bounded and discard one late response; process exit fails pending calls as `process_crashed`, starts a fresh process, and repeats initialization before becoming ready.
- Failed checks: The first development formatting pass caught one escaped default-argument typo, and the first focused development test run reported two default-argument warnings; both were corrected before the proof-runner receipts above. No final proof failed.
- Spec updates: Task 8 checked complete with its Delivered record. Slice status remains `In Progress`; Task 1 is now the next executable task, and no capability becomes ready from Task 8 alone.

### 2026-07-31 - Provider-neutral runtime foundation approved

- Completed: Approved personal connection ownership, credential locality, live catalog provenance, unsupported-catalog behavior, model and effort selection, arbitrary quota buckets, explicit scarcity and paid-use consent, no silent fallback, per-session spending ceilings, immutable configuration pinning, concurrent reuse, operational observation, and access-safe visibility.
- Remaining: Implement Tasks 1 through 6, publish the two capabilities at Tasks 4 and 5, add focused project-shared funding and support consumers, and update Slice 07 before lifecycle integration.
- Failed checks: None recorded at creation.
- Spec updates: Created the focused runtime child and limited its active slice to provider-neutral personal connection, catalog, quota, session, observation, and governance foundations.

### 2026-08-02 - Personal-worker AI RPC transport delivered (Task 7)

- Completed: Task 7 on `slice/11-ai-runtime-governance` under the recorded parallel-ownership partition with `specs/14-repository-execution-profile#Task 1`. Added `SddOrchestratorWeb.PersonalAIWorkerSocket` and `PersonalAIWorkerChannel` (dedicated `/personal_ai_worker` endpoint socket), `SddOrchestrator.AIRuntime.PersonalWorkerProtocol`, `SddOrchestrator.AIRuntime.PersonalWorkerRPC` with its unique connection registry, personal AI protocol fixtures, a personal AI worker double, and focused socket, channel, and RPC tests. The Slice 07 gateway, Delivery modules, router, and all Task 14.1 surfaces are untouched; shared-file edits are one registry child in `application.ex` and one socket block in `endpoint.ex`.
- Proof: `MIX_TEST_PARTITION=117 mix test test/sdd_orchestrator_web/channels/personal_ai_worker_socket_test.exs test/sdd_orchestrator_web/channels/personal_ai_worker_channel_test.exs test/sdd_orchestrator/ai_runtime/personal_worker_rpc_test.exs` exit 0 (40 passed, 0 failures); `mix compile --warnings-as-errors` exit 0; `mix format --check-formatted` on the eleven owned files exit 0; `git diff --check` exit 0; `python3 .agents/scripts/validate_spec.py specs/11-ai-runtime-governance` exit 0; `python3 .agents/scripts/validate_spec.py --all specs` exit 0. Repository-wide gates remain serialized until the parallel Task 14.1 work is reconciled.
- Mechanisms: protocol version `personal-ai/1` with capability allowlist `catalog/1`, `connection/1`, `observation/1`, `quota/1`; request envelope is exactly `account_id`, `capability`, `device_workspace_id`, `idempotency_key`, `params`, `request_id` and response is exactly `account_id`, `request_id`, `result` — unknown fields are refused, which is what keeps credentials, provider identity, raw provider errors, and project content off the transport. Limits configurable under `:personal_ai_protocol_limits` (defaults: 64 KiB envelope, 128-byte idempotency key, 16 announced capabilities, 128 cached responses). Reconnect uses a unique `{device_workspace_id, worker_id}` registry key: the joiner replaces the stale channel, whose pending callers fail typed as `worker_disconnected`. An idempotency key already in flight is refused as `duplicate_request` even for identical content — only a completed identical request returns the cached response; revisit only if Task 8 needs join-on-in-flight semantics. Default request timeout 15s; a late response after timeout or completion is refused as a replay. A response must echo its request's account scope or it is refused as `cross_account` while the request stays pending.
- Failed checks: None.
- Spec updates: Task 7 checked complete with its Delivered record; status advanced to In Progress with Task 8 as the next executable task.

### 2026-08-01 - Local Codex runtime contract technically refined

- Completed: Scoped the first provider setup to an already authorized paired local worker; approved multiple labelled worker-local Codex profiles; assigned the account-level AI Connections workflow; selected a separate personal-worker RPC and version-checked Codex App Server standard-input and standard-output adapter; separated ChatGPT quota from API-key billing; approved strict conservative cost reservation; kept per-agent presentation with Slice 07 and Slice 12; and split the implementation, lifecycle, and governance work into standard tasks.
- Remaining: Implement Tasks 7, 8, 1, 9, 2, 3, 10, 4, 11, 12, 5, 13, 16, 14, 17, 15, and 6 in dependency order, recording runtime-session readiness after Task 11, observation readiness after Task 5, and governance readiness after Task 6.
- Failed checks: None recorded during discovery; run the individual validator, validator regression suite, global capability graph, and patch-integrity check after applying this agreement.
- Spec updates: Added the local-worker capability edge; moved runtime-session readiness from Task 4 to Task 11; added `RuntimeCostLedger`; mapped every transport, adapter, UI, domain, lifecycle, privacy, and proof surface; and updated the Slice 10, Slice 12, and Slice 16 consumer edges.
