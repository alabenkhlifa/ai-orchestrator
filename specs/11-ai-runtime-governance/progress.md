# AI Runtime Governance Progress Log

### 2026-08-03 - Version-checked Codex App Server adapter delivered (Task 8)

- Completed: Added the worker-local `CodexAppServer` GenServer, fail-closed compatibility registry, stdio process boundary, protocol fixtures, deterministic process double, and focused adapter tests. The adapter uses newline-delimited JSON-RPC with the `jsonrpc` header omitted, initializes before admitting calls, allowlists only `account/login/start`, `account/login/cancel`, `account/read`, `model/list`, `account/rateLimits/read`, and `account/usage/read`, and admits only the `account/login/completed`, `account/rateLimits/updated`, and `thread/tokenUsage/updated` notifications. Managed ChatGPT browser and device-code login plus worker-local API-key login are supported; external ChatGPT tokens, unknown authentication modes, and non-stdio transports fail closed. Task 1 connection persistence, Task 2 catalog normalization, Task 3 quota normalization, AI Connections UI, and Slice 14 surfaces are untouched.
- Proof: `MIX_TEST_PARTITION=118 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` passed through the focused proof runner (17 passed, 0 failures); focused `mix format --check-formatted` on the six owned implementation, fixture, double, and test files passed through the proof runner. `git diff --check`, the Slice 11 validator, and the global specification graph validator also pass.
- Proof receipt: `Task 8` — scope `Focused` — command `MIX_TEST_PARTITION=118 mix test test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` — exit `0`.
- Proof receipt: `Task 8` — scope `Focused` — command `mix format --check-formatted lib/sdd_orchestrator/ai_runtime/codex_app_server.ex lib/sdd_orchestrator/ai_runtime/codex_app_server/compatibility.ex lib/sdd_orchestrator/ai_runtime/codex_app_server/process.ex test/support/codex_app_server_fixtures.ex test/support/codex_app_server_process_double.ex test/sdd_orchestrator/ai_runtime/codex_app_server_test.exs` — exit `0`.
- Mechanisms: Compatibility is an explicit configured map from an installed Codex version to one or more lowercase SHA-256 generated-schema digests and defaults to no accepted production pair; the actual supported deployment pair stays in the release gate. The fixed stdio launcher passes the discovered executable as a quoted positional argument, invokes only `app-server --stdio`, and suppresses stderr at the operating-system boundary. Responses are capped at 256 KiB, require exact correlated JSON-RPC result or error shapes, and are recursively scanned for credential-shaped keys, bearer tokens, API-key shapes, and JWT shapes. Raw App Server errors normalize to `app_server_error`; default request timeout is 15 seconds; cancelled and timed-out ids are bounded and discard one late response; process exit fails pending calls as `process_crashed`, starts a fresh process, and repeats initialization before becoming ready.
- Failed checks: The first development formatting pass caught one escaped default-argument typo, and the first focused development test run reported two default-argument warnings; both were corrected before the proof-runner receipts above. No final proof failed.
- Spec updates: Task 8 checked complete with its Delivered record. Slice status remains `In Progress`; Task 1 is now the next executable task, and no capability becomes ready from Task 8 alone.

### 2026-07-31 - Provider-neutral runtime foundation approved

- Completed: Approved personal connection ownership, credential locality, live catalog provenance, unsupported-catalog behavior, model and effort selection, arbitrary quota buckets, explicit scarcity and paid-use consent, no silent fallback, per-session spending ceilings, immutable configuration pinning, concurrent reuse, operational observation, and access-safe visibility.
- Remaining: Implement Tasks 1 through 6, publish the two capabilities at Tasks 4 and 5, add focused project-shared funding and support consumers, and update Slice 07 before lifecycle integration.
- Failed checks: None recorded at creation.
- Spec updates: Created the focused runtime child and limited its active slice to provider-neutral personal connection, catalog, quota, session, observation, and governance foundations.

### 2026-08-02 - Personal-worker AI RPC transport delivered (Task 7)

- Completed: Task 7 on `slice/11-ai-runtime-governance` under the recorded parallel-ownership partition with `specs/14-repository-execution-profile#Task 1`. Added `SddOrchestratorWeb.PersonalAIWorkerSocket` and `PersonalAIWorkerChannel` (dedicated `/personal_ai_worker` endpoint socket), `SddOrchestrator.AIRuntime.PersonalWorkerProtocol`, `SddOrchestrator.AIRuntime.PersonalWorkerRPC` with its unique connection registry, personal AI protocol fixtures, a personal AI worker double, and focused socket, channel, and RPC tests. The Slice 07 gateway, Delivery modules, router, and all Task 14.1 surfaces are untouched; shared-file edits are one registry child in `application.ex` and one socket block in `endpoint.ex`.
- Proof: `MIX_TEST_PARTITION=117 mix test test/sdd_orchestrator_web/channels/personal_ai_worker_socket_test.exs test/sdd_orchestrator_web/channels/personal_ai_worker_channel_test.exs test/sdd_orchestrator/ai_runtime/personal_worker_rpc_test.exs` exit 0 (40 passed, 0 failures); `mix compile --warnings-as-errors` exit 0; `mix format --check-formatted` on the eleven owned files exit 0; `git diff --check` exit 0; `python3 .agents/scripts/validate_spec.py specs/11-ai-runtime-governance` exit 0; `python3 .agents/scripts/validate_spec.py --all specs` exit 0. Repository-wide gates remain serialized until the parallel Task 14.1 work is reconciled.
- Mechanisms: protocol version `personal-ai/1` with capability allowlist `catalog/1`, `connection/1`, `observation/1`, `quota/1`; request envelope is exactly `account_id`, `capability`, `device_workspace_id`, `idempotency_key`, `params`, `request_id` and response is exactly `account_id`, `request_id`, `result` — unknown fields are refused, which is what keeps credentials, provider identity, raw provider errors, and project content off the transport. Limits configurable under `:personal_ai_protocol_limits` (defaults: 64 KiB envelope, 128-byte idempotency key, 16 announced capabilities, 128 cached responses). Reconnect uses a unique `{device_workspace_id, worker_id}` registry key: the joiner replaces the stale channel, whose pending callers fail typed as `worker_disconnected`. An idempotency key already in flight is refused as `duplicate_request` even for identical content — only a completed identical request returns the cached response; revisit only if Task 8 needs join-on-in-flight semantics. Default request timeout 15s; a late response after timeout or completion is refused as a replay. A response must echo its request's account scope or it is refused as `cross_account` while the request stays pending.
- Failed checks: None.
- Spec updates: Task 7 checked complete with its Delivered record; status advanced to In Progress with Task 8 as the next executable task.

### 2026-08-01 - Local Codex runtime contract technically refined

- Completed: Scoped the first provider setup to an already authorized paired local worker; approved multiple labelled worker-local Codex profiles; assigned the account-level AI Connections workflow; selected a separate personal-worker RPC and version-checked Codex App Server standard-input and standard-output adapter; separated ChatGPT quota from API-key billing; approved strict conservative cost reservation; kept per-agent presentation with Slice 07 and Slice 12; and split the implementation, lifecycle, and governance work into standard tasks.
- Remaining: Implement Tasks 7, 8, 1, 9, 2, 3, 10, 4, 11, 12, 5, 13, 16, 14, 17, 15, and 6 in dependency order, recording runtime-session readiness after Task 11, observation readiness after Task 5, and governance readiness after Task 6.
- Failed checks: None recorded during discovery; run the individual validator, validator regression suite, global capability graph, and patch-integrity check after applying this agreement.
- Spec updates: Added the local-worker capability edge; moved runtime-session readiness from Task 4 to Task 11; added `RuntimeCostLedger`; mapped every transport, adapter, UI, domain, lifecycle, privacy, and proof surface; and updated the Slice 10, Slice 12, and Slice 16 consumer edges.
