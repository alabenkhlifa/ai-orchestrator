# Empty Repository Initialization Progress Log

### 2026-08-11 - Verification Gate passed, one documented exception; slice Verified

- Completed: Ran the full Verification Gate. `MIX_TEST_PARTITION=624990 mix check` (format, compile --warnings-as-errors, credo --strict, full test suite), `mix dialyzer`, `mix sobelow --config`, `mix deps.audit`, `npm --prefix assets ci`, `MIX_TEST_PARTITION=624990 npm --prefix assets run test:e2e` (256 passed across desktop + mobile chromium, 4 skipped — the already-documented `test.skip`'d repository-initialization Playwright scenarios from Tasks 2/3/6, the recurring `device_worker_stub_folder`/`E2EBootstrapController` seam limitation, not new), `env MIX_ENV=prod mix assets.deploy`, `env MIX_ENV=prod mix release`, `validate_spec.py specs/16-empty-repository-initialization`, `validate_spec.py --all specs` (34 specifications), and `split_progress_log.py --check` all pass with real exit `0`.
- Environment note, not an implementation defect: `run_proof.py`'s `MIX_TEST_PARTITION` auto-injection matches the literal command `mix test` only, not the `mix check` alias (which internally runs its own `ecto.create`/`ecto.migrate`/`test`) — so a bare `run_proof.py slice -- mix check` targets the shared, unpartitioned default test database rather than this worktree's own, which was left in an inconsistent migration state by unrelated concurrent worktree activity (`relation "repository_initialization_runs" does not exist` during `ecto.migrate`, reproducing identically even after a full `ecto.drop`/`create`/`migrate` reset of the *partitioned* database, because that reset targeted the wrong database). Resolved by supplying the worktree's own partition explicitly as a caller override — `run_proof.py slice -- MIX_TEST_PARTITION=624990 mix check` — the documented, intended mechanism ("a caller-supplied value still wins"), not a workaround. No source file, migration, or `run_proof.py` itself was changed; this is a reusable operational note for the next slice that runs `mix check` (rather than literal `mix test`) through the slice gate.
- Documented exception, not new: `mix check`'s full test run (3519/3525 passed, 6 failed) hit two categories, both confirmed by isolated re-runs, not by this slice: (1) `SddOrchestrator.RepositoryInitialization.StagingWorkspaceTest` and `SddOrchestrator.Participation.IdentityLifecycleCompatibilityTest` each failed once in the full 3525-test concurrent run but passed cleanly and repeatably in isolation (17/17 and 28/28 together across three different seeds) — full-suite concurrency ordering, not a real regression, and neither touches any file this slice or Task 7 changed. (2) `SddOrchestrator.Delivery.LocalWorkerRuntimeProjectionTest`, `SddOrchestrator.Participation.AcceptanceTest`, `SddOrchestrator.Delivery.RevocationConsumerTest`, and `SddOrchestratorWeb.InvitationAcceptanceLiveTest` reproduce identically in complete isolation — these are the exact same four pre-existing, unrelated failures `specs/35-guided-delivery-feature-specification-link` already investigated, reproduced against unmodified `main`, and had explicitly accepted as a documented exception the same day (see `specs/35-guided-delivery-feature-specification-link/progress.md`, "2026-08-11 - Verification Gate passed, one documented exception; slice Verified"); confirmed here that `origin/main`'s own current `invitation_acceptance_live.ex` already carries the same `Map.fetch!(@accept_messages, reason)` gap for `:identity_lifecycle_conflict`, proving it predates and is independent of this slice. No file any specs/16 task touched is implicated in either category. Following the same precedent rather than expanding this slice's scope to investigate or fix another specification's own defect.
- specs/16's own delivered surfaces remain fully clean throughout: every `repository_initialization*`, `repository_initialization_live`, and the new `privacy/repository_initialization_privacy_test.exs` suite passed in every run, isolated and as part of the full suite.
- `## Status` set to `Verified`. Implementation and local-verification readiness are complete. Release readiness remains open per this slice's own already-declared Release gates (live configured worker/working-agent smoke proof on each supported OS profile; deployment-specific controller, processor, model, worker, region, transfer, notice, retention-enforcement, incident, package-distribution, and accountable privacy/legal review evidence) — unaffected by this gate.
- Failed checks: None outstanding for this slice's own scope. The four pre-existing failures above remain open as a separately-owned, cross-cutting issue already tracked via the specs/35 precedent, not owned by this specification.

### 2026-08-11 - Task 7 implemented: governance and initialization-readiness capability

- New `RepositoryInitialization.SecurityLog` (AC-15, mirroring `Specifications.SpecificationSecurityLog`'s fixed-event/redacted-outcome shape rather than `Portability.SecurityLog`'s heavier correlation-id struct — proportionate to a governance retrofit, not a new feature): a closed `@events` list (`:confirm_plan, :start_run, :build, :publish, :handoff`), logging only on failure via `Logger.warning`, mapping every already-documented closed typed error atom from those five functions to one of four redacted outcome classes (`validation_rejected`, `denied_or_missing`, `identity_conflict`, `operation_failed`). Wired around `RepositoryInitialization.confirm_plan/3`, `StagingBuilder.start_run/4`, `StagingBuilder.build/3`, `Publisher.publish/3`, and `Handoff.complete/4` — a plan/run/result value, target reference, product answer, kit choice, worker id, commit sha, and tree digest are never logged, only the fixed event name and outcome class.
- Role-access fix: `RepositoryInitialization.get_plan/1` fetched by id alone, unscoped by device workspace — unlike every other cross-workspace-sensitive fetch in this codebase (`Devices.get_project/1`, `PackageProvenances.get/2`). Changed to `get_plan/2(device_workspace_id, id)`, refusing `{:error, :not_found}` for a plan belonging to a different workspace exactly as for an unknown id. `confirm_plan/2` became `confirm_plan/3(plan, device_workspace_id, client_snapshot)`, threading the caller's own workspace through its internal re-fetch instead of trusting the passed-in plan struct's own field. Both real call sites (`repository_initialization_live.ex`, `staging_builder.ex`'s plan-staleness re-check) and their test call sites updated; no path was exploitable today (both call sites already held their own workspace/plan context), but the function itself was unscoped by construction, unlike the rest of the codebase's convention.
- `Privacy.ProcessingInventory`: new `:repository_initialization` processing-activity record and a new `repository_initialization_field_purposes/0` function (mirroring `specification_field_purposes/0`/`ai_runtime_field_purposes/0`) covering every persisted `Plan`/`Run`/`Result` field.
- `Privacy.Retention`: new dual-regime rule for `RepositoryInitialization.{Plan,Run,Result}` (AC-15's retention/deletion rule) — a plan with no run at all is deleted 24 hours after its last update (mirrors `prune_onboarding_attempts/1`'s own unconsumed-abandonment idiom); a run left `"failed"`/`"canceled"` with no `Result` is deleted 24 hours after it finished, together with its now-unreferenced plan; `"pending"`/`"running"` runs are untouched (in-flight, out of this task's scope); once a `Result` exists, its plan/run/result are never touched by this timer regardless of `onboarding_handoff_state` — this module's own existing moduledoc rule for "confirmed project metadata" ("kept while the account or project requires them and are removed by account erasure, not by time") now explicitly covers them too, since a completed result is the project's own confirmed birth record.
- `Privacy.Rights`: `export_account/1` gains a `repository_initialization_plans` entry (each plan's own run and result nested in, field-selected, no raw struct/path/secret exposure); `erase_account/2`'s `Multi` gains an account-scoped delete (results → runs → plans, explicit child-before-parent order — no DB-level cascade exists between these three tables, by design, since they are a separate store from device-project data).
- Deliberately not built: a device-project-deletion cascade into these hosted rows. `Result`/`Run`/`Plan` are hosted Postgres rows that softly reference a device-local project id (no FK, by the entities' own original design) — reaching from `Devices.delete_project/1`'s device-local DETS store into hosted Postgres would cross a store boundary this slice has held throughout Tasks 4-6, and no live caller today needs it. Account-scoped `Rights.erase_account/2` is the correct, reachable exit path instead (Plan already carries `account_id`), matching the exact "removed by account erasure, not by time" precedent `Retention`'s own moduledoc already established for encrypted GitHub credentials and confirmed project metadata.
- New `test/sdd_orchestrator/privacy/repository_initialization_privacy_test.exs` (21 tests) covering every category Task 7's own proof line names: authority-transition (dual retention regime, including a `far_future` survival check for a completed result mirroring `RepositoryPilotAndReadinessPrivacyTest`'s own idiom), worker-locality, source-upload negative (`SupportDispatch` never projects staging/source content), access (cross-workspace `get_plan/2` denial), lifecycle/cancellation cleanup, rights (export/erasure), processor/transfer (no HTTP-client import; `transfers` field), redaction (capture_log marker-absence test on `SecurityLog`), backup (`DeploymentPrivacyProfile.retention_requirements/0` unchanged at 30/35 days), and no-analytics/no-secondary-use.
- Capability readiness: `capability:initialized-sdd-repository` is ready after Task 7 (`specs/13-sdd-adoption#Task 1` is its one consumer, blocked umbrella work unaffected by this change).
- Proof receipt: `Task 7` — scope `Focused` — command `mix test test/sdd_orchestrator/privacy/repository_initialization_privacy_test.exs test/sdd_orchestrator/repository_initialization_test.exs test/sdd_orchestrator/repository_initialization/staging_builder_test.exs test/sdd_orchestrator/repository_initialization/publisher_test.exs test/sdd_orchestrator/repository_initialization/handoff_test.exs test/sdd_orchestrator_web/live/repository_initialization_live_test.exs` — exit `0`.
- 119 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`, real exit status, not piped) after reviewing every changed/new file's diff directly. Also independently reconfirmed by the main thread (not just relayed from the sub-agent): `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict` (657 files, no issues), `mix dialyzer`, `mix sobelow --config` (scan complete, no findings), and `mix deps.audit` (no vulnerabilities) all exit `0`. `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` (34 specifications) both pass.
- Status: Task 7 complete. This was the slice's last task; the Verification Gate is next.
- Failed checks: None.

### 2026-08-11 - Task 6 implemented: onboarding and specification handoff

- New `RepositoryInitialization.Handoff` (AC-13), continuing directly from a `Publisher.publish/3` success. Pipeline: `Devices.select_repository/2` on the now-published `target_path` (portable repository identity — computable only now, since the directory was empty before Task 5 published into it) → `Projects.start_device_onboarding_attempt/2` → `select_local_repository/3` → a duplicated `readiness_receipt/2` (same shape as `LocalOnboardingLive`'s own private helper) → `record_device_receipt/3` → `select_storage_mode(..., "device")` → `register_device_project/3` (`allocate_suffix?: true` — no user-edited name to protect in this fully automated flow, unlike `LocalOnboardingLive`'s own `false`) → `SpecificationRenderer.render/1` → `SpecificationStore.create/4` (reusing `result.id` as the specification's own id, a stable 1:1 tie needing no new UUID) → `Result.handoff_changeset/2` setting `project_id`/`specification_id`/`onboarding_handoff_state: "completed"`. Idempotency is gated purely by `onboarding_handoff_state`: `"completed"` short-circuits every later call before touching the device store or hosted Postgres again; any failure before that leaves `Result` completely unchanged at `"pending"` (safe to retry from scratch, since nothing before project registration has a side effect outside a fresh, disposable `ProjectOnboardingAttempt`).
- Extended `Result` (Task 5's own entity) with `project_id`/`specification_id` (migration `20260811120000_add_repository_initialization_result_handoff.exs`, plain `:binary_id`, no FK — device projects and device specifications live only in the worker-owned device-local store, never in hosted Postgres) and a new `handoff_changeset/2`, mirroring how Task 3 already extended Task 2's `Plan` schema for its own later concern.
- New `RepositoryInitialization.SpecificationRenderer`: a pure `render/1` turning one confirmed `Plan` into the three document strings `capability:project-specification-store` needs. `requirements` carries purpose/users/first_outcome/constraints verbatim (not paraphrased). `design` carries `technical_foundation` (rendered for both its possible shapes — a free-text `"summary"` or a structured key/value map), `kit_choice`, and the fixed `Skeleton.content()`'s own structure/git-behavior. `tasks` explicitly states no implementation tasks are defined yet and points at `add-spec` for the first real feature — deliberately not inventing a task breakdown with no source data, the same discipline `Skeleton`'s own content and Task 3's plan-review already established for this slice.
- New `RepositoryInitialization.Readiness` (AC-14): a small, Task-6-owned four-axis view, deliberately distinct from `SddOrchestrator.RepositoryReadiness` (mirrors its stage/axis_status/`earliest_blocked_stage` vocabulary only) since that module requires an approved execution profile and a selected pilot (Slice 14/specs/30) that a repository fresh out of empty-repository initialization has never gone through. `:assistant` is ready once a worker is paired; `:specification` is ready once `result.specification_id` is set; `:agent_execution` and `:release` are always blocked (`:no_approved_execution_profile`, `:release_gate_pending`) — this slice's own Release gates (live worker/working-agent smoke proof, deployment-specific privacy/legal evidence) remain outstanding for every project this task hands off, with no exception.
- Wired the LiveView's `:reviewing_plan` confirmed state: a new "Start building" action (`start_build` event) runs `StagingBuilder.start_run/4` (Task 4) → `Publisher.publish/3` (Task 5) → `Handoff.complete/4` in sequence, using a deterministic `"init:" <> plan.id` idempotency key (not a random one) so re-clicking after a page reload replays safely end to end. A missing paired worker shows an inline error without leaving `:reviewing_plan`; any later pipeline failure (2- or 3-tuple error shapes from any of the three modules) moves to a new `:failed` step showing the reason. Full success moves to a new `:building_result` step showing the commit/tree and the four readiness axes, each with its own `data-readiness-*` attribute plus a `data-earliest-blocked-stage` attribute. This is the point this slice's own Tasks 4/5 progress notes both anticipated and deliberately deferred ("no LiveView wiring calls `Publisher.publish/3` yet") — Task 6 is where the full build→publish→handoff pipeline finally gets a live trigger, closing Task 2's own long-open "resolve `target_reference` back to a path" gap for this one continuous session (`socket.assigns.target_path` was already held there the whole time).
- One cross-file fix, mechanical not a design change: `mix dialyzer` failed against the new `Handoff` module because `SpecificationStore.create/4`'s own `@spec` (`lib/sdd_orchestrator/specification_store.ex`, specs/09-owned) declared only `PersonalWorkspace.t()` as its first argument even though the function already has a real `%DeviceWorkspace{}` clause — unlike its sibling functions (`get_current/3`, `append_revision/5`, etc. in the same file), which already correctly declare both types. No existing caller had ever passed a `DeviceWorkspace` to `create/4` until now, so this pre-existing type-annotation bug was never triggered before. Widened to `PersonalWorkspace.t() | DeviceWorkspace.t()`, matching the sibling functions exactly — a one-line type-annotation correction with zero runtime behavior change, confirmed by the full regression suite passing unchanged before and after.
- Proof receipt: `Task 6` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_initialization/specification_renderer_test.exs test/sdd_orchestrator/repository_initialization/readiness_test.exs test/sdd_orchestrator/repository_initialization/handoff_test.exs test/sdd_orchestrator_web/live/repository_initialization_live_test.exs` — exit `0`.
- 42 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`) after reviewing every new/changed file, plus a regression run of the full `repository_initialization/` directory, `repository_initialization_live_test.exs`, `local_onboarding_live_test.exs`, and `initialization_dispatch_test.exs` together (142 passed, repeated across three random seeds, no flakiness). Also confirmed: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer`, `mix sobelow --config`, and `mix deps.audit` all clean; both databases migrated (`mix ecto.migrate` and the worktree's own `MIX_TEST_PARTITION=624990` test database); `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization`, `python3 .agents/scripts/validate_spec.py --all specs`, and `python3 .agents/scripts/split_progress_log.py --check` all pass.
- One item written but not executed, matching Tasks 2/3's own precedent exactly: the new Task 6 Playwright scenario in `assets/e2e/empty-repository-initialization.spec.js` is `test.skip`'d for the same `device_worker_stub_folder`/`E2EBootstrapController` seam limitation already documented there, plus the additional `:initialization_staging_root` configuration the running server would need for a real browser run — not fixed here, same recurring out-of-scope surface flagged repeatedly across this slice.
- Status: Task 6 complete. Task 7 (`Depends on: Task 6`) is next; no capability blocker.
- Failed checks: None.

### 2026-08-11 - Task 5 implemented: verify, commit, and publish

- New `RepositoryInitialization.Result` (`entity:RepositoryInitializationResult`, migration `20260811110000_create_repository_initialization_results.exs`): one immutable row per `run_id` (unique) holding the plan/run references, the plan's own opaque `target_reference` (never a real path), `commit_sha`/`tree_digest`, the frozen kit snapshot, empty `check_evidence`, `completed_at`, and an `onboarding_handoff_state` that starts `"pending"` for Task 6 to advance later.
- New `RepositoryInitialization.Publisher`, continuing directly from a Task-4-`"completed"` `Run` (staging built, no commit yet). Like `StagingBuilder`, it never routes through `AgentAdapter` — verifying the target, running `git add`/`git commit`, and copying already-known files are fully deterministic. Pipeline: revalidate target (AC-09) → idempotent first commit + checked-tree binding (AC-11) → rollback-safe transfer → `Result` persistence.
  - Idempotent replay is decided by `Result` existing for `run_id`, not by `run.state` — a repeat call short-circuits before touching Git or the filesystem again. Before that row exists, an already-present staging commit (`git rev-parse HEAD` succeeding) is reused rather than re-committed, which is the actual mechanism that makes retrying after a publish-phase failure safe.
  - Target revalidation re-runs `Eligibility.classify/1` and requires it to match the plan's *originally recorded* eligibility exactly (`:target_changed` on a mismatch, e.g. a still-eligible but different-kind target), independently checks the top-level entry is not a symlink via `File.lstat/1` (`:target_symlinked`), and independently checks `File.stat/1`'s `access == :read_write` (`:target_permission_changed`) — three distinct failure reasons rather than one collapsed one, so the visible failure stage/evidence (AC-12) says which boundary moved.
  - The staging precondition is "does this run's staging directory still physically exist," not `run.state in [...]` — `StagingBuilder`'s own failure/cancellation paths already `rm_rf` staging, so state alone can't distinguish "Task 4 failed, nothing to publish" from "Task 5 failed on a prior attempt, staging (and maybe a commit) is still there, retry it." A Task-5-level failure therefore never cleans up staging (unlike `StagingBuilder`'s own failure handling) so a retry after the underlying problem is fixed can resume from wherever it stopped.
  - Rollback-safe publication copies staging's contents *into* the already-revalidated-empty target directory (`File.cp_r/2`, verified to merge into an existing directory rather than nest under it) rather than replacing the target directory itself, then removes staging only after a successful copy. A partial-copy failure sweeps away only the entries this attempt itself just placed under target (safe because target was just proven empty moments earlier — never pre-existing user data) and returns `:publish_failed` with staging left intact.
  - Required-check evidence is a typed empty list (`{"step" => "checks_passed", "checks" => []}`) — the fixed skeleton's own `commands`/`checks` are `[]` (Task 3's decision), so there is nothing to actually execute; building real command execution here would be inventing scope the plan doesn't have yet. Flagged explicitly so this isn't later mistaken for a completed "run the confirmed checks" mechanism.
  - Deliberately not done, matching this slice's own established pattern (Task 4 left `AgentAdapter`/live-caller wiring for later; Task 2 left `target_reference`-to-path resolution for later): no LiveView wiring calls `Publisher.publish/3` yet. `target_path` is a plain function argument the caller must already hold — the confirmation step's own "Plan confirmed — building the repository continues in a later step" placeholder is untouched. This task's own proof line omits "LiveView" (unlike Tasks 2/3/6), only naming "browser" — read as the same pre-existing, still-`test.skip`'d Playwright gap Tasks 2/3 already left for the `E2EBootstrapController` seam, not a new obligation to wire a live trigger.
- Fixed one real bug found while writing the target-revalidation tests: comparing `plan.eligibility` (a string) against `Eligibility.classify/1`'s return (an atom) with `==` pattern-matching never matches regardless of value — corrected to `Atom.to_string/1` comparison.
- Fixed one real, pre-existing gap in Task 4's own `StagingBuilder`, discovered while confirming this task's own `mix sobelow --config` cleanliness (not previously run/recorded by any task in this slice): `write_readme_file/3` and `maybe_chmod/2` write through `StagingWorkspace`-contained paths exactly like `write_kit_file/3` and `disable_git_hooks/1` in the same file already do, but were missing that pair's own `# sobelow_skip ["Traversal.FileModule"]` documented-false-positive annotation. Added it, matching the existing pattern exactly; no behavior changed. `mix sobelow --config` (`exit: "low"`) was failing on `main`/this branch before this fix and is clean now.
- Fixed one test-isolation issue found while running this task's tests alongside `StagingBuilderTest`: both files mutate the same global `Application.put_env(:sdd_orchestrator, :initialization_staging_root, ...)` key from `async: true` test cases, which raced across files once a second file used the same key (`PublisherTest`'s own 14 tests were fully green in isolation, but 10 failed intermittently when run together with `StagingBuilderTest`). Resolved by marking `PublisherTest` `async: false`, which fully serializes it after every async module — `StagingBuilderTest` itself is unchanged and outside this task's owned surfaces. Reconfirmed stable across three repeated runs with random seeds.
- Proof receipt: `Task 5` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_initialization/publisher_test.exs` — exit `0`.
- 14 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`) after reviewing every new/changed file, plus a regression run of the full `repository_initialization/` directory, `repository_initialization_live_test.exs`, and `initialization_dispatch_test.exs` together (110 passed, repeated three times with random seeds, no flakiness). Also confirmed: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer`, `mix sobelow --config`, `mix deps.audit`, and `mix ecto.migrate`/`mix ecto.migrations` all clean; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` both pass.
- Status: Task 5 complete. Task 6 (`Depends on: Task 5`) is next; no capability blocker.
- Failed checks: None.

### 2026-08-11 - Task 4 implemented: staging build

- Implemented against the preflight decision recorded immediately below (staging execution bypasses `AgentAdapter` entirely). New `RepositoryInitialization.StagingWorkspace` mirrors `Delivery.Worker.Workspace`'s symlink-safe, real-path containment pattern (specs/33), adapted from `ExecutionManifest`'s `project_id`/`run_id` to this pre-project flow's own `Run.id` (a UUID, proven safe via `Ecto.UUID.cast/1` rather than `WorkerProtocol.valid_id?/1`, which is the delivery protocol's own distinct id grammar). `StagingWorkspace.join/2` layers two independent defenses per relative path — a lexical `..`/absolute-path guard (necessary because neither `Path.join/2` nor the symlink-following `real_path` check collapses a literal `..` segment) and the same real-path containment check `prepare/1` uses for the staging root itself — since a purely symlink-based check cannot catch a literal traversal string on its own.
- New `RepositoryInitialization.Run` (`entity:RepositoryInitializationRun`, migration `20260811100000_create_repository_initialization_runs.exs`): state machine (`pending -> running -> completed | failed | canceled`), a frozen kit-choice/package snapshot taken at run creation (defense in depth beyond `Plan`'s own invalidation rules — the "immutable plan command" business rule), a unique `idempotency_key` so a retried run request never starts a second build, and an ordered `progress` log using the same typed-event vocabulary `AgentAdapter.observe/2` already normalizes to (`progress`/`evidence`/`failed`, minus `blocked`, which doesn't apply to a fully deterministic build) — chosen for consistency even though nothing here goes through `AgentAdapter`, so a later task rendering run activity has one event shape to work from regardless of which task produced it.
- New `RepositoryInitialization.StagingBuilder`: `start_run/4` re-derives the plan's live confirmation snapshot and recomputes its digest exactly the way `RepositoryInitialization`'s own (private) `persist_confirmation/2` does — canonical JSON via `CanonicalJson`, sha256 hex — and compares against the live plan row's stored `confirmation_digest`, never trusting the caller's passed-in plan struct (the actual "plan-staleness" proof, not cosmetic); refuses unless the caller's connection negotiated `staging_write` (`InitializationDispatch.authorize_grant/2`, reused as-is — "agent separation"/"capability denial"). `build/3` walks a small deterministic pipeline (workspace → README → kit vendoring → two cancellation checkpoints → `git init` with hooks disabled via `core.hooksPath` pointed at an empty directory, chosen over `/dev/null` for portability across the supported OS profiles) updating `run.state`/`progress` as it goes, and removes the entire partial staging directory on any refusal, failure, or cancellation — no orphaned state survives.
- Kit vendoring re-validates and re-hashes independently at three layers rather than trusting any one of them: `RepositoryKits.get_by_digest/1` re-fetches the exact package by the plan's frozen digest rather than trusting the snapshot alone ("package tamper" — a removed or non-matching package refuses the whole run); each `file_manifest` entry's path is re-validated (`..`, absolute, null-byte rejection) independently of both `RepositoryKits.publish_package/2`'s own publish-time check and `StagingWorkspace.join/2`'s own guard ("path escape" — proves a manifest entry that somehow smuggled a traversal path past those is still caught here); each file's base64 content is re-decoded and re-hashed against its recorded `sha256` before being written (tamper detection at the byte level). A kit's declared `scripts` are vendored — written to disk, executable bit preserved — but never invoked as a subprocess anywhere in this module ("no-hook"); nothing in `StagingBuilder` imports or calls any HTTP client ("no-network"). "Target read-only posture" (this task's own owned-surfaces line) is satisfied by construction, not by an explicit check: `StagingBuilder` never receives or resolves the plan's real selected-directory path at all, only its opaque `target_reference` and `id` — there is no code path by which it could touch the target even if it tried.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_initialization/staging_workspace_test.exs test/sdd_orchestrator/repository_initialization/staging_builder_test.exs test/sdd_orchestrator/repository_initialization/run_test.exs` — exit `0`.
- 47 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`) after reviewing every new file, plus a regression run of `repository_initialization_test.exs`, `initialization_dispatch_test.exs`, and `repository_initialization_live_test.exs` (64 passed, unaffected). Also confirmed: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer`, and `mix ecto.migrate`/`mix ecto.migrations` all clean; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` both pass.
- Status: Task 4 complete. Task 5 (`Depends on: Task 4`) is next; no capability blocker. No commit exists yet at the end of Task 4 — that is Task 5's own owned surface ("staging Git initialization, first commit" in its owned-surfaces line; re-read that line's wording carefully when starting Task 5, since it may describe using the already-`git init`'d staging state Task 4 produced rather than re-initializing it — do not assume without checking).
- Failed checks: None.

### 2026-08-11 - Task 4 preflight: staging execution does not route through AgentAdapter

- Discovered during Task 4 preflight, before any implementation: `SddOrchestratorWeb.InitializationWorkerChannel.handle_in("dispatch", payload, socket)` (Task 1) calls `InitializationDispatch.dispatch/2` directly inside a Phoenix Channel `handle_in` callback — which always executes on whatever machine runs the control-plane process, not on the paired worker's own machine (Phoenix Channel server callbacks are server-side by construction; there is no code path by which `handle_in` runs on the connected client). This was invisible and harmless for Task 2's read-only conversation turns (no `working_directory` is ever projected into `agent_input` there, so nothing filesystem-shaped was ever at stake), but Task 4 needs to actually write files to a real local staging directory, which only makes sense on the paired worker's own disk for a genuinely remote worker. `InitializationDispatch.start_agent/1`'s projection also has no `working_directory` key at all today, so even ignoring the execution-location question, dispatching Task 4's work through the real `AgentAdapter`/Codex/Claude Code adapters as they exist would still hit `:agent_unavailable` (the same already-flagged Task 1/2 gap, for the same underlying reason).
- Resolved consistently with the pattern already established twice in this slice (Task 1's dispatch mechanism, Task 2's real-CLI-adapter gap): **Task 4 does not route through `AgentAdapter` or a coding-agent CLI at all.** The skeleton content is fixed (Task 3) and the kit's files are fully known in advance (`RepositoryKitPackage.file_manifest`) — there is nothing for a generically-capable coding agent to decide, so launching one would only add unproven risk without benefit. Task 4 instead builds its own deterministic, worker-shaped execution path: a new `RepositoryInitialization.StagingWorkspace` module mirrors `Delivery.Worker.Workspace`'s symlink-safe, real-path containment pattern (a configured root + safe-segment validation + contained/2, adapted from `project_id`/`run_id` segments to the plan's own id/`target_reference`) rather than reusing that module directly (it is `ExecutionManifest`-typed and specs/33-owned). `InitializationManifest`/`InitializationDispatch` (Task 1) are reused as-is for authorization and capability-grant enforcement only (`InitializationManifest.new/1`, `InitializationDispatch.negotiate/1`/`authorize_grant/2`) — not modified, not extended, and `InitializationDispatch.dispatch/2` itself is deliberately not called, since it is hardcoded to route through `AgentAdapter`.
- The path-escape/undeclared-output/package-tamper/no-network/no-hook proof surface in Task 4's own proof line (tasks.md) is about defending against the *vendored kit package's own content* (externally-sourced, per `RepositoryKitPackage`'s own provenance/license/scripts fields), not about containing an unpredictable LLM agent — each kit file's path is re-validated and its content re-hashed against its recorded `sha256` at vendor time (defense in depth beyond what `RepositoryKits.publish_package/2` already checks at publish time), and a kit's declared `scripts` are vendored (written, permission bit preserved) but never executed.
- This is a genuine, deliberate scope decision, not a shortcut through an unresolved question — it keeps Task 4 provable today (real temp-directory filesystem tests, no test double needed, matching how `Delivery.Worker.Workspace`'s own tests already work) and defers "does a genuinely remote paired worker actually receive and execute this staging push" to the same release gate this slice's own tasks.md already lists ("Live configured worker and working-agent smoke proof on each supported local operating-system profile") — not a new deferral, the same one Task 1/2 already recorded.

### 2026-08-11 - Task 3 implemented: plan review and confirmation

- Resolved one genuine open design question before implementation, by user decision: AC-04 requires the plan to show explicit structure, files, commands, checks, and Git behavior, but nothing in `requirements.md`/`design.md` says how that content originates from the free-text `technical_foundation` answer, and this codebase has no mechanism for turning agent narration into structured file/command/check data (`AgentAdapter`'s normalized events only ever carry free text; every test in this slice exercises `AgentAdapterDouble`, never a real CLI). Chose the smallest-scope option: the plan's structure/commands/checks/Git-behavior content is a small, fixed, deterministic constant (`RepositoryInitialization.Skeleton.content/0` — a `README.md` only, no commands, no checks, `git init` with hooks disabled and one first commit on `main`), not generated at all. This matches the existing business rule that "the first plan must be the smallest runnable and verifiable foundation" and avoids inventing a new, unproven structured-output contract from a coding-agent turn. A later slice can add real per-stack scaffolding; explicitly out of this task's scope. The two declined alternatives (an LLM-proposed structure via a new structured "evidence" event; stopping for a full `update-spec` pass) are recorded here so they aren't silently forgotten if this needs revisiting.
- Extended `entity:RepositoryInitializationPlan` (migration `20260811090000_add_repository_initialization_plan_confirmation.exs`) with `kit_choice` (default `"included"`, per "permanent kit proposed by default"), `kit_package_id`/`kit_package_digest` (nullified together when declined or unavailable), `disclosure_version` (AC-05), and `confirmed_at`/`confirmation_digest` (AC-06). New `RepositoryInitialization` functions, all refusing unless `plan.current_field == "ready"`: `default_kit/0` (newest-version package from `RepositoryKits.list_packages/0`), `set_kit_choice/2` (always clears any prior confirmation — the changed-input invalidation default), `disclose_processing_boundary/1`, `confirmation_snapshot/1` (the exact bound-field map: version, target_reference, technical_foundation, kit_choice, kit_package_digest, the skeleton's commands/checks, disclosure_version), and `confirm_plan/2` (re-fetches the plan by id and re-derives its live snapshot rather than trusting the caller's plan struct or claimed snapshot — this is what makes the changed-input invalidation proof real, not just cosmetic; refuses `{:error, :plan_changed}` on a mismatch, `{:error, :disclosure_required}` before disclosure, and otherwise binds `confirmation_digest` to the sha256 hex of the canonical-JSON-encoded snapshot).
- New `SupportDispatch.provider_preview/1`: a read-only preview of which account/connection/model would be used, reusing the same accountless-friendly `{:skip, reason}` shape as `dispatch_turn/3` — deliberately does not pin a new `ai-runtime-session` just to render a preview (pinning has real side effects: spending-ceiling checks, idempotent-per-consumer-ref binding). The worker half of "worker and provider summary" (AC-04) shows only `os_family`/`os_major`/`app_version` from `Devices.Pairing.active_workers/1` — the control plane's `LocalWorker` schema has no coding-agent/CLI field at all (that configuration is purely local to the paired worker's own machine, per specs/33's own design), so a "which CLI will run" claim was correctly left out rather than invented.
- New `:reviewing_plan` LiveView step (replaces the old placeholder "plan questions complete" notice), rendering every AC-04 element, the kit include/decline toggle with AC-06's decline copy, the no-kit-available case, the disclosure gated confirm control, and a changed-input "this plan changed, review it again" path that reloads the plan rather than silently retrying.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_initialization/skeleton_test.exs test/sdd_orchestrator/repository_initialization_test.exs test/sdd_orchestrator/repository_initialization/support_dispatch_test.exs test/sdd_orchestrator_web/live/repository_initialization_live_test.exs` — exit `0`.
- 65 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`) after reviewing every new/changed file, plus a regression run of `test/sdd_orchestrator_web/live/local_onboarding_live_test.exs` and `test/sdd_orchestrator/delivery/initialization_dispatch_test.exs` (21 passed, unaffected). Also confirmed: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer`, and `mix ecto.migrate` all clean; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` both pass.
- Minor doc-precision note: a few new code comments/moduledocs reference "AC-07" alongside AC-04/05/06 (the confirmation-clears-on-kit-change behavior does implement the *mechanism* AC-07 describes). `tasks.md` assigns AC-07's actual ownership to Task 4, not Task 3 — that acceptance criterion is framed around "when initialization is requested" (the real build/run flow Task 4 owns), so Task 3 only proves the narrower confirmation-binding mechanism, not the full criterion. Not corrected in the code comments (harmless, still directionally accurate); flagged here so Task 4's own preflight doesn't mistake AC-07 as already fully proven.
- The e2e Playwright review/confirm scenario is written but `test.skip`'d, not run: `select_folder` resolves through `Application.get_env(:sdd_orchestrator, :device_worker_stub_folder)`, and nothing currently sets that per-scenario from outside the BEAM process for a browser-driven run (same limitation Task 2's own spec already had, just now explicit). Fixing that would mean touching `E2EBootstrapController`, a known recurring cross-slice conflict surface, and is out of this task's owned surfaces.
- Status: Task 3 complete. Task 4 (`Depends on: Task 3`) is next; no capability blocker.
- Failed checks: None.

### 2026-08-11 - capability:sdd-kit-package unblocked; Task 3 ready

- The 2026-08-10 correction below found `capability:sdd-kit-package` `pending` again (specs/15 Task 1's own completion had never reached `main`). User decision: rather than wait for all of specs/15 (blocked on its own Task 2, which needs a new `specs/35` capability), cherry-pick just specs/15 Task 1's commit onto `main` ahead of the rest of that slice — justified by the cross-specification capability system's own principle of depending on the smallest stable capability, not a whole slice.
- Cherry-picked `231778b` (`feat: establish the immutable SDD kit package catalog (specs/15 Task 1)`) from `slice/15-repository-sdd-kit-integration` onto `main` as commit `0cc5d04`, applying without conflict. Reconciling it against current `main` surfaced one genuine `mix credo --strict` nesting-depth violation in `RepositoryKits.error_atom/1` (not present when that commit was authored against an older `main`); fixed by extracting `error_atom_for_field/1`, mirroring the same class of fix `main`'s own `e1c2272` had just applied elsewhere. Reconfirmed on `main`: `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer`, and the focused `mix test test/sdd_orchestrator/repository_kits_test.exs test/sdd_orchestrator_web/live/repository_kits_live_test.exs` (32 passed) all clean. Pushed to `origin/main` together with one unrelated pre-existing local-only commit (`e1c2272`, a format/credo/dialyzer gate fix, not authored in this session).
- Rebased this branch (`slice/16-empty-repository-initialization`, never pushed, so no force-push needed) onto the updated `main` — clean, no conflicts. `capability_index.py --capability sdd-kit-package` now reports `ready`, correctly attributed to `specs/16-empty-repository-initialization#Task 3`. Reconfirmed both this slice's own focused proofs after the rebase: Task 1 (46 passed) and Task 2 (38 passed), both exit `0`. Corrected `tasks.md`'s Status back to `In Progress` and cleared the Blocked Decisions entry.
- specs/15 itself remains its own concern on its own branch — its Task 2 is still blocked on `capability:guided-delivery-feature-specification-link` (specs/35). Not this slice's problem to solve.
- Status: Tasks 1-2 complete, no capability blocker remains. Task 3 is next.
- Failed checks: None.

### 2026-08-10 - Task 2 implemented: guided plan flow without mutation

- Implemented `entity:RepositoryInitializationPlan` (`SddOrchestrator.RepositoryInitialization.Plan`, new `repository_initialization_plans` table) as a versioned record whose `current_field` cursor (`purpose -> users -> first_outcome -> constraints -> technical_foundation -> ready`) is both the product-first/technical-foundation question gate (AC-03) and the versioning mechanism: `RepositoryInitialization.answer_field/3` accepts an answer only for exactly `current_field`, and every accepted answer advances the cursor and bumps `version` by one in the same update. New: `lib/sdd_orchestrator/repository_initialization.ex`, `repository_initialization/{plan,eligibility,support_dispatch}.ex`, `lib/sdd_orchestrator_web/live/repository_initialization_live.ex`, one migration. Modified: `lib/sdd_orchestrator_web/router.ex` (new accountless `/onboarding/empty-repository` route in the existing `live_session :public` block, alongside `LocalOnboardingLive`).
- Eligibility (AC-01) reuses `Devices.RepositoryValidation.root_commit_ids/1` rather than reimplementing Git-state classification, layering one additional `File.ls/1` check in the new `RepositoryInitialization.Eligibility` module: that shared validator alone collapses "genuinely empty directory" and "non-empty directory with no `.git`" into the same `:not_a_git_repository` value, which is not sufficient for this slice's requirement to accept only a truly empty directory or an unborn Git repository and reject a non-empty non-Git directory as out of scope. No mature-repository-assessment flow exists anywhere in this codebase yet; a repository with commits just stops with a message, which AC-01's "stops **or** routes" wording already permits. `repository_validation.ex` itself was not modified (specs/02's owned surface, has other consumers).
- Target path privacy (part of AC-01's "opaque target reference" rule): the plan's persisted `target_reference` is a random opaque token (`WorkerProtocol.generate_id/0`); the real selected path is kept only in the LiveView's own `socket.assigns` for that session's lifetime and never appears in the plan record or in any dispatched manifest. Re-resolving a `target_reference` back to a path for a later session, or for Task 4's staging build, is an explicitly open gap left for whichever task needs it next (mirrors how Task 1 left staging-root containment for Task 4).
- The read-only support conversation (AC-02) dispatches through Task 1's `InitializationDispatch` foundation via a new `RepositoryInitialization.SupportDispatch` module, always negotiating exactly `["plan_discovery"]` — never `"staging_write"` — so a turn structurally cannot reach the working-agent path; proved directly by dispatching a `staging_write` manifest on this same code path and confirming `capability_grant_denied`. Two points required confirming against the actual code rather than following my original brief literally (both flagged explicitly by the implementing agent, not silently substituted):
  - `AIRuntime.RuntimeSessions.pin_session/3` is genuinely account-scoped (requires an active account with an already-linked personal AI connection; no funded/implicit fallback), while empty-repository initialization's entry, eligibility, and plan are accountless like `LocalOnboardingLive`. Resolution: the plan/entry stay fully accountless; a guided-question turn is an opportunistic enhancement that returns `{:skip, reason}` whenever no signed-in account or no eligible connection is available, and the LiveView falls back to a static per-field question. AC-03 does not require a model in the loop — it requires every question to stay visible, sequential, and explicitly answered, which the `current_field` cursor enforces regardless of whether a turn ran.
  - Dispatch is a direct function call to `InitializationDispatch.dispatch/2` (matching how Task 1's own tests exercise it, and design.md's "each conversation turn is one such capability-restricted dispatch" framing), not a live `InitializationWorkerSocket`/`Channel` round-trip — a LiveView process has no legitimate way to hold a paired worker's raw `Devices.Pairing` credential.
- `SupportDispatch` additionally calls `AgentAdapter.adapter().observe/1` directly (the same "generic helper only" reuse Task 1 established) to surface a real adapter's `progress` event text as the assistant's question, since `InitializationDispatch.dispatch/2` itself only starts the agent and does not observe it.
- Known, already-flagged, unaddressed gap (not this task's or Task 1's to close): `lib/sdd_orchestrator/delivery/agent_adapter/codex.ex` and `claude_code.ex` still build their subprocess prompt and required `working_directory` entirely from the project-scoped `ExecutionManifest` shape. A live dispatch through the real adapters (as opposed to `AgentAdapterDouble`, which is what every test in this task uses) would still hit `:agent_unavailable` today. This remains a release-gate concern — this slice's own tasks.md already lists "Live configured worker and working-agent smoke proof on each supported local operating-system profile" under Release gates, not under any task's proof line — and is not addressed here.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_initialization/eligibility_test.exs test/sdd_orchestrator/repository_initialization_test.exs test/sdd_orchestrator/repository_initialization/support_dispatch_test.exs test/sdd_orchestrator_web/live/repository_initialization_live_test.exs` — exit `0`.
- 38 tests passed. Reconfirmed independently by the main thread with the same command (exit `0`) after reviewing every new file, plus a regression run of `test/sdd_orchestrator_web/live/local_onboarding_live_test.exs` (10 passed) since the router changed. Also confirmed: `mix format --check-formatted`, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer` all clean; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` both pass.
- One item is written but not executed: `assets/e2e/empty-repository-initialization.spec.js` (a Playwright spec mirroring `local-onboarding.spec.js`'s pattern) exists but has not been run against a live browser — full browser-matrix proof is a slice-verification concern (`npm --prefix assets run test:e2e` via `run_proof.py slice`), not this task's focused proof, per the Proof Scope Gate. Recorded here so it isn't mistaken for verified browser coverage before the slice verification gate actually runs it.
- Status: Task 2 complete. Task 3 (`Depends on: Task 2`) is otherwise unblocked by this slice's own chain, but `python3 .agents/scripts/capability_index.py --capability sdd-kit-package` now reports `pending` (provider `specs/15-repository-sdd-kit-integration#Task 1`, unchecked in that specification's own `tasks.md` on `main`), contradicting this progress log's own 2026-08-09 "Task 2's remaining capability blocker cleared" entry below, which recorded it as ready after specs/15 Task 1 "completed on its own slice branch." That completion did not reach `main` (or was reverted there) — `git log` shows no `specs/15` commits on `main` this branch is missing, so this is not a stale-rebase artifact, the capability is genuinely unavailable again right now. Corrected `tasks.md`'s Status and Blocked Decisions accordingly; this slice is `Blocked` on Task 3 until specs/15 Task 1 is (re-)completed and merged.
- Failed checks: None. This is a capability-availability correction, not an implementation defect.

### 2026-08-09 - Task 1 implemented: pre-project capability-scoped dispatch foundation

- Implemented `entity:InitializationDispatch` as a device-workspace-scoped analog of the already-proven `PersonalAIWorkerSocket`/`PersonalAIWorkerChannel`/`PersonalWorkerProtocol` stack (a live, capability-negotiated worker connection authenticated by the raw `Devices.Pairing` credential), not as a scaled-down copy of specs/33's project-scoped `WorkerGatewayCredentialController`/`WorkerSocket`/`CommandOutbox`/`Worker.GatewayConnection` pipeline. That heavier machinery is a durable, crash-resilient, reconnect-over-a-project's-lifetime queue; a pre-project dispatch is one ephemeral session with no comparable lifetime to protect, so replicating it here would have been out of proportion to a Standard task and is left for whichever later task (if any) actually needs redelivery guarantees.
- New files: `lib/sdd_orchestrator/delivery/initialization_manifest.ex` (immutable manifest: `device_workspace_id`, `dispatch_id`, `capability_grant` enum `plan_discovery`/`staging_write`, `agent_ref`, `instructions`; validation pattern mirrors `ExecutionManifest` but is deliberately independent of it — no project/feature/run identity), `lib/sdd_orchestrator/delivery/initialization_dispatch.ex` (`negotiate/1`, `authorize_grant/2` — the capability-grant enforcement point — and `dispatch/2`, which routes a validated manifest to the configured `AgentAdapter` using only its generic helpers `adapter/0`, `version/1`, `environment/0`; never `project/2` or `launch/3`, which are hard-coupled to `ExecutionManifest` and the project-scoped `Delivery.Worker.Workspace`), `lib/sdd_orchestrator_web/channels/initialization_worker_socket.ex` and `initialization_worker_channel.ex` (connect via the raw Pairing credential, join negotiates the capability-grant intersection, `"dispatch"` routes through `InitializationDispatch`), plus fixtures/test-double/test files mirroring the existing `PersonalAIWorkerDouble`/`DeliveryProtocolFixtures` conventions. Modified: `lib/sdd_orchestrator_web/endpoint.ex` (new `/initialization_worker` socket mount, same shape as the existing `/worker` and `/personal_ai_worker` mounts).
- Filesystem staging (a working directory, path containment) is explicitly not this task's concern — `start_agent/1` projects only `device_workspace_id`/`dispatch_id`/`capability_grant`/`agent_ref`/`instructions` into `agent_input`, with no working-directory proof. Task 4 owns building and proving a staging root and will fold it into a manifest's `agent_ref`/`instructions` when it calls this same foundation with a `staging_write` grant.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/initialization_manifest_test.exs test/sdd_orchestrator/delivery/initialization_dispatch_test.exs test/sdd_orchestrator_web/channels/initialization_worker_socket_test.exs test/sdd_orchestrator_web/channels/initialization_worker_channel_test.exs` — exit `0`.
- 46 tests passed. Reconfirmed independently by the main thread with the same command (also exit `0`) after reviewing every new file. Also confirmed: `mix format --check-formatted` on all new/changed files, `mix compile --warnings-as-errors`, `mix credo --strict`, `mix dialyzer` all clean; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization` and `python3 .agents/scripts/validate_spec.py --all specs` both pass.
- Status: Task 1 complete. Task 2 (`Depends on: Task 1`) is now unblocked by this slice's own chain; its external capability (`capability:ai-runtime-session`) was already ready. No task in progress.
- Failed checks: None.

### 2026-08-09 - Corrected the dispatch mechanism; added a foundation task

- Discovered during Task 1 implementation preflight, verified directly against code: design.md's plan to dispatch the read-only support conversation and the working-agent build through `capability:local-worker-run-execution` (specs/33) does not work pre-project. `Delivery.RunCommand` requires `project_id` (`run_command.ex:102-104`), `Delivery.ExecutionManifest.new/1` enforces `project_id`/`feature_id`/specification-revision keys (`execution_manifest.ex:29-44`), and the `/worker` channel topic is itself keyed by project id — none of which exist before this slice's own project creation (now Task 7). `Delivery.WorkerProtocol`'s capabilities (`worker_protocol.ex:16-30`) are also a fixed, whole-connection protocol-operation allowlist, not a per-dispatch read-only-vs-write grant, so the design's "same mechanism, differentiated by capability grant" premise had no corresponding implementation. No implementation had started; nothing was rolled back.
- Resolved (user decision: fix inside specs/16, do not modify specs/33 — Verified, has another consumer specs/34 — and do not reorder onboarding ahead of the first commit): specs/16 now owns a project-independent dispatch foundation. It reuses `capability:workspace-bound-local-worker-authorization` (specs/02 Task 3, ready — `Devices.Pairing.authenticate_worker/1`/`authorize_for_workspace/2` are genuinely workspace-scoped, not project-scoped, and this is exactly the primitive specs/33 Task 1 itself narrows to build its own project-scoped credential) for worker authorization, and reuses `Delivery.WorkerProtocol`/`Delivery.ProtocolCodec` vocabulary plus the `AgentAdapter` behaviour as library code, without touching specs/33's schema, topic, or contract.
- Inserted a new Task 1 ("Establish pre-project capability-scoped worker dispatch foundation," entity:InitializationDispatch, no AC owned — matches the AC-less-foundation precedent of specs/33 Task 2) and renumbered the former Tasks 1-6 to Tasks 2-7, updating every `Depends on:`, the Cross-Specification Dependencies block, `capability:initialized-sdd-repository`'s readiness point (now after Task 7), and the Proof Scope Gate's `Applies to:` list. Fixed the resulting stale provider reference in the one consumer this affects, `specs/13-sdd-adoption` (`capability:initialized-sdd-repository` provider task 6 → 7).
- Slice size after the split: 7 tasks, critical path 7 — still within the legacy plan's implicit standard bounds (no formal Slice Size Gate section is adopted in this legacy `tasks.md`; not retrofitted).
- Spec updates: `design.md` (Proposed Approach, Components Affected, Data and Access Boundaries, Interfaces, the "Read-Only Support And Separate Working Agent" decision, and a new "Own A Project-Independent Dispatch Foundation" decision). `tasks.md` (Status, Cross-Specification Dependencies, Task Size Gate wording, Proof Scope Gate, the full Tasks list, Blocked Decisions). `specs/13-sdd-adoption/tasks.md` (provider task reference only). `requirements.md` needed no change — AC-02/AC-08 and the related business rules were already mechanism-agnostic.
- Failed checks: None — this is a specification-only correction; `python3 .agents/scripts/validate_spec.py specs/16-empty-repository-initialization`, `python3 .agents/scripts/validate_spec.py --all specs`, `python3 .agents/scripts/capability_index.py --capability workspace-bound-local-worker-authorization`, and `python3 .agents/scripts/split_progress_log.py --check` all pass.
- Remaining: Implement Tasks 1-7 and complete the verification gate. No task is in progress.

### 2026-08-09 - Task 2's remaining capability blocker cleared

- Rebased this branch onto `main` (through `specs/30-repository-execution-profile-completion`'s merge and a `specs/15` capability-wiring correction; no conflicts).
- `capability:sdd-kit-package` is now ready — `specs/15-repository-sdd-kit-integration` Task 1 completed on its own slice branch. Task 2 no longer has any external capability blocker; it is gated only by this slice's own `Depends on: Task 1` chain. Corrected the now-stale "Task 2 remains blocked on `capability:sdd-kit-package`" note recorded below.
- Remaining: Implement Tasks 1–6 and complete the verification gate.
- Failed checks: None.
- Spec updates: Corrected the Status and Blocked Decisions sections in `tasks.md`.

### 2026-08-09

- Discovered during Task 1 implementation preflight: `capability:ai-runtime-session` only pins model/connection/effort/cost-ceiling configuration and has no message-send or tool-call execution path anywhere in the codebase. The only ready mechanism that can actually dispatch an agent turn is `capability:local-worker-run-execution` (specs/33), a one-shot capability-restricted local-worker CLI dispatch already used for coding-agent runs. `specs/12-project-assistant`'s interactive conversation design was considered and rejected as the dependency: it is scoped to an existing project and participant and cannot host a pre-project conversation without redesigning that spec's data model.
- Resolved: Added `capability:local-worker-run-execution` — provider `specs/33-local-worker-run-execution#Task 12` (ready) — as a Task 1 dependency. Updated `design.md`'s Proposed Approach, Interfaces, Data and Access Boundaries, and the "Read-Only Support And Separate Working Agent" decision to describe the support conversation and the Task 3 working agent as two capability-restricted dispatches through the same `capability:local-worker-run-execution` mechanism, differing only in granted capabilities. Reworded one `requirements.md` business rule that had named `capability:ai-runtime-session` as the plan-mutation host, since that overstated what the capability provides.
- Adopted the Proof Scope Gate prospectively for all six unfinished tasks (`Proof scope: Focused`) while refining Task 1's dependencies, per the project's task-proof contract.
- Status: Task 1 is no longer capability-blocked (`capability:ai-runtime-session` and `capability:local-worker-run-execution` are both ready). Task 2 remains blocked on `capability:sdd-kit-package` (specs/15 Task 1, pending), which keeps Tasks 3–6 blocked through the `Depends on:` chain. No implementation started.

### 2026-07-31

- Completed: Approved the empty-local-repository discovery, confirmation, working-agent, staging, first-commit, default-kit, decline, onboarding, authoritative-specification, readiness, governance, and capability contracts.
- Remaining: Complete the prerequisite capabilities, implement Tasks 1–6, and pass the verification gate.
- Failed checks: None.
- Spec updates: Created the initial approved specification and first executable slice.
