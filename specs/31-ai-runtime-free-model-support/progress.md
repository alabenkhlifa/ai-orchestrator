# AI Runtime Free Model Support Progress Log

### 2026-08-05 - Free-model support specified as a consumer extension of Slice 11

- Completed: Created the specification that takes over Slice 11's deferred free-model work. The product agreement separates a proven-free price from an unknown one as a positive operator declaration, keeps the strict non-exceeding ceiling and its fail-closed refusals intact, and defines reservation, reconciliation, withdrawal, and projection behavior for a free turn. Six acceptance criteria are owned by four tasks with a four-task critical path; no new data entity is introduced.
- Scope classification: Focused specification. One primary outcome, one end-to-end workflow from operator registration to owner-visible cost boundary, and one verification gate.
- Capability graph: Consumer only. `capability:ai-runtime-session` (`specs/11-ai-runtime-governance#Task 11`), `capability:ai-runtime-governance` (`#Task 6`), and `capability:ai-runtime-observation` (`#Task 5`) are all complete and ready. This slice provides `capability:ai-runtime-free-model-pricing` after Task 4 so the deferred presentation follow-up has one stable edge to depend on.
- Remaining: Resolve the six open product questions, then implement Tasks 1 through 4 in dependency order with focused proof and task-boundary commits, run the full slice verification gate, and publish readiness.
- Failed checks: None. `python3 .agents/scripts/validate_spec.py specs/31-ai-runtime-free-model-support`, `python3 .agents/scripts/validate_spec.py --all specs`, and `python3 .agents/scripts/split_progress_log.py --check` all pass. Implementation has not started.
- Proof receipts: None.
- Spec updates: Recorded the engineering mechanisms that preserve the product outcome, namely the tagged free registration instead of a numeric zero, the persisted pricing mode paired with the ledger's unit-price constraint in both the changeset and the database, the reuse of the existing over-reconciliation guard as the free settlement rule, and the decision to extend the Slice 11 modules in place rather than fork a second ceiling authority. Recorded three implementation facts the tasks depend on: the `runtime_cost_ledgers_price_check` database constraint repeats the strictly-positive unit-price rule and must become a paired rule, the ledger's pause-reason vocabulary is constrained in the schema, the database, and the projection mapping and must be extended in all three, and the privacy field-purpose map and rights-export allowlist are both proven equal to the ledger schema's own fields so the added column must be covered in the same task.
