# Worker-Driven Repository Selection Progress Log

### 2026-08-31 - Task 5: one definition of an available worker

- `Devices.worker_available?/1` reads `Delivery.WorkerAttachment.attached/1` for the worker's own `device_workspace_id` and matches the registry entry's `worker_id`. It is the one definition. `WorkerDiscovery.status/2` now derives `:detected` from it, so `:unavailable` covers a paired, compatible worker that is not attached.
- `last_seen_at` stays, as `design.md` requires, as a second condition: an attachment beside an ancient heartbeat is a contradiction about one worker, and the safe reading of a contradiction is unavailable.
- Deviation from the brief, kept deliberately: `RepositoryAssessments.authorize_worker/2` still calls `WorkerDiscovery.status/2` rather than `worker_available?/1` directly. Replacing it would have dropped the macOS and protocol compatibility filter from the action while the list kept it, reintroducing drift in the other direction, and would have made `repository_binding_preparation_test.exs:313` unprovable, which proves a stale worker is refused by advancing `now` 120 seconds. Because `status/2` now reads `worker_available?/1`, the list and the action already reach one answer through one expression, which is the defect the task exists to close.
- One owned wording: the assessment list's empty notice and the refused submit both render `RepositoryAssessmentLive.worker_unavailable_message/0`, "No worker is available right now. Start or reconnect one, then try again." It replaces two sentences that said the same thing differently. No em dash, and it does not claim to see the Mac.
- Stand-in: `worker_available?/1` returns true early when `:device_worker_stub` is set, which is on in the test environment and off in a production build. That is what keeps scenarios that are not about attachment passing.
- One existing test updated rather than weakened: `pairing_redemption_live_test.exs`'s `app_finishes/1` helper asserted `data-worker-status="detected"` with the stub off, which encoded the old definition. It now also registers a real attachment, which is what the Mac app actually does, so the assertion stands unchanged.
- Proof receipt: `Task 5` — scope `Focused` — command `mix test test/sdd_orchestrator/devices/worker_availability_test.exs` — exit `0`.
- That run was 5 tests, 5 passed. Regression across the changed definition's callers: `mix test test/sdd_orchestrator/devices/ test/sdd_orchestrator/repository_assessments/ test/sdd_orchestrator_web/live/repository_assessment_live_test.exs test/sdd_orchestrator_web/live/pairing_redemption_live_test.exs` exit `0`, 243 passed.

### 2026-08-31 - Task 3: the worker answers with identity and folder name only

- `SddOrchestrator.Worker.RepositorySelection` is a `GenServer` under `Worker.Supervisor`, started before `GatewayConnection` so a request arriving on the first join has somewhere to land. It holds one request; a new one replaces it, so a worker-side request cannot outlive the control-plane request it belongs to.
- The two files live beside `connection_status.json` under `Configuration.home/1`, mode `0600`, written atomically through a temp file and rename, copying `ConnectionStatus`. `pending_selection.json` holds the request id and expiry only: the candidate identities stay in the release's memory because the app has no use for them.
- The path rule is enforced by ordering. `take_answer/1` reads `selection_answer.json` and deletes it before anything is computed from it, so a crash during the Git check cannot leave a path on disk. A stale answer at start is deleted unread. The path lives only in the answering function's local variables and is never in state, the pending file, the reply payload, or a log line.
- Added after the first pass, on the same reasoning: `init/1` also deletes a stale `pending_selection.json`. `design.md` says that file is meaningless once the release stops, and leaving one would make the app show a panel for a request the control plane has already timed out.
- `GatewayConnection` gained two `handle_message/4` clauses before its catch-all and one `handle_info({:repository_selection_result, topic, payload}, socket)` clause. The reply is deferred to the connection process because only it owns the socket, which is the same shape `{:observe_agent, _}` already uses.
- Worker boundary widened deliberately and narrowly: `test/sdd_orchestrator/worker/boundary_test.exs` had `SddOrchestrator.Devices` on the hard forbidden list, so the worker could not call the two modules `design.md` names as running on the Mac. `Devices` now has an explicit two-module allowlist, `RepositoryValidation` and `PortableRepositoryIdentity`, following the file's existing `Delivery` and `Accounts` pattern. Both were confirmed free of `Repo.` and `Ecto.Schema` by grep, which is why they can run without a database. Every other `Devices` reference still fails closed.
- `supervisor_test.exs` updated for the new child. Its "no database repo" assertion used a `"Repo"` substring check that `RepositorySelection` trips; it now asks whether the id's last segment is `Repo`, which is the rule it meant.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/repository_selection_test.exs` — exit `0`.
- That run was 11 tests, 11 passed. Regression on the whole worker suite: `mix test test/sdd_orchestrator/worker/` exit `0`, 150 passed. Safety checks at the same commit: `mix format --check-formatted` exit `0`, `mix compile --warnings-as-errors` exit `0`.

### 2026-08-31 - The app-to-release hop moves from rpc to two files

- Trigger: `specs/43-distribution-free-worker-control/` removed every `bin/worker rpc` call the Mac app made, because a managed Mac's firewall blocks incoming `epmd`. `native/worker-app/MenuBarApp/Tests/SDDOrchestratorWorkerCoreTests/DistributionFreeCallSitesTests.swift` scans every app source and fails on any `["rpc"` argument array. This slice's `design.md` had the app polling the release over `bin/worker rpc`, which would have failed that guard and, before it, the firewall.
- `eval` cannot stand in. A pending request lives in the release's memory, so a fresh VM answers that nothing is pending. That is exactly why `specs/43` gave the connection status a file rather than an `eval` call.
- Decision taken with the user: two owner-only JSON files under `Configuration.home/1`, beside `connection_status.json`. The release writes `pending_selection.json` while a request is open. The app polls it, shows the panel, and writes `selection_answer.json` with the request id and either the chosen path or a cancellation. The release reads that answer, deletes it at once, and only then runs the Git check. Considered and rejected: reimplementing the identity HMAC in Swift, which would need two byte-identical implementations of one digest forever and reverses the recorded decision that the worker computes and the control plane compares; and a unix-domain socket, which adds a third transport shape `specs/43` deliberately avoided.
- Privacy consequence, recorded rather than assumed: the chosen path is now written to disk for at most one poll interval. It is owner-only, deleted on read, and deleted unread at the next start if a release stopped in that window. Because the mechanism widens where a path exists, the deletion is `AC-11` rather than an implementation detail, and the slice verification gate now also asks that no answer file survives a completed selection.
- Specification changes: `requirements.md` gained the in-Mac path rule and `AC-11`. `design.md` gained the file mechanism in Context, Proposed Approach, Interfaces, Data and Access Boundaries (`PendingSelectionFile`, `SelectionAnswerFile`), a replaced tradeoff, and the stale-file risk. `tasks.md` gained a `capability:distribution-free-worker-control` edge required before `Task 3`, and rewrote `Task 3` and `Task 4` owned surfaces and proof.
- Second correction in the same update: `design.md` had said `Delivery.WorkerProtocol` was untouched while also requiring the worker to declare a capability at attach. Those conflict, because `negotiate/1` is the only attach-time capability mechanism and it drops any name outside its vocabulary. The text now says the capability name lives in that vocabulary while the codecs stay separate, which is what `Task 2` implemented.
- Third: `repository_selection_cancel` is now named in Interfaces. The design already required the behaviour, in the risk about a request outliving a closed tab, but its Interfaces list named only the request and result pair. `Task 2` shipped it.
- No code changed in this update. `Task 1` and `Task 2` stay complete; `Task 3` and `Task 4` are unstarted and now describe a buildable mechanism.

### 2026-08-29 - Task 2: the request and its result over the Mac-scoped attachment

- `RepositorySelection.Transport.Attachment` implements Task 1's behaviour. It looks the worker up in `Delivery.WorkerAttachment` by device workspace, filters to the named `worker_id`, and refuses `:no_worker` when nothing matches. A matched worker that did not negotiate `repository_selection`, or whose protocol version is no longer supported, is refused `:worker_needs_update`. A duplicate-keyed reconnect overlap takes the first capable entry, exactly as command delivery does.
- `RepositorySelection.AttachmentCodec` is this slice's own codec, separate from `Delivery.ProtocolCodec`. A request encodes to exactly `request_id`, `candidates`, `generate`, and `expires_at`; a cancellation to exactly `request_id`. A worker's answer is refused whole when it carries any key outside `request_id`, `outcome`, `folder_name`, `matches`, and `identity`, so a `path`, `folder_path`, or `remote_url` never reaches the request table. `matches` is bounded at 100 entries, `folder_name` at 255 bytes, and `identity` at 512 bytes.
- `SddOrchestratorWeb.WorkerWorkspaceChannel` gained two outbound pushes and one inbound frame. The workspace and worker an answer is credited to are read from the socket's authenticated assigns and never from the frame, which is what makes a result from another attachment refuse as `:foreign_answer` in Task 1's lifecycle. The channel writes no log line and the session stays open through a refusal.
- Interface added beyond `design.md`'s Interfaces list: `repository_selection_cancel`, a third attachment message. The design requires the behaviour ("the request is bound to the requesting process and is cancelled on its exit, and the app is told so the panel closes") but its Interfaces section names only the request and result pair. Recorded here so the slice gate can fold it into `design.md`.
- Design correction needed at the slice gate: `design.md` says "`Delivery.WorkerProtocol` and `ProtocolCodec` are untouched" in the same breath as "a capability flag the worker declares at attach time". Those conflict. `WorkerProtocol.negotiate/1` is the only attach-time capability mechanism and it filters out any announced name outside `capabilities()`, so `repository_selection` was added to `@optional_capabilities` in `WorkerProtocol` and in its hardcoded worker-side twin in `Worker.GatewayConnection`. Nothing else in either file changed: `@required_capabilities`, `@envelope_types`, `@command_operations`, and `@event_types` are as they were, `ProtocolCodec` is genuinely untouched, and picker requests keep their own codec, which is what that sentence was arguing for. Every existing assertion compares against `WorkerProtocol.capabilities()` rather than a literal list, so none of them changed.
- `config/config.exs` selects `Transport.Attachment` as the default everywhere. Task 6 overrides it with the stand-in under `E2E_MODE` and in tests.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_selection/attachment_codec_test.exs test/sdd_orchestrator_web/channels/worker_workspace_channel_repository_selection_test.exs` — exit `0`.
- That run was 27 tests, 27 passed. Regression on the two suites that assert the shared capability list, plus Task 1's: `mix test test/sdd_orchestrator/worker/gateway_connection_test.exs test/sdd_orchestrator_web/channels/worker_workspace_channel_test.exs test/sdd_orchestrator/repository_selection_test.exs` exit `0`, 30 passed. No existing test was edited.
- Safety checks at the same commit: `mix format --check-formatted` exit `0`, `mix compile --warnings-as-errors` exit `0`.

### 2026-08-29 - Task 1: the selection request lifecycle

- `SddOrchestrator.RepositorySelection` is the one entry point. `request/3` validates the scope, the worker, and the candidates, pushes through `RepositorySelection.Transport`, and returns a request id without waiting. `cancel/1` closes the caller's own request. `answer/2` is the seam Task 2's channel will call.
- `RepositorySelection.Server` is a control-plane-only `GenServer` holding the in-memory table. It monitors the requester and the pushed-to channel pid, arms one expiry timer, and removes the entry before sending, so a requester receives exactly one `{:repository_selection, request_id, outcome}` and never a second.
- Outcome mapping: worker `selected` becomes `{:selected, result}`, `cancelled` becomes `:cancelled`, and `not_a_git_repository`, `empty_repository`, and `inaccessible` become `{:refused, reason}`. A dead channel gives `:worker_lost`; expiry gives `:timeout`; a requester exit cancels the panel and tells nobody, because nobody is left.
- Refusals answer only `:unknown_request`, `:foreign_answer`, or `:invalid_result`. An already-answered request is indistinguishable from an unknown one on purpose, so a repeat answer cannot learn it lost a race.
- Privacy: `SelectionRequest` has no field that could hold a location. `SelectionResult.new/1` refuses any key outside its allowlist, so `path`, `folder_path`, and `remote_url` are refused by the same check that refuses any stray key, and a `folder_name` containing `/` or `\` is refused as a path. No `Logger` call in the new modules.
- `RepositorySelection.Transport` is the behaviour Task 2 implements, following the `Delivery.CommandTransport` idiom. `push/1` answers with the attached channel's pid so the server can monitor it. The default `Transport.Unavailable` refuses with `:no_worker`, which is the honest answer until Task 2 lands.
- Test transport: `SddOrchestrator.RepositorySelectionTransportDouble` keeps its recording in an unlinked `Agent` reached through application environment, not the process dictionary, because the server is a different process from the test. Unlinked on purpose, so the agent does not die with the test process and crash the server mid-cancel.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_selection_test.exs` — exit `0`.
- That run was 8 tests, 8 passed, and the main thread confirmed it by real exit status rather than by the sub-agent's report.
- Safety checks at the same commit: `mix format --check-formatted` exit `0`, `mix compile --warnings-as-errors` exit `0`, `git diff --check` exit `0`.
- Noted for Task 2: `capability:mac-scoped-worker-connection` now reports `ready` (`specs/39` Task 8 is complete and merged), so Task 2's recorded `Status: Blocked` line is stale and is cleared when Task 2 starts.

### 2026-08-28 - Specification created from a browser walk-through of the local setup

- Found by clicking through `http://localhost:4000` with the installed worker app running: `/onboarding/local` reported the worker as paired but not running while the app answered `Configuration.load() => {:error, :not_paired}`; `Check again` then showed `Code accepted. Finishing on your Mac…` with no code entered; the assessment page listed `Available worker 1` and refused it as no longer available; and every `Open folder picker` was answered by the development stand-in only.
- Diagnosis by reading the code rather than the specifications: `LocalOnboardingLive.select_folder/3`, `RepositoryInitializationLive`, and `Portability.HostedLocalRepositoryFolder.picker_available?/0` all gate on `:device_worker_stub`; the control plane has no request-and-reply to a worker and no path from the worker release to the Swift app; `WorkerDiscovery` answers from `last_seen_at` while the assessment action re-checks at its own `now`.
- Product decisions taken with the user: the hosted local-repository project is the first target and the accountless path second; empty-repository initialization is deferred to its own follow-on; the dashboard may show the folder name only; pairing again keeps the old worker record.
- Engineering decisions recorded in `design.md`: the request rides the Mac-scoped attachment from `specs/39`; the worker computes the Git check, identity, and matches and answers with references; the app polls for a pending request over `bin/worker rpc`; one availability definition read from attachment; the stand-in becomes a configured adapter present only under `E2E_MODE` and in tests.
- Scope: classified `focused specification`. One outcome, one workflow, one verification gate, nine tasks, longest `Depends on:` path of six. `Task 2` is blocked until `capability:mac-scoped-worker-connection` is ready; `Task 1` is executable now.
- No code changed in this update. `config/dev.exs` turned the stand-in off for a plain development server in the harness change on `main` (`8b4d551`), which this slice's adapter decision builds on.
