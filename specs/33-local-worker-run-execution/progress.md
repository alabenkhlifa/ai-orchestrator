# Local Worker Run Execution Progress Log

### 2026-08-05 - Specification created and approved (slice)

- Completed: Created `requirements.md`, `design.md`, and `tasks.md` for the local worker that the approved delivery contract already expects. No implementation file, migration, test, or dependency changed.
- Discovery result: every control-plane contract this slice consumes is already merged and verified, and the gap is exactly the counterpart. `SddOrchestratorWeb.WorkerSocket.issue/3` mints the gateway credential and has no caller in `lib/`; `Delivery.AgentAdapter` has one implementation, `AgentAdapter.Unavailable`, which refuses every callback by design; and `Delivery.Worker.Workspace`, `Branch`, `ProcessLock`, and `ArtifactUpload` are implemented and proved but composed by nothing.
- Decisions resolved with the user: the worker is a developer-run local process started from this repository's toolchain rather than a signed native application, because the protocol client and the agent execution are identical in both form factors and packaging is already release-gated; both the Claude Code and the Codex command-line agents are supported behind the unchanged agent-adapter boundary, split into one task each because two command-line integrations in one task is a task-size split signal; and the run lifecycle covers an explicit start through a terminal state.
- Decision raised rather than assumed, and it widened the agreed scope: `Delivery.WorkerProtocol.negotiate/1` fails a join closed when any required capability is missing, and the required set includes run start, resume, retry, cancel, and reconcile together with isolated-branch and required-check support. A worker that only starts runs therefore cannot join at all, so the agreed start-to-terminal boundary could not be delivered as a start-only worker. The user resolved it on 2026-08-05 in favor of honoring the full required set; narrowing it through `update-spec` on the `Verified` `specs/07-guided-specification-delivery` was rejected, and that specification is unchanged. The resolution is why the plan carries twelve tasks with a dependency path of eight.
- Scope classification: focused specification. One outcome, one entry-to-completion workflow, one verification gate. Twelve tasks with a longest `Depends on:` path of eight, both at the standard limit, sized by the protocol's required-capability set rather than by preference. No smaller boundary produces an independently verifiable outcome, because a worker that cannot join proves nothing.
- Readiness by stage: product requirements `Approved`; technical design complete with no open question; implementation not started and unblocked; verification not started; release blocked by its own gate, which lists signed worker packaging, the deployment's recorded coding-agent versions, a live run on a machine other than the development machine, and the accountable privacy and security review.
- Remaining: Implement Task 1 and Task 2, which are independent and both executable, with focused proof and task-boundary commits. No implementation has started and none was requested.
- Failed checks: None.
- Proof receipts: None. No task is complete.
- Spec updates: Created the specification, then moved `requirements.md` from `Draft` to `Approved` and the task plan from `Blocked` to `Not Started` once the required-capability question was resolved. No other specification changed; `specs/11-ai-runtime-governance` and `specs/07-guided-specification-delivery` are referenced but untouched.
