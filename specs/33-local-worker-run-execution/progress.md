# Local Worker Run Execution Progress Log

### 2026-08-06 - Task 4 implemented and verified

- Completed: Added `SddOrchestrator.Worker.RunState` (the worker-local durable record of the attempt currently or most recently in flight, stored the same way as `Configuration` — owner-only JSON under the worker home directory, typed load errors instead of exceptions, an absent file is a legitimate "never received a command" state rather than an error) and `SddOrchestrator.Worker.CommandHandler` (decides and durably records the outcome of one inbound `"start"` command: duplicate check against the worker's own recorded `command_id` first, then stale-fence and superseded-attempt refusal, then a worker-side structural manifest check, then accept-and-persist-before-acknowledging). Wired into `GatewayConnection.handle_message/4` with a new `"command"` clause that calls the handler and pushes the resulting acknowledgement; `cancel`/`resume`/`retry`/`reconcile` are refused cleanly (Task 11's job) rather than crashing the connection. Work happened on `slice/33-local-worker-run-execution` in the `sdd-orchestrator-s33` worktree, continuing from Task 3.
- Discovery result: AC-06's "acknowledges an identical repeated command as a duplicate without executing it again" is the worker's own property, not just the control plane's — `CommandOutbox.acknowledge/3` already absorbs a duplicate acknowledgement so the control plane's own record stays correct, but that alone would not stop the worker from re-validating and re-accepting a redelivered command, so `RunState` is checked worker-side first, independent of the server. On manifest validation scope: the command envelope pushed to a worker has already passed the control plane's own full protocol-codec and execution-manifest validation before delivery, and the worker boundary forbids referencing any control-plane context module (including the manifest/protocol-codec modules) even in comments, matching the pattern `GatewayConnection` (Task 3) already established for `WorkerProtocol`. So `CommandHandler`'s manifest check is deliberately narrower than the control plane's: a worker-local digest-format and project/feature/run/attempt binding cross-check against the envelope's own fields, not a re-derivation of the canonical-JSON digest or a duplicate of the control plane's manifest validator. "Any attempt already running under the superseded fence stops" (AC-07) is modeled as a durable state fact (the older entry's `lifecycle` flips to `"stopped"` in `RunState.previous`) rather than a real process kill, since no process exists to kill until Task 5/6/7 — a later task's process/lock code reads this same record.
- Failed checks: None.
- Proof receipts:
- Proof receipt: `Task 4` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 4` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/run_state_test.exs test/sdd_orchestrator/worker/command_handling_test.exs test/sdd_orchestrator/worker/gateway_connection_test.exs test/sdd_orchestrator/worker/boundary_test.exs` — exit `0`.
- Spec updates: `tasks.md` — checked Task 4 complete. No requirement, design, or acceptance criterion changed.
- Remaining: Task 5 (prepare the isolated workspace and feature branch) depends on Task 4, now complete, and is next.

### 2026-08-06 - Task 3 implemented and verified

- Completed: Added `SddOrchestrator.Worker.GatewayConnection` (`use Slipstream`), the worker-runtime side of `WorkerSocket`/`WorkerChannel` negotiation: exchanges the gateway credential over `Req` (Task 1's endpoint), opens a real websocket to the control plane's `/worker/websocket` path, joins the `worker:` topic for the configured `project_id` with a hardcoded (regression-tested equal to `WorkerProtocol`) protocol version and full capability set, distinguishes a join refusal (`handle_topic_close/3` on `{:failed_to_join, response}` — reported once, never rejoined) from a transport-level drop (`handle_disconnect/2` — reconnects and rejoins the same topic, reusing the same already-obtained credential). Wired as a second child of `SddOrchestrator.Worker.Supervisor`, after `State`. Added `{:slipstream, "~> 1.1"}` (a maintained, Mint-based Phoenix channel client with built-in reconnect/backoff, per design.md's stated tradeoff). Work happened on `slice/33-local-worker-run-execution` in the `sdd-orchestrator-s33` worktree, continuing from Task 2.
- Discovery result: `SddOrchestrator.Worker.Configuration` (Task 2) had no project reference, but the `/worker` channel topic is scoped to exactly one project per connection and there is no other source for that value — resolved by adding a required `project_id` field to `WorkerConfiguration` and a required `--project` flag to `mix worker.pair`, which is why this task also touched five of Task 2's own files (`configuration.ex`, `configuration_test.exs`, `pairing_test.exs`, `supervisor_test.exs`, `worker_pair_test.exs`, `worker_start_test.exs`) to add the field/flag everywhere `WorkerConfiguration` is built — a minimal, intended ripple from extending the entity, not a redefinition of Task 2's pairing, storage, or supervisor-refusal behavior, all of which are unchanged and still pass. Focused tests exercise the real `WorkerSocket`/`WorkerChannel` over an actual listening websocket (a dedicated `Bandit` bound to the already-running `SddOrchestratorWeb.Endpoint` on an OS-assigned port, started with `start_supervised!/1` so ExUnit owns teardown ordering) rather than `Phoenix.ChannelTest`'s in-process fake transport, since Slipstream is a real wire-level client with no fake-transport mode — this is what "the connection test harness against the real socket" (tasks.md) requires.
- Failed checks: None.
- Proof receipts:
- Proof receipt: `Task 3` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 3` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/gateway_connection_test.exs test/sdd_orchestrator/worker/configuration_test.exs test/sdd_orchestrator/worker/pairing_test.exs test/sdd_orchestrator/worker/supervisor_test.exs test/sdd_orchestrator/worker/boundary_test.exs test/mix/tasks/worker_pair_test.exs test/mix/tasks/worker_start_test.exs` — exit `0`.
- Spec updates: `tasks.md` — checked Task 3 complete; `capability:local-worker-gateway-client` is now ready. No requirement, design, or acceptance criterion changed.
- Remaining: Task 4 (accept commands and own the attempt lease and durable run state) depends on Task 3, now complete, and is next.

### 2026-08-06 - Task 2 implemented and verified

- Completed: Added `SddOrchestrator.Worker.Configuration` (worker-local `WorkerConfiguration`, stored as a single owner-only JSON file under a resolvable home directory: explicit override, else `:worker_home` application env, else `~/.sdd_orchestrator/worker`), `SddOrchestrator.Worker.State` (an unnamed `Agent` holding the loaded configuration in memory), and `SddOrchestrator.Worker.Supervisor` (loads configuration before starting any child, refusing to start with a typed reason on a missing or invalid file rather than starting partially). Added two mix tasks: `mix worker.pair` (runs `Mix.Task.run("app.start")`, then calls `Devices.Pairing.complete_pairing/2` in-process and persists the result) and `mix worker.start` (never starts the application, `Repo`, or `Endpoint`; only `SddOrchestrator.Worker.Supervisor`). Work happened on `slice/33-local-worker-run-execution` in the `sdd-orchestrator-s33` worktree, continuing from Task 1.
- Discovery result: `design.md` does not spell out how pairing is actually completed by a worker process, and there is no HTTP endpoint for it — the only existing caller of `Devices.Pairing.complete_pairing/2` is a UI stub in `local_onboarding_live.ex` standing in for a real worker. Resolved this as: for this slice's developer-run, same-repository-checkout worker, `mix worker.pair` is a one-shot, explicitly local, database-backed setup step (mirroring the existing stub's call shape), while `mix worker.start`, the worker's ongoing runtime, must never reference `SddOrchestrator.Repo` or any control-plane context module — matching design.md's own stated risk ("a focused proof asserts the worker path opens no repository connection"). This is implementation detail consistent with, not a change to, the documented design, so it is recorded here rather than through `update-spec`.
- Failed checks: None. The implementing sub-agent found and fixed one real, intermittent test race (a fixed-name `Agent` under `SddOrchestrator.Worker.State` colliding across non-async test modules, and a directly-linked process racing ExUnit's own teardown) before reporting; the fix (unnamed `State` process addressed by pid, `start_supervised!/1` in the supervisor success-path tests) was verified independently by the main thread across three repeated runs with different random seeds, all passing.
- Proof receipts:
- Proof receipt: `Task 2` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 2` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/configuration_test.exs test/sdd_orchestrator/worker/pairing_test.exs test/sdd_orchestrator/worker/supervisor_test.exs test/sdd_orchestrator/worker/boundary_test.exs test/mix/tasks/worker_pair_test.exs test/mix/tasks/worker_start_test.exs` — exit `0`.
- Spec updates: `tasks.md` — checked Task 2 complete. No requirement, design, or acceptance criterion changed.
- Remaining: Task 3 (connect and negotiate the delivery gateway) depends on Task 1 and Task 2, both now complete, and is next.

### 2026-08-06 - Task 1 implemented and verified

- Completed: Added `SddOrchestratorWeb.WorkerGatewayCredentialController` (`POST /worker/gateway_credentials`), the first caller of `WorkerSocket.issue/3`. It authenticates the presented pairing credential through `Devices.Pairing.authenticate_worker/1`, resolves the owning worker and re-checks the requesting credential's device workspace through `Devices.Pairing.authorize_for_workspace/2`, then issues the gateway credential. Added its focused controller test and the one router line. Work happened on `slice/33-local-worker-run-execution` in a dedicated worktree (`sdd-orchestrator-s33`), per the project's per-slice branching rule.
- Discovery result: design.md names `authenticate_worker/1` and `authorize_for_workspace/2` as the reused authorization seam but does not say how a project resolves to the device workspace `authorize_for_workspace/2` compares against. Traced the schema and migrations to confirm this: `projects.workspace_id` is DB-constrained (`projects_workspace_storage_mode_fkey`, composite with `storage_mode`) to reference only a `kind = 'hosted'` row in `workspaces` — a device workspace is never persisted there (`workspaces_hosted_kind_only` check constraint), and a migration guard explicitly forbids any `projects` row with `storage_mode = 'device'`. So a project's own `workspace_id` can never name a device workspace directly. The only existing link between a hosted project and a device workspace is `Portability.HostedLocalRepositoryBinding` (`specs/06-project-portability`, keyed by `project_id`, naming the worker whose device workspace was proven to control that project's local repository). Task 1 resolves "the device workspace owning a project" by loading that binding, loading its bound worker, and comparing device workspaces from there. This is implementation detail consistent with, not a change to, the documented design decision, so it is recorded here rather than through `update-spec`.
- Failed checks: None.
- Proof receipts:
- Proof receipt: `Task 1` — scope `Focused` — command `mix format --check-formatted` — exit `0`.
- Proof receipt: `Task 1` — scope `Focused` — command `mix compile --warnings-as-errors` — exit `0`.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator_web/controllers/worker_gateway_credential_controller_test.exs` — exit `0`.
- Spec updates: `tasks.md` — checked Task 1 complete and moved slice `Status` from `Not Started` to `In Progress`. No requirement, design, or acceptance criterion changed.
- Remaining: Task 2 (worker runtime, configuration, and credential custody) is next and independent of Task 1.

### 2026-08-05 - Specification created and approved (slice)

- Completed: Created `requirements.md`, `design.md`, and `tasks.md` for the local worker that the approved delivery contract already expects. No implementation file, migration, test, or dependency changed.
- Discovery result: every control-plane contract this slice consumes is already merged and verified, and the gap is exactly the counterpart. `SddOrchestratorWeb.WorkerSocket.issue/3` mints the gateway credential and has no caller in `lib/`; `Delivery.AgentAdapter` has one implementation, `AgentAdapter.Unavailable`, which refuses every callback by design; and `Delivery.Worker.Workspace`, `Branch`, `ProcessLock`, and `ArtifactUpload` are implemented and proved but composed by nothing.
- Decisions resolved with the user: the worker is a developer-run local process started from this repository's toolchain rather than a signed native application, because the protocol client and the agent execution are identical in both form factors and packaging is already release-gated; both the Claude Code and the Codex command-line agents are supported behind the unchanged agent-adapter boundary, split into one task each because two command-line integrations in one task is a task-size split signal; and the run lifecycle covers an explicit start through a terminal state.
- Decision raised rather than assumed, and it widened the agreed scope: `Delivery.WorkerProtocol.negotiate/1` fails a join closed when any required capability is missing, and the required set includes run start, resume, retry, cancel, and reconcile together with isolated-branch and required-check support. A worker that only starts runs therefore cannot join at all, so the agreed start-to-terminal boundary could not be delivered as a start-only worker. The user resolved it on 2026-08-05 in favor of honoring the full required set; narrowing it through `update-spec` on the `Verified` `specs/07-guided-specification-delivery` was rejected, and that specification is unchanged. The resolution is why the plan carries twelve tasks with a dependency path of eight.
- Scope classification: focused specification. One outcome, one entry-to-completion workflow, one verification gate. Twelve tasks with a longest `Depends on:` path of eight, both at the standard limit, sized by the protocol's required-capability set rather than by preference. No smaller boundary produces an independently verifiable outcome, because a worker that cannot join proves nothing.
- Readiness by stage: product requirements `Approved`; technical design complete with no open question; implementation not started and unblocked; verification not started; release blocked by its own gate, which lists signed worker packaging, the deployment's recorded coding-agent versions, a live run on a machine other than the development machine, and the accountable privacy and security review.
- Remaining: Implement Task 1 and Task 2, which are independent and both executable, with focused proof and task-boundary commits. No implementation has started and none was requested.
- Failed checks: None.
- Proof receipts: None. No task is complete.
- Spec updates: Created the specification, then moved `requirements.md` from `Draft` to `Approved` and the task plan from `Blocked` to `Not Started` once the required-capability question was resolved. No other specification changed; `specs/11-ai-runtime-governance` and `specs/07-guided-specification-delivery` are referenced but untouched.
