# Live Repository Metadata Binding Progress Log

### 2026-09-04 - Task 8 complete: all eight tasks done, live path wired end to end

- `load_context/3` gains `repository_local?`, true only for a hosted project whose `repository_provider == "local"` (always true for a device project — confirmed by tracing `DeviceStore.Local.do_register/3`, which hardcodes `repository_provider: "local"` unconditionally). The disclosure stage's confirm form is gated on it; a non-local project sees a sibling refusal section instead, naming the repository and stating plainly that this assessment needs a repository a worker can verify locally. Screen admission (`assessable_hosted_project?/1`, `authorize_project/2`) is untouched.
- AC-02 is proven through the REAL pipeline, not the file's own fake `Adapter`: `RepositoryMetadataAdapter.Worker` configured, `RepositoryMetadataTransportDouble` installed, a deliberately distinguishing root and commit scripted, and `Adapter.events() == []` asserted so a passing test could not be satisfied by the fake adapter still being wired in.
- One real, necessary widening beyond the original brief: nothing on the disclosure stage rendered the repository name before this task, so `data-repository-name` was added to make AC-10's refusal — and specs/46's own regression test — actually able to name the repository pre-confirmation.
- Five existing tests needed their project fixture swapped from the default GitHub-shaped `context.hosted_project` to `hosted_local_project(context)`, since a GitHub project no longer reaches the confirm form at all: the two Task 7 added, one Task 6-era test proving `:repository_mismatch`/staleness wording, the original "hosted owner confirms" test, and one label test not anticipated in the task brief but found broken the same way.
- `test/sdd_orchestrator_web/mac_repository_assessment_test.exs`'s `"a GitHub project is unchanged"` regression test (specs/46's own AC-05) rewritten to match the narrowed agreement recorded in specs/46's own progress log: the repository is still named correctly pre-confirmation, no confirmation is offered, and `execution_profile` stays unmet. No other describe block in that file touched.
- 20 tests total across both files (5 new/rewritten, 15 fixed-in-place). `mix compile --warnings-as-errors` clean. Re-run independently twice with no flakiness.
- Proof receipt: `Task 8` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/repository_assessment_live_test.exs test/sdd_orchestrator_web/mac_repository_assessment_test.exs` — exit `0`.
- All eight tasks in this slice are now complete. `capability:live-repository-metadata-binding` is ready. What remains before `Verified`: the full verification gate (`mix check`, `npm run test:e2e`, production build, both specification validators) and the two product-proof click paths against the installed Mac app.

### 2026-09-04 - Task 7 complete: the screen waits and can be stopped instead of freezing

- `confirm_boundary` no longer calls `prepare_binding/4` inline. It builds `attrs` synchronously (all pre-flight validation unchanged), then hands the call to `start_async(:prepare_binding, ...)` and moves to a new `:preparing` stage. A live worker answer can take up to ~130 seconds; the old inline call would have frozen the whole page for that long with no way to cancel, the moment Task 6 made the adapter real.
- A new `handle_async(:prepare_binding, ...)` callback owns every outcome: success, an ordinary refusal (`preparation_error/1`, unchanged, just called from a new place), an explicit stop, and any other task exit. `handle_event("stop_preparing", ...)` only calls `cancel_async(socket, :prepare_binding)`; the stage reset happens once, in `handle_async/3`, when the cancellation arrives back as `{:exit, {:shutdown, :cancel}}` — confirmed to be the real shape by a passing test, not assumed from documentation alone.
- Proving the waiting and stop states needed a gate the existing test `Adapter` didn't have; one was added, test-only, confined to that module. Proving a task that dies without answering needed the crash to be a real untrapped process exit rather than `RepositoryAssessments.invoke/3`'s own rescue swallowing an ordinary `raise` into `{:error, :worker_unavailable}` — the test unlinks before killing itself, mirroring the same unlink-then-exit order `cancel_async/3` itself uses, so the forced crash doesn't take the LiveView down with it.
- `render_async/1` was found not to reliably wait for a *cancelled* async operation (only a normal completion, where the result is sent before the task process exits) — the stop-button test polls the rendered stage directly instead, with the reasoning recorded in the test's own comment.
- The Playwright suite (`assets/e2e/repository-assessment.spec.js`) needed no change: its `toBeVisible()` assertions already auto-retry past the new interstitial waiting stage.
- 3 new tests. Regression: both files' existing tests pass with two `render_async(view)` insertions after the two `#assessment-binding-form` submits outside this task's own new tests. `mix compile --warnings-as-errors` clean. Re-run independently twice with no flakiness.
- Proof receipt: `Task 7` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/repository_assessment_live_test.exs test/sdd_orchestrator_web/mac_repository_assessment_test.exs` — exit `0`.

### 2026-09-03 - Task 6 complete: the live adapter is configured, the gap is closed

- Two corrections made to this task's own plan before dispatch, recorded here rather than discovered mid-implementation. First, `RepositoryMetadata.inspect/2`'s public error type collapsed every worker refusal to `:invalid_worker_response`, including a mismatched folder — Task 2's own progress note anticipated this exact widening, so `RepositoryMetadata.Server#requester_outcome/1` gained one clause splitting `:repository_mismatch` out, and `RepositoryMetadata`'s moduledoc and typespec were updated to match. Second, Task 3 added `:repository_metadata_transport` as a config key but no file set it, so it still resolved to `Unavailable` everywhere including production; setting only `:repository_metadata_adapter` would have changed nothing observable. Both keys are now set together.
- `RepositoryMetadataAdapter.Worker` is a thin pass-through: `prepare/1` and `revalidate/1` are the identical call, since the adapter's `request()` type is field-for-field the same as `RepositoryMetadata.request()` and the no-second-panel behavior is entirely a worker-side property (Task 5) invisible at this layer. This adapter's only job is narrowing `RepositoryMetadata.inspect/2`'s error union to the two atoms `RepositoryAssessments.invoke/3` reads specially.
- `config/config.exs` sets `:repository_metadata_transport` to `Transport.Attachment` and `:repository_metadata_adapter` to `RepositoryMetadataAdapter.Worker` — the real path is now the default everywhere. `config/test.exs` pins both explicitly to `Unavailable`, matching what every existing test already resolved to, so nothing changed behind any existing suite. `config/dev.exs` needed no change.
- 18 new/changed tests across three files (`repository_metadata_adapter/worker_test.exs`, `repository_metadata_adapter/live_adapter_test.exs`, and two tests in the existing `repository_metadata_test.exs`, one renamed to keep its old coverage after the widening changed its expected outcome). Regression: `repository_binding_preparation_test.exs` and `repository_assessment_test.exs`, 26 tests, unmodified and passing. `mix compile --warnings-as-errors` clean.
- `RepositoryMetadataAdapter.configured/0` now resolves to a real worker-backed adapter outside tests. What remains before a person can click through: the assessment screen (Tasks 7-8) still calls `prepare_binding/4` inline and blocks the LiveView process on the native panel, and there is no waiting state, stop action, or live readout wired up yet.
- Proof receipt: `Task 6` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_assessments/repository_metadata_adapter/ test/sdd_orchestrator/repository_metadata_test.exs` — exit `0`.

### 2026-09-03 - Task 5 complete: the worker answers a metadata question for real

- `Worker.RepositoryMetadata` is the worker-side responder. Its held-folder cache is keyed by `selection_ref` (stable across a prepare and its later revalidate), while a wire cancellation names the in-flight question by `request_id` (fresh per call) and is resolved back to `selection_ref` from `state.awaiting` — the two ids are deliberately never conflated.
- Getting a folder reuses the already-built `RepositorySelection.request_path/3` (Task 4), with `selection_ref` standing in for `RepositorySelection`'s own unrelated internal request id. That reuse is what makes `RepositorySelection.close(selection_ref)` exactly the right call to cancel an in-flight pick, with no separate id-mapping table needed.
- A held folder's expiry is fixed at the moment it is first stored, from the `expires_at` the finding request carried, and is never extended by a later revalidate that reuses it. A refusal holds nothing, so the next question for the same reference opens a new panel rather than reusing anything that failed the identity check.
- Identity matching duplicates `Worker.RepositorySelection`'s own private `compare_identity/3`/`workspace_salt/1` rather than extracting a shared module between the two, following the same call this codebase's `RepositoryKits.WorkerKitComparison` already makes for a near-identical situation: two callers of one comparison, not one module reaching into another's private surface.
- `Worker.GatewayConnection` gains `repository_metadata`/`repository_metadata_cancel` inbound handlers and a deferred-push `repository_metadata_result` handler, verbatim mirrors of the existing `repository_selection` trio. `Worker.Supervisor` starts `RepositoryMetadata` beside `RepositorySelection`, before `GatewayConnection`, for the same first-join-delivery reason.
- 8 new tests. Regression: `repository_selection_test.exs` (18) and `gateway_connection_test.exs` (13) both pass unmodified, 31 together. `mix compile --warnings-as-errors` clean. No filesystem path appears in any log line this module emits, proven by a captured-log assertion across a full successful flow.
- Proof receipt: `Task 5` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/repository_metadata_test.exs` — exit `0`.

### 2026-09-03 - Task 3 complete: a metadata question now reaches a real attached worker

- `RepositoryMetadata.Transport.Attachment` and `RepositoryMetadata.AttachmentCodec` mirror `RepositorySelection`'s own attachment transport and codec exactly: the same `WorkerAttachment` registry lookup keyed by device workspace then filtered by `worker_id`, the same three-way refusal (`:no_worker`, `:worker_needs_update`, `:transport_error`), the same closed-key encode/decode boundary. The outbound request carries exactly six fields (`request_id`, `selection_ref`, `repository_provider`, `repository_id`, `selected_root`, `expires_at`); the inbound answer is allowlisted to exactly the seven keys `MetadataAnswer.@keys` already accepts, so the codec's outer boundary and `MetadataAnswer.new/1`'s inner boundary agree by construction.
- `WorkerWorkspaceChannel` gains `handle_in("repository_metadata_result", ...)` and outbound `handle_info` clauses for `:repository_metadata` and `:repository_metadata_cancel`, purely additive beside the unchanged `repository_selection` handling; the moduledoc now says the channel carries two request-and-answer pairs instead of one.
- Nothing on the worker side understands these messages yet — that is the next task. The worker's own `Worker.GatewayConnection` was not touched.
- 10 new tests. Regression: the existing 12-test `repository_selection` channel suite passes unmodified. `mix compile --warnings-as-errors` clean.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator_web/channels/worker_workspace_channel_repository_metadata_test.exs` — exit `0`.

### 2026-09-03 - Task 2 complete: a blocking metadata request lifecycle, no worker wired yet

- `SddOrchestrator.RepositoryMetadata.inspect/2` mirrors `RepositorySelection` closely but blocks the calling process instead of returning at once: the outer `GenServer.call` is `:infinity`, and `RepositoryMetadata.Server` owns the real expiry through its own timer, replying via `GenServer.reply/2` exactly once from whichever path settles the request. A transport push failure replies immediately from within `handle_call`; every other resolution (a worker's answer, the worker's channel dying, the expiry timer) defers the reply until the entry is removed first, so a second attempt at any of them finds nothing and replies to nobody.
- The requester's own exit is the cancellation path (no public `cancel/1`): the server monitors `elem(from, 0)`, and on that process's `:DOWN` it cancels via the transport and cleans up without replying, since `GenServer.reply/2` to an exited caller is a safe no-op with nobody left to tell.
- `MetadataAnswer.new/1` mirrors `SelectionResult.new/1`'s strict allowlisted-key parsing: an unrecognised key, an unknown outcome, or a missing value field for the outcome that requires it is refused as `:invalid_result` before it can reach a requester. Three outcomes: `:metadata` (resolves the block to `{:ok, result}`), `:refused` (resolves to `{:error, :invalid_worker_response}` for now — `:repository_mismatch` is validated and stored but not yet surfaced at the public API, so a later task can widen the mapping without re-touching this parsing), `:cancelled` (resolves to `{:error, :cancelled}`).
- Only the `Unavailable` transport exists (`{:error, :no_worker}` always). Nothing calls `inspect/2` yet outside its own tests. `SddOrchestrator.RepositoryMetadata.Server` added to the application supervision tree beside `RepositorySelection.Server`.
- 10 tests. `mix compile --warnings-as-errors` clean.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_metadata_test.exs` — exit `0`.

### 2026-09-03 - Task 4 complete: the Mac's one panel owner can answer a raw path to an in-release caller

- `Worker.RepositorySelection` gains `request_path/3`, additive beside the unchanged `open/3`. Both now cast into one `handle_cast({:open, payload, reply, home_override, result_builder}, state)` clause; the held-request map gains a `:result_builder` field (`&result/2` for `open/3`, `fn _request, choice -> choice end` for `request_path/3`), and `finish/2` calls `request.reply.(request.result_builder.(request, choice))` instead of hardcoding `result/2`.
- Nothing about the pending file, the answer file, their names, the poll, the replace-on-open behavior, or the expiry/stale-file cleanup changed for either entry point — they share the one held request exactly as design intended, so the installed Mac app needs no change and no rebuild.
- 5 new tests; all 13 pre-existing tests in `repository_selection_test.exs` pass unmodified.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator/worker/repository_selection_test.exs` — exit `0`.

### 2026-09-03 - Task 1 complete: repository_metadata is a negotiable capability

- `repository_metadata` added to `WorkerProtocol.@optional_capabilities` and to `Worker.GatewayConnection`'s hardcoded twin, in the same position in both. No other list in either file changed.
- `WorkerProtocol`'s own `negotiate/1` tests assert against `WorkerProtocol.capabilities/0` dynamically, so nothing there needed a change, and the existing `gateway_connection_test.exs` test that the two files' capability lists match exactly still passes unmodified.
- 23 tests passed.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/worker_protocol_test.exs test/sdd_orchestrator/worker/gateway_connection_test.exs` — exit `0`.

### 2026-09-03 - Specification created for the binding half of a live assessment

- Cause confirmed in code before anything was designed. `RepositoryMetadataAdapter.configured/0` falls back to `Unavailable`, no configuration file in any environment sets `:repository_metadata_adapter`, and the only setter is the `/_e2e` bootstrap controller, which is compiled out of normal builds. `specs/46-assessing-a-repository-on-a-mac/` recorded the same finding and classified it as `specs/14-repository-execution-profile/`'s release gate.
- Two further facts were found while scoping and they changed the plan. First, the worker-local computation already exists and is verified: `RepositoryAssessments.WorkerRepositoryMetadata.inspect/4` resolves the repository top level, proves root containment through links, and reads `HEAD`, returning exactly the four fields the contract allows. Nothing needs to be added to it. Second, nothing on either side knows where the repository is. `specs/40-worker-repository-selection/` deletes the chosen path on read by design, `HostedLocalRepositoryBinding` stores the project, the worker, and a validation time, and the project stores a portable identity, not a location.
- A third fact set the slice boundary. `finish_assessment/6` has no live caller either, and no command carries a scan. So a live adapter alone lets an assessment start and never complete. The user chose to deliver the binding half now and the live scan as a named follow-on, because the scan depends on the binding and the two are independently valuable.
- Product decisions taken with the user. The person points at the repository folder when a binding is prepared, reusing the panel the installed Mac app already serves, so no path is ever stored and the app needs no rebuild. The live proof covers both the hosted route and the device route, which is what `specs/14`'s gate means by each deployment profile.
- Engineering decisions recorded in `design.md`. The question rides the Mac-scoped attachment as `specs/40`'s selection does, the adapter stays synchronous and the screen moves the call into a monitored task so it can render a wait and a stop, the worker holds the answered folder in memory for the binding's 120 second life so the revalidation opens no second panel, `Worker.RepositorySelection` stays the Mac's one panel owner, and the live adapter is configured in `config/config.exs` with the test environment pinned to `Unavailable` so no existing suite changes behaviour.
- Privacy boundary is unchanged and stated as a rule and an acceptance criterion. The question and the answer carry only what `specs/14` already defined, refusals are single atoms so no Git message can travel, and the folder exists only in the answer file for at most one poll interval and in the release's memory for at most the binding's life.
- One widening is recorded rather than hidden. `specs/40` kept the path inside one function; this slice keeps it in memory for up to two minutes. That is why the release-restart case is AC-08 and not an implementation detail.
- Scope: classified `focused specification`. One outcome, one workflow, one verification gate, eight tasks, longest `Depends on:` path of seven, no size exception. All three required capabilities are already provided by verified slices, so no task starts blocked.
- Consequence for neighbours, with nothing reopened. `specs/14`'s live-worker gate closes only when this slice and the deferred scan are both verified, and `specs/41-feature-delivery-from-the-ui/` and `specs/46` stay release-blocked until then.
- No code changed in this update.
