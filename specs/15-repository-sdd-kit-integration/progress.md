# Repository SDD Kit Integration Progress Log

### 2026-08-11 - Task 5 implemented and verified: idempotent apply and kit update

- Completed: AC-09, AC-10. Added `plan_type` (`"install"`/`"update"`) to `RepositoryKitChangePlan`, extending `@severities` with a new `"drifted"` conflict flavor (a kit-owned file whose live content no longer matches what was recorded at install/last update) folded into the existing `has_ordinary_conflicts` flag so `apply_plan/4`'s conflict gate needed zero changes to also block on it. Added `history` and widened `state` to `~w(applied updated)` plus a new `update_changeset/2` on `RepositoryKitInstallation` — exactly what Task 4's own moduledoc anticipated ("Task 5 needs it"). Added a unique index on `RepositoryKitInstallation.project_id`, closing a latent gap (nothing previously stopped two installation rows existing for one project) that Task 5 is the first task to actually depend on being impossible.
- New `WorkerKitUpdateComparison.compare/6`: a three-way classification `WorkerKitComparison` never needed — not-kit-owned (identical to install's own logic), kit-owned-and-unchanged-since-record (safe to overwrite, `"create"`), and kit-owned-but-drifted (modified or deleted since record, `"drifted"` conflict, never silently touched). Reuses `existing_sha256` (not a new operation `kind`) to distinguish "will be updated" from "brand new" — deliberately no schema-level `kind` addition.
- `WorkerKitApply.write_create/2` strengthened: previously only refused an unexpected file when `existing_sha256` was `nil`; now re-verifies the live file's content still matches a *present* `existing_sha256` immediately before overwriting. Closes a real TOCTOU gap update introduces (a kit-owned file could change again between planning and the owner's confirmed apply, up to the plan's 15-minute window) that install never needed to worry about (it never expected pre-existing content to match). Re-ran the full Task 4 apply suite unmodified after this change — no regression, since Task 4's own "identical content already present" case already sets a non-nil `existing_sha256` equal to the live content, so the strengthened check is a no-op there.
- `apply_plan/4` is now genuinely idempotent (AC-09): a repeat call with the same `plan_id` short-circuits to the already-persisted installation, before touching the repository or re-running expiry/conflict gates — resolves the gap Task 4's own progress entry flagged and deferred here. Plan-type aware: an `"install"` plan still inserts a new row; an `"update"` plan updates the project's existing row in place via `update_changeset/2`, prepending a small JSON-safe snapshot of the pre-update state (no absolute paths, no secrets, same evidence-hygiene rule as `evidence`) to `history`.
- New `plan_update/4`: sibling to `plan_change/4`, reusing `ManagedRuntimeProfile.build/3` and the existing-instruction precedence machinery but deliberately skipping the pilot-eligibility gate (`eligible_for_kit_offer?/2` governs only the *initial* offer; an update is available once something is installed, independent of pilot lifecycle column). Refuses `{:error, :not_installed}` when nothing is installed yet.
- `RepositoryKitOfferLive`: replaced Task 4's transient (reload-losing) `:applied` stage with a persistent `:installed` stage derived from a new `RepositoryKits.current_installation/3` inside `load_offer/1`, so "just applied" and "reload the page" now render through the exact same path. A newer catalog package shows as pure information (AC-10) with an explicit "Review update" action; the resulting `:update_plan` stage is reload-derivable exactly like `:plan` already was (not transient like Task 4's old `:applied`, which is the pattern it replaces), so a mid-review reload never loses it. The existing apply button's conflict guard already covers a drifted conflict for free, since `has_ordinary_conflicts` was widened at the schema level rather than needing separate LiveView-side branching.
- Real, deliberate deviation from the brief (reviewed and confirmed correct, not just accepted on faith): the brief called `:update_plan` "transient"; the implementation made it reload-safe instead, matching `:plan`'s existing, better-established pattern rather than copying `:applied`'s inferior one. Verified the load-bearing mechanism — a repeated pattern variable (`installed_plan_id`) appearing twice across separate function-head arguments in `stage/5`, which only matches when `installation.plan_id == plan.id` — actually enforces equality in Elixir (confirmed empirically with a standalone script, not assumed from memory) before accepting this design.
- An existing Task 4 test necessarily changed: `repository_kits_apply_test.exs`'s "duplicate insert of the same plan row" test previously deleted a plan's branch specifically to defeat `WorkerKitApply`'s natural `:branch_conflict` refusal and observe the `:already_installed` mapping — that scenario is now correctly intercepted earlier, by the idempotent-replay short-circuit itself, exactly as that test's own prior comment already predicted ("idempotent re-apply is explicitly Task 5's job"). Updated the test to assert the new correct behavior; the underlying unique-constraint mapping remains independently covered by its sibling test.
- Found (during test-writing, not application code) and fixed a flakiness source: `RepositoryAssessment.pending/2` stamps `inserted_at` from the caller-supplied `now` verbatim, and `AssessmentStore`/`ProfileStore`'s hosted "latest" reads break an `inserted_at` tie on `id desc` — a random UUID. Tests needing two profile approvals advance `now` by 60s between them (same pattern `managed_runtime_profile_test.exs` already uses for the identical reason). Confirmed clean across multiple re-runs with varying seeds. The underlying tie-break itself is a specs/14/specs/30 concern, not this slice's — recorded under `## Blocked Decisions` as a discovery, not fixed here.
- Sobelow (informational, not required at focused-task scope): the new `verify_existing/2`'s `File.read/1` needed the same `# sobelow_skip ["Traversal.FileModule"]` annotation `do_write_create/2` already carries for the identical reason (a worker-local, containment-checked path, not user input) — missing from the sub-agent's initial delivery, added during this thread's own review before accepting the task. Re-ran the full focused proof after adding it — unaffected, still 20 passed.
- Playwright/e2e: skipped, same reasoning as Tasks 3 and 4. Left open for slice verification.
- Proof receipt: `Task 5` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_kits_update_test.exs test/sdd_orchestrator_web/live/repository_kit_offer_live_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly (20 passed, real exit 0, both before and after the sobelow fix), plus `mix compile --warnings-as-errors` and `mix format --check-formatted` (both clean), the full Task 2/3/4 regression suite (`repository_kits_apply_test.exs` + `repository_kit_change_plan_test.exs` + `repository_kits_test.exs`, 64 passed), and the new update-test file three additional times with varying seeds to confirm the flakiness fix holds. Read every delivered and edited file directly (both schemas, both migrations, `WorkerKitUpdateComparison`, the `WorkerKitApply` strengthening, `repository_kits.ex`'s new/changed functions, and the full LiveView diff) rather than trusting the sub-agent's self-report — this is what surfaced the missing sobelow annotation, fixed before accepting. A repo-wide `mix credo --strict` run (informational only, excluded from focused task scope) surfaces one new nesting-depth note in the new `apply_plan/4` nested `with`/`case` — a direct, foreseeable consequence of the idempotency short-circuit's own required shape, not a defect; left for slice verification or a future cleanup, same treatment as Task 4's own pre-existing credo note.
- Capability graph: unaffected — Task 5 owns no published capability of its own.
- Failed checks: None.
- Remaining: Task 6 (implement kit removal), `Depends on: Task 5`, now satisfied. Stopping here — Task 6 is not started.

### 2026-08-11 - Task 5 split via update-spec: idempotency-and-update (Task 5) and removal (new Task 6) separated

- Completed: Task 5 preflight (before dispatching a sub-agent, per implement-spec's own stop condition) found the original Task 5 ("Implement idempotent update and removal", AC-09+AC-10+AC-11) bundled two independently-testable lifecycle workflows — update (one state transition: `applied` → `updated`) and removal (a different state transition: `applied`/`updated` → `removed`) — plus a general idempotency fix into one task. Each of update/removal needs its own planning, kit-owned-file comparison, conflict detection, isolated-branch apply, and LiveView UI, comparable in size to Task 4 alone (~1800 lines). This is the same "combines independently testable behaviors" / "more than one primary state transition" Task Size Gate trigger that split Task 2, evidenced concretely by Task 4's own delivered size rather than abstract worry. Presented the finding to the user with a recommendation; user confirmed splitting via `update-spec` over implementing it as one oversized task.
- Split: Task 5 keeps its label and narrows to AC-09 (idempotent apply-retry — a general fix to `apply_plan/4`, fully testable using only Task 4's existing install-plan machinery, no update/removal plan types required) + AC-10 (update planning, kit-owned file comparison, user-modification conflicts, confirmation, isolated-branch apply, explicit newer-version-available presentation). New Task 6 owns AC-11 alone (removal planning, kit-owned file comparison, ownership-safe deletion, confirmation, isolated-branch apply), `Depends on: Task 5` (reuses Task 5's idempotency mechanism and lifecycle-history schema extension rather than redefining them). The former Task 6 (governance, AC-12/AC-13, `capability:repository-sdd-kit` provider) renumbered to Task 7, `Depends on: Task 6`. No acceptance criterion, business rule, or requirement changed — purely a task-boundary and numbering split, identical in kind to the earlier Task 2 → Task 2/Task 3 split.
- Updated `## Cross-Specification Dependencies`: `capability:project-storage-governance`, `capability:project-specification-store`, `capability:project-specification-governance` now read `required before Task 7`; `capability:repository-sdd-kit` now reads `ready after Task 7`. Corrected the one external consumer of that edge, `specs/13-sdd-adoption/tasks.md`'s `capability:repository-sdd-kit` requirement, from `#Task 6` to `#Task 7` in the same change (confirmed via repo-wide grep this is the only consumer of a specs/15 task-6-or-later reference; `specs/16-empty-repository-initialization`'s reference to `specs/15#Task 1` needs no change). Corrected two stale forward-references inside already-completed task entries (Task 3's own "hosted-only pending Task 6" note, and a Blocked Decisions bullet) to point at the new Task 7.
- Extended `## Proof Scope Gate`'s `Applies to:` from `Task 1, Task 2, Task 3, Task 4.` to include `Task 5` (now narrowed), since it is being refined right now — the same incremental-adoption pattern already used for Tasks 2–4. Did not add the new Task 6 or renumbered Task 7 yet, since neither is being worked.
- Verified: `validate_spec.py` on both `specs/15-repository-sdd-kit-integration` and `specs/13-sdd-adoption` individually, plus the global graph check across all 35 specifications — all passed. Task count is now 7 (was 6), still well within the Slice Size Gate's 12-task and 8-task critical-path limits.
- Failed checks: None.
- Spec updates: `tasks.md` restructured as above; `specs/13-sdd-adoption/tasks.md`'s capability reference corrected in the same change.
- Remaining: Implement Task 5 (idempotent apply-retry + update), then Task 6 (removal), then Task 7 (governance). Task 5 is not started.

### 2026-08-11 - Task 4 implemented and verified: isolated-branch apply

- Completed: AC-06, AC-07, AC-08, entity:RepositoryKitInstallation. Added `RepositoryKitInstallation` (project-scoped, hosted-only for now — same reasoning as `RepositoryKitChangePlan`; NOT immutable like its two siblings, since Task 5 will transition its `state` for update/removal lifecycle), `RepositoryKits.WorkerKitApply` (worker-local isolated-branch apply engine — every git invocation carries `-c core.hooksPath=/dev/null`, not only the commit step, so a `pre-commit` or `post-checkout` hook can never run), and `RepositoryKits.apply_plan/4`.
- Proof Scope Gate adopted for Task 4 as this session began working it, per the contract's own "adopt when the next unfinished task is refined" activation rule: extended `## Proof Scope Gate`'s `Applies to:` from `Task 1, Task 2, Task 3.` to include `Task 4.` and added `Proof scope: Focused` to its entry, before any implementation started. Re-ran `validate_spec.py` and the global graph check — both passed with the extension in place; this is mechanical bookkeeping activating an already-approved contract for the newly active task, not a product or verification-expectation change, so it was done directly rather than through `update-spec`.
- `apply_plan/4` gate order: authorize (`{:hosted, account_id}` owning the project only — `Participation.owned_project/2`, same as `plan_change/4`'s own persistence step; any other authority shape refused with `:unauthorized`) → fetch the plan scoped to the project → refuse an expired plan (`:plan_expired`, compared against `plan.expires_at`) → refuse a plan with `safety_blocked` or `has_ordinary_conflicts` (AC-06, no override mechanism exists or was built) → call `WorkerKitApply.apply/5` → persist `RepositoryKitInstallation` from the plan's own bound fields plus the apply result.
- `WorkerKitApply.apply/5` gates every check (exact-commit staleness re-verified live, clean-working-tree, default-branch refusal, existing-branch refusal, operation-kind allowlist, path/symlink containment for every `"create"` operation) before mutating anything, mirroring `Delivery.Worker.Branch.prepare/2`'s "fail closed before the repository is touched" discipline without importing that module (it's coupled to `ExecutionManifest`/`Workspace`/`run_id`; this isn't). Only `"create"` operations are written; `"omit"` operations are never touched, preserving Task 2's existing-instruction precedence. A TOCTOU defense re-checks `File.exists?/1` immediately before each write (`:unexpected_file` if a path the plan believed absent turns out to exist). Commit is skipped (result is the unchanged `base_commit`) when every proposed file was already present identically — a legitimate no-op, not a failure.
- Rollback: any failure from branch creation onward checks out back to the original ref, deletes the created branch, and runs `git clean -fd` — found and fixed during this session's own independent review (not the implementing sub-agent's initial delivery): the first version restored the branch/HEAD correctly but left physically-written, never-staged files as untracked debris in the working tree after a partial-failure rollback, which the original test suite didn't catch because it only asserted branch/HEAD restoration, not working-tree cleanliness. Since `apply/5`'s own precondition proves the working tree was byte-for-byte clean before this invocation touched anything, any untracked file present after a failed apply was created by this invocation alone, making `git clean -fd` safe. Strengthened the partial-failure test to assert `git status --porcelain` is empty and the partially-written file no longer exists after rollback; confirmed by temporarily reverting the fix and observing the strengthened assertion fail as predicted, then restoring it.
- Evidence (`RepositoryKitInstallation.evidence`) never contains `repository_path`, `repository_root`, or any other absolute filesystem path — verified directly in tests via `Jason.encode!/1` on the persisted evidence map, per design.md's "Diffs and stored evidence exclude secret values and absolute local paths."
- `RepositoryKitOfferLive` extended (not a new route): an "Apply this plan" button appears only for the owner and only when the current plan has neither conflict kind; its handler calls `apply_plan/4` with the exact same documented `opts[:repository_path]` omission `build_plan/1` already uses, so it hits the identical, already-accepted `{:error, :repository_path_required}` honest refusal — not a new gap. Success moves to a transient (non-persisted, like `:declined`) `:applied` stage showing the branch, resulting commit, and installed file list.
- Duplicate-apply: a unique index on `RepositoryKitInstallation.plan_id` is a minimal accidental-duplicate safety net, not idempotent replay (AC-09/Task 5's job). A naive retry against the exact same (still-existing) branch actually refuses earlier, at `WorkerKitApply`'s own `:branch_conflict` gate, before ever reaching persistence; the unique-index path only fires in the narrow case where the created branch was separately removed between two attempts on the same plan row. Recorded under `## Blocked Decisions` in `tasks.md` as deferred to Task 5, not a Task 4 gap.
- Playwright/e2e: skipped, same reasoning as Task 3 (chaining assessment → profile approval → pilot selection → feature linking → kit publish → apply through a real browser is out of one focused task's scope to author unverified). Left open for slice verification.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_kits_apply_test.exs test/sdd_orchestrator_web/live/repository_kit_offer_live_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly twice (once before, once after the rollback fix) — 29 then 30 passed, both real exit 0. Read every delivered and edited file directly (schema, migration, `WorkerKitApply`, `apply_plan/4`, the LiveView diff, and the full test suite) rather than trusting either sub-agent's self-report; this is what surfaced the rollback working-tree gap, which was sent back and fixed before accepting the task. Also ran `mix compile --warnings-as-errors` (clean), `mix format --check-formatted` (clean), and the full Task 2/3 regression suite (`repository_kit_change_plan_test.exs` + `repository_kits_test.exs`, 44 passed) directly. A repo-wide `mix credo --strict` run (informational only — the Task Proof Gate contract excludes repository-wide quality gates from focused task scope) surfaces one pre-existing nesting-depth note in Task 2's already-merged `worker_kit_comparison.ex`, untouched by this task; not fixed here as out of scope, left for slice verification or a future cleanup. `mix sobelow --config` reported no findings.
- Capability graph: unaffected — Task 4 owns no published capability of its own.
- Failed checks: None.
- Remaining: Task 5 (implement idempotent update and removal), `Depends on: Task 4`, now satisfied. Stopping here — Task 5 is not started.

### 2026-08-11 - Task 3 implemented and verified: post-pilot offer, decline, and reviewable diff

- Completed: AC-01. Added `SddOrchestratorWeb.RepositoryKitOfferLive` at `/projects/:id/kit`, hosted-only (no device route — `RepositoryKitChangePlan` persistence is itself hosted-only pending Task 6, so a device route would have nothing to show). Context loading mirrors `RepositoryPilotLive`'s hosted branch exactly (owner vs. participant resolution, denied/unauthorized redirect to `/projects`). States: not-yet-eligible (no pilot, or pilot present but short of `ready_for_review`/`done`), the optional offer with build and decline actions, declined (UI-only, see below), and the built plan grouped into new files / left-alone (omitted, protected) / needs-manual-resolution (ordinary conflict) / blocked-cannot-override (safety conflict), each showing decoded proposed content bounded to 20,000 bytes with UTF-8-safe truncation. No "Apply" control anywhere — that stays Task 4's job.
- Decline is UI-only by design: `RepositoryKitChangePlan` has no "declined" field (Task 2 built none), and the business rule only requires that declining leave managed runtime SDD available, not that a decline be recorded. Clicking it flips a local, non-persisted assign back to a calm state; no `RepositoryKits` function is called, verified by asserting zero rows exist afterward.
- Real, honestly-surfaced gap found during implementation, not worked around: no code path in this codebase resolves a worker-local repository checkout path synchronously inside a hosted LiveView process. Every worker-facing precedent (`RepositoryAssessmentLive`'s disclosure items, design.md's own boundary that repository/worker credentials "never enter package content or project-visible diffs") keeps raw repository content and filesystem paths strictly worker-local, reached only through an asynchronous paired-worker protocol that does not exist yet for this flow. Rather than fabricate a path, `build_plan/1` calls `RepositoryKits.plan_change/4` for real with no `:repository_path` option and surfaces its own already-defined `{:error, :repository_path_required}` refusal as an honest message ("not available from this screen yet — nothing was changed"). Confirmed this is not new scope creep: it is exactly the release gate specs/15's own tasks.md already declared ("Live authorized worker and repository-host smoke proof"), and it mirrors the identical characteristic already accepted in the already-`Verified` specs/14/specs/30 (`WorkerRepositoryAssessment.scan/3` has the same "someone else supplies a real path" shape, tested the same way — directly, in-process, never through a real dispatch). Tests prove the diff-rendering surface for real by building a `RepositoryKitChangePlan` directly via `plan_change/4` with an explicit `repository_path` (test-only), then loading the page and asserting it renders that real, persisted plan correctly.
- Playwright/e2e scenario: skipped, not attempted. Explained reasoning: a real slice-gate scenario needs assessment completion, profile approval, pilot selection, feature creation/linking/lifecycle transitions, and kit publishing chained correctly, and this task's focused scope explicitly excludes running `npm run test:e2e` to verify it — landing unverifiable fixture code was judged worse than skipping. Left open for slice verification.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/repository_kit_offer_live_test.exs test/sdd_orchestrator/repository_kit_change_plan_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly, 25 passed with a real exit 0, plus `mix compile --warnings-as-errors` and `mix format --check-formatted` on every touched file — both clean. Read the full LiveView and test file directly rather than trusting the sub-agent's self-report; confirmed the hosted-only context-loading logic, the honest repository_path refusal, the decline no-persistence guarantee, and the operation-group rendering are all correct as designed.
- Capability graph: unaffected — Task 3 owns no published capability of its own.
- Failed checks: None.
- Remaining: Task 4 (apply one confirmed plan on an isolated branch), `Depends on: Task 3`, now satisfied. Stopping here — Task 4 is not started; the Playwright scenario for this screen and the live worker-dispatch path are both still open, tracked as release-gate and Task 6 concerns respectively, not as defects in Tasks 1–3.

### 2026-08-11 - Task 2 implemented and verified: worker-local change-plan engine

- Completed: AC-04, AC-05. Added `RepositoryKitChangePlan` (project-scoped, append-only, hosted-only for now — see below), `RepositoryKits.WorkerKitComparison` (the read-only git comparison/classification engine, deliberately duplicating a handful of `WorkerRepositoryAssessment` primitives — exact-commit staleness, selected-root containment, path-safety and symlink/submodule exclusion — rather than reaching into that module's private functions), and three new `RepositoryKits` functions: `eligible_for_kit_offer?/2`, `plan_change/4`, `current_plan/3`.
- Design confirmed during implementation (engineering mechanism, not a business-rule change): `RepositoryExecutionProfile.instruction_precedence` (already computed at profile-approval time from assessment findings, sorted deepest-path-first) is reused directly as the protected-path set for AC-05's "existing rules win" omission — every kit file whose target path is in that set is always `kind: "omit"`, regardless of content, never a conflict. `ManagedRuntimeProfile.build/3` (the `capability:repository-execution-profile` read) supplies the validated base commit, root, repository identity, and pilot specification_id in one call, already handling profile/pilot staleness; the matching raw `RepositoryExecutionProfile` (for `instruction_precedence`) is fetched separately and matched by `.version` against `ManagedRuntimeProfile`'s result, not just taken as the latest, so the two reads can never disagree.
- Classification: create (absent, or present with identical content), omit (protected path), conflict/ordinary (present with different content, non-protected, non-safety-shaped path), conflict/safety (present at a path matching the same secret/credential/generated-directory shape `WorkerRepositoryAssessment` already excludes — existing content is never read for these, even transiently, so a secret's bytes never enter worker memory).
- Acknowledged, deliberate gap carried over from the same tradeoff `WorkerRepositoryAssessment` already makes: a kit path that lands on an existing symlink or submodule compares as "does not exist" (both are excluded from `git ls-tree` results the same way). `RepositoryKitInstallation`'s apply-time root/symlink containment check (Task 4) is the actual safety backstop for that case, not this task.
- Persistence is hosted-only, not the full `Device`/`Hosted` dual-authority split `RepositoryAssessments.ProfileStore` uses for other project-scoped append-only records. Reasoning: Task 6 explicitly owns "Hosted and device storage parity" as its own surface, and building the split for real means extending `Devices.DeviceStore`'s behaviour and its `Local` adapter — cross-cutting infrastructure beyond "produce a plan." `plan_change/4` runs the full read-only comparison for any authority, including `{:device, _}`, and refuses only at the final persistence step with `{:error, :unsupported_authority}`, so no device-authoritative content is silently dropped into hosted PostgreSQL. Recorded as a deferred, non-blocking item under `## Blocked Decisions` in `tasks.md`, owned by Task 6.
- `expires_at` is 15 minutes after creation, a documented engineering parameter (not a product decision): short enough that a repository or profile that changed underneath the plan is unlikely to silently outlive it, long enough to read one rendered diff without expiring mid-review.
- `target_branch` format: `sdd-kit/` followed by a slugified publisher, the version, and an 8-character digest prefix, dash-separated — publisher (not the full `source` URL) for a legible branch name, plus a digest prefix for uniqueness against the catalog's actual `{source, publisher, version}` uniqueness triple.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_kit_change_plan_test.exs test/sdd_orchestrator/repository_kits_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly, 44 passed with a real exit 0, plus `mix compile --warnings-as-errors` and `mix format --check-formatted` on every touched file — both clean. Read every delivered file directly (schema, comparison engine, public API, migration, and the full test suite) rather than trusting the sub-agent's self-report; confirmed the classification algorithm, staleness handling, and no-repository-write guarantee (asserted directly in the test suite against `git status`/`HEAD`/branch list before and after planning) are all correct as designed.
- Capability graph: unaffected — Task 2 owns no published capability of its own; `capability:repository-sdd-kit` remains `ready after Task 6`.
- Failed checks: None.
- Remaining: Task 3 (eligibility/decline/diff-review LiveView), `Depends on: Task 2`, now satisfied. Stopping here at the user's explicit request; Task 3 is not started.

### 2026-08-11 - Task 2 split via update-spec: domain (Task 2) and UI (new Task 3) separated

- Completed: Task 2 preflight (before broad code exploration, per implement-spec's own stop condition) found it combined a substantial worker-local git-diff and conflict-classification domain engine — reusing `WorkerRepositoryAssessment`'s exact-commit staleness check, selected-root containment, tree-entry listing, and blob-content-read primitives — with a full eligibility, decline, and diff-review LiveView. That is "domain foundation plus UI" and "work expected to need more than one meaningful implementation commit," an explicit Task Size Gate split trigger, not just subjective complexity. User confirmed splitting via `update-spec` over implementing it as one oversized task.
- Split: Task 2 keeps its label and stays domain-only (`RepositoryKitChangePlan`, worker-local comparison, protected-file precedence, conflict classification, expiry, no-mutation planning; owns AC-04, AC-05, entity:RepositoryKitChangePlan). New Task 3 owns the eligibility/decline/diff-review LiveView and AC-01 alone (AC-01 is fundamentally about presentation and decline behavior, not the plan computation). Tasks 3, 4, 5 (apply, update/removal, governance) renumbered to 4, 5, 6 with no other change to their scope, owned surfaces, or acceptance criteria.
- Updated `## Cross-Specification Dependencies`: the two capabilities required before old Task 5 (`project-storage-governance`, `project-specification-store`, `project-specification-governance`) now read `required before Task 6`; `capability:repository-sdd-kit` now reads `ready after Task 6`. Updated the one external consumer of that edge, `specs/13-sdd-adoption/tasks.md`'s `capability:repository-sdd-kit` requirement, from `#Task 5` to `#Task 6` in the same change. Confirmed `specs/16-empty-repository-initialization`'s reference to `specs/15#Task 1` needs no change (Task 1 was not renumbered).
- Extended `## Proof Scope Gate`'s `Applies to:` from `Task 1.` to `Task 1, Task 2, Task 3.`, since both are being refined right now and the proof runner is available — same incremental-adoption pattern already used for Task 1.
- Remaining: Implement Task 2 (domain), then Task 3 (UI), then Tasks 4–6.
- Failed checks: None.
- Spec updates: `tasks.md` restructured as above; `specs/13-sdd-adoption/tasks.md`'s capability reference corrected in the same change.

### 2026-08-11 - Task 2 fully unblocked: specs/35 Verified and merged, slice rebased onto main

- Completed: `specs/35-guided-delivery-feature-specification-link` reached `Verified` (both tasks complete, Verification Gate passed with one documented pre-existing-test exception unrelated to that slice) and was merged into `main` (`3a7fdb4`). Separately, this slice's own Task 1 was cherry-picked directly onto `main` ahead of the rest of the slice (`0cc5d04`), a deliberate early merge since Task 1 was independently complete and proven — this repository's capability graph is meant to let a consumer depend on the smallest stable capability rather than wait on a whole slice.
- Rebased `slice/15-repository-sdd-kit-integration` onto the updated `main`. This produced one conflict: `lib/sdd_orchestrator/repository_kits.ex`, an add/add conflict between this slice's own Task 1 commit (`231778b`) and the cherry-picked copy already on `main`. Confirmed by diffing both sides that the only difference was the exact credo `--strict` nesting-depth fix the cherry-pick's own commit message described making during reconciliation — main's copy is a strict improvement, not a divergent change. Resolved with `git rebase --skip`, dropping the now-fully-redundant commit; the slice's remaining three commits (all `specs/15` doc-only) don't touch that file. `mix compile --warnings-as-errors` and both spec validators pass clean post-rebase.
- All four of Task 2's required capabilities are now `ready`: `repository-execution-profile`, `guided-delivery-data-surfaces`, `guided-delivery-feature-specification-link` (specs/35, new), and `project-storage-authority`.
- `## Status` set to `In Progress`.
- Remaining: Implement Task 2 through Task 5, then the verification gate.
- Failed checks: None.
- Spec updates: `## Status` and `## Blocked Decisions` in `tasks.md`.

### 2026-08-10 - Task 2 AC-01 correlation gap resolved via update-spec plus new specs/35

- Completed: Task 2 preflight (delivery-coverage and execution-order simulation) found AC-01's eligibility gate — "the selected pilot reaches `Ready for review` or `Done`" — had no implementable read path. `RepositoryPilotSelection` (`specs/30`, `Verified`) stores only `specification_id`; `Delivery.Feature` (`specs/07`, `Verified`) owns `lifecycle_column` but has no `specification_id`; `Delivery.Readiness.current_revision/2` just reads a project's first current specification regardless of feature, confirmed by direct code reading, not a real correlation; no test or fixture anywhere ties the two together.
- User was asked how to resolve it and chose to stop and route it through `update-spec` rather than accept a guessed mechanism (title-matching and project-wide board state were offered and declined). Ran `update-spec` on this slice, which determined the correlation is new, independently valuable, and separately verifiable scope, so — since both `specs/07` and `specs/30` are fully `Verified`/closed — it belongs in a new child specification rather than reopening either (same pattern `specs/30` used to continue `specs/14`).
- Created `specs/35-guided-delivery-feature-specification-link` via `add-spec` (`requirements.md`, `design.md`, `tasks.md`, `progress.md`, `Approved`, `Not Started`, 2 tasks, both required capabilities already ready so it is immediately executable): `Delivery.Feature` gains an optional, owner-only `specification_id` link, unique per `(project_id, specification_id)`, and publishes `capability:guided-delivery-feature-specification-link` (a `project_id` + `specification_id` → linked-feature read, `{:ok, feature} | {:error, :not_linked}`) after its Task 1. Linking is owner-only because the specification picker depends on `capability:project-specification-store`, which remains owner-only readable per `specs/09`'s current scope — this does not change any existing participant-level `Feature` mutation.
- Updated this slice's `design.md` "Post-pilot eligibility interface" to name the concrete read path (resolve the linked feature via the new capability by the pilot's `specification_id`, then read `lifecycle_column` through the existing board read; no link means not-yet-eligible, not an error) and added `capability:guided-delivery-feature-specification-link` — provider `specs/35#Task 1` — required before `Task 2` to `tasks.md`.
- Remaining: Task 2 stays capability-blocked until `specs/35#Task 1` completes; re-check with `capability_index.py --capability guided-delivery-feature-specification-link` before starting Task 2.
- Failed checks: None.
- Spec updates: `specs/15-repository-sdd-kit-integration/design.md` and `tasks.md` (this entry's summary above); new `specs/35-guided-delivery-feature-specification-link/{requirements,design,tasks,progress}.md`.

### 2026-08-09 - Rebased onto main after identity-lifecycle work merged; Task 2 preflight confirmed

- Completed: `slice/15-repository-sdd-kit-integration` had fallen 5 commits behind `main` (unrelated `specs/25-participation-identity-lifecycle` work: `d7eeb97`, `3e30880`, `3d8492e`, `2d7eb68`, `ded49ba`). Confirmed no file overlap between those commits and this slice's own two commits, then ran `git rebase main` in the `sdd-orchestrator-s15` worktree — rebased cleanly, no conflicts. `mix compile --warnings-as-errors` clean afterward. Re-ran Task 1's own focused proof as a post-rebase sanity check (not a new task): `mix test test/sdd_orchestrator/repository_kits_test.exs test/sdd_orchestrator_web/live/repository_kits_live_test.exs` — 32 passed, real exit 0. `python3 .agents/scripts/validate_spec.py specs/15-repository-sdd-kit-integration` and `--all specs` both pass post-rebase.
- Re-confirmed via `capability_index.py --capability` that all three of Task 2's required capabilities remain `ready`: `repository-execution-profile` (specs/30#Task 2), `guided-delivery-data-surfaces` (specs/07#Task 54), `project-storage-authority` (specs/05#Task 4).
- Remaining: Implement Task 2 (architecture research dispatched before implementation, mirroring Task 1's closed-brief sub-agent approach) through Task 5, then the verification gate.
- Failed checks: None.
- Spec updates: None.

### 2026-08-09 - Corrected Task 2's mis-wired capability dependency

- Completed: Corrected Task 2's Cross-Specification Dependencies. It required `capability:guided-specification-delivery` (provider `specs/24-guided-delivery-completion#Task 1`, the terminal task of the entire 17→24 guided-delivery GDPR governance chain), but neither `requirements.md` nor `design.md` ever referenced that capability, and Task 2's actual purpose — comparing the repository against the approved profile to produce a change plan — only needs to read the selected pilot's delivery-state data. That edge wrongly serialized this slice behind eight unrelated slices. Replaced it with `capability:guided-delivery-data-surfaces` (provider `specs/07-guided-specification-delivery#Task 54`), the same capability specs/18, specs/19, specs/20, and specs/24 already consume to read guided-delivery's field and status data for their own downstream purposes. That capability, `capability:repository-execution-profile`, and `capability:project-storage-authority` are all already ready, so Task 2 is now blocked only on this slice's own Task 1 — which is already complete (see below), so Task 2 is unblocked.
- This supersedes the "still capability-blocked on `guided-specification-delivery`" note in the entry directly below.
- Remaining: Implement Tasks 2–5 and complete the verification gate.
- Failed checks: None.
- Spec updates: Corrected the Task 2 capability dependency in `tasks.md`.

### 2026-08-09 - Rebased onto main after specs/30 merged; still capability-blocked

- Completed: `specs/30-repository-execution-profile-completion` merged to `main` (`d8f0ef1`). Rebased `slice/15-repository-sdd-kit-integration` onto it; the only conflict was the recurring `e2e_bootstrap_controller.ex` surface, resolved as a union of both sides' additions (kept both the `RepositoryKits`/`SpecificationStore` aliases and both new `run/3` scenario clauses — `repository_pilot` from specs/30 and `repository_kits` from this slice's Task 1 — as separate, complete functions; the diff had interleaved them because they were adjacent, not because they overlapped). `mix compile --warnings-as-errors` clean after rebase; re-ran Task 1's own focused proof directly (not a new task, just a post-rebase sanity check) — 32 passed, real exit 0.
- `capability:repository-execution-profile` and `capability:project-storage-authority` are now both `ready`. `capability:guided-specification-delivery` (provider `specs/24-guided-delivery-completion#Task 1`) is still `pending`, so Task 2 remains blocked on that one capability alone.
- Superseded: the dependency on `guided-specification-delivery` was itself a mis-wiring, corrected in the entry above — Task 2 no longer depends on that capability at all.
- Remaining: watch `specs/24-guided-delivery-completion#Task 1`; once it's ready, re-run the full Task 2 preflight (capability confirmation, delivery-coverage and execution-order checks) before starting Task 2 — do not assume the two-thirds-ready state carries over without re-checking.

### 2026-08-09 - Task 1 implemented and verified: immutable kit package catalog

- Completed: AC-02, AC-03. Adopted the Task Proof Gate for this spec (`## Proof Scope Gate` — `Applies to: Task 1.`, `Proof scope: Focused` on Task 1) since Task 1 was the next unfinished task being refined and the proof runner is available; `## Status` set to `In Progress`. Started the shared `sdd_orchestrator_postgres` container (it was stopped) so the worktree's proof commands could run.
- Implementation design decided for Task 1 (engineering mechanism, not a business-rule change, so not written to design.md): `repository_kit_packages` is a global, non-project-scoped table (no `project_id`) with a Postgres reject-update trigger for true immutability, mirroring `repository_execution_profiles`'s pattern. The digest is a server-computed sha256 over the sorted per-file `{path, sha256, size, executable}` manifest — never accepted as caller input, so tamper is always detectable by recomputation. Provenance requires `ref_type: "commit"` and a 40/64-hex `ref`; a branch- or tag-shaped ref (`"main"`, `"latest"`, `"HEAD"`) is rejected as a mutable reference (AC-03). `supported_adapters` validates against the known `~w(claude_code codex)` set. Supersession is derived at read time via `Version.compare/2` over `{source, publisher}` families, never stored, so no immutable row is ever mutated to record it. Ingestion is split into a pure `RepositoryKits.publish_package/2` (no disk/network I/O, package content is only measured/hashed/base64-encoded, never executed) plus a thin `mix repository_kits.publish` CLI wrapper that walks a directory and refuses (never dereferences) a symlink. Inspection UI is a new global `/repository-kits` LiveView under the existing `:authenticated` live_session (same boundary as `/ai-connections`), read-only by construction — no form, edit, or delete control anywhere.
- Delivered: `priv/repo/migrations/20260809021721_create_repository_kit_packages.exs`, `lib/sdd_orchestrator/repository_kits/repository_kit_package.ex`, `lib/sdd_orchestrator/repository_kits.ex`, `lib/mix/tasks/repository_kits.publish.ex`, `lib/sdd_orchestrator_web/live/repository_kits_live.ex` (+ router entry), `test/support/repository_kit_fixtures.ex`, `test/sdd_orchestrator/repository_kits_test.exs` (20 cases covering identity/tamper, path, size, license, provenance/mutable-reference, adapter/permission, scripts-vs-manifest, no-network/no-execution, semver ordering, and supersession), `test/sdd_orchestrator_web/live/repository_kits_live_test.exs` (5 cases covering the auth boundary, empty state, list, every AC-02 detail field, and the supersession badge), and `assets/e2e/repository-kits.spec.js` plus a new `"repository_kits"` scenario in `e2e_bootstrap_controller.ex` (written for slice-gate browser verification, not run at this task's focused scope).
- Task development was dispatched to a sub-agent with a closed, fully-decided brief (architecture above was fixed in advance, not left to the sub-agent to invent); this thread independently re-read every delivered file and re-ran the proof rather than trusting the sub-agent's self-report. One real defect found by that independent review, not caught by the sub-agent's own test run because it lives in a code path no Elixir test exercises: the new `"repository_kits"` e2e-bootstrap scenario's `seed_kit_package/1` published two versions ("1.0.0", "1.1.0") from byte-identical file content. Since the digest is content-addressed over the file manifest alone (not the version string), the second publish would have collided on the unique digest index and returned `{:error, :already_exists}` against a `{:ok, package} = ...` match, crashing the scenario the moment the browser spec runs it. Reproduced directly with a throwaway test before fixing, confirmed the fix resolves it the same way, then deleted the throwaway test. Fixed by giving each seeded version's `SKILL.md` distinct content (`"# skill \#{version}\n"`), with a comment at the call site explaining why distinct content is required.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/repository_kits_test.exs test/sdd_orchestrator_web/live/repository_kits_live_test.exs` — exit `0`.
- Verified independently by this thread: re-ran the same command directly after the e2e-bootstrap-controller fix, 32 passed with a real exit 0, plus `mix format --check-formatted` and `mix compile --warnings-as-errors` — both clean. Confirmed via `git status`/`git diff` that only the expected files changed and no other in-flight work was touched.
- Capability graph: `capability:sdd-kit-package` is ready — its named provider task (Task 1), its complete focused proof, and this readiness write-back are all satisfied. No current consumer; Task 2 remains capability-blocked independent of this (see the entries above for its current, corrected capability set).
- Failed checks: None outstanding — the one defect found above was fixed before this write-back, not left disclosed-but-unfixed.
- Remaining: Task 2 (`Depends on: Task 1`, now satisfied) stays blocked until its required capabilities are ready; re-check with `capability_index.py --capability` before starting it.

### 2026-07-31

- Completed: Approved the optional post-pilot package, exact diff, precedence, conflict, branch, update, removal, source-of-truth, governance, and capability contracts.
- Remaining: Implement Tasks 1–5 and complete the verification gate.
- Failed checks: None.
- Spec updates: Created the initial approved specification and first executable slice.
