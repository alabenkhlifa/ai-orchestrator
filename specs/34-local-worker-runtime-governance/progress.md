# Local Worker Runtime Governance Progress Log

### 2026-08-09 - AC-06 corrected before Task 4: no project-owner exception to owner-exact access

- Completed: Task 4 preflight found requirements.md and design.md had the run initiator *and* the project owner both seeing the owner-exact projection (AC-06, two Business Rules bullets, Users, a Primary Workflow step, one In Scope bullet, one design.md sentence and one Data-and-Access-Boundaries bullet). `RuntimeProjections.owner_projection/3` (specs/11, unmodifiable) authorizes strictly by account ownership of the session's connection, with no project-owner exception, and specs/11's own business rule is explicit: "Exact account-wide quotas, credits, and spend are visible only to the connection owner." Since only the run initiator's own connections are ever eligible (Task 1), initiator and connection owner are the same account by construction — but the project owner is a different account whenever they didn't start the run, and extending them the exact view would mean either contradicting specs/11's already-Verified access rule or silently exposing another user's account-wide quota/spend to a third party. Neither is acceptable, so there was exactly one available correction, not a product choice to ask about: the project owner sees the owner-exact view only when they are also the initiator, and otherwise gets the same safe project-run view as any other current authorized participant.
- Spec updates: requirements.md (Users, In Scope, Primary Workflow step 7, two Business Rules bullets, AC-06) and design.md (Proposed Approach, one Data-and-Access-Boundaries bullet) reworded to state the initiator-only owner-exact boundary explicitly. No task count, AC count, or `Owns:` mapping changed. `validate_spec.py` passed after the edit.

### 2026-08-08 - Task 2 implemented and verified: session pinning

- Completed: AC-02, AC-03, AC-04, entity:LocalWorkerRunGovernance. New migration creating `local_worker_run_governance` (run_id unique FK → agent_runs, session_id FK → ai_runtime_sessions, both `on_delete: :delete_all`) and its schema/context module `SddOrchestrator.Delivery.LocalWorkerRunGovernance` (`for_run/1`, idempotent `record/2`). `Start.start/4` now resolves the current-or-default model/effort via `ModelCatalogs.current_catalog/3`, pins a session via `RuntimeSessions.pin_session/3` (`consumer: :working_agent`, `consumer_ref: "local_worker_run:" <> manifest.run_id`), and refuses the start on any pin failure (unavailable catalog, missing required spending ceiling, etc.) with no `AgentRun`, `RunCommand`, or governance row left behind.
- Implementation deviation from the dispatched brief, resolved rather than guessed past: the brief asked for the governance row to be written before `commit/6`, but `run_id`'s FK requires the `AgentRun` row `commit/6` itself creates — reproduced the resulting FK violation directly rather than assuming it. Resolved by pinning the session (satisfying "before the worker is commanded to start," since pinning still precedes the `RunCommand` `commit/6` enqueues) before `commit`, and writing the governance row as a plain insert immediately after `commit` succeeds, never inside `steps/6`'s own transaction. A commit failure after a successful pin leaves the session unlinked; `pin_session/3`'s own `consumer_ref` idempotency reconciles that on the run's next attempt, exactly as the brief's own accepted limitation anticipated. No AC depends on the row's write order relative to `commit`, only on pin-before-worker-command and refuse-on-failure — both hold.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/start_test.exs` — exit `0`.
- Verified independently by this thread (not only the sub-agent's self-report): re-ran the same command directly (30 passed, real exit 0), plus `mix format --check-formatted` and `mix compile --warnings-as-errors` on every changed file — both clean.
- Remaining: Task 4 (depends on Task 2 + Task 3, both now done) is next, followed by the Task 5/Task 6 parallel pair.

### 2026-08-08 - Task 1 and Task 3 implemented and verified in parallel

- Completed: Task 1 (AC-01) — a private `resolve_ai_connection/3` step in `lib/sdd_orchestrator/delivery/start.ex`, wired into `Start.start/4`'s `with` chain between `not_already_started` and `manifest_for`. It resolves the project's bound local worker via `HostedLocalRepositoryBinding`/`LocalWorker` (the same lookup `worker_gateway_credential_controller.ex` already uses), filters the initiator's active `PersonalConnections` to that worker, and returns `{:ok, nil}` on zero eligible, `{:ok, connection_id}` on exactly-one or an explicit `opts[:ai_runtime_connection_id]` match, or `{:error, {:ai_connection_selection_required, ids}}` otherwise — refusing the start in that last case. Task 3 (owns no AC directly; supports AC-05) — new file `lib/sdd_orchestrator/delivery/local_worker_runtime_snapshot.ex`, a pure, DB-free `snapshot/3` returning `elapsed_seconds` (from `attempt.inserted_at`, floored at 0), `status` (`attempt.state` passed through unchanged), and `tokens`/`cost` always `:unknown`. Both were dispatched as parallel sub-agents in the same working tree, each scoped to disjoint files, per the verified independence from the earlier assessment.
- Proof receipts, each re-run and confirmed by this thread with a real, unmasked exit code, not the sub-agent's self-report (26 passed for Task 1: 17 pre-existing cases unmodified plus 9 new; 11 passed for Task 3):
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/start_test.exs` — exit `0`.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator/delivery/local_worker_runtime_snapshot_test.exs` — exit `0`.
- Discovery: one transient whole-project compile error surfaced mid-flight in Task 3's sub-agent, caused by Task 1's sub-agent editing `start.ex` concurrently in the same working tree (an in-progress, momentarily-invalid `in` guard). It resolved on its own before either final proof ran, and the final state has only each task's own intended files changed — recorded here as evidence the shared-working-tree parallel-dispatch approach works but is not glitch-free under true concurrent compilation; no code or test was affected.
- Remaining: Task 2 (depends on Task 1, now unblocked) is next.

### 2026-08-08 - Design corrected before implementation: no `ObservationAdapter.LocalWorker`, no ingestion

- Completed: `implement-spec` preflight for Task 3 read `lib/sdd_orchestrator/ai_runtime/observation_adapter.ex` in full before writing any code and found `ObservationAdapter.validate_provenance/3` — the gate `RuntimeObservations.ingest/3` runs unconditionally on every call — has exactly one accepting clause: `provider: "openai_codex"`, `source: "official_client"`, with a Codex-CLI-specific `source_version` pattern. Everything else is refused. A local-worker-sourced result, for either agent, can never honestly satisfy that provenance, so the originally planned `ObservationAdapter.LocalWorker` (this entry's predecessor below still describes it) was a dead end confirmed before any code existed for it, not a runtime failure discovered later. Zero implementation commits existed on `slice/34-local-worker-runtime-governance` at the time of this finding.
- Fix verified against the actual code, not assumed: `RuntimeProjections.owner_projection/3` and `participant_projection/4` (`lib/sdd_orchestrator/ai_runtime/runtime_projections.ex`) already handle an empty observation list as an ordinary case — `list_observations/3` returns `{:ok, []}`, `latest_observation/2` returns nothing, availability degrades to `:unknown` — so the pinned connection's model, effort, and quota still project correctly with zero ingested observations. The mechanism this slice now uses: a live, read-only elapsed-time-and-status snapshot computed directly from the referenced `AgentRun`/`RunAttempt`'s own already-existing state (no new module under `ai_runtime/`, no schema, no persisted observation), combined with the unchanged reused projection for connection/model/effort/quota. `capability:ai-runtime-observation` is still required and still consumed — through its projection functions only, never through `ingest/3`.
- Spec updates: `requirements.md` (Outcome, In Scope, Primary Workflow steps 6–7, two Business Rules, AC-05/AC-06/AC-07/AC-08 reworded from "ingested observation" to "computed"/"live runtime snapshot" — same substance, corrected mechanism), `design.md` (Context, Proposed Approach, Components Affected, Data and Access Boundaries, Interfaces, the observation-adapter Decision entry replaced with "A live computed snapshot instead of a persisted, ingested observation", one Risk bullet), `tasks.md` (Task 3 redefined from "Implement the lifecycle-derived local-worker observation adapter" to "Compute a governed run's live runtime snapshot"; Task 4 redefined from "Ingest one ordered observation at each governed run's lifecycle transition" to "Assemble the combined projection for a governed run"; Task Size Gate and Slice Size Gate prose updated to match). Task count (seven), critical path (five), and every `Owns:`/AC/entity mapping are unchanged — this was a mechanism correction, not a scope or task-count change. `validate_spec.py` passed after the edit.
- Business rules, acceptance substance, and both accepted product forks from the entry below (observation-only enforcement, optional per-run connection) are unaffected: elapsed time and status still come only from the worker's own lifecycle, tokens and cost are still always unknown, and quota is still informational-only.

### 2026-08-08 - Specification created to bridge specs/33 and specs/11

- Completed: Created the child specification that closes the gap `specs/33-local-worker-run-execution` named in its own Out of Scope section and its tasks.md deferred list ("a run funded by a personal AI connection, a pinned model and effort, an enforced spending ceiling, and ingested runtime observations"), and that `specs/11-ai-runtime-governance`'s design document flagged as needing "an approved `update-spec` [that] defines manifest references, pause, resume, stop, cancellation, and continuation mapping" before a Slice 07-family consumer could use `capability:ai-runtime-session` and `capability:ai-runtime-observation`. Eight acceptance criteria are owned by seven tasks with a five-task critical path. One new owned entity, `LocalWorkerRunGovernance`, references both providers' identities without redefining either.
- Decisions resolved by the accountable user on 2026-08-08, both accepted as recommended: (1) enforcement depth stays observation-only — the worker's existing agent launch and credential resolution are unchanged, and spending-ceiling or quota-exhaustion pausing is not enforced against a local-worker run in this slice; (2) linking a personal AI connection stays optional per run rather than mandatory, so a run with no eligible connection keeps working exactly as the already-verified `specs/33-local-worker-run-execution` baseline, and Claude Code local-worker runs are not blocked by the absence of a Claude Code usage or quota adapter in `specs/11-ai-runtime-governance`.
- Scope classification: focused specification. One primary outcome (attribution, session pinning, lifecycle-derived observation, and presentation for a local-worker run), one coherent workflow from start-development through terminal state, and one verification gate proving both a governed and an ungoverned run end to end.
- Capability graph: consumer only. `capability:local-worker-run-execution` (provider `specs/33-local-worker-run-execution#Task 12`), `capability:ai-runtime-session` (provider `specs/11-ai-runtime-governance#Task 11`), and `capability:ai-runtime-observation` (provider `specs/11-ai-runtime-governance#Task 5`) are all `ready`; no provider file was edited. This specification provides `capability:local-worker-runtime-governance`, ready after `Task 7`, with no current consumer.
- Architecture finding that shaped the design: `RuntimeObservations.ingest/3`'s default `RPC` adapter queries the live Codex App Server process, which has no relationship to the CLI subprocess a local worker actually launches. Reusing it here would report facts about the wrong process, so this slice adds `ObservationAdapter.LocalWorker`, a second implementation of the same behaviour, sourced from the worker's own attempt lifecycle instead, with tokens and cost always reported unknown rather than guessed.
- Slice Size Gate: Standard — seven tasks, five-task longest `Depends on:` path, well under the twelve-task and eight-task limits. Task Size Gate: all seven tasks `Size: Standard`, no exception used. Proof Scope Gate: `Focused` on all seven tasks, including the end-to-end task, matching the precedent `specs/33-local-worker-run-execution#Task 12` set for a real-repository integration proof.
- Status: requirements and design are `Approved`, no open product or technical question. The task plan is `Not Started`; Task 1 and Task 3 are immediately executable.
