# Hosted Projects From A Local Repository Progress Log

### 2026-09-02 - AC-05 narrowed to the repository identity after the proof exposed it

- Found while reviewing Task 4's coexistence proof. AC-05 as written ("a hosted project for that repository") can never refuse anything in the real click path. `PortableRepositoryIdentity.generate/1` draws a fresh salt on every selection, deliberately, so two selections of one repository produce two different identity strings and the per-account uniqueness index never sees a conflict.
- The gap is not a coding mistake. The selection step's candidate list is `Devices.list_projects()`, which is the device store, and `/onboarding/local` runs in `live_session :public` with no hosted identity at all. Selection happens before sign-in, so at the moment the worker compares folders there is no account whose hosted projects it could be given as candidates.
- Decision taken with the user: narrow AC-05 to the identity the confirmed attempt names. Two separate selections of one repository legitimately produce two hosted projects, which is what the fresh-salt design intends and what the product's own first-connection disclosure already tells the person ("An independent connection gets a different identifier").
- The alternative was weighed and rejected for this slice: asking the worker to compare the chosen folder against the account's hosted identities after sign-in. It needs the worker to hold the proven folder past the picker it already closed, or to open a second picker, which the design rejects for making the person prove the same repository twice. Recorded as deferred rather than dropped.
- Requirements, design, and tasks now agree on the identity-level rule. `design.md` gains the decision and its consequence; `tasks.md` narrows the implementation boundary, `Task 3`'s owned surface and proof, and adds the deferred detection.
- `design.md`'s `Proposed Approach` also corrected where it read as if a local project writes a `RepositoryConnection` row. Its `Data and Access Boundaries` already defined the entity correctly; the approach paragraph now says the same thing.
- No code changed in this update, and no task was reopened. `Task 3`'s delivered behaviour already matches the narrowed criterion and its proof already asserts it.

### 2026-09-02 - Task 4 complete: the click path, coexistence, and the record and log review

- The flow suite now drives the hosted path as a person does: pair the worker, continue to selection, select the real Git folder, hand off to the storage step, sign in so hosted becomes available, choose hosted, resume at review, and confirm. It asserts the redirect to the project's landing address and a project that is hosted, `repository_provider: "local"`, carrying the portable identity, and bound to the worker that proved the repository.
- AC-04 proven in both orders. Hosted first, then device, is entirely clicks: the hosted project lives in PostgreSQL, so the device store is still empty and the second selection is not a duplicate. Device first, then hosted, cannot be all clicks: the second selection is refused by the device store's own one-project-per-repository rule before it can reach the storage step. That test drives the refusal through the UI so the reason is visible, then registers the hosted half through `Projects.register_project/3`, and says so in its own comment.
- A correction to an assumption in the task brief, found by the proof. Two projects for one repository do not share an identity string. `PortableRepositoryIdentity.generate/1` draws a fresh salt every call, on purpose, so independent onboarding creates no global repository-equality signal. The coexistence test proves both records name the same repository with `PortableRepositoryIdentity.match/2` and asserts the two strings differ.
- AC-06's record review is data-driven, not a hand-listed set of columns. It reads `to_jsonb(t)::text` for every row of every base table in `public` after the creation and asserts no forbidden value appears, so a column or table added later is covered without being listed. Verified non-vacuous two ways: it asserts the portable identity is present in the same dump, and storing the repository path as the display name was temporarily introduced and made the scan fail with the leaked path before the file was restored.
- The repository fixture is a real Git repository with a real remote, a real commit, and real file content, built under a uniquely named parent directory. The folder's display name is the one repository value the approved design lets cross (`specs/05` AC-17), so the parent segment, the absolute path, and the relative path are forbidden while the display name is not.
- The creation is silent. The captured log at `:info` is the empty string, so an absence scan would pass for free. Two tests guard that: one asserts the log is empty, so a future log line on this path fails and gets reviewed, and one proves the capture is live at `:info`. Test logging is configured at `:warning`, so the case raises the level in setup and restores it, which forces `async: false`.
- Also confirmed for AC-06: no `RepositoryConnection` row exists, so there is nothing GitHub-shaped to hold a URL, and the consumed attempt's `selected_repository` keys are exactly `fingerprint`, `name`, `provider`, and `worker_id`.
- `capability:hosted-local-repository-projects` is ready. `Task 4` is complete, and the click path from folder selection through the hosted choice to a bound hosted project is proved end to end.
- Proof receipt: `Task 4` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/local_onboarding_flow_test.exs test/sdd_orchestrator/privacy/hosted_local_repository_project_boundary_test.exs` — exit `0`.
- 20 tests passed across those two files. Regression run outside the focused proof: `local_onboarding_live_test.exs` and `project_registration_test.exs` (67 passed).

### 2026-09-02 - Task 3 complete: the hosted choice creates a project

- The `%{storage_mode: "hosted"}` clause no longer answers `Saving local projects to a hosted account is coming soon.`. Both modes now resume at `:review` through one guarded clause, so the person names the project and reads the disclosure before anything is created. Arriving from the storage step creates nothing, which is what the approved workflow asks for.
- `create_project/2` dispatches on the resumed attempt's storage mode. The device branch is unchanged. `register_hosted_project/3` resolves the workspace with `Projects.proven_hosted_workspace/1`, calls `Projects.register_project/3`, and on success leaves LiveView with `redirect/2` to the project's landing address `/projects/:id`, the same one the GitHub path uses.
- Identity is read from the attempt because it has to be. `/onboarding/local` sits in `live_session :public` and holds no hosted identity assign at all, so there is no session for this screen to read even if the design allowed it.
- One sentence per refusal, all rendered beside the create control with the choice still usable and nothing written. No proven sign-in: `The sign-in for this project wasn't recorded. Go back and sign in again to save it to your account.` No usable worker (`:proving_worker_required`, `:worker_unavailable`, `:unauthorized_worker`, `:not_found`): the one owned value `RepositoryAssessmentLive.worker_unavailable_message()`, so this refusal and the discovery state cannot drift. A folder that no longer matches: `The folder you chose couldn't be matched to this repository. Choose it again.` A lapsed storage choice: `This project has no storage choice any more. Go back and choose where to save it.` Anything else: `Nothing was saved. Try again.`
- AC-05's per-account refusal names the project the account already holds and links to it at `/projects/:id`. The review step's duplicate notice used to hardcode the device wording and the device path; it now renders one owned `duplicate_notice/2` value per branch instead of two copies that could drift. The device branch's rendered copy is unchanged.
- The two name refusals became module attributes shared by both branches, because the device store answers `:name_taken` and the hosted transaction answers a changeset for the same fact and the person should read one sentence either way.
- Copy corrected where the new branch would otherwise assert something false. The disclosure's third notice said `This project has no account` and the confirm checkbox said project history cannot be recovered without an export. Neither is true of a hosted project. The first two notices are identical for both branches because they stay true; only the third notice and the checkbox clause change, and the accountless branch renders byte-for-byte what it rendered before.
- `Back` on a hosted attempt navigates to `/onboarding/local/storage/:attempt_id` rather than the folder picker, so AC-03's still-available storage choice is one click away. The device branch keeps returning to selection.
- Proof receipt: `Task 3` — scope `Focused` — command `mix test test/sdd_orchestrator_web/live/local_onboarding_live_test.exs test/sdd_orchestrator_web/live/local_onboarding_flow_test.exs test/sdd_orchestrator_web/live/local_storage_selection_live_test.exs` — exit `0`.
- 53 tests passed across those three files. Regression run outside the focused proof: `test/sdd_orchestrator/project_registration_test.exs` and `test/sdd_orchestrator/portability/` (175 passed).

### 2026-09-02 - Task 2 complete: the proving worker binds in the creation transaction

- The attempt's selection now names the worker. `LocalOnboardingLive` keeps a `proving_worker_id` assign from the moment `available_worker/1` picks the Mac that will open the picker, and `continue_to_storage` passes it into `Projects.select_local_repository/3`. The id has to outlive `reset_selection/1`, which runs the instant the worker answers, so it is cleared with `:selected` in `clear_selection_feedback/1` instead.
- `local_metadata/1` adds `"worker_id"` only when the selection names one. Two files outside this slice assert the device-storage selection's exact key set (`test/sdd_orchestrator/privacy/storage_boundary_test.exs`, `test/sdd_orchestrator/project_storage/onboarding_origin_test.exs`), and an always-present nil key would have broken them. Conditional inclusion is also the smaller stored shape.
- `prepare_binding/4` adds a `:binding` step to the registration multi for a device-origin hosted attempt whose repository is local. It calls the existing `HostedLocalRepositoryBindings.put_validated_binding/6` with the workspace, the freshly inserted project, the attempt's device workspace, the worker the selection named, and the project's portable identity. A hosted-origin GitHub attempt gains no step.
- A selection that names no worker is refused as `{:error, :proving_worker_required}` in `register_project/3`'s guard `cond`, before any transaction opens, rather than committing a project no Mac can serve.
- Mechanism confirmed rather than assumed. `put_validated_binding/6` opens its own `Repo.transaction` and calls `Repo.rollback/1`, so the worry was that the throw would escape `Ecto.Multi` and collapse the result into a two-element tuple. It does not. `DBConnection.transaction/3` catches its own nested throw and answers `{:error, reason}`, so the refusal reaches `do_register/4` as the ordinary `{:error, :binding, :worker_unavailable, changes}` that the existing clause already handled. Observed by instrumenting `do_register/4`, then reverting the instrumentation. No dead compatibility clause was added.
- `@registration_preloads` now also loads `:hosted_local_repository_binding`, and `committed_project/2` uses the same list, so an idempotent retry answers the same shape as a fresh commit.
- Proof receipt: `Task 2` — scope `Focused` — command `mix test test/sdd_orchestrator/project_registration_test.exs test/sdd_orchestrator_web/live/local_onboarding_live_test.exs test/sdd_orchestrator_web/live/local_onboarding_flow_test.exs` — exit `0`.
- 68 tests passed across those three files. Regression runs outside the focused proof: `test/sdd_orchestrator/portability/`, `project_confirmation_live_test.exs`, and `local_storage_selection_live_test.exs` (161 passed), plus the other fixture consumers under `privacy/`, `project_storage/`, and `projects/` (64 passed).

### 2026-09-02 - Task 1 complete: hosted registration from a device-origin attempt

- `Projects.register_project/3` accepts a device-origin attempt whose storage mode is hosted. A `proven_workspace?/2` guard runs before every other branch: the attempt commits only into the workspace its own verified sign-in recorded in `hosted_prerequisite_workspace_id`, and any other workspace, or none, answers `{:error, :hosted_prerequisite_required}` with nothing written. A hosted-origin attempt is unaffected.
- `Projects.proven_hosted_workspace/1` resolves that workspace from the attempt, so the LiveView in `Task 3` reads identity from the attempt and never from the session.
- Design reading recorded, because `design.md` phrases it loosely. A local-provider project writes no `RepositoryConnection` row. That schema is GitHub-shaped: `provider_repository_id` is a required integer and a local repository has no provider-issued id. The project's own `canonical_repository_id` is its repository link, which is exactly the shape `HostedRestore`, `HostedLocalRepositoryConnection.connect/6`, `HostedLocalRepositoryBindings`, and `ProjectDashboardLive` already read. `design.md`'s `Data and Access Boundaries` already defines `HostedLocalRepositoryProject` that way; only one sentence in `Proposed Approach` reads as if a connection row is written.
- Consequence of that shape, unchanged from the local projects restore already creates: `Projects.configured?/1` inner-joins `:repository_connection`, so a hosted local project is never `configured?` and `/projects/:id` always lands on its overview. That is the dashboard this slice's workflow names, so nothing is widened.
- `canonical_repository_id/1` and `repository_provider/1` are shared by the project insert and by `existing_project_for/2`, so the per-account duplicate refusal resolves a local repository by `(workspace_id, "local", fingerprint)`. With no connection step for a local repository the conflict now surfaces on the `:project` step, which `handle_project_conflict/6` already covered.
- Reachability noted for the slice gate, not a Task 1 defect. The storage step's hosted choice unlocks on the passwordless hosted session cookie, while `/projects/:id/overview` sits behind the application session that only GitHub sign-in issues. Both resolve to one `PersonalWorkspace` only when the GitHub account and the hosted identity are linked. `specs/03-hosted-passwordless-access/` and `specs/04-github-identity-linking/` own that; it is confirmed at the product proof.
- Proof receipt: `Task 1` — scope `Focused` — command `mix test test/sdd_orchestrator/project_registration_test.exs` — exit `0`.
- 28 tests passed in that file, 6 of them new.
- Regression checks outside the focused proof: `mix test test/sdd_orchestrator/portability/hosted_restore_test.exs test/sdd_orchestrator_web/live/project_confirmation_live_test.exs` (19 passed) and `test/sdd_orchestrator/participation/owner_profile_test.exs` (17 passed), the other `register_project/3` callers.

### 2026-09-01 - Specification created after the gap blocked another slice's product proof

- Found while proving `specs/41-feature-delivery-from-the-ui/` in a real browser. Choosing `In my SDD Orchestrator account` for a local repository answers `Saving local projects to a hosted account is coming soon.`, and `LocalOnboardingLive`'s own comment records that the work belongs to an atomic-registration task nobody had specified.
- The contract already existed. `specs/05-project-storage-lifecycle/` is `Verified`, its AC-06 and AC-07 require that choosing hosted storage creates a project under an authorized identity with one authoritative storage mode, its business rule says repository source does not restrict project-data storage, and it defers nothing. Its release boundary expects both source-owned onboarding integrations to pass, and the local one never landed. `specs/02-local-project-onboarding/` is `Verified` and its Task 7 delivered only the accountless branch of that same step.
- Routing decided with the user: neither `Verified` parent is reopened. `specs/05` keeps its status because it explicitly excludes onboarding mechanics and its own slice is delivered; its release boundary now names the missing local integration and points here. This specification owns delivering it.
- A question was asked that the repository had already answered, and the answer was corrected rather than applied. `specs/02` AC-25 requires a device project and a hosted project for one repository to remain separate entries, and `specs/05` AC-11 forbids merging, reassigning, migrating, or changing either. The device store's one-project-per-repository rule is scoped to the device, not across the boundary. Coexistence is the recorded behavior and this slice follows it.
- Product decision taken with the user: the worker binding commits with the project, because the worker proved that exact repository moments earlier to offer the folder picker. Creating the project unconnected would make the person prove the same repository twice and land them somewhere that cannot start a run.
- Engineering decisions recorded in `design.md`: the hosted workspace resolved from the attempt's `hosted_prerequisite_workspace_id` rather than the session, reuse of `Projects.register_project/3` so the GitHub and local paths share one committed writer, and one transaction covering the project, its connection, its storage mode, and its binding.
- Scope: classified `focused specification`. One outcome, one workflow, one verification gate, four tasks, longest `Depends on:` path of four. `Task 2` records a size exception because the four writes are one invariant.
- Release consequence: `specs/41-feature-delivery-from-the-ui/`'s release gate is blocked on this capability, since its start path needs a worker bound to a hosted local-provider project. That is a consequence of this work, not its purpose.
- No code changed in this update.
