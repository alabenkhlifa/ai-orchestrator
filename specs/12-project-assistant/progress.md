# Read-Only Project Assistant Progress Log

### 2026-08-15 — Task 8 implemented: deliver the assistant panel and private conversation experience

- Completed: AC-01, AC-22. Built the project-scoped assistant panel — the LiveView/UI layer that makes Tasks 1–7's already-approved workflow reachable and understandable from every project screen, calling only their existing public functions.
  - Architecture finding, resolved before writing any UI code: this codebase has no `Phoenix.LiveComponent` anywhere — every other stateful surface fits inside one LiveView. Investigated rather than assumed the natural "shared function component + top-level `handle_event` delegation" alternative, and found a concrete, disqualifying reason: `FeatureDetailLive` already defines its own unrelated `"confirm_boundary"` event (the delivery/agent execution processing boundary, a different agreement from this panel's own `BoundaryGate`), and generic names like `"cancel"`/`"ask"` recur elsewhere. A `Phoenix.LiveComponent`'s `phx-target={@myself}` isolates every event this panel defines from its host LiveView's own event names without renaming either side, so `lib/sdd_orchestrator_web/live/project_assistant_panel.ex` (`SddOrchestratorWeb.ProjectAssistantPanel`) is the first LiveComponent in this codebase — documented in its own moduledoc as a deliberate, justified departure from precedent, not an oversight.
  - Mounted with one `live_component` call (`module: ProjectAssistantPanel`) placed immediately after the existing `project_nav` call — the exact "add one line near `project_nav`" convention its own moduledoc calls out — in all six LiveViews `project_nav` itself renders in: `feature_board_live.ex`, `feature_detail_live.ex` (which also hosts the run and evidence views), `participation_live.ex`, `project_dashboard_live.ex`, `project_backup_live.ex`, `repository_assessment_live.ex`. Confirmed via `router.ex` that no separate run/evidence LiveView exists (`feature_detail_live.ex` alone hosts `/projects/:id/features/:feature_id` and everything nested under it) and that this six-screen set is complete. For the three LiveViews that also serve a device-authoritative path (`project_dashboard_live.ex`, `project_backup_live.ex`, `repository_assessment_live.ex`), the panel is guarded `:if={... == "hosted"}` identically to `project_nav`'s own existing guard — device screens have no established cross-screen navigation surface yet for this panel to mirror, so wiring it there is deferred, documented in the moduledoc rather than silently skipped. Four of the six LiveViews needed one added `:actor` assign (mirroring `feature_board_live.ex`'s own existing `actor/1` construction exactly); the other two already had `@actor`. All six already carry `@current_account` (verified against every covering `live_session`'s `on_mount` list) as the panel's `account` attr.
  - The panel is a floating toggle (always rendered, `position: fixed`, safe even when every backend dependency is down) plus a conditionally rendered drawer (`w-96` corner box on `sm:` and larger, full-screen `inset-0` on mobile). `update/2` resolves `authority` once via a plain `Repo.get(Project, project_id)` + `Repo.get(PersonalWorkspace, workspace_id)` lookup — general Ecto infrastructure, not a rewrite of any Task 1–7 store — and every domain call already fails closed to `{:error, :unauthorized}` for an authority it does not recognize, so an unresolvable or device project needs no special-cased handling here.
  - History and citations: `ProjectAssistantStore.list_history/3` (Task 1) returns bare turns — Task 7's citations live in their own table, joined only where a caller needs them, per that task's own moduledoc. This panel is the first private-history reader Task 7's citations need to reach, so it decorates hosted turns with `Repo.preload(turns, :citations)` (general Ecto, not new domain logic); the device branch is a documented no-op, consistent with the hosted-only mount decision above.
  - Ask flow: `handle_event("ask", ...)` calls `TurnOrchestrator.answer/6` (Task 7) inside `start_async/3`, rendering a genuine pending → complete/failed transition (`Phoenix.LiveView.AsyncResult`) rather than token-level streaming — confirmed via Task 6/7's own architecture notes that no live model-calling loop exists anywhere in this codebase yet, so streaming UI would have nothing real to stream. `cancel_async/3` kills the in-flight task outright; since `TurnAnswerStore.persist/5` only ever runs at the very end of a turn, a canceled task leaves no partial row. `safe_answer/5` wraps the call in `rescue`/`catch` so an unnormalized adapter exception can never crash the LiveView the panel is mounted in — found necessary during focused-test debugging (see below) and kept as a permanent, generically justified backstop ("the panel must remain visible... never disappear" applies to a genuine internal error exactly as much as a normalized one).
  - Citation-open re-authorizes rather than re-resolving: `Guard.authorize_action(project_id, actor, :open_citation)` for every citation, plus `RepositorySourceAuthorization.authorize/3` (Task 4) specifically for a `"repository"`-typed citation, matching design.md's "a stored citation does not preserve access after participation or source permission ends." Neither function's own resolution logic is touched or duplicated.
  - Degraded states render from `BoundaryGate.status/5`'s already-safe `RuntimeAvailability` states only (`setup_needed`, `unavailable`, `temporarily_limited`) — never a raw `reason` atom, never anything from `RuntimeProjections.owner_projection/3`-shaped data (that function is never called, directly or transitively, anywhere in this file). `source-unavailable` and `unstable` surface as Task 7's own `UncertaintyMarker` entries (`marker["detail"]` rendered verbatim — already safe, human-authored text, not reconstructed here). `failed` outcome renders `turn.failure_reason` through a small closed safe-copy map with a generic fallback, never the raw reason string. First-use/changed-boundary confirmation renders `ProcessingSummary`'s fields directly and calls `BoundaryGate.confirm/5`; `askable?/1` requires both `state: :available` and `confirmation_required: false` before the ask form enables, so a stale digest re-requires confirmation automatically the next time the panel loads status.
  - Delete calls `ProjectAssistantStore.delete_conversation/3` (Task 1's existing function) directly — no new deletion path.
  - Accessibility/keyboard: toggle and close controls carry a visible focus ring (this project's shared `focus-visible:outline` convention); opening the panel moves focus to its close control via `phx-hook="FocusOnMount"` (existing `app.js` hook, not a new one); Escape and the close button both return focus to the toggle via `Phoenix.LiveView.JS.focus/2`; the scrollable history region carries `tabindex="0"` (an axe `scrollable-region-focusable` violation, found and fixed during the browser proof below); status is never color-only (the shared `badge`/`notice` components pair color with an icon and text throughout, matching `ui.ex`'s own established rule). Added three new bundled icons (`message-circle`, `send`, `trash-2`) to `lib/sdd_orchestrator_web/components/icons.ex` — the existing bundle had no chat/send/delete glyph.
  - specs/12 Task 8's own proof line explicitly requires browser scenarios (unlike Tasks 1–7, which deferred all browser/live proof to Task 10). Proving "readable citations and uncertainty," "every degraded state," and a real ask/citation/failure/retry/delete cycle in an actual browser needed a real `:available` `RuntimeAvailability` and a real answered turn, with no live model or worker anywhere in the codebase yet. Rather than build new live-RPC worker simulation (the existing `ai_connections` e2e scenario's responder answers only a `connection/1`-shaped request and would corrupt a real `model/list`/quota round trip), added three small deterministic adapters to `e2e_bootstrap_controller.ex` — `E2EPersonalConnectionAdapter`, `E2EModelCatalogAdapter`, `E2EQuotaAdapter` (each mirrors an existing `test/support` double's `opts[:adapter_result]` pass-through, but lives in `lib/`, compiled only under the same `Application.compile_env(:sdd_orchestrator, :e2e_bootstrap, false)` gate as the rest of that file) — plus `E2EModelCompletionAdapter` (mirrors a small subset of `SddOrchestrator.ProjectAssistant.FakeModelCompletionAdapter`'s own proven `question_text`-prefix scenarios: `spec-valid:`, `repository-valid:`, and `fails:` followed by a reason atom). `config/dev.exs` points `:model_completion_adapter` at it only when `E2E_MODE=true` (guarded so an ordinary `mix phx.server` still gets the real `.Unavailable` fallback via the module's own default, never a `nil` config value). A new `"project_assistant"` bootstrap scenario seeds a project, a current specification, and a feature with recorded run/evidence, plus (per a `state` param) the owner's personal AI connection through the real `PersonalConnections.link_personal_connection/4` → `ModelCatalogs.refresh/3` → `Quotas.refresh/3` chain with these adapters — no worker pairing, no `PersonalWorkerRPC` round trip. Repository citations were left genuinely unresolvable in e2e (no worker bound to the seeded project at all), which incidentally proves the `source-unavailable` marker for free inside a real turn rather than needing a fourth adapter.
  - Two real bugs found and fixed via the browser proof, not just the adapter plumbing above: (1) the quota adapter result's first hand-built shape failed `QuotaAdapter.validate_result/3` (`{:error, :invalid_response}`, a real 500) until its `unknown_fields` lists were corrected to name every field actually left `nil`/placeholder, matching the exact pattern `test/support/ai_runtime_fixtures.ex`'s own `quota_bucket/1` already established; (2) axe flagged the scrollable history region (fixed via `tabindex="0"`, above).
- **Task proof** (focused, not a broad/slice gate):
  - `mix format --check-formatted` (all new/changed files) — exit `0`.
  - `mix compile --warnings-as-errors` — exit `0`, zero warnings.
  - `mix credo --strict` scoped to `project_assistant_panel.ex` + its test file — exit `0`, no issues (85 mods/funs). `mix credo --strict` scoped to the six changed LiveViews + `icons.ex` — exit `0`, no issues (240 mods/funs). `mix credo --strict` scoped to `e2e_bootstrap_controller.ex` — exit `0`, no issues (107 mods/funs).
  - `mix test test/sdd_orchestrator_web/live/project_assistant_panel_test.exs` — exit `0`, 13 tests, 0 failures (verified stable across three different `--seed` values after fixing an initial `render_async` timing gap and an HTML-entity string-match false alarm).
  - Regression: `mix test test/sdd_orchestrator_web/live/project_assistant_panel_test.exs test/sdd_orchestrator/project_assistant/` — exit `0`, 226 tests, 0 failures (Tasks 1–7's own suites unaffected). `mix test` across the panel test plus all six modified LiveViews' own test files — exit `0`, 190 tests, 0 failures.
  - Browser proof, run directly through Playwright with an isolated database (not `npm run test:e2e`, which always runs the full fixed-DB desktop+mobile matrix): `E2E_MODE=true E2E_BASE_URL=http://localhost:4316 E2E_DATABASE_NAME=sdd_orchestrator_e2e_task8g E2E_DEVICE_STORE_PATH=[scratchpad]/task8g.dets E2E_ISOLATED_RUN=true MIX_BUILD_PATH=[repo]/_build/e2e_task8g ./assets/node_modules/.bin/playwright test --config=assets/playwright.config.js --project=chromium e2e/project-assistant.spec.js` — exit `0`, 8/8 passed. Same command with `--project=mobile-chromium` — exit `0`, 8/8 passed. The eight scenarios: reachability across overview/board/feature/participation/backup without navigating away; keyboard open/close with a visible, returning focus ring; no serious axe violations open, light and dark; no horizontal overflow on mobile; safe `setup_needed` copy with no mutation control and no quota/credential/token/`@`-address text anywhere in the panel; a linked-but-incompatible connection's safe `unavailable` state; the full confirm → ask → readable specification citation → repository claim resolving as a visible `unavailable` marker (never a fabricated citation) → normalized failure with a refilling (not auto-resubmitting) retry → immediate delete cycle; and private-history isolation (a second, unauthenticated browser context cannot reach the screen at all).
- Remaining: Task 9 stays capability-blocked per `## Status` (deferred to a later session); Task 10's slice verification gate, which additionally owns the full cross-slice browser matrix, live-runtime smoke evidence, and release-gate separation this task's own focused proof deliberately did not run.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, the full/cross-feature e2e matrix via `npm run test:e2e`, production proof) intentionally deferred to Task 10 per this task's proof scope.
- Spec updates: `tasks.md` — checked off Task 8 and removed its `Status: In Progress.` sub-line.

### 2026-08-15 — Task 7 implemented: produce grounded answers, exact citations, and explicit uncertainty

- Completed: AC-10, AC-11, AC-12, entity:ProjectAssistantCitation. Built the turn orchestration that turns bounded current context into one reviewable answer with authorization-checked citations and visible uncertainty:
  - Architecture finding, confirmed rather than assumed before writing code: `AIRuntime.CodexAppServer` still exposes only `account/...`/`model/list` RPC methods — no live model-calling or tool-invocation loop exists anywhere in this codebase. Followed the brief's proposed pattern exactly, mirroring Task 4/5's `RepositoryObservationAdapter`: added `SddOrchestrator.ProjectAssistant.ModelCompletionAdapter` as a `@behaviour` + deterministic `.Unavailable` fallback (`{:error, :model_unavailable}`), plus a fixture-driven `FakeModelCompletionAdapter` test double keyed off `question_text` prefixes (mirroring `FakeRepositoryObservationAdapter`'s `repository_ref` convention). Everything provable for AC-10/11/12 lives downstream of that boundary — citation resolution, uncertainty, and rejection — and is fully proven with fixture-driven candidate answers (valid, stale, fabricated, and inaccessible claims) without any live model call.
  - Second finding, acted on rather than deferred: Task 1's own `ProjectAssistantTurn`/`Guard` moduledocs explicitly forecast this task's work ("Later tasks add the answer, citations, context-version references, runtime state, uncertainty markers, and cancellation or failure outcome through their own migrations and changesets"; "A later task adds `open_citation` (Task 7/8) without changing this contract"). Extended `project_assistant_turn.ex` and its device-struct pair with the new fields as explicitly invited, and added `open_citation` to `Guard.protected_actions/0` as its own moduledoc forecasts — a purely additive change to a closed list, not a reinterpretation of the contract. Did not touch `project_assistant_store.ex`, `hosted.ex`, or `device.ex` (Task 1's own store/adapter files): a full answered turn is persisted through a new, separate `TurnAnswerStore` (hosted + device), so Task 1's bare question-only `append_turn/4` keeps its original contract unmodified and usable by its own tests.
  - Migration `20260815120000_extend_project_assistant_turns_and_create_citations.exs`: adds `answer_text` (binary, Vault-encrypted at the schema level like `question_text`), `context_version`, `uncertainty_markers` (`jsonb`, default `'[]'`, array-typed check constraint, mirroring `run_attempts.required_checks`'s own array-as-jsonb default), `outcome`, and `failure_reason` to `project_assistant_turns` — all nullable/optional so Task 1's own bare-question insert stays valid — plus a check constraint on `outcome` (`answered`/`cancelled`/`failed` or `NULL`). Creates `project_assistant_citations` (turn_id + denormalized project_id FKs, `source_type` with a closed check constraint, a `reference` map holding the exact typed identity per type, and an encrypted `excerpt`).
  - `lib/sdd_orchestrator/project_assistant/project_assistant_citation.ex` + `device_project_assistant_citation.ex` (`entity:ProjectAssistantCitation`): the hosted/device pair, following `ProjectAssistantConversation`/`DeviceProjectAssistantConversation`'s exact pairing convention. `reference` carries only the exact typed identity per source type (specification id + revision id; repository path/start_line/end_line/branch/commit/dirty/stable; board feature id; run id/attempt/state; evidence id/kind/outcome) — never a raw document body or unbounded tool result. `excerpt` is capped at 500 bytes and Vault-encrypted like `question_text` — the "minimal excerpt policy" owned surface. A full secret/PII redaction pass over an excerpt's *content* (as opposed to denying it before it is read at all, which `RepositoryExclusions` already does) is explicitly out of this task's scope and belongs to Task 9.
  - `lib/sdd_orchestrator/project_assistant/uncertainty_marker.ex`: the closed six-kind vocabulary (`partial`, `stale`, `excluded`, `unavailable`, `conflicting`, `unstable`). `:unavailable`, `:unstable`, `:stale`, and `:excluded` are attached authoritatively by the orchestrator from a concrete resolution outcome it can verify (repository observation failure or denial, an unstable scan, a superseded specification revision, or any other claim that failed to resolve). `:partial` and `:conflicting` are the answer-composition step's own uncertainty signal — validated and passed through, never invented by the orchestrator, since only the candidate answer itself can notice "I could not fully answer" or "two sources disagree."
  - `lib/sdd_orchestrator/project_assistant/citation_resolver.ex`: pure claim-to-source validation against already-authorized, already-fetched data (Task 3's assembled context for specification/board/run/evidence; a freshly taken `RepositoryObservation` for repository). A specification claim naming a superseded revision is refused (`:stale`), never silently narrowed to "closest match"; any claim naming an id absent from current context is refused (`:not_found`); `build_repository_reference/2` only ever shapes an already-stable observation's provenance and is never called by the orchestrator for an unstable one.
  - `lib/sdd_orchestrator/project_assistant/turn_orchestrator.ex` (`TurnOrchestrator.answer/6`): the single entry point. Runs `BoundaryGate.authorize_turn/5` (Task 2) then `RuntimeContract.open_turn/1` (Task 6) before anything else — no turn is persisted on a pre-tool refusal. Assembles Task 3's stored context first ("Stored Context First And Source On Demand"); calls the model-completion adapter through the runtime contract's model-usage budget; observes the repository through `RepositoryObserver.observe/4` (Task 4) only when a claim actually requests it, and only once per turn; fetches each surviving repository claim's minimal excerpt through `RepositoryDiscoverer.lines/6` (Task 5, itself authorization- and exclusion-checked, so a claim naming a denied path like `.env` fails closed before ever reaching a citation); resolves every claim through `CitationResolver`; drops any claim that cannot be verified — fabricated, stale, unstable, unavailable, or path-denied — rather than showing it as fact; drops a `material: true` claim with no citation at all the same way; and persists the complete turn through `TurnAnswerStore`. A worker-offline or source-denied repository observation never fails the whole turn: repository claims are dropped with an `:unavailable` marker while every stored-context claim still resolves (AC-10). A model-completion failure (unavailable, timeout, or any other normalized reason) *does* persist, with `outcome: "failed"` and a normalized `failure_reason` — never a raw provider error and never silently dropped (answer failure recovery).
  - `lib/sdd_orchestrator/project_assistant/turn_answer_store.ex` (+ `turn_answer_store/hosted.ex`, `turn_answer_store/device.ex`): the atomic question+answer+citations persistence Task 1 deferred. Hosted duplicates (rather than calls into) Task 1's own conversation lock/create/sequence logic inside one `Repo.transaction`, to keep Task 1's `ProjectAssistantStore.Hosted` completely untouched. Device duplicates Task 1's `ProjectAssistantStore.Device` logic the same way, and writes citations through the same generic device-delivery seam keyed by `:project_assistant_citation`. Both revalidate current participation on every call via `Guard.authorize_action/3` (`:open_panel`), matching every other project-assistant surface.
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` — exit `0`. `mix compile --warnings-as-errors` — exit `0`, zero warnings. `mix credo` scoped to the 12 new/extended `lib/` files (`--all`) — exit `0`, no issues (111 mods/funs). `mix test test/sdd_orchestrator/project_assistant/uncertainty_marker_test.exs test/sdd_orchestrator/project_assistant/model_completion_adapter_test.exs test/sdd_orchestrator/project_assistant/turn_orchestrator_test.exs` — exit `0`, 24 tests, 0 failures. Regression check against Tasks 1–6's own focused suites (conversation/authorization, context projection, repository observation and discovery, boundary gate, runtime contract) run together with the three new files above — exit `0`, 155 tests, 0 failures — proving Task 7's new store, schema extension, and `Guard` addition changed no existing behavior. The new tests prove: a specification citation resolves to the exact current revision and a stale (superseded) or fabricated one is rejected and marked `stale`/`excluded` rather than shown as fact; a board, run, and evidence citation each resolve to their current entry and a fabricated one is rejected; a repository citation resolves with exact branch/commit/dirty/stable provenance and a minimal excerpt against a stable observation; a changed tree during observation (`FakeRepositoryObservationAdapter`'s `"unstable"` scenario) never yields a citation and is marked `unstable`; the repository worker being offline still answers from current stored project data only, explicitly marked `unavailable`, with the repository fact absent from the answer text (AC-10); a claim citing a configured denied path (`.env`) fails closed before ever reaching a citation, marked `excluded`; an uncited material claim is dropped rather than shown as fact; candidate-declared `partial` and `conflicting` markers are preserved; a model-completion failure normalizes to `outcome: "failed"` with a typed `failure_reason` and still persists; and an unconfirmed participant is refused before any turn is persisted at all. Hosted and device authorities both proven for the specification-citation and worker-offline scenarios.
- Remaining: Task 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 7 and removed its `Status: In Progress.` sub-line.

### 2026-08-14 — Task 6 implemented: enforce the trusted read-tool and skill-bundle runtime contract

- Completed: AC-14, AC-15. Built the closed policy contract every later task's turn orchestration (Task 7) must go through. Confirmed before writing any code that no live model-calling loop exists anywhere in this codebase yet — `AIRuntime.CodexAppServer`'s adapter admits only `account/...` and `model/list` RPC methods, no chat/tool-invocation method — so this task is pure/testable policy code, not turn orchestration, matching how Tasks 4 and 5 built adapter *contracts* without live worker wiring:
  - Architecture finding, resolved rather than assumed: the trusted skill-bundle question. This repository's own `.agents/skills/` canonical skills (`add-spec`, `implement-spec`, `review-spec`, `update-spec`) are Codex/Claude Code's own SDD development-workflow tooling for building SDD Orchestrator itself, not a skill injected into an end participant's project-assistant runtime session — no such injected skill bundle content has been authored anywhere in this codebase. Built only the generic versioning/integrity-check CONTRACT (`TrustedSkillBundle`), never skill content, per the brief's documented fallback for this exact ambiguity.
  - No migration: Task 6 is pure, non-persisted policy. `TurnBudget`/`RuntimeContract` are per-turn in-memory values threaded by the caller, mirroring `ProgressiveDiscoveryPlanner`'s budget/exploration pattern and matching `ProjectAssistantTurn`'s own moduledoc note that runtime-state fields are added by a later task's own migration.
  - `lib/sdd_orchestrator/project_assistant/read_tool_manifest.ex` (`ReadToolManifest`): the closed nine-operation allowlist design.md's "Read-tool broker interface" names (`project-summary`, `specification-current`, `board-current`, `recent-run`, `evidence-current`, `repository-state`, `repository-tree`, `repository-search`, `repository-lines`), mirroring `Delivery.InitializationManifest`'s `@enforce_keys`/version/digest shape without reusing it directly (that module is `Delivery`-owned, pre-project, and carries a `capability_grant` enum with no equivalent here — a project-assistant turn never negotiates a write capability). `current/0` is the sole, argument-less constructor, so no code path can build a manifest from a tool result or model output. `operation_bindings/0` names the exact live Task 3/4/5 `{module, function, arity}` each name dispatches to (`ProjectContextAssembler.assemble/3` for the five stored-context surfaces; `RepositoryObserver.observe/4`, `RepositoryDiscoverer.tree/4`, `search/5`, `lines/6` for the four repository ones) — informational/audit-only, never reimplementing those functions' own project/participant/source-authority scoping. `authorize_operation/2` refuses any other name explicitly (`{:error, :tool_not_allowed}`), never silently.
  - `lib/sdd_orchestrator/project_assistant/trusted_skill_bundle.ex` (`TrustedSkillBundle`): the pinned `{name, version, digest}` identity and `negotiate/2` compatibility check, mirroring `InitializationManifest.from_map/1`'s exact-version-match-or-typed-error pattern (distinct typed refusals: unknown name, wrong version, mismatched digest, unsupported manifest version, malformed request). Digest is a stable self-consistency hash over compile-time constants (own length-prefixed canonical form, matching `ProcessingSummary.digest/1`'s local convention rather than reaching into the worker-protocol-scoped `Delivery.CanonicalJson`) — it proves internal consistency and that negotiation never widens or upgrades, not cryptographic unforgeability against an adversary who already knows the public formula; actual signing/distribution-channel integrity is release-gate territory, and nothing in this codebase's real call paths constructs a negotiation request from repository, board, run, or evidence content in the first place.
  - `lib/sdd_orchestrator/project_assistant/turn_budget.ex` (`TurnBudget`): cumulative tool-call, elapsed-time, context-byte, result-byte, and model-usage budgets as a pure threaded struct, plus sticky cancellation. Every usage counter only grows and elapsed time only advances with caller-supplied `now`, so a hit ceiling stays hit for every later check; cancellation is the one explicit flag, checked first by every function. No natural "model usage" hook exists to tie into: `RuntimeAvailability` deliberately never reads `RuntimeProjections.owner_projection/3` because it returns exact account-wide quota, credits, and spend this feature must never receive at all — so `model_usage` here is a simple project-assistant-configured ceiling, independent of and never reading any AIRuntime quota field. Configured via `Application.get_env(:sdd_orchestrator, :project_assistant_turn_budget, %{})`, mirroring `RepositoryExclusions.configured/0`'s Application-env-with-fallback shape.
  - `lib/sdd_orchestrator/project_assistant/untrusted_content.ex` (`UntrustedContent`): tags the six business-rule content classes (repository source, repository instructions, specification, board text, run output, evidence) `:untrusted`, unconditionally — `tag/2` never inspects content to grant it a different trust level. `suspected_injection?/1` is an explicitly non-authoritative best-effort signal for audit only; design.md states plainly that tool policy is enforced by the control plane and worker, not by prompt text, and no manifest/budget/skill-bundle function ever consults its result.
  - `lib/sdd_orchestrator/project_assistant/runtime_contract.ex` (`RuntimeContract`): the top-level closed contract binding `manifest` + `skill_bundle` + `budget` together immutably at `open_turn/1`, the sole constructor. `authorize_call/4`/`record_call/5` gate the manifest and budget together without ever invoking the underlying operation; `authorize_model_call/1`/`record_model_call/1` gate model-usage budget; `cancel/1` is sticky; `audit/1` is a compact, content-free inspection mirroring `AIRuntime.SecurityLog`'s allowlisted-outcome style (manifest version/operation count, skill identity/digest, every budget counter and ceiling — never a prompt, answer, citation, path, or project/participant identifier).
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` — exit `0`. `mix compile --warnings-as-errors` — exit `0`, zero warnings. `mix credo` scoped to the 5 new `lib/` files — exit `0`, no issues (47 mods/funs). `mix credo` scoped to the 5 new test files — exit `0`, no issues (6 mods/funs). `mix test test/sdd_orchestrator/project_assistant/read_tool_manifest_test.exs test/sdd_orchestrator/project_assistant/trusted_skill_bundle_test.exs test/sdd_orchestrator/project_assistant/turn_budget_test.exs test/sdd_orchestrator/project_assistant/untrusted_content_test.exs test/sdd_orchestrator/project_assistant/runtime_contract_test.exs` — exit `0`, 59 tests, 0 failures. The new tests prove: the manifest's nine operation names are fixed and every allowed name is bound to a real, currently-exported Task 3/4/5 function; an operation name outside the closed set — including shell/network/write-shaped names and hostile strings crafted to look like an escalation attempt — is denied explicitly, never silently ignored; only the exact pinned `{name, version, digest}` skill identity negotiates, with a distinct typed refusal for an unknown name, wrong version, mismatched digest, unsupported manifest version, or malformed/repository-shaped request; every budget ceiling (tool-call count, elapsed time, context bytes, result bytes, model usage) refuses further calls once hit and never partially commits an over-budget result; cancellation is sticky and refuses every further call regardless of remaining budget; five deliberately hostile payloads (secret/shell/network/policy-override/budget-widening phrasing, and a fake SKILL.md claiming the pinned bundle's own name and version) tagged as untrusted tool-result content leave the canonical manifest, skill bundle, and a live open contract byte-for-byte unchanged; a SKILL.md-shaped `repository-lines` result is tagged untrusted like any other source and never negotiates as a trusted skill bundle; the audit surface exposes only an allowlisted key set and never a hostile payload; and none of the three closed-contract primitive modules (manifest/skill-bundle/budget) reference `UntrustedContent` at all, while `runtime_contract.ex`'s own code never aliases, imports, or calls it (its moduledoc names it only in prose, to document that structural absence).
- Remaining: Tasks 7 and 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 6 and removed its `Status: In Progress.` sub-line.

### 2026-08-14 — Task 5 implemented: bounded progressive source discovery and worker-local indexing

- Completed: AC-13, AC-18, entity:RepositorySourceIndex. Built the tree, text-search, and line-read operations Task 4's `RepositoryObserver` deliberately left out of scope, plus the invalidation key those operations run against:
  - Architecture finding, confirmed rather than assumed: `RepositorySourceIndex` is not persisted anywhere Orchestrator controls, in Postgres or the device DETS store, for either storage authority. design.md's own words ("Keep a `RepositorySourceIndex` entirely inside the worker boundary for both hosted and device projects... The hosted control plane stores no source index, raw file, bulk scan result, or tool payload") and the entity definition itself ("no hosted control-plane copy") leave no room for a migration or a device-store record — a device project's own specification/board/conversation data is Orchestrator's authoritative record, but a repository's derived index deliberately is not, even though a device project's worker and control plane happen to share a process. Implemented it as a plain, non-persisted struct — the same treatment Task 4 already gives `RepositoryObservation`, one step further: it carries only identity and versioning metadata (project id, repository authority reference, branch, commit, a working-tree-state key, an index version), never index content.
  - `lib/sdd_orchestrator/project_assistant/repository_observation_adapter.ex`: extended (not forked) `RepositoryObservationAdapter` with `tree/1`, `search/1`, and `lines/1` callbacks and matching typed request/result shapes, each carrying its own `max_entries`/`max_results`/`max_bytes` and configured `exclusions` — the adapter-level bounded contract Task 6's later trusted-tool-broker budgets wrap, not replace. Added matching `Unavailable` fallback clauses.
  - `lib/sdd_orchestrator/project_assistant/repository_source_index.ex` (`entity:RepositorySourceIndex`): `build/1` derives the key from a `RepositoryObservation` (working-tree-state key from the dirty flag + post-scan digest, the same digest `RepositoryObservation.build/2` already uses for `stable?`); `current?/2` is the invalidation/refresh check — any drift in project, repository authority, branch, commit, or working-tree state (including a different project entirely) invalidates a prior index; `scope/1` is the project- and source-authority scope two indexes must share to ever be interchangeable.
  - `lib/sdd_orchestrator/project_assistant/repository_exclusions.ex` (`RepositoryExclusions`): the configured path/file exclusion half of AC-19 (denial, not redaction — Task 9 owns redaction of content that already crossed the boundary). `Application.get_env(:sdd_orchestrator, :repository_discovery_exclusions, @default_exclusions)`-backed, with a sensible built-in default list covering generated/vendored trees (`node_modules/`, `_build/`, `deps/`, `dist/`, `build/`, `vendor/`) and conventional secret-bearing paths (`.env`, `*.pem`, `*.key`, `id_rsa*`, `.ssh/`, `secrets/`, `credentials.*`).
  - `lib/sdd_orchestrator/project_assistant/progressive_discovery_planner.ex` (`ProgressiveDiscoveryPlanner`): a pure decision function per the brief — `new/2` (call/entry budget), `start/1` (frontier seeded at the repository root), `next/2` (`{:list, path} | :halt_budget_exhausted | :halt_complete`), `record/4` (folds one `tree/1` result back into the frontier, breadth-first, never revisiting a path). No process, no scheduler, no adapter dependency — fully unit-tested in isolation.
  - `lib/sdd_orchestrator/project_assistant/repository_discoverer.ex` (`RepositoryDiscoverer`): the orchestrator. `tree/4`, `search/4`, `lines/6` each repeat `RepositoryObserver`'s exact authorization-then-worker-availability composition (`RepositorySourceAuthorization.authorize/3` then `RepositoryWorkerAvailability.available?/2`, both reused unmodified) before ever calling the adapter, then independently re-enforce configured exclusions and byte/result budgets on the adapter's raw result (defense in depth — a real worker's own enforcement is never trusted blindly). `lines/6` denies a configured sensitive path before the adapter is ever called. `classify/2` reuses `RepositoryInitialization.Eligibility`'s exact `:empty_directory | :unborn_repository | :mature_repository` vocabulary — documented as vocabulary reuse, not a literal call to `Eligibility.classify/1`, since that function inspects a local filesystem path and nothing on this side of the worker boundary ever receives one. `discover/4` is the convenience loop wiring the planner to `tree/4` across multiple bounded calls, proving true "progressive" behavior end-to-end (a large/deep fixture halts with unlisted work remaining rather than walking the whole tree).
  - Extended (not forked) `test/support/fake_repository_observation_adapter.ex` with `tree/1`, `search/1`, `lines/1` scenarios keyed off the same `repository_ref` convention Task 4 established, plus one new `observe/1` scenario (`"empty"`, no branch/no commit) needed to distinguish `:empty_directory` from `:unborn_repository`. Fixed a fallout compile warning in Task 4's own `repository_observer_test.exs`: its local `PoisonAdapter` now needs stub `tree/1`/`search/1`/`lines/1` clauses to keep implementing the extended behaviour (mechanical, no assertion or logic change).
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` (scoped to the 4 new + 1 extended `lib/` files, the extended fake, and the 4 new + 1 fallout-fixed test files) — exit `0`. `mix compile --warnings-as-errors` — exit `0`, zero warnings. `mix credo` scoped to the 5 `lib/`+fake files — exit `0`, no issues (68 mods/funs). `mix credo` scoped to the 4 new test files — exit `0`, no issues (13 mods/funs). `mix credo` scoped to the fixed `repository_observer_test.exs` — exit `0`, no issues (9 mods/funs). `mix test test/sdd_orchestrator/project_assistant/` (full directory, Tasks 1–5) — exit `0`, 130 tests, 0 failures, 0 warnings. `python3 .agents/scripts/validate_spec.py specs/12-project-assistant` — passes. The new tests prove: an empty repository (no branch/no commit) classifies `:empty_directory` with a zero-entry root listing; an unborn repository (`.git`, zero commits) classifies `:unborn_repository`; a mature repository classifies `:mature_repository`, never an error; a non-SDD repository's search returns zero matches, not an error; `node_modules/`/`_build/` and `.env`/`secrets/` paths are excluded from both tree and search results even though the fake adapter itself returns them unfiltered; reading a denied path directly fails closed as `:path_denied` before the adapter runs (poison-adapter proof); a large repository's tree/search/lines each truncate to the configured entry/result/byte budget and report `truncated: true`; `discover/4` over a nested fixture halts on call budget with unlisted work remaining (bounded, not a full walk) and completes cleanly within a larger budget; an index built from one project's observation is no longer current after that same project's working tree changes (canonical-repository-id update simulating a source change), and is never current against a different project's observation (cross-project denial, both structurally via `scope/1` and via the unauthorized-identity path); unauthorized, source-denied, and worker-unavailable outcomes never reach the adapter on `tree/4`/`search/4`/`lines/6` (poison-adapter proof, mirroring Task 4); device-authority parity (list + mismatched-workspace denial); and no migration or hosted table is named for tree/search/line/source-index content, `RepositorySourceIndex` exposes no `__schema__/1`, `RepositoryDiscoverer` exposes no persistence/write function, its own source never references `Repo.insert`/`Repo.update`/`commit_delivery`/`Logger.`/`Cachex`, and running bounded discovery leaves an unrelated hosted assistant table's row count unchanged.
- Remaining: Tasks 6 through 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 5 and removed its `Status: In Progress.` sub-line.

### 2026-08-14 — Task 4 implemented: observe the authorized participant's current working tree safely

- Completed: AC-08, AC-09, AC-16, entity:RepositoryObservation. Built the single bounded "observe current state" operation (branch/commit/dirty/before-after/scan-time/exclusions) — not the progressive tree/search/line-read discovery or `RepositorySourceIndex`, which stay Task 5's scope:
  - Architecture finding, resolved rather than a blocker: no per-participant repository/source authorization concept existed anywhere in this codebase before this task (`Participation.Capabilities` explicitly excludes repository access from every project-participation capability group, matching design.md's "Project participation does not confer repository authority"). Confirmed the brief's proposed composition works exactly as described for a hosted GitHub-connected project — `Accounts.valid_access_token/1` (the acting participant's *own* stored credential, resolved from their own `account_id`, never the project owner's) plus `GitHubIntegration.check_repository_access/2` and `list_accessible_repositories/2`, matched against the connected repository's exact `provider_repository_id` — the same mechanism `Projects.Connections.access_outcome/1` already uses for the analogous connection-revalidation purpose.
  - Also found, and reconciled rather than treated as a conflict: Task 2's own `RepositoryWorkerAvailability` (already merged) answers "is a worker reachable for this project" purely from `HostedLocalRepositoryBindings.connection_state/3` (hosted `repository_provider: "local"` projects, paired `LocalWorker`) or `Devices.worker_status/1` (device projects) — it does not consult `RepositoryConnection`/GitHub at all. This is a genuinely separate, orthogonal question from source *authorization*: a hosted GitHub-connected project (`repository_provider: "github"`) has no paired local worker under the current data model, so it is always `worker_unavailable` regardless of GitHub access, and a hosted project with no GitHub connection has no separate per-participant credential to differentiate on, so current project participation alone is the full authorization answer there (worker reachability is what actually gates whether an observation can run). `RepositorySourceAuthorization` answers only the identity question; `RepositoryObserver` composes it with the unmodified, reused `RepositoryWorkerAvailability` check.
  - `lib/sdd_orchestrator/project_assistant/repository_source_authorization.ex` (`RepositorySourceAuthorization`): hosted path uses `Delivery.ParticipantGuard.authorize/2` (the same general membership check `ProjectContextAssembler.Hosted` already uses for a read surface outside `ProjectAssistant.Guard`'s five actions, so Task 1's `guard.ex` needed no edit) plus the GitHub-access composition above when a `RepositoryConnection` exists; device path mirrors `ProjectAssistantStore.Device`/`ProjectContextAssembler.Device`'s device-workspace-identity check exactly. Returns `{:ok, target}` with only opaque project/repository/actor references (never a credential), `{:error, :unauthorized}` for a stale/absent/removed/cross-project identity (no existence disclosure, matching every other project-assistant surface), or the distinguishable `{:error, :source_denied}` for a currently valid participant who lacks repository access (a disclosed outcome AC-16 itself wants shown).
  - `lib/sdd_orchestrator/project_assistant/repository_observation_adapter.ex` (`RepositoryObservationAdapter` + `.Unavailable`): mirrors `RepositoryAssessments.RepositoryMetadataAdapter`'s behaviour/`configured/0`/`Unavailable`-fallback pattern precisely — a typed `observe/1` callback over an opaque request (project id, repository provider/ref, exclusions; never a filesystem path or credential), defaulting to `Application.get_env(:sdd_orchestrator, :repository_observation_adapter, __MODULE__.Unavailable)`. The live worker RPC wiring stays out of scope, matching this task's brief and the Verification Gate's live-worker item being slice-level (Task 10), not task-level.
  - `lib/sdd_orchestrator/project_assistant/repository_observation.ex` (`entity:RepositoryObservation`): a plain, non-Ecto struct — not persisted. design.md describes it as "one bounded source-read result" a turn consumes and cites, not stored history, and persisting it here would recreate exactly the durable hosted source copy the "Separate Destination-Local And Worker-Local Indexes" decision rules out. No migration was added. `build/2` derives `stable?` from a before/after digest equality comparison (AC-09) rather than accepting it as adapter input.
  - `lib/sdd_orchestrator/project_assistant/repository_observer.ex` (`RepositoryObserver`): the orchestrator — authorize source access, then check worker reachability (reusing `RepositoryWorkerAvailability.available?/2` unchanged, with a test-only `:worker_available` override so content-behavior tests do not need live `LocalWorker`/pairing infrastructure), then call the configured adapter, then build the entity. A denied authorization or an unreachable worker never invokes the adapter (proven with a poison-pill test double that raises if called).
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` (scoped to the 8 new files) — exit `0`. `mix compile --warnings-as-errors` — exit `0`. `mix credo` scoped to the 8 new files — exit `0`, no issues (37 mods/funs). `mix test test/sdd_orchestrator/project_assistant/repository_observation_test.exs test/sdd_orchestrator/project_assistant/repository_source_authorization_test.exs test/sdd_orchestrator/project_assistant/repository_observer_test.exs` — exit `0`, 33 tests, 0 failures. `python3 .agents/scripts/validate_spec.py specs/12-project-assistant` — passes. The new tests prove: a clean tree (stable, not dirty), a dirty tree with uncommitted changes (still stable), an unborn branch (`commit: nil`, mirroring `RepositoryInitialization.Eligibility`'s classification), and a concurrently changing tree (`stable?: false`) all report exact provenance; a hosted participant whose own GitHub identity independently has (or lacks) access to the connected repository is authorized (or denied `:source_denied`) regardless of the project owner's own access — proving credential non-substitution in both directions; a participant with no linked GitHub identity at all, no installation, or only a granted-but-empty installation are all denied `:source_denied`; a stale/removed participant, a cross-project identity, and a mismatched device workspace all deny identically as `:unauthorized` without disclosure; the real (unmocked) `RepositoryWorkerAvailability` check denies as `:worker_unavailable` by default with no bound worker; and every denial path never reaches the adapter.
- Remaining: Tasks 5 through 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 4 and removed its `Status: In Progress.` sub-line.

### 2026-08-13 — Task 2 implemented: personal AI availability and processing-boundary confirmation

- Blocker found and corrected before implementation, not worked around silently: the original brief directed consuming `SddOrchestrator.AIRuntime.RuntimeProjections.participant_projection/4` as the "current-participant-safe available/constrained/paused/unknown runtime projection." Investigation confirmed that function structurally and deliberately refuses a `support_assistant` session — `runtime_projections.ex`'s `run_session/1` hard-requires `%{consumer: :working_agent}`, and `specs/11-ai-runtime-governance/tasks.md` Task 5's own "Delivered" note states it "refuses a support-assistant session because a personal support conversation is not a project run." `owner_projection/3` was also rejected: it works mechanically (each participant pins their own session under their own account) but returns exact quota, credits, spend, and connection reference — fields the assistant panel must never receive at all under AC-22, not merely never display. Stopped and reported this rather than improvising a workaround. The coordinating session confirmed this was a flaw in the original brief (not a specs/11 cross-spec conflict needing `update-spec`) and directed the corrected pattern below.
- Completed: AC-04, AC-05, AC-06, entity:AssistantBoundaryConfirmation. Built the runtime-availability, processing-disclosure, and pre-tool confirmation gate every later task's read tool or model call must pass:
  - Migration `priv/repo/migrations/20260813100000_create_assistant_boundary_confirmations.exs`: `assistant_boundary_confirmations` (`unique_index` on `project_id`+`account_id` — one current confirmation per participant per project, mirroring `project_assistant_conversations`) with `boundary_digest`, `boundary_version` (`> 0` check), and `confirmed_at`. No credential, exact quota, or provider-diagnostic column.
  - `lib/sdd_orchestrator/project_assistant/runtime_availability.ex` (`RuntimeAvailability`): mirrors `RepositoryInitialization.SupportDispatch`'s eligibility-then-pin flow exactly, generalized to one independent `support_assistant` session per project-assistant consumer reference (`"project_assistant:" <> project_id`) instead of one shared per-account reference. Built entirely from `AIRuntime.PersonalConnections.list_personal_connections/1`, `resolve_support_connection/2`, and `RuntimeSessions.pin_session/3`'s own typed error vocabulary (`:no_eligible_connection`, `:incompatible`, `:revoking`, `:revoked`, `{:pause, reason}`, etc.) — never `RuntimeProjections`. Normalizes to `:available | :setup_needed | :unavailable | :temporarily_limited`; more than one active connection is treated the same as none (no fallback selection); a nil account (accountless device path, no signed-in hosted account) resolves to `:setup_needed` without touching the runtime, exactly like `SupportDispatch` skips rather than substitutes.
  - `lib/sdd_orchestrator/project_assistant/processing_summary.ex` (`ProcessingSummary`): the versioned disclosed summary business rule 69 requires (execution location/provider/authentication mode, whether a repository worker may be used, transfer boundary, conversation/index storage location, 30-day retention boundary) and its stable SHA-256 digest, built only from `RuntimeAvailability`'s already-safe fields and a plain worker-available boolean.
  - `lib/sdd_orchestrator/project_assistant/repository_worker_availability.ex`: reports only whether a repository worker exists for a project (not an observation), reusing existing signals — `Portability.HostedLocalRepositoryBindings.connection_state/3` (hosted) and `Devices.worker_status/1` (device) — rather than inventing one; Task 4 owns the actual observation adapter.
  - Hosted schema `lib/sdd_orchestrator/project_assistant/assistant_boundary_confirmation.ex` and device struct `.../device_assistant_boundary_confirmation.ex`, plus `.../project_assistant_boundary_store/{hosted,device}.ex` and top-level dispatcher `lib/sdd_orchestrator/project_assistant_boundary_store.ex` (`ProjectAssistantBoundaryStore`) — mirror Task 1's `ProjectAssistantStore` pairing and dispatch shape exactly, including the device store's own `authorize/3` (a device-authoritative project has no hosted participant `Guard` can check).
  - `lib/sdd_orchestrator/project_assistant/boundary_gate.ex` (`BoundaryGate`): `status/5` (availability + disclosed summary + stored confirmation + whether reconfirmation is required), `confirm/5` (authorizes first, then refuses `:setup_needed` since there is nothing concrete yet to confirm, else replaces the stored confirmation in place), and `authorize_turn/5` — the pre-tool gate: `:ok` only when currently authorized to ask, the runtime is available now, and the stored confirmation's digest matches the freshly computed one. A material boundary change is never a separately stored flag; the stored digest simply stops matching, so there is no invalidation state that can drift out of sync. A transient `:temporarily_limited` pause blocks the turn without touching the stored confirmation, since availability state is deliberately excluded from the digest (AC-05 lists execution, provider, worker, transfer, storage, and retention — not moment-to-moment availability — as the boundary dimensions that invalidate confirmation).
  - `lib/sdd_orchestrator/project_assistant/guard.ex`: added `confirm_boundary` and `ask` to `@protected_actions`, per Task 1's own moduledoc anticipation.
  - Found during implementation and fixed before proof rather than left latent: `confirm/5` originally computed runtime availability (and could return `{:error, :setup_needed}`) before checking participation authorization, so a removed/stale participant with no linked connection would learn `:setup_needed` instead of failing closed as `:unauthorized`. Reordered to authorize first, matching `status/5`'s and `authorize_turn/5`'s already-correct ordering.
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` — exit `0`. `mix compile --warnings-as-errors` — exit `0`. `mix credo --strict` scoped to the 10 new/changed `lib/` files — exit `0`, no issues (82 mods/funs). `mix credo --strict` scoped to the 3 new test files — exit `0`, no issues (8 mods/funs). `mix test test/sdd_orchestrator/project_assistant/runtime_availability_test.exs test/sdd_orchestrator/project_assistant/processing_summary_test.exs test/sdd_orchestrator/project_assistant/boundary_gate_test.exs` — exit `0`, 21 tests, 0 failures (full `test/sdd_orchestrator/project_assistant/` directory, including Tasks 1 and 3's own suites, also passes: 47 tests, 0 failures). The new tests prove: no eligible connection (zero or more than one active) and an incompatible/revoked connection surface as safe typed states without ever pinning a session (no fallback provider ever runs, verified against `Repo.aggregate(AIRuntimeSession, :count)`); quota exhaustion without paid-continuation approval surfaces `:temporarily_limited` with an atom reason, never a quota number, and never pins a session; the available/temporarily-limited/unavailable results carry only an exact safe key allowlist (no `quota`, `credits`, `spend`, `connection_id`, or `worker_profile_ref`); `authorize_turn/5` refuses before any confirmation exists, succeeds once confirmed, and succeeds again unchanged without reconfirmation (AC-06); pairing a reachable repository worker after confirmation changes the digest and reinstates `:confirmation_required` until reconfirmed (AC-05's material-change invalidation), proven end-to-end on the device authority since `Devices.worker_status/1` is the easiest of the digest's inputs to flip deterministically in a test; a removed hosted participant is refused `status`/`confirm`/`authorize_turn` identically as `:unauthorized`; and reconfirming replaces the stored row in place (`Repo.aggregate(AssistantBoundaryConfirmation, :count) == 1` across two confirms).
- Remaining: Tasks 4 through 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 2 and removed its `Status: In Progress.` sub-line.

### 2026-08-13 — Task 3 implemented: assemble and project current stored project context

- Completed: AC-07, AC-17, entity:ProjectContextProjection. Built the read-only context assembler and its destination-local, replaceable stored projection for both hosted and device projects, consuming only already-published capabilities:
  - Migration `priv/repo/migrations/20260813110000_create_project_context_projections.exs`: `project_context_projections` (`unique_index` on `project_id` — one replaceable projection per project) with `context_version` (a deterministic digest, not a random token), `content` (`:map`/jsonb), and `refreshed_at`. No specification `requirements`/`design`/`tasks` document body, repository path, source, or source index column exists — the projection points at the authoritative specification snapshot by stable id and revision id rather than becoming a second copy of it, the same choice `RepositoryPilotSelection` already makes for the specification its pilot references.
  - `lib/sdd_orchestrator/project_assistant/project_context_assembler.ex` + `.../project_context_assembler/{shared,hosted,device}.ex`: the pure assembly and minimization layer. `Shared` builds `content` (`project`, `specifications`, `board`, `recent_runs`, `accepted_evidence`) and its `context_version` (a canonical-term SHA-256 digest, key-order independent) from already-narrowed entries; `Hosted`/`Device` do only the raw reads and revalidate authorization on every call — general current participation plus `:view_board`/`:read_evidence` for hosted (`Delivery.ParticipantGuard`, per the brief), and the device-workspace-identity check `SpecificationStore.Device`/`ProjectAssistantStore.Device` already use for device (`Delivery.ParticipantGuard`/`Participation` is explicitly hosted-only). Specification content comes from `SpecificationStore.current_snapshot/2` (`capability:project-specification-store`); board/run/evidence come directly from `Delivery.DeliveryStore.{list_features,list_evidence,list_activity,fetch_run}` (`capability:guided-delivery-data-surfaces`) rather than `Features.board/2` or the `Activity` wrapper, both of which are hosted-only (a raw Postgres query gated by the hosted-only participation boundary) and therefore cannot serve a device-authoritative project; `DeliveryStore` is the same authority-dispatching surface `Delivery.RevocationConsumer` already consumes directly for both authorities. "Recent run status" for one feature is the same rule `FeatureDetailLive.current_run_id/1` already uses (the most recent `run_started` activity entry), read through `DeliveryStore.list_activity/4` instead of the hosted-only `Activity` module so the identical logic works unmodified for device. "Accepted evidence" is `DeliveryStore.list_evidence(authority, project_id, current: true)` — current (non-superseded) items only, narrowed to status fields (kind/name/outcome/source/recorded_at), never `command`, `exit_code`, `duration_ms`, or `artifact_ref`.
  - `lib/sdd_orchestrator/project_assistant/project_context_store.ex` + `.../project_context_store/{hosted,device}.ex`: `refresh/3` (assemble then idempotently replace the project's one stored projection — hosted via `Repo.insert` with `conflict_target: :project_id`, `on_conflict: {:replace, [...]}` excluding `:id` from the replace list, `returning: true` so the returned struct reflects the real pre-existing row identity rather than the freshly autogenerated one the local changeset attempted; device via the generic `Devices.commit_delivery/2` seam with `state_version` optimistic concurrency, mirroring `ProjectAssistantStore.Device`), `get/3` (re-authorizes with the exact same gates `refresh/3` used to build the content — a participant who currently lacks evidence-read capability cannot read cached evidence merely because a more privileged participant's earlier refresh cached it), and `delete/3` (hosted: `Repo.delete_all`; device: tombstone, matching `ProjectAssistantStore.Device`'s conversation-delete treatment — a later refresh starts a fresh version count rather than restoring stale content).
  - `lib/sdd_orchestrator/project_assistant/project_context_projection.ex` (hosted Ecto schema) and `.../device_project_context_projection.ex` (device struct + `to_value/1`/`from_value/1` codec), following Task 1's hosted/device pairing convention.
  - Architecture note found and worked around rather than a blocker: a device-authoritative project currently has no way to create a `Feature` at all in production code (`Features.create/3` is hosted-only, gated by the hosted-only participation boundary; `DeliveryStore` defines no `insert_feature` operation). This is a genuine, pre-existing product gap, not something Task 3 introduced or needs to close — the assembler reads through the fully authority-generic, already-published `DeliveryStore.list_features/3`, so it will correctly pick up device features the moment a future capability starts writing them. The focused proof plants one directly through the same `Feature.to_value/1` + `Devices.commit_delivery/2` seam production code will eventually write through, exactly like `delivery_store_test.exs`'s own device-parity fixtures already rely on the absence of cross-kind referential-integrity enforcement in the device store.
  - Corrected the stale `capability:guided-specification-delivery` reference in Task 3's own "Owned surfaces" bullet to `capability:guided-delivery-data-surfaces`, matching the Cross-Specification Dependencies section (already correct per the 2026-08-09 entry below) and `design.md` line 59.
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` (scoped to the 10 new files) — exit `0`. `mix compile --warnings-as-errors` — exit `0`. `mix credo --strict` scoped to the 9 new `lib/` files — exit `0`, no issues (64 mods/funs). `mix credo --strict` scoped to the new test file — exit `0`, no issues (6 mods/funs). `mix test test/sdd_orchestrator/project_assistant/project_context_store_test.exs` — exit `0`, 13 tests, 0 failures. The new test file proves, for both hosted and device: only current metadata, current specification identity/revision, current board state, the current feature-linked run's status, and current (non-superseded) evidence are assembled; a prior specification revision never appears after `append_revision`; superseded evidence never appears after `supersede_fixture`; unrelated feature activity (a comment payload containing a sensitive-looking path) never surfaces; a stale/removed participant, an absent identity, a cross-project identity, and (device) a stale device workspace are denied identically for `refresh`/`get`/`delete` without existence disclosure; the projection rebuilds idempotently (same `context_version`, same row identity, exactly one stored row) and changes `context_version` only when the underlying data genuinely changes; deletion is immediate and idempotent; a device refresh writes nothing into PostgreSQL; and neither `ProjectContextAssembler` nor `ProjectContextStore` exposes any delivery-mutation function.
- Remaining: Tasks 4 through 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate. Task 2 is concurrently in progress on this same branch.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 3, removed its `Status: In Progress.` sub-line, and corrected the stale `capability:guided-specification-delivery` capability name in its "Owned surfaces" bullet to `capability:guided-delivery-data-surfaces`.

### 2026-08-13 — Task 1 implemented: private conversation identity, persistence, and authorization

- Completed: AC-02, AC-03, AC-23, entity:ProjectAssistantConversation, entity:ProjectAssistantTurn. Built the durable private-history boundary both hosted and device projects use, mirroring `SpecificationStore`'s dispatch shape:
  - Migration `priv/repo/migrations/20260813090000_create_project_assistant_conversations.exs`: `project_assistant_conversations` (`unique_index` on `project_id`+`account_id` — one private conversation per participant per project) and `project_assistant_turns` (`unique_index` on `conversation_id`+`sequence`, `sequence > 0` check — strict per-conversation ordering). `question_text` is `:binary`, encrypted at rest through `SddOrchestrator.Vault` (the same treatment `evidence_artifacts.content` gets). No answer, citation, context-version, runtime-state, or uncertainty columns — those belong to Tasks 2, 3, and 7's own migrations.
  - Hosted Ecto schemas: `lib/sdd_orchestrator/project_assistant/project_assistant_conversation.ex`, `.../project_assistant_turn.ex` (question text via `SddOrchestrator.Encrypted.Binary`, `redact: true`).
  - Device structs with `to_value/1`/`from_value/1` codecs for the existing generic device-delivery seam (`SddOrchestrator.Devices.commit_delivery/2` etc.): `lib/sdd_orchestrator/project_assistant/device_project_assistant_conversation.ex`, `.../device_project_assistant_turn.ex`. Question text is sealed with `Vault.encrypt/1` before it reaches the DETS-backed store and opened with `Vault.decrypt/1` on read, since the device store's file is not encrypted (same treatment `ArtifactStore.Device` gives artifact bytes).
  - `lib/sdd_orchestrator/project_assistant/guard.ex` (`SddOrchestrator.ProjectAssistant.Guard`): the hosted fail-closed authorization primitive, reusing `Participation.Boundary.current_member/2` directly (re-asked on every call, no caching), covering `open_panel`, `read_history`, `delete` (Task 2 adds `ask` under its own processing-boundary gate; Task 7/8 add `open_citation`). An unknown action denies rather than allows, matching `Delivery.ParticipantGuard`'s contract exactly.
  - Architecture note (would have been a blocker if undiscovered): `SddOrchestrator.Participation` is explicitly hosted-only — `Participation.owner/1`'s own moduledoc states "a device-authoritative project has no hosted owner and cannot participate in hosted collaboration," and its internal `owner_for/1` requires `storage_mode: "hosted"`. `Boundary.current_member/2` therefore denies every device project unconditionally. `ProjectAssistantStore.Device` authorizes the acting device workspace directly instead (mirroring `SpecificationStore.Device`'s own private `authorize/2`), since a device project has exactly one implicit participant: the device workspace itself. This is the same hosted/device authorization split `SpecificationStore.Hosted`/`SpecificationStore.Device` already draw, documented in `Guard`'s moduledoc so a future reader does not mistake it for a missed wiring.
  - `lib/sdd_orchestrator/project_assistant/project_assistant_store/hosted.ex` and `.../device.ex`: get-or-create conversation, atomic turn-append-plus-last-activity-touch (hosted: `Repo.transaction` with `SELECT ... FOR UPDATE`, mirroring `SpecificationRevision`'s append pattern; device: single `commit_delivery/2` batch with `state_version` optimistic-concurrency, mirroring `Feature`'s device value shape), ordered private-history listing, and immediate idempotent deletion (hosted: `Repo.delete` with FK cascade to turns; device: tombstone overwrite of the conversation and its turns, so a later re-open starts fresh rather than restoring history).
  - `lib/sdd_orchestrator/project_assistant_store.ex` (`SddOrchestrator.ProjectAssistantStore`): top-level dispatcher pattern-matching `PersonalWorkspace`/`DeviceWorkspace` to select `Hosted`/`Device`, with a denying fallback clause — mirrors `SpecificationStore` exactly.
  - No shared-activity projection exists or is called from any of the above (verified negatively in tests against `Delivery.ActivityEntry` / `Devices.list_delivery(project_id, :activity)`), satisfying AC-23's "never copies private conversation... into shared activity" for this slice.
- **Task proof** (focused, not a broad/slice gate): `mix format --check-formatted` — exit `0`. `mix compile --warnings-as-errors` — exit `0`. `mix credo` scoped to the 8 new `lib/` files — exit `0`, no issues (57 checks, 66 mods/funs). `mix test test/sdd_orchestrator/project_assistant/project_assistant_store_test.exs` — exit `0`, 13 tests, 0 failures. The new test file proves: exactly one conversation per (participant, project) for both authorities; independent, correctly ordered private histories for two hosted participants (owner + invited participant); empty history before any turn; a removed hosted participant is denied identically on open/read-history/delete without exposing their prior history's existence or count; an absent identity, a cross-project identity, and an unknown project all deny identically (`{:error, :unauthorized}`); a stale device workspace and a cross-project id deny identically for device; immediate idempotent deletion with FK-cascade/tombstone cleanup and a working fresh restart for both authorities; and zero rows/records in `ActivityEntry`/`:activity` delivery kind after conversation and turn activity for both authorities.
- Remaining: Tasks 2 through 8 (Task 9 stays capability-blocked per `## Status`), then Task 10's slice verification gate.
- Failed checks: None. Repository-wide gates (`mix check`, `mix dialyzer`, `mix credo --strict` repo-wide, e2e, production proof) intentionally deferred to Task 10 per this task's Proof scope.
- Spec updates: `tasks.md` — checked off Task 1 and removed its `Status: In Progress.` sub-line.

- Completed: Corrected Task 3's Cross-Specification Dependencies. It required `capability:guided-specification-delivery` (provider `specs/24-guided-delivery-completion#Task 1`, the terminal task of the entire 17→24 guided-delivery GDPR governance chain). This capability's provider was mechanically repointed there during the 2026-08-02 split of specs/07 into continuation slices (commit `b9a2997`), which kept the reference valid but never re-justified it against what Task 3 actually needs. `design.md` (line 59) already documented the real need precisely: "read-only board, run, and evidence queries" — matching `capability:guided-delivery-data-surfaces` (provider `specs/07-guided-specification-delivery#Task 54`), the same capability specs/15, specs/18, specs/19, specs/20, and specs/24 already consume for analogous field-level reads of guided-delivery data. Replaced the dependency accordingly and updated `design.md`'s matching prose. That capability is already ready. This is the same mis-wiring pattern found and corrected in `specs/15-repository-sdd-kit-integration` Task 2; both consumers were repointed in the same 2026-08-02 commit.
- Remaining: Implement Tasks 1 through 10 in dependency order.
- Failed checks: None.
- Spec updates: Corrected the Task 3 capability dependency in `tasks.md` and its matching reference in `design.md`.

### 2026-07-31 — Initial approved specification

- Completed: Classified the work as one focused read-only question-and-answer specification, separated confirmed mutations into a later child specification, defined exact authorization, context, repository observation, citation, privacy, tool, skill, retention, browser, and release behavior, and mapped every active criterion and entity to one standard task.
- Remaining: Implement Tasks 1 through 10 in dependency order. Task 1 and later consumers wait at their named provider-capability boundaries.
- Failed checks: `python3 .agents/scripts/validate_spec.py specs/12-project-assistant`, `python3 .agents/scripts/test_validate_spec.py`, and the slice-scoped `git diff --check` pass. The global graph check currently fails only on concurrently authored provider declarations outside `specs/12-project-assistant`; it reports no Slice 12 edge, cycle, coverage, task-size, or sequence error.
- Spec updates: Created the approved `requirements.md`, `design.md`, and `tasks.md` agreement for `specs/12-project-assistant`; no source specification or implementation file changed.
